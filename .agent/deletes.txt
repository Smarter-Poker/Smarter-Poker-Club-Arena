# Dead parallel emote system, removed 2026-08-15.
#
# TABLE_EMOTE had zero emitters. EmoteBroadcast was imported by TablePage and
# never rendered. EmotePanel (the picker that would have produced an emote) was
# never rendered either — only re-exported from the barrel. So the entire path
# was unreachable from end to end.
#
# It is being deleted rather than wired because the feature it duplicates
# ALREADY WORKS: TableReactions sends [REACTION:emoji:seat] over the chat
# channel, useTableChat parses it, and TablePage renders the floating emoji.
# Wiring the second system on top of a working first one would double-render
# every reaction. One emote system, and it is the one that works.
src/components/table/EmoteBroadcast.tsx
src/components/table/EmoteBroadcast.css
src/components/table/EmotePanel.tsx
src/components/table/EmotePanel.css
