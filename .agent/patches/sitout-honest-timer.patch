diff --git a/src/components/table/SitOutModal.css b/src/components/table/SitOutModal.css
index e84c466..3a1e655 100644
--- a/src/components/table/SitOutModal.css
+++ b/src/components/table/SitOutModal.css
@@ -369,0 +370,16 @@
+
+/* Replaces .sitout-modal__warning (2026-08-16). That warned "You will be
+   removed from the table if you don't return", which was false — there is no
+   sit-out deadline anywhere in the system. This states what is actually true,
+   so it is informational rather than alarming. */
+.sitout-modal__note {
+  margin: 12px 16px 0;
+  padding: 10px 12px;
+  border-radius: 8px;
+  background: rgba(255, 255, 255, 0.05);
+  border: 1px solid rgba(255, 255, 255, 0.09);
+  color: rgba(255, 255, 255, 0.72);
+  font-size: 13px;
+  line-height: 1.45;
+  text-align: center;
+}
diff --git a/src/components/table/SitOutModal.tsx b/src/components/table/SitOutModal.tsx
index e97b722..ed2414d 100644
--- a/src/components/table/SitOutModal.tsx
+++ b/src/components/table/SitOutModal.tsx
@@ -27,2 +27,22 @@ export interface SitOutModalProps {
-  timeRemaining: number; // seconds until auto-kicked
-  maxSitOutTime: number; // total allowed sit-out time
+  /**
+   * HONESTY FIX 2026-08-16: this used to be `timeRemaining` — "seconds until
+   * auto-kicked" — counting down from 300, alongside the warning "You will be
+   * removed from the table if you don't return".
+   *
+   * None of that was true. There is NO sit-out deadline anywhere in the
+   * system: no server timer, no sweeper, no cron, no seat-reclaim rule. A
+   * player may sit out indefinitely and keeps their seat and their stack. The
+   * only real rule is the opposite direction — repeated action timeouts PUT
+   * you into sit-out (DisconnectEngine.recordConnectedTimeout, capped by
+   * maxConsecutiveTimeouts) — and it never removes you afterwards.
+   *
+   * Worse, `timeRemaining` was hard-wired to 300 and never updated: the state
+   * behind it had no setter call anywhere in the repo. So the modal invented a
+   * countdown, reset it every time you reopened it, threatened the player with
+   * losing a seat that was never at risk, and did it about a table their money
+   * was sitting on.
+   *
+   * Now it reports the truth: how long you have actually been sitting out.
+   * Epoch ms of when sit-out began, or null if that is not known.
+   */
+  sitOutSince: number | null;
@@ -53,2 +73 @@ export function SitOutModal({
-  timeRemaining,
-  maxSitOutTime,
+  sitOutSince,
@@ -59 +78 @@ export function SitOutModal({
-  const [displayTime, setDisplayTime] = useState(timeRemaining);
+  const [elapsed, setElapsed] = useState(0);
@@ -61 +80,3 @@ export function SitOutModal({
-  // Update countdown
+  // Count UP from when sit-out began. One interval for the lifetime of the
+  // open modal — the old countdown listed `displayTime` in its own dependency
+  // array, so it tore down and recreated the interval on every single tick.
@@ -63,10 +84,5 @@ export function SitOutModal({
-    setDisplayTime(timeRemaining);
-  }, [timeRemaining]);
-
-  useEffect(() => {
-    if (!isOpen || displayTime <= 0) return;
-
-    const timer = setInterval(() => {
-      setDisplayTime((t) => Math.max(0, t - 1));
-    }, 1000);
-
+    if (!isOpen) return;
+    const since = sitOutSince ?? Date.now();
+    const tick = () => setElapsed(Math.max(0, Math.floor((Date.now() - since) / 1000)));
+    tick();
+    const timer = setInterval(tick, 1000);
@@ -74,5 +90 @@ export function SitOutModal({
-  }, [isOpen, displayTime]);
-
-  // Progress percentage
-  const progress = (displayTime / maxSitOutTime) * 100;
-  const isUrgent = displayTime <= 60;
+  }, [isOpen, sitOutSince]);
@@ -138,2 +150,3 @@ export function SitOutModal({
-        {/* Timer Circle */}
-        <div className={`sitout-modal__timer ${isUrgent ? 'sitout-modal__timer--urgent' : ''}`}>
+        {/* Elapsed sit-out time. The ring is a full, static ring now: there is
+            no deadline for it to deplete towards. */}
+        <div className="sitout-modal__timer">
@@ -147 +160 @@ export function SitOutModal({
-              strokeDasharray={`${progress * 2.83} 283`}
+              strokeDasharray="283 283"
@@ -151,2 +164,2 @@ export function SitOutModal({
-            <span className="sitout-modal__time">{formatTime(displayTime)}</span>
-            <span className="sitout-modal__time-label">remaining</span>
+            <span className="sitout-modal__time">{formatTime(elapsed)}</span>
+            <span className="sitout-modal__time-label">sitting out</span>
@@ -156,6 +169,5 @@ export function SitOutModal({
-        {/* Warning */}
-        {isUrgent && (
-          <div className="sitout-modal__warning">
-            You will be removed from the table if you don't return
-          </div>
-        )}
+        {/* What is actually true: the seat and the stack are held. */}
+        <div className="sitout-modal__note">
+          Your seat and chips are held while you are sitting out. Return whenever
+          you are ready.
+        </div>
diff --git a/src/components/table/TableModalsLayer.tsx b/src/components/table/TableModalsLayer.tsx
index fdc7113..ad50b6c 100644
--- a/src/components/table/TableModalsLayer.tsx
+++ b/src/components/table/TableModalsLayer.tsx
@@ -123 +123,3 @@ export interface TableModalsLayerProps {
-  sitOutTimeRemaining: number;
+  /** Epoch ms when sit-out began, or null. See SitOutModal for why this is
+   *  no longer a countdown. */
+  sitOutSince: number | null;
@@ -392 +394 @@ export function TableModalsLayer(props: TableModalsLayerProps) {
-    sitOutTimeRemaining,
+    sitOutSince,
@@ -606,2 +608 @@ export function TableModalsLayer(props: TableModalsLayerProps) {
-        timeRemaining={sitOutTimeRemaining}
-        maxSitOutTime={300}
+        sitOutSince={sitOutSince}
diff --git a/src/pages/TablePage.tsx b/src/pages/TablePage.tsx
index 1d184c9..987f554 100644
--- a/src/pages/TablePage.tsx
+++ b/src/pages/TablePage.tsx
@@ -1014 +1014,6 @@ export default function TablePage({
-  const [sitOutTimeRemaining, setSitOutTimeRemaining] = useState(300); // 5 min default
+  // HONESTY FIX 2026-08-16: was `sitOutTimeRemaining = 300`, a countdown to a
+  // deadline that does not exist. setSitOutTimeRemaining had zero call sites,
+  // so it never moved, and SitOutModal warned the player they would be removed
+  // from a table they were never at risk of losing. Track when sit-out started
+  // instead and report elapsed time.
+  const [sitOutSince, setSitOutSince] = useState<number | null>(null);
@@ -1015,0 +1021,13 @@ export default function TablePage({
+  // Stamp the clock off the SERVER's view of the hero's seat, not off any
+  // local button press — a player can be put into sit-out by the engine
+  // (repeated action timeouts) without ever touching the menu, and the
+  // reconnect path re-derives it from the snapshot too.
+  const heroIsSittingOut =
+    tableState.heroSeat > 0 &&
+    tableState.players[tableState.heroSeat - 1]?.status === 'sitting_out';
+  useEffect(() => {
+    setSitOutSince((prev) => {
+      if (heroIsSittingOut) return prev ?? Date.now();
+      return null;
+    });
+  }, [heroIsSittingOut]);
@@ -7662 +7680 @@ export default function TablePage({
-        sitOutTimeRemaining={sitOutTimeRemaining}
+        sitOutSince={sitOutSince}
