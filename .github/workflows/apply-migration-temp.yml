name: Apply Migration Temp

on:
  push:
    branches:
      - feat/wallet-cashier-claim-back

jobs:
  apply_sql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply SQL using psql pooler
        env:
          PGPASSWORD: "215SlalomCt!!!"
        run: |
          psql -h aws-0-us-west-1.pooler.supabase.com -U postgres.kuklfnapbkmacvwxktbh -d postgres -p 6543 -c "SELECT 1"
          psql -h aws-0-us-east-1.pooler.supabase.com -U postgres.kuklfnapbkmacvwxktbh -d postgres -p 6543 -c "SELECT 1" || true
