dist
dist-native
# Native shells are generated by the Capacitor CLI and formatted by Xcode /
# Android Studio; Prettier must not reformat them.
ios
android
node_modules
coverage
.vercel
*.min.js
*.min.css

# Generated by scripts/ci/gen-schema-manifest.mjs from the live schema.
# Prettier collapses short arrays onto one line; JSON.stringify expands them, so
# the two formatters can never agree and the file oscillated between two byte
# states on every commit. scripts/ci/detect-silent-revert.mjs correctly read that
# as a silent revert (PR #360, 2026-08-23) even though the 9,791 column keys were
# identical each time. The generator is the single formatter now.
scripts/ci/supabase-schema-manifest.json
scripts/ci/supabase-columns-manifest.json
# The required-column manifest was added on 2026-08-28, after this block was
# written, and was never listed here -- so the exact oscillation described above
# was live again for the one file nobody had exempted. Regenerating it churned
# 3,965 lines with ZERO content change (verified: 766 tables before and after,
# none added, none removed, none altered), which is the reason an agent reverted
# it out of PR #1708 rather than bury that in a money-adjacent diff. Listing it
# here is the whole fix; the generator already writes it with the same
# `JSON.stringify(x, null, 2)` as the other two.
scripts/ci/supabase-required-columns-manifest.json

# Generated and checksummed by World Hub as the canonical cross-repository
# solver-policy contract. Club Arena verifies these exact bytes at boot/tests;
# formatting either copy independently would invalidate the published checksum.
server/src/gto/contracts/solver-policy.v1.schema.json
server/src/gto/contracts/fixtures/chart-policy.v1.json
server/src/gto/contracts/fixtures/exact-policy.v1.json
server/src/gto/contracts/fixtures/chart-policy-artifact.v1.json

# Captured financial authorities are checked byte-for-byte by the required
# Spin expiry qualification. Preserve their original reviewed serialization.
scripts/ci/probes/spin-expiry/inputs/captured-spin-catalog.json
scripts/qualification/spin-expiry-committed-refund.authority.json
scripts/qualification/spin-expiry-lock-order.authority.json

# Preserve the reviewed serialization of captured Production Alert baselines.
scripts/ci/probes/hand-index-writer-order/baseline.json
scripts/ci/probes/hand-stat-writer-order/baseline.json
scripts/ci/probes/rake-attribution-atomic/baseline.json

# Captured and byte-pinned alert qualification source records.
scripts/qualification/duplicate-structure-record-evidence.manifest.json
scripts/qualification/duplicate-structure-record-evidence.md
scripts/qualification/fixtures/rake-repair-record-evidence/images.json
scripts/qualification/rake-repair-record-evidence.manifest.json
scripts/qualification/rake-repair-record-evidence.md
scripts/qualification/spin-repair-evidence.manifest.json
scripts/qualification/spin-repair-evidence.md
scripts/ci/probes/production-alert-core/inputs/core-images.json
scripts/ci/probes/production-alert-core/notification/manifest.json
scripts/ci/probes/class4-hand-outcome/captured-functions.json
scripts/ci/probes/class4-hand-outcome/manifest.json
scripts/qualification/class4-hand-outcome-evidence.md
