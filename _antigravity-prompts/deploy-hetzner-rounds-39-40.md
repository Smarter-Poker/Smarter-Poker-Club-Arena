# Historical Deployment Prompt — Retired

This prompt is retained only as a record of Rounds 39 and 40. It is not an
executable deployment procedure.

The direct-SSH, mutable-host checkout, manual Docker restart, and local
credential assumptions that used to appear here are retired. They must never
be copied or run.

Every Club Arena engine release is dispatched and certified by the Club
Arena-owned `.github/workflows/auto-deploy-hetzner.yml` workflow. Read
`.claude/skills/deploy-hetzner/SKILL.md` for the current exact-SHA,
non-forced procedure. World Hub and Vercel are not Club Arena publishers.
