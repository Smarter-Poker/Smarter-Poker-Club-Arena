# EMERGENCY STOP + CONTROLLED RESTART

# PROJECT: BUTTON BUILDER / #CLUBBUTTONS

STOP ALL CURRENT #CLUBBUTTONS IMPLEMENTATION WORK IMMEDIATELY.

Do not continue generating components.
Do not continue polishing the current laboratory.
Do not continue migrating any production application.
Do not continue World Hub work.
Do not continue Club Commander work.
Do not continue Club Arena production migration.
Do not touch live gameplay.
Do not merge additional #ClubButtons product-migration work.

The current visual implementation direction is REJECTED.

The current runtime-assembled panels, generic shells, generic overlays,
generic fonts, generic white text, repeated chrome frames, and overly
reused silhouettes are NOT the approved #ClubButtons design system.

We are restarting the VISUAL IMPLEMENTATION of Button Builder from a
clean design direction while preserving any useful non-visual
engineering foundation.

---

1. DO NOT DESTROY EXISTING WORK
   \==================================================

Do NOT:

- hard reset
- force push
- delete branches
- rewrite Git history
- discard documentation
- remove reference images
- remove tests that remain applicable
- delete working accessibility logic
- delete useful dynamic-data architecture
- delete useful semantic-control architecture
- blindly revert unrelated code
- modify production applications

Preserve useful foundation work.

The objective is NOT to erase the project.

The objective is to STOP the bad visual implementation and restart
the Button Builder factory correctly.

## ================================================== 2. FREEZE ALL PRODUCT APPLICATIONS

Button Builder is now FACTORY ONLY.

Until explicitly authorized, you must NOT modify:

- Club Arena production pages
- Club Arena live gameplay
- World Hub
- World Hub global header
- World Hub navigation
- Club Commander
- Commander global header
- Commander menus
- Diamond Store
- any other production application

Do not migrate a single additional production control.

The only UI environment you may actively build is:

/dev/club-ui

or the equivalent private Button Builder visual laboratory.

## ================================================== 3. PRESERVE THE GOOD FOUNDATION

Keep and reuse where appropriate:

- #ClubButtons master references
- BBJ master
- Cash Wallet master
- Tournament Wallet master
- Diamond Wallet master
- reference repository
- asset manifest
- design documentation
- semantic HTML controls
- accessibility patterns
- focus behavior
- loading/error/stale/offline data states
- tabular numeral support
- real-time data separation
- mobile breakpoint testing
- dynamic value zones
- tests that validate functionality rather than the rejected visuals
- preview / laboratory infrastructure

These are foundation pieces.

They do NOT authorize reuse of the rejected generic visual shells.

## ================================================== 4. REJECT THE CURRENT VISUAL SYSTEM

The following visual approach is REJECTED:

GENERIC FRAME +
LIVE TITLE +
LIVE LABELS +
LIVE ICON +
LIVE WHITE TEXT +
CSS GLOW

Do not continue from this visual pattern.

Reject the current examples resembling:

- generic chrome panel around Tournament Info
- runtime-rendered Tournament Info heading
- runtime-rendered field labels
- generic white values
- generic Player Name Controls panel
- Lucide/generic icons placed over shared frames
- one generic utility shell reused for many controls
- one generic navigation shell repeated across navigation

Do not spend time polishing these rejected designs.

They must be replaced.

## ================================================== 5. NEW ABSOLUTE ARCHITECTURE: ASSET-FIRST

#ClubButtons is now an ASSET-FIRST production system.

Every major component must be approximately 90–95% visually complete
BEFORE runtime data is added.

The production architecture is:

PREBUILT HYPER-REALISTIC FINISHED ASSET +
ONLY TRUE DYNAMIC DATA / USER TEXT +
REAL SEMANTIC INTERACTION

Permanent content belongs in the artwork.

Dynamic content belongs in the DOM.

## ================================================== 6. WHAT MUST BE BAKED INTO THE ART

Bake ALL permanent visual information into each production asset.

This includes where applicable:

- component title
- button name
- section headings
- permanent labels
- icons
- medallions
- chevrons
- arrows
- info icons
- decorative symbols
- permanent status decoration
- dividers
- chrome
- brushed gunmetal
- black-glass surfaces
- metallic typography
- blue crystals
- lighting
- reflections
- bevels
- shadows
- screws
- joints
- recessed channels
- decorative hardware
- permanent captions

If it does not change at runtime, it should normally be baked into
the artwork.

## ================================================== 7. WHAT MAY REMAIN LIVE

Only content that genuinely changes at runtime should be placed over
the finished asset.

Examples:

- jackpot value
- wallet balance
- player count
- prize pool
- start time
- tournament level
- blinds
- timer
- player-entered name
- selected dropdown value
- changing status when the entire status cannot be represented by
  a prebuilt state asset
- other genuine data supplied by application state

Do NOT overlay permanent headings or labels just because HTML makes
them easier to code.

## ================================================== 8. BUTTON LABELS MUST BE BAKED IN

For fixed-purpose controls, bake the label directly into the asset.

Examples:

JOIN TABLE
BUY IN
REGISTER
CONFIRM
CANCEL
LEAVE TABLE
CHAT
STATS
TIME BANK
RABBIT HUNT
PREVIOUS HAND
MENU
SETTINGS
ADD
VIEW DETAILS

Do not render these labels using generic browser fonts over a shell.

Each control is its own finished manufactured part.

## ================================================== 9. ICONS MUST BE PURPOSE-BUILT

For permanently associated controls, bake the icon into the artwork.

Do NOT use the pattern:

same shell + different Lucide icon

Chat must have its own finished Chat hardware artwork.

Stats must have its own finished Stats hardware artwork.

Time Bank must have its own finished timer hardware artwork.

Rabbit Hunt must have its own finished artwork.

Previous Hand must have its own directional artwork.

Menu must have its own purpose-built artwork.

Same factory.

Different parts.

## ================================================== 10. MATERIAL QUALITY AUTHORITY

The quality authority is:

1. BBJ master
2. Cash Wallet master
3. Tournament Wallet master
4. Diamond Wallet master

These control:

- chrome realism
- brushed metal
- black-glass depth
- dimensionality
- physical thickness
- lighting
- reflections
- blue crystal energy
- bevel quality
- shadow quality
- premium material feel

Overview charts are architecture references ONLY.

They do not define final rendering quality.

## ================================================== 11. DYNAMIC FONT SYSTEM

Do NOT use generic white Inter text for live values.

The only overlaid text should use a deliberately defined premium
dynamic-data typography system.

PRIMARY DYNAMIC FONT:

Cinzel SemiBold
font-weight: 600

LARGE HERO VALUES:

Cinzel Bold
font-weight: 700

SECONDARY DYNAMIC TEXT:

Cinzel Medium
font-weight: 500

NUMERIC DATA:

Use tabular numerals:

font-variant-numeric: tabular-nums;

Do not substitute generic system fonts unless the required font is
genuinely unavailable.

## ================================================== 12. DYNAMIC TEXT COLOR

Dynamic text is NOT generic white.

Use premium metallic silver.

Target palette:

Highlight:
#F1F2F3

Upper Silver:
#D7DADF

Mid Silver:
#A9AFB5

Lower Silver:
#B8BDC2

Muted:
#8F969D

Secondary live text:
approximately #D1D5D9

Use subtle metallic treatment and dark contact shadow.

Blue text should be used only when intentionally energized by the
master design.

Do not make all dynamic information blue.

## ================================================== 13. COMPONENTS MUST HAVE UNIQUE PURPOSE-BUILT ASSETS

Do not rely on a few generic shells.

Create purpose-specific assets.

Examples:

join-table-default.webp
join-table-hover.webp
join-table-pressed.webp
join-table-disabled.webp

buy-in-default.webp

register-default.webp

confirm-default.webp

cancel-default.webp

leave-table-default.webp

utility-chat.webp
utility-stats.webp
utility-time-bank.webp
utility-rabbit-hunt.webp
utility-previous-hand.webp
utility-menu.webp
utility-settings.webp

wallet-diamonds.webp
wallet-club-bank.webp
wallet-promo.webp
wallet-agent.webp
wallet-player.webp
wallet-union.webp
wallet-rake.webp
wallet-backup-bbj.webp
wallet-spins.webp

panel-tournament-info.webp

panel-table-info.webp

modal-tournament-registration.webp

The number of assets is NOT a problem.

Quality and consistency matter more than minimizing file count.

## ================================================== 14. TOURNAMENT INFO EXAMPLE

Tournament Info artwork must already visually contain:

TOURNAMENT INFO

BUY-IN

PLAYERS

PRIZE POOL

START TIME

LATE REG

All icons, dividers, frame construction, title treatment and labels
must be part of the artwork.

ONLY these example values should be live overlays:

$150 + $15
18 / 150
$24,750
8:00 PM
Level 8

The component should look almost finished even if all live values
are temporarily hidden.

## ================================================== 15. PLAYER NAME CONTROLS EXAMPLE

Player Name Controls artwork should already contain:

PLAYER NAME CONTROLS

TABLE TYPE

INSURANCE

Info icon

Input housing

Select housing

Slider housing

Permanent decorative hardware

Only these remain interactive/live:

typed player name
current table type value
slider knob position
other genuinely changing values

Do NOT assemble the visual panel from CSS.

## ================================================== 16. INPUT ARCHITECTURE

Create the full input visual as artwork.

Place a real semantic input over the designated black-glass entry
zone.

The input itself should have:

transparent background
no generic border
no generic browser styling

Only typed text should be visible above the finished asset.

Maintain:

keyboard behavior
focus
mobile keyboard
accessibility
validation

## ================================================== 17. SELECT ARCHITECTURE

Prebuild the complete select artwork.

Bake:

label
arrow
frame
icon
hardware

Only the currently selected option renders live.

Keep a real accessible select or equivalent semantic interaction.

## ================================================== 18. TOGGLE ARCHITECTURE

Use finished rendered states:

toggle-on.webp
toggle-off.webp

Do not construct the toggle hardware using generic CSS.

The semantic checkbox/switch remains functional underneath or over
the artwork.

## ================================================== 19. SLIDER ARCHITECTURE

Use:

finished slider-track artwork

-

finished knob artwork

Only the knob position changes dynamically.

Do not draw premium slider hardware using CSS.

## ================================================== 20. INTERACTION STATES

For premium controls, create actual visual states where necessary:

default
hover
pressed
selected
disabled

If the physical visual change is meaningful, use a dedicated
rendered state asset.

Do not use brightness(), grayscale(), or generic box-shadow as the
primary visual state system if it reduces realism.

Lightweight overlays may be used only when visually indistinguishable
from high-quality rendered artwork.

## ================================================== 21. MOBILE FIRST

Every component must be designed for mobile.

Test:

320px
375px
390px
430px

If the desktop artwork does not scale elegantly, create a separate
mobile asset.

Examples:

panel-tournament-info-desktop.webp
panel-tournament-info-mobile.webp

wallet-club-bank-desktop.webp
wallet-club-bank-mobile.webp

Do not compress desktop art into unreadable mobile geometry.

## ================================================== 22. BUILD ONLY THREE PROOF COMPONENTS FIRST

DO NOT rebuild the entire library yet.

Restart using exactly THREE proof components:

1. JOIN TABLE

2. TOURNAMENT INFO

3. CLUB BANK WALLET

These three prove:

- fixed-label action button
- structured information panel
- dynamic financial row

Nothing else should be produced until these three are approved.

## ================================================== 23. PROOF REQUIREMENTS

For JOIN TABLE provide:

- final full-size master artwork
- desktop production asset
- mobile production asset if necessary
- default state
- hover state
- pressed state
- disabled state
- real semantic button implementation

The words JOIN TABLE and permanent icon must be baked into artwork.

For TOURNAMENT INFO provide:

- final full-size master artwork
- all permanent labels baked in
- only live values overlaid
- desktop
- mobile
- stress-test values

For CLUB BANK provide:

- final full-size wallet row artwork
- CLUB BANK baked into art
- bank icon baked into art
- only balance live
- stack-safe desktop
- stack-safe mobile
- extreme-value test

## ================================================== 24. /DEV/CLUB-UI IS THE ONLY ACTIVE UI TARGET

Build these proof components ONLY in:

/dev/club-ui

Do not deploy them into production application pages.

Do not modify World Hub.

Do not modify Commander.

Do not migrate Club Arena production pages.

Do not modify the live poker table.

## ================================================== 25. ROLLBACK / PREVIOUS MIGRATION WORK

Do NOT attempt another application migration during this restart.

Preserve the previously documented rollback information.

Any already-merged premature World Hub/Commander migrations should
remain handled by their dedicated rollback agents.

Button Builder does not own those rollbacks.

## ================================================== 26. CLEAN RESTART INSIDE THE FACTORY

Archive/reject the old generic shells for production use.

Do not delete history.

Mark them clearly as:

REJECTED / LEGACY / DO NOT USE

Examples:

generic utility shell
generic navigation shell
generic runtime-assembled panel approach

Do not let future developers accidentally reuse them.

## ================================================== 27. VISUAL REVIEW GATE

Do not declare success based on code or tests alone.

Deploy the private /dev/club-ui lab to an accessible preview.

The three proof components must be visually inspected.

Do not proceed beyond them until approved.

## ================================================== 28. STOP CONDITION

After building:

JOIN TABLE
TOURNAMENT INFO
CLUB BANK

STOP.

Do NOT build the next controls.

Do NOT migrate applications.

Return:

ASSET-FIRST #CLUBBUTTONS PROOF READY FOR VISUAL REVIEW: YES / NO

Also provide:

- preview URL
- three master images
- three production implementations
- mobile screenshots
- desktop screenshots
- dynamic-data examples
- explanation of what remains live versus baked in

---

## FINAL COMMAND

STOP THE CURRENT VISUAL IMPLEMENTATION.

PRESERVE THE GOOD ENGINEERING FOUNDATION.

REJECT THE GENERIC FRAME / GENERIC OVERLAY APPROACH.

RESTART BUTTON BUILDER AS AN ASSET-FIRST,
HYPER-REALISTIC COMPONENT FACTORY.

BUILD ONLY:

JOIN TABLE
TOURNAMENT INFO
CLUB BANK

Make each one look like a finished premium casino hardware asset
before dynamic values are overlaid.

Then STOP and wait for visual approval.
