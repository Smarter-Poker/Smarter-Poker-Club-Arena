# Raster State Variants

The foundation implements hover, pressed, selected, disabled, loading, updating, stale, error, and offline with lightweight overlays. Store a raster state here only when a state changes the physical artwork in a way that cannot be represented without lowering fidelity.
