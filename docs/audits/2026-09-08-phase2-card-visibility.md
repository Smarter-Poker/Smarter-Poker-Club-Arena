# Phase 2: Private Cards And Reveal Resync

C08/G01, September8,2026. Two actual-engine snapshot tests reproduced voluntarily shown cards disappearing from HTTP resync while remaining visible on the live broadcast. One folded and one mucked hand each selected exactly one card. HTTP now preserves the same selected card and null placeholder; owners still receive their own full hand, and no selection becomes public before the hand ends.

Baseline:2failed5passed after correcting the test fixture to initialize the optional showHandCards map. The first draft's null-map errors were fixture errors, not production failures. After correction:49passed5files, server TypeScript passed. Coverage includes folded/mucked privacy without consent, all-in tabled hands excluding folds, no deck in public state, private delivery isolated by user AND table, and no private-event replay to a newly subscribed spectator. Existing showdown and hub-replay tests pass.

Production SQL read-only verification at23:12UTC: table_hole_cards, hand_discards and ca_hand_facts all have RLS enabled with own-user SELECT policies. For each, a transaction set the authenticated role and a locally scoped existing owner identity: an owned row was visible and another user's row was not. Anonymous table_hole_cards SELECT returned no row. No card contents or user identifiers were exported. hand_state_snapshots has forced RLS and neither anon nor authenticated has SELECT. insert_hole_cards has execution permission only for service_role among these roles and is SECURITY INVOKER. These are actual installed permissions and role-scoped read checks, not a full Auth/PostgREST/session audit.

The public HTTP handler authenticates the request, authorizes table viewing, and passes auth.userId rather than a caller-supplied player ID. WebSocket user identity is taken from verified authentication. Private hole-card delivery captures the hand number before retry and sends only to the user's sockets for that table; private events are not retained. Direct stored-card reader RPCs and their authority guards remain under review. The legacy getObserverState method has its own observer-show option; current HTTP uses getTableState after viewer authorization, so broader observer-option wiring belongs to Phase8 and is not certified here.

Logs: /tmp/phase2-card-visibility-before.log (fixture errors), /tmp/phase2-card-visibility-baseline.log (two actual failures), /tmp/phase2-card-visibility-after.log, /tmp/phase2-card-visibility-tsc.log. CI, automatic merge and deployed adoption pending. Phase2 is not complete.

Stored-reader follow-up:12 authenticated-callable routines directly reference these card stores. Their full installed bodies were reviewed for hidden-card output. Personal class/grid/EV/nemesis routines use ca_assert_self before own-user facts; rake stats uses caller identity. Club-member routines return aggregates rather than cards. fn_ca_operator_read_hand requires club control, checks the hand's club, requires a reason and writes an audit record before returning holdings. Read-only authenticated negative calls to another user's class-hands RPC, get_active_hand_snapshot, and unauthorized operator read each returned SQLSTATE42501. No views or materialized views depend directly on the four stores. This is a direct-reader review; transitive wrappers and full session authorization remain Phase6 scope. fn_nit_check accepts another user's ID and can expose aggregate VPIP when enabled; it returns no hole cards. Its caller authorization is an explicit Phase6/9 follow-up, not certified by this card-privacy check.

The reveal branch's first push hit the same PendingAddOnIdleSweep fixture failure (1failed433passed18skipped). The already-tested database-isolation correction from cc30d85d5aa856f3aba1c3b6269e7ee9dad4be2c was applied identically to that one fixture; no production add-on behavior or assertion was weakened. New push verification remains required.
