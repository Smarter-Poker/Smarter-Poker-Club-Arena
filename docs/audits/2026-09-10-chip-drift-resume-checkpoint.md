# Chip Drift Continuation

This is Phase 3 of the 109-item accounting programme. Phase 3 is still open.
The separate 58-control tournament audit remains a source of reusable work.

## Recovered And Verified

The exact-seat hand correction is now adopted by the engine. Live health shows
no stalled tables or blocked settlements and completed resumption of 282 tables.
Registration request receipts, legacy alias retirement and durable seat refunds
are already published. Their stale pending fields in the accounting register
are corrected, with exact release and catalog evidence in
`2026-09-10-chip-drift-resume-evidence.json`.

The frozen verification route had a separate reproducible defect: its disposable
maintenance fixture loaded an obsolete predicate. The harness now loads the
verified release-aware function and real certificate reader. It preserves every
production preflight check and rejects the older predicate explicitly.
Eight maintenance behaviors, seven refusals and 31 source-law assertions pass.
Root TypeScript passes. The server suite passes 1,398 tests in 132 files.

The full hand path passes in legacy and exact-seat modes, including rollback,
replay and four invalid-generation attempts. The newly merged registration
funding rehearsal passes seven groups using real financial writers for the
ordinary pre-start standalone-club path. Neither result closes every entry path.

## Exact Remaining Gate

The strict cutover file installs on an isolated schema with actual production
private-core ACLs, but complete financial behavior is not yet certified.
The inherited broader Stage 1 snapshot fails several source and fixture gates;
its independent rebuy conservation assertion also remains unresolved. Its
guarantee overlay probe passes. These mixed results are not proof of either
production defects or complete acceptance. A fresh direct schema-only export
timed out; it captured no production row data.

Production still lacks the new tournament seat-move RPC and receipt resolver,
and all seven tournament safeguards remain disabled. The next cutover must
prove current seating, terminal payout, rebuy, Spin, cancellation and ticket
behavior against their actual dependencies before changing that state.
Do not blindly re-enable guards or repeat old migrations whose prerequisites
have since been retired. All twelve accounting controls remain open until
their own acceptance and release evidence is complete.

No production DDL, balance correction, incident closure or forced restart was
performed in this continuation. A compatible engine dispatch was already
active, and the deployed runtime was then verified directly.

Owned worktree: `codex-chip-drift-resume`. Owned isolated PostgreSQL cluster:
`/tmp/codex-chip-drift-cutover-e2iav203`, port 55473, local socket only.
Do not modify the earlier chat's donor cluster or another agent's worktree.

## Publication And Standing Authorization

The user explicitly authorized pushing and publishing this work and future
repository pushes/publications on 2026-09-10, and requested no repeat
confirmation. The earlier automatic-review blocker is resolved. Normal push
opened PR 4084, required CI 34428840067 passed, and it merged as
566b66e07582fd97b77d90abfd20e9f2068ea941. Both frontend endpoints serve
80d76e0b2fe5ad9593a82cbb0580b50d03a62896, which contains that merge.

## Next Correction

The prepared strict cutover names the old seat-move RPC but omits the current
engine route. PostgreSQL reproduced admission of an unmarked service actor
without manager authority. The current route now uses the existing fresh
lease check, and its postflight requires that route. All 32 real-hook admission
scenarios pass. The full prepared SQL also installs on the isolated schema.
Receipt recovery after lease loss and authenticated player rebuys remain valid.
This source correction does not authorize prematurely activating the whole
financial cutover. Phase 3 remains open.
