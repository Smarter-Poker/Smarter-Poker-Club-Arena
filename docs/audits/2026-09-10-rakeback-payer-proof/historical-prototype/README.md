# Historical Precheckpoint Artifacts

These original local artifacts are retained byte-for-byte for audit history. No current proof runner references them. The initial plan, pre-fix synthetic result and captured installed definitions are not current success evidence. The old retirement proposal is superseded by the separately committed retirement checkpoint and must not be applied as a new migration. SQL is stored as text to keep this historical material out of execution paths. The manifest preserves each original name and SHA256.
