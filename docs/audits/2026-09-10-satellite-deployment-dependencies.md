# Satellite Proposal Deployment Dependencies

Read-only live verification on 2026-09-10 confirms the final wrapper body remains `486d0e6729de8d518d7faf0c253b65d3`; the proposal requires `6babe5451efa9bc2bfa12cb0fede27e2`. The separately owned `fn_settle_satellite_tournament_seat_exit_core_v2(uuid,uuid)` is absent. The prepare, complete, outcome-resolver and own-qualification read RPCs are all absent. No activation was performed.

| Local commit | Changes                                                                                                                       | Deployment dependency                                                                                                                    |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `33aac7681`  | Exact-K migration, schema and new RPCs; manager/transport/recovery callers; parser and tests                                  | Hold publication. External Stage-B final-seat authority must be verified, then exact-K SQL installed and verified before callers deploy. |
| `9bb88f1e0`  | Qualification UI, read DTO parser/service, TablePage callbacks and tests                                                      | Hold caller publication until the new own-qualification RPC exists and returns the verified DTO.                                         |
| `3a392a573`  | UI source-hash evidence                                                                                                       | Proof only; no runtime dependency.                                                                                                       |
| `0b463e71f`  | Corrected final-verifier postpayment rollback, actual SQL-role tests and evidence                                             | Proof only; no runtime dependency.                                                                                                       |
| `61c83e564`  | Spin peer review and historical lease-liveness review note                                                                    | Documentation only. Its historical FOR UPDATE observation is resolved by the later liveness commit.                                      |
| `ba84a5b91`  | KEY SHARE correction in the same undeployed exact-K migration, exact claimant/heartbeat prerequisite gate and native evidence | Hold migration activation with core. Requires the reviewed lease owners and external Stage-B prerequisite.                               |

Publishing core callers first would park eligible exact-K source engines and retry missing preparation RPCs. The new satellite COMPLETING recovery branch also skips recovery when its qualification-boundary relation cannot be read. This would obstruct existing satellite recovery without the database schema.

The UI requests the new DTO for an unranked-winner or durable qualification path. Missing RPCs throw and enter retry handling; an ordinary ranked result does not request it. The caller is nevertheless held under the requirement that deployed callers have real RPCs.

Required order: independently owned Stage-B deployment and verification; exact-K migration with all current source gates verified; current-main integration and verification; manager/UI publication; live end-to-end qualification acceptance. The local native proof does not certify full production financial composition: seven observed live guards remain disabled, including the financial-certificate guard. Running-target physical assignment and process-level recovery/browser acceptance remain open.
