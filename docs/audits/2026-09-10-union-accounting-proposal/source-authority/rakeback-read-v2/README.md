# Captured Rakeback Read V2 — Unactivated Checkpoint

The actual getter in `02-getter.sql` implements the payer lane's ABI v2.
It reads accepted generation/hash sources and immutable payment evidence in
one statement snapshot. Financial identity is the captured club, original
Union, route, generation and payer. Current membership is not a debt filter.

Valid prebank entitlement remains visible with a nullable compatible pool.
Closed UTC weeks carry exact fractions within that scope; pending cash is
the cumulative floor minus actual paid cash. Open weeks remain visible but
do not enter closed totals or spending. Whole-cent payment DTOs come from
the shared actual payment helper, with exact source/week allocation slices.
The getter refuses internally inconsistent cash/slice totals. Genuine
unassigned and invalid contracts stay explicit; self-agent zero rebate
policy creates no player liability. Missing booked-club facts use the
requested club only as unresolved display context, never a financial retag.

The source capture guarantees a hierarchy JSON array containing numeric
contract fields. The getter preserves the actual fractional 70% agent
policy, minimum player margin and source-specific club funding fit.
Raw member override 15 cannot fit the real numeric(5,4) column; stored
invalid 1.5 remains unresolved, with no clipping or percent conversion.

`01-capability.sql` creates an EMPTY private append-only authority table.
It supplies no activation RPC or production witness. A complete cutover
must provide an explicit reviewed integration evidence hash and canonical
function-definition pins, including the actual public accepted owner,
source boundary, bank/receipt assertion, release/payment owners, read/claim
and separate legacy Union, Round2, direct-agent and player paths. Aliases
cannot stand in for those owners. This manifest is not proof of every
schema, trigger, ACL or producer dependency. Hashing today's unexcluded
legacy functions is not real cutover acceptance.

Run with `PAYER_PROOF_DIR` set to the companion
`docs/audits/2026-09-10-rakeback-payer-proof` directory:
`bash docs/audits/2026-09-10-union-accounting-proposal/source-authority/rakeback-read-v2/run-local.sh`.
The companion native runner uses PostgreSQL17 on a private Unix socket.
Its exact framework hashes are recorded alongside local inputs.

All 17 native groups passed. Actual accepted, bank, Round1, Round2 and
player money owners run; no money primitive is replaced. Closed dates are
explicit synthetic fixture timestamps. The native-only capability witness
is also synthetic and does not claim legacy exclusion or rollout approval.
Exact legacy declarations are captured solely for canonical pin existence;
they are not executed. Their deferred body checking avoids expanding
unexecuted legacy dependencies, and normal checking is restored before the
new getter and capability functions are loaded.

`native-proof.json` records the scope, hashes and checks.
`native-read-prebank.json` and `native-read-cross-week-payment.json` preserve
actual SQL responses for browser contract verification. The latter proves
0.009 + 0.009 exact earnings, 0.01 actual cash, source slices 0.009 + 0.001,
and 0.008 remaining exact entitlement.

The vendored player schema/DTO/claim declarations are fixture inputs, not
additional production owners. This lane verifies the getter; the payer lane
owns claim replay/concurrency proof and final integration. Real legacy
exclusions, complete outer cascade, producer/funding finality and coordinated
browser/backend activation remain separate release gates.
