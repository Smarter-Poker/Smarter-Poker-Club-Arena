# CHIP DRIFT Phase 3: reconnected activation review

The exact activation candidate is prepared and its outside-window native rehearsal passes 138 assertions with exact business, catalog and migration-history rollback. The 15 creation assertions, 27 edit assertions, TypeScript check and 31 creation-mapping tests also pass. This does not certify completion of all twelve Phase 3 controls.

The engine rollout succeeded through the existing workflow. A fresh database leader and public health identify c58dfafd. The entire required server tree is byte-identical to that deployed commit, the timer repair is an ancestor, and all four required client files match the published frontend. Public health remains HTTP503 from statistics lag; liveness and settlement status are okay, with zero blocked settlements and zero stalled tables in the recorded observations.

## Production activation result

The Supabase application attempted the exact 275856-byte candidate with SHA-256 `9e0771e13df999bcf4fc3809865afa53d6b70f2eb947be531c4894dca9e148b0`. It was refused with SQLSTATE55P03 because `public.engine_tournament_leases` was busy. Readback confirmed the original Stage A hook, absence of the new satellite scope and zero migration-history receipts. No lock was killed, timeout extended, guard removed or automatic retry performed.

A separate active continuation's saved record, discovered after this attempt, records automatic approval rejection of the same candidate because its production privilege, legacy-RPC and shared-lock changes require specific approval. Production mutation is stopped. The candidate is reviewable at `scripts/deploy/phase-three-activate-current-terminal-authorities.sql`; approval must name the seven-guard activation, exact satellite and manager scope, route/legacy retirement and bounded shared locks. A suitable quiet window is also required. The separate exact-consent activation remains pending.

## Native environment scope

The recovered production migration receipts are byte-pinned. Native initdb uses a superuser postgres role, while Supabase postgres is not a superuser. Only the historical installer's role-fixture assertion is adapted; the actual production guard function definitions and event triggers remain unchanged. The native fixture restores the exact deployed service-role EXECUTE grant before the bootstrap migration's unchanged preimage checks. All source checks pass. This certifies the ordinary outside-window path with current guards, not the stopped-engine transport exception inside the protected interval. No production override is proposed.

## Remaining acceptance

The signed-in site displays a live connection, but its terms acknowledgement blocks interactive testing. The agent has not accepted terms. The detailed twelve-control review also identifies remaining Spin terminal, runtime/clock, offered-option persistence and browser evidence; those controls remain open rather than being closed by aggregate test counts.

Another continuation is actively editing the original recovery worktree. This publication is isolated at `codex-chip-drift-final`, inherits its completed commits, and verifies that every copied native source input matches the passing evidence. Active original work, incident payloads and unapplied migration reservations remain preserved in the original tree.
