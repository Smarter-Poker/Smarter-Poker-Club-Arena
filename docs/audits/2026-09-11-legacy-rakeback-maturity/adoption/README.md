# Held standalone adoption checks

No production apply, financial RPC or migration-history write was executed by this agent. Root reported automatic approval rejection of the exact live security-definer financial owner/guard change because the broad audit/push/publish approval was insufficient. No retry, alternate route or override is authorized by these artifacts. They preserve read-only checks for a future specifically approved root-coordinated application.

The source remains the immutable 988db864469e0d2543b969fc0032c4d8eb7b9fa3 subtree and exact atomic entrypoint SHA256 a55e792f12040799a20fcf6d54969059efecaea3869c3aa148191fe1b083c4c7. The reserved normal migration filename is 20260911070726_legacy_rakeback_closed_period_single_payer.sql. The Supabase apply tool accepts the bare snake_case name legacy_rakeback_closed_period_single_payer and has no version parameter. These artifacts do not alter or replace the source SQL or the reserved file.

## Read-only queries

1. 01-admission.sql checks original owner/body/normalized ACL identities, absent new guard/helper names and capture state, receipt uniqueness, the current append-only identity, relevant relation-lock counts, and the existing break-window refusal helper. Every owner must pass, required state must hold, and break_window_refusal must be NULL at application time. The helper refuses UTC minutes 50 through 02; no override is permitted. The atomic source retains its own one-second lock timeout and ten-second statement timeout.
2. 02-malformed-closed-periods.sql scans at most 5001 closed pending rows and returns counts only. The complete-for-filter flag must be true and the four malformed counts zero before claiming this limited data prerequisite passed. The 07:07:21 UTC observation covered 1,767 rows.
3. 03-post-apply.sql verifies exactly six replacement/internal function bodies, postgres ownership, SECURITY DEFINER flags, normalized ACLs including grantor/grantee/EXECUTE/grantability, exact search paths, parameter names/default counts and result types. It also checks the two enabled noninternal row-level BEFORE triggers, their exact functions and status-column filter. all_runtime_checks_pass must be true after a specifically approved apply. It currently returns false because nothing was applied.
4. 04-migration-receipt.sql reads only the reserved version or matching semantic name and reports source/statement-array hashes. The post-rejection batch returned no matching receipt. A future application receipt must be matched to exact source and installed runtime; a bare success response is insufficient.

## Migration history identity conditions, not an executable repair

The tool exposes no requested-version argument. If a specifically approved successful application later generates another version, root must first preserve that exact autogenerated version, exact semantic name, the statement array and its hash, the submitted source SHA256, and passing six-function/two-trigger readback. No migration SQL may be rerun solely to obtain another version.

Any separately authorized metadata alignment must lock only that verified generated row, require the reserved destination absent, require the exact name and unchanged statements hash, prove the stored representation corresponds to the exact reviewed source bytes, update only version, assert one affected row, and verify both resulting identity and unchanged statements afterward. A concurrent destination conflict must abort atomically. If parser splitting or formatting prevents source-byte equivalence, stop for explicit evidence review; do not weaken the check or assume a join reproduces the source. No executable history mutation template is included here.

The automatic rejection supersedes the earlier proposed application flow. The 07:14:36 UTC read-only check shows all three original payment owners unchanged and candidate helpers/triggers absent. No matching history row exists. These are scoped observations, not production acceptance, full-data cleanliness, or a promise about later contention. The source candidate's old-caller deadlock retry and current-guard native-coverage limits remain in its existing README.
