# Seven Tournament Guards: Native Rehearsal

Prepared on September 11, 2026. No production application or engine restart is claimed.

The current normal terminal caller did not create the immutable finish claim required by the disabled COMPLETING guard. Simply enabling the seven guards on the preserved E2 fixture refused normal completion. Its older atomic-place guard also lacked a contract for the current, strictly receipted place writer.

The additive migration reuses the existing canonical version 2 place-batch and readiness contracts. It verifies the same bust-witness ordering as the installed place writer, and obtains the existing finish claim inside the normal terminal transaction. A later failure rolls back that claim with payment. All seven reviewed trigger definitions are pinned before alteration, all seven are enabled in one transaction, and function ownership, grants and configuration are preserved. The new batch verifier remains owner-only. No historical standings, payments, receipts or other application rows are rewritten by the migration.

## Reproduce

Run `python3 scripts/ci/rehearse-d12-tournament-guards.py`. On Linux, pass `--pg-bin /usr/lib/postgresql/17/bin`. The runner accepts no production database destination. It creates an isolated socket-only PG17 cluster, verifies the sealed fixture and source pins, executes the migration and real functions, records evidence and stops its cluster even after failure. The readable runner and lifecycle SQL must match their sealed copies.

The fixture preserves 521 observed function bodies and 263 trigger definitions/states. Its original schema donor omitted function ACLs; the runner restores separately observed exact ACLs for five prerequisites before applying the migration. This is a local fixture correction, not a production ACL change. Additional source reads were limited to the actual launch, entry-close and cancellation call graph, one missing table schema and its trigger.

## Verified Behavior

- E2 completes through the real terminal function with 17 payouts totaling 304.00, all 167 places correct and replay unchanged. Historical bust sequences are not rewritten.
- Eighteen native lifecycle cases include lease-bound launch and replay; an open entry window; Free Buy entry closure deferred to its promised add-on window; add-on finalization and replay; zero-value cancellation with all three seats released; and seven refusals for premature or forged transitions. Creation uses actual publication triggers. Opening roster and custody fixtures are synthetic; operational calls run with triggers active. Deferred constraints are checked before the synthetic lifecycle transaction is rolled back.
- Two native migration attempts with an unexpected client grant or changed function owner refuse and roll back. All seven guards remain enabled after real completion. Service-only entry points and owner-only verifiers retain their security boundaries.
- Separate D1 and D10 composition receipts exercise the reviewed historical rulings. D10 completes before this migration; twenty application-table hashes remain unchanged across migration and exact terminal replay.
- The D8 workstream also exercised a funded exact refund of 35.00 with these seven guards active. Its satisfied replay correctly refuses and leaves the complete money/receipt snapshot unchanged. This composed proof is explicitly identified by both migration hashes; it does not replace the standalone D8 review.

## Remaining Work and Activation

This is the seven-guard restoration slice of D12. Satellite/final-table-deal bust ranking, non-satellite ruling retirement, legacy events lacking historical elimination sequences and other D12 investigations remain separate work. D9 has identified an unfinished partly paid event requiring a preserved-history disposition; no blanket refund or fabricated winner is authorized by this migration.

Before activation, reconcile the current production owner, function/trigger pins, named-event prerequisites and external release state. Use the existing permitted migration window, detached execution and receipts. The migration has a two-second lock timeout and 120-second statement timeout. Neither this rehearsal nor a backup push proves production activation or 24-hour engine qualification.
