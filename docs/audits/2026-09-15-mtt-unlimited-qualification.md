# R46 protected qualification supplement

Status: required work, not passed evidence. Derive the complete repository catalog again for the final source revision and qualified providers. This supplement adds requirements; it cannot replace the full required Club Arena catalog or authorize direct local execution. Read `/Users/smarter.poker/Documents/AGENTS.md` and the pipeline operator guide before any execution or handoff. No approved execution identity is currently assigned to R46.

September 16 route update: the current owner override restores existing GitHub workflows with local self-hosted computation. The new maintained `scripts/ci/test-mtt-unlimited.py` is wired into their existing accounting job; the discontinued custom native catalog is not required. Its captured actual schema, missing-authority supplements and strict result consumer have source review and focused pure regression evidence. An allocated real PostgreSQL run and final required CI still have to establish native/application acceptance. Existing final publication locks and coordinated schema/engine compatibility remain required.

Read the current `/Users/smarter.poker/Documents/AGENT-HARDENING-STANDARD.md` as well. The [finite R46 checklist and wiring record](2026-09-15-r46-hardening-checklist.md) identifies the exact completion items, existing unit discovery, missing native-probe invocation and dependency-preparation ownership. No recurring repair or release mechanism is authorized.

## Source and schema intake

- Bind final local revision, changed-file manifest, migration bytes, both applications and all SQL probe bytes to the protected request. Reconcile captured `pg_proc.prosrc` hashes against fresh admitted schema history; inspect owner, security, ACL and search-path metadata as well as body text.
- Compose the complete current function/trigger/constraint graph in an isolated native fixture. Include auth/session guards, maintenance serialization, union hosts, the unique Diamond arena identity, immutable contract guards, launch generations, escrow, ledgers and ticket obligations. Do not substitute the historical reduced ticket composer for this graph.
- Apply the migration once and verify exact postimages, max-column nullability, fixed-field constraint, normalization-before-creation trigger order and private/service ACLs. Inject one function-body drift and one constraint drift; each must roll back the entire migration without partial schema changes.
- Exercise the real nested loader and compiled runtime composition with qualified providers. Mock-only tests and source-string assertions cannot satisfy runtime admission.

## Client and engine catalog

- Run all affected creation, schedule, lobby, details, routing, management, fee, discovery, seating, blinds, break, payout and satellite tests plus app/server TypeScript and required build/lint/contract checks.
- Render new and restored MTT/satellite drafts at phone width. Verify there is no maximum-entry control or denominator, no new capacity waitlist, no “full” refusal after a stale numeric cap, and no accidental direct-seat route. Fixed SNG/Spin and physical seat controls remain correct.
- Exercise the real scheduler with NULL and legacy two-player MTT/satellite metadata: no early start, three-player minimum, full selected blind ladder, synchronized break, normal resume and bounded isolated failure. Registration and late registration must grow beyond the old cap and allocate additional physical tables safely.
- Exercise recurring satellite target selection, configured pure re-entry economics, future feeder-relative cutoff, same-named events with different targets, existing feeders before a missing target, and unknown/malformed creation receipts. Existing feeders must not spend the creation budget; total attempts stay bounded.
- Exercise actual restart copying with both target columns, a changed current clock and a prior grandparent pointer. Every child uses its immediate source identity, keeps its exact upstream target and remains an unlimited MTT. Missing/closed/unsupported targets refuse new clones.
- Exercise manual plain-type feeders with explicit bounty/Spin configuration as well as bounty-family type labels. Refuse incompatible payout mixes without silently dropping the requested funding contract.

## Native single-session sources

- `scripts/ci/probes/mtt-unlimited-entry-cap-native.sql`: actual helpers and trigger bodies, known/unknown classification, legacy MTT admission, fixed-field refusal, physical table sizes, waitlist rejection and new MTT minimum. Its isolated trigger tables do not establish a complete funded registration.
- `scripts/ci/probes/mtt-unlimited-ticket-redemption-native.sql`: explicitly enables R46 cases in the current authenticated redemption probe. Include past-old-cap redemption, exact prize/fee split, identical replay and genuine fixed-field full refusal. The older composer without this wrapper is not R46 evidence.
- `scripts/ci/probes/mtt-satellite-creation-native.sql`: real creator and installed guards; ACL/body refusal, input and minimum-funding refusal, freeze, Diamond/ambiguous bounty refusal, finite horizon, one created feeder, same-target changed-name replay, legacy target spelling and active feeder after cutoff. It also exercises restart identity rollback, different start timestamps, immutable pointers, deletion refusal, terminal children and A→B→C chaining through real insert guards. Uses synthetic owner/target seeds and the existing unique Diamond arena. No funding or terminal proof is claimed.
- Existing current cancellation, full-terminal, ticket-return and bounty/prize probes remain required. Historical cloned-authority probes retain their historic scope and cannot qualify changed current functions by themselves.
- The creation probe also updates a real upstream target through both label columns with each incoming pointer spelling. Unsupported labels with false flags must refuse; an existing feeder cannot add a PKO split. Verify unchanged historical rows and supported plain SNG/nested target contracts separately. Synthetic union-host currency transitions remain required; ordinary club currency immutability does not establish that case.
- The same probe then uses separate Repeatable Read, Serializable and Read Uncommitted transactions with isolated rows attached to the actual guard. Require exact `0A000` refusal for a linked insert and a substantive target edit even when the snapshot shows no incoming feeder; preserve no-op and status updates. These cases are authored, not executed. Read Uncommitted is outside the explicitly admitted mode even though PostgreSQL treats its reads like Read Committed.

## Mandatory multi-session and fault cases

These require the protected full-fixture driver; authoring a SQL probe is not evidence that they ran.

Six concrete stock PostgreSQL isolation-test assets now cover the two same-target creator outcomes, unsupported target-label edits in both orders, and the two restart-worker outcomes. See [the fixture and integration requirements](../../scripts/ci/probes/mtt-isolation/README.md) and its `catalog-candidate.json`. Each spec has one explicit permutation in a fresh database. Actual backend PID registrations and a separate lock observer require genuine contention. Exact committed identities/outcomes, unchanged enumerated financial history and all seventeen zero escrow monetary amounts are asserted. The target-first case uses a direct real-table insert to exercise the common trigger's post-wait check.

These sources were independently reviewed and remain unparsed, unexecuted and unenforced. The candidate manifest neither installs a catalog nor supplies admission. The pipeline owner must qualify the full-schema fixture, stock binary, strict result consumer, actual invocation and stopped-worker/database-disposal proof. A raw isolation-test exit code is insufficient. The six assets cover only the identified creation/restart/label races; every additional case below remains required where applicable.

1. Hold first owner/target creation transaction after insert. A second same-key call must wait; first commit yields one row and `existing_active` to the second. First rollback instead permits exactly one new row. Repeat with two engine processes and a changed feeder name.
2. Concurrent different-target calls retain independent identities. Union-owner calls use the union host contract, accept member-club and host targets in that union, and refuse other-union or unscoped club targets. No ordinary membership row may be required for the synthetic union host. Exercise legacy feeders whose nominal club differs but effective union owner matches.
3. Serialize entry-maintenance freeze against creation and actual registration. Include target cancellation/start/repricing/unsupported-label changes racing creation in both transaction orders under READ COMMITTED. Verify actual writers use that admitted mode; other isolation modes must refuse relevant creation/contract edits before reading an incoming-feeder snapshot. Include an older snapshot taken before the other worker commits. Preserve the existing lock order, finite deadlines and unambiguous original outcomes; a lost response is not remote rollback proof.
4. Concurrent paid entry, re-entry, late entry, ticket redemption and satellite award beyond a stale MTT cap must preserve one debit/receipt per entry intent and exact integer-unit conservation. The same race against fixed SNG/Spin must retain capacity. Include full-roster stale denormalized counters and several physical table allocations.
5. Inject timeout after a committed satellite creation, entry or award. Recover through durable identity/receipt; do not mint a second fee, ticket, entry or table. Recheck target eligibility at actual ticket delivery and keep outstanding liability visible when delivery cannot occur.
6. Two restart workers read the same completed source and calculate different start times. Exactly one insertion may commit; rollback frees an uncommitted slot, whereas an existing terminal child still consumes it. Exercise wrong owner, non-completed/manual-disabled source, changed target, pointer assignment by UPDATE, attempted deletion and source→child→grandchild chains. Retire old workers that omit the pointer; test this mixed-writer limitation explicitly.

## Funded journeys and release boundary

Run legal-hand journeys for regular, deep, turbo, hyper, rebuy/re-entry, Free Buy, fixed bounty, PKO, mystery and supported satellites: scheduled start; table allocation; multiple blind transitions; break admission/drain/thaw; table moves; eliminations; winner/qualifiers; atomic settlement; closed tables and terminal event. Reconcile wallets, escrow, fee, prize, bounty, tickets and refunds before and after every journey, including duplicate requests and recovery.

Diamond and bounty-target satellite funding remain unsupported. Keep creation refusal until their separate custody/funding contract and full journey are implemented and qualified. Advanced format gaps in the main blueprint remain open; no parity claim follows from this cap-removal work.

The owner has restored publication authorization through the existing delivery path, with concurrent independent submissions/builds and serialized same-target cutovers. Required checks still have to pass. Qualify the old-engine/new-schema transition and coordinate its database/runtime window with the release owner; read back installed function definitions, process versions and served assets. Only fresh observed hands, seating, break progress and reconciled terminal receipts can establish live results. Previous September 14 snapshots are historical evidence, not current health.
