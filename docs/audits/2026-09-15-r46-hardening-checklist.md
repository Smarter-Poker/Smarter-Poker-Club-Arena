# R46 finite hardening and completion checklist

Status: OPEN. This is the current change within the larger MTT assignment, not a declaration that the broader audit is complete. Owner mandate read in full from `/Users/smarter.poker/Documents/AGENT-HARDENING-STANDARD.md` and `/Users/smarter.poker/Documents/AGENTS.md` on September 15, 2026. Use their current versions before final validation. No application, database or pipeline execution was performed for this mandate-adoption checkpoint.

September 16 continuation: the current owner override restores GitHub coordination and existing locally hosted CI. The source gap now has a concrete fixture composer, direct native caller, strict result consumer and existing-job/classifier wiring. Source reviews accepted the new integration. Pure consumer, cancellation/source-binding and classification regressions passed (18, 9 and 8 cases respectively); these are not database or application acceptance. The native source package requests an allocated run of two migration-drift refusals, three probes and six actual races. Native execution remains OPEN.

## Scope and preserved behavior

Deliver unlimited MTT/satellite entry admission and consistent creation, restart, classification and display in the owning Club Arena engine and database. Preserve physical table/deck limits, genuine SNG/Spin field sizes, minimum entrants, time/level admission cutoffs, individual rebuy/re-entry limits, approved fees and all accepted financial plans/receipts. No guessed historical payout/rank repair, new format economics, new scheduled functionality or takeover of lease/movement/pipeline ownership is part of R46.

Existing scheduled tournament creation is business behavior. Changes put correctness in its existing request and authoritative transaction; no new repair loop, observer, cron or scheduled agent task is used. Publication now follows the owner's restored delivery authorization, required checks, migration windows and existing final-target locks; this package has not qualified them.

Original source ownership: `fix/mtt-unlimited-entries-sep15`, based on `9fa0cbad3d71d24ded5f6107aaa72438c7d4253e`, in the root-owned `codex-mtt-lifecycle-sep13` worktree. R46 was retained uncommitted at that September 15 checkpoint. The task's `work/r46-source-checkpoint.json` records those exact changed-file hashes; it is historical source custody, not test evidence for the recovered candidate.

That original tree remains preserved. The recovered integration candidate is now owned at `/Volumes/SmarterWork/.agent-trees/Smarter-Poker-Club-Arena/codex-ca-mtt-release-20260916`, branch `agent/codex/ca-mtt-release-20260916`, base `57bcc6018beb85721dc36e0f97a94d301541c207`. Pipeline's accepted PKO slice is separately submitted as PR4680; this continuation does not re-audit or redeploy it. The finite new source package/captured runtime is retained under the task's `outputs/r46-native-qualification-20260916/`.

## Acceptance checklist

| Item | Completion evidence required | Current state |
| --- | --- | --- |
| 1. Scope and source custody | Current owner instructions read; finite change and preserved behavior recorded; final file identity retained. | Documented; refresh identity after each edit. |
| 2. Defect reproduction | Safely reproduce old capacity, lost target, duplicate restart, target-mutation and stale-snapshot failures; relevant regressions fail before and pass after. | Source-backed counterexamples and test sources only. Runtime reproduction UNRUN. |
| 3. Canonical entry policy | Manual/scheduled/restart creators persist NULL; all affected admission and reader paths ignore legacy MTT caps; fixed games remain finite. | Source authored and bounded review performed; actual connected behavior UNRUN. |
| 4. Creation and restart identity | Exact owner/target checks, durable one-child restart identity, rollback/replay and both legacy target spellings through actual callers. | Source and deterministic commit/rollback race assets authored and reviewed; real simultaneous-worker cases UNRUN. |
| 5. Target and contract protection | Reject incompatible source/target payout mixes and later unsupported target edits; enforce the admitted isolation mode; preserve no-op/status behavior. | Source and native refusal cases reviewed; actual database execution UNRUN. |
| 6. UI and consumer wiring | Create, save, restore, register, refresh and display correct MTT behavior with no max-entry controls, full refusal or cap waitlist; preserve fixed/physical controls. | Source mappings and regression cases present; final rendered flows UNRUN. |
| 7. Full native composition | Apply candidate in isolated current schema; verify source/ACL/trigger/constraint pins, rollback-on-drift and all three new probes using real nested authorities. | Captured real catalog and missing-authority supplements now compose with exact bindings; final new integration independently reviewed. Native execution UNRUN. Legacy rehearsal is not used. |
| 8. Concurrent and interrupted effects | Both-order creation/edit races, registration/re-entry/ticket races, unknown responses and source restart races preserve one economic intent and exact outcome. | Six creation/restart/label race specs and actual-table fixture authored and reviewed; all execution and remaining scenarios OPEN. |
| 9. Direct regression enforcement | Final required catalog includes applicable client/engine tests and native probes; relevant dependency changes trigger them; missing/failed evidence rejects acceptance. | Direct caller wired to existing accounting_postgres; classifier and strict consumer regressions pass. Actual CI/native invocation and required-check enforcement remain OPEN. |
| 10. Final version verification | Protected affected/full-required catalogs, app/server typechecks/build and relevant phone flows; independent review bound to final bytes. | Bounded source reviews and whitespace inspection only; protected results UNRUN. |
| 11. Compatibility and recovery | Qualified schema/engine/client transition; retire old incompatible writers; demonstrate recovery without restoring numeric MTT caps or rewriting accepted contracts. | Transition requirements documented; execution UNRUN. |
| 12. Authorized release and observable result | Once authorized, exact installed/deployed identities; actual seating/hands, blind transitions, break/resume, tickets and reconciled terminal closure. | Owner delivery authorization restored; this package's required checks, installation and live acceptance remain UNVERIFIED. |

Do not check off items 2–12 based on source inspection, mocks, a test file's existence, old results or transport acknowledgment. The detailed cases remain in [R46 qualification requirements](2026-09-15-mtt-unlimited-qualification.md).

## Actual verification wiring inspected

The table below retains the September 15 discovery baseline. Its missing-invocation finding is repaired in September 16 source: `test-mtt-unlimited.py` is selected by the existing self-hosted accounting job, invokes all three probes and six specs, and requires the actual guarded migration plus two rollback refusals. `mtt_isolation_results.py` rejects incomplete output and exit-zero SQL failures. The maintained classifier now selects that job for changes to any of these inputs, with a red-to-green regression. Runtime/CI enforcement remains unverified; the historical composer is still excluded.

| Retained regression | Existing discovery or caller | What this establishes |
| --- | --- | --- |
| `tests/unit/unlimitedMttCreationContract.test.ts`, `unlimitedMttLobby.test.ts` and affected existing client tests | Root `package.json` test script uses Vitest; `vitest.config.ts` includes `tests/**/*.test.ts` and `tests/**/*.test.tsx`. | Source discovery covers these paths. No actual execution or required-gate receipt. |
| `server/src/tournament/tournamentEntryCapacity.test.ts`, `UnlimitedMttEntryCreation.test.ts` and affected engine tests | `server/package.json` test script uses Vitest; `server/vitest.config.ts` includes `src/**/*.test.ts` and requires the server working directory. | Source discovery covers these paths. Actual protected runtime composition remains unverified. |
| `mtt-unlimited-entry-cap-native.sql`, `mtt-unlimited-ticket-redemption-native.sql`, `mtt-satellite-creation-native.sql` | No executable caller found in repository `scripts`, root/server package scripts or historical workflow files. The ticket wrapper includes the existing ticket probe. | Concrete enforcement gap: presence of these files is not automatic invocation. |
| `scripts/ci/probes/mtt-isolation/fixture.sql` and six `.spec` files | Declarative `catalog-candidate.json` lists exact asset hashes, cases and fail-closed intake requirements for the existing protected catalog. No caller, provider or consumer installed here. | Concrete concurrency assets exist; parsing, actual invocation, execution, disposal and enforcement remain OPEN. |
| Existing ticket rehearsal | `scripts/ci/rehearse-existing-ticket-current.py` selects the original ticket probe and installs captured pre-R46 functions, including the old capacity body. | Historical bounded fixture only. Do not alter pins or swap its probe to claim full current composition. |

Current required verification uses the restored self-hosted GitHub workflow. Its existing `accounting_postgres` job now directly selects the maintained caller, all three native probes and six stock isolation cases. That caller binds its source, fixture inputs, migration and case assets, requires complete results, and disposes its own cluster before reporting success. Actual execution and the final revision's required client/engine/typecheck/build checks remain pending. A manual qualification run consumes a root-admitted local execution slot; it does not replace the required PR checks. The superseded custom native catalog/registration programme is not a submission prerequisite.

The six new concurrency cases use stock PostgreSQL isolation-test inputs, one permutation per fresh disposable database. The caller reads each case from the candidate manifest and requires successful actual lock/final-state assertions and every step, not merely process exit; missing markers and uncaught SQL errors fail the result. Native verification must prove worker/connection termination and database disposal. See [the race case requirements](../../scripts/ci/probes/mtt-isolation/README.md). Review identified and repaired a test gap where zero final balances could conceal offsetting escrow activity; every monetary escrow component must now remain zero. No runtime before/after result is claimed.

## Narrow outside-owner dependency

The existing server `pretest` invokes `server/scripts/fix-vitest-esbuild.mjs`. Its fallback downloads a package and copies a binary into the dependency tree, and reports repair failure without terminating that script. This source is directly relevant to how engine tests start, but dependency preparation and protected runner qualification belong to the pipeline owner. It was not executed or modified here. The owner must qualify matching dependency bytes before application execution and demonstrate the admitted route does not rely on this repair fallback. A package-script listing alone cannot establish a safe or working test route.

## Closure record

Record final revision/commit, reproduction results, exact test and native case outcomes, enforcement receipts, independent review, installation/deployment identity and observable behavior separately. Current implemented source remains untested by the required runtime route. The broader MTT lifecycle, historical reconciliation and feature gaps remain in their existing audit records; this checklist neither restarts nor closes them.
