**Decision: Needs Changes — document candidate only.**

Assignment G8-REVIEW-0001, generation 1. Independent reviewer task `01a0923b-657e-78f3-94c7-58584d18073a`; accountable coordinator `01a0908a-36f9-7042-b4a2-02fb14d5a53e`. This decision does not accept or reject any technical implementation, Phase 1, delivery phase, foundation, deployment, or final program. Six documentation changes are requested. Permitted research and recovery can continue.

The reviewed candidate is the 26-file snapshot captured at **2026-09-11T20:49:55.895740Z** from `/Users/smarter.poker/Documents/Codex/2026-09-11/you-are-the-program-overseer-for-2/work/g8/program-records/`. All 26 candidate files were unchanged at the 2026-09-11T20:52:47.096963Z recheck. The sorted candidate hash-list SHA256 is `209186b0ea82338fdb2579de57fbe77c49af908b39732cebb127511b78214f10`. Individual paths, sizes, hashes, source modification times, and snapshot locations are recorded in [the manifest](G8-REVIEW-0001-generation1-manifest.json) and [hash list](G8-REVIEW-0001-generation1-sha256.txt). Findings below use line numbers from that snapshot.

**Verified governing inputs and checks**

The complete handoff is 600,042 bytes, SHA256 `da7f5bb24393d7a30145509d669d5b64925eaf1cae70ffcf0990402d5fe184a6`. Its payload is 584,144 bytes, SHA256 `12904b8264d125e72949ceab6cf87d45f135e2cc062e8afb6946688705867550`, starting at zero-based byte 15,829 and ending before byte 599,973. The package markers and final handoff marker are present. All eight derived prompt copies match their original prompt files and manifest hashes, and each occurs exactly once in the complete handoff.

I independently compared all 921 paragraphs by exact bytes, reconstructed the 337 distinct paragraphs, verified equality with `all-distinct-paragraphs.md` and every entry in `read-integrity.json`, and read the entire distinct text, lines 1–950. This covers all eight distinct project sections, including the complete G8 and G5 governing scope. No summary substituted for omitted prompt text.

Read-only Git inspection confirmed repository `Smarter-Poker/Smarter-Poker-Club-Arena`, branch `agent/g8-rev4/docs/program-coordination`, HEAD `11a37306788567e9b60d6ac1ab32455b865de5c1`, common object store `/Users/smarter.poker/Documents/club-arena/.git`, and the separate G8 worktree index. Exact-revision reads confirmed 109 backlog rows and 216 requirement entries. These are inventory sizes, not accepted or unresolved finding counts. Stored inventory arrays contain 44 PRs, 85 issues, and 20 workflow runs. I did not refresh those network inventories.

The candidate correctly preserves G5-first ordering, queued broader projects, bounded foundation delegates, no new shared runtime reservations, the existing release operator and accounting owner, research before new substantive audit, distinct pre-release and post-delivery checkpoints, and explicit G8-only foundation/final acceptance. It makes no completed technical phase or foundation claim. It correctly treats the Spin evidence as requiring focused applicability review: its accessible G5 manifest reports 31 matching inputs of 39 and 26 isolated groups, with production, browser, and adoption limitations. I inspected that metadata without rerunning its tests.

**Requested corrections**

1. **G8-REVIEW-0001-R1 — P2: distinguish the current draft location from the proposed repository location.** `PROGRAM_INDEX.md:5` calls `docs/audits/club-arena-program/` on the G8 branch authoritative. At the verified HEAD, `git ls-tree` returns no such path, and that directory does not exist in the G8 worktree. The records currently exist only under the main task's `work/g8/program-records/`. Four canonical relative links resolve at the intended repository destination but do not resolve from the actual draft folder. This prevents another task from reliably finding the current coordination record by following the declared authority. State the exact current draft location, proposed repository destination, owner, acceptance/integration status, and repository revision separately. Provide exact-revision recovery references for canonical registers until the relative links become valid. Do not publish or create a second register to satisfy this review; retain the existing release lane. Governing basis: G8-full-prompt.md, “Authoritative Program Records.”

2. **G8-REVIEW-0001-R2 — P2: complete the four support-task assignment envelopes.** `ASSIGNMENTS.json:132–235` omits `full_prompts`, `evidence_inputs`, and `allowed_runtime_objects`, and uses the unresolved text “Own task work/ and outputs/ only” instead of actual task directories. It also omits the concrete document/evidence review checkpoint. The original dispatch supplied stronger restrictions, including no financial tests and no child delegation, but the durable envelope does not fully preserve them. Add the exact main task ID, own absolute directories, relevant full prompt paths/hashes, required source/evidence inputs, metadata-only runtime scope, prohibition on both live and isolated financial tests/runtime mutations, and return checkpoint for each task. Distinguish this task's independent document verdict from G8's acceptance of returned support evidence; G8 authored the candidate and cannot supply its independent document approval. Preserve generation 1 as history, and record any scope amendment rather than silently broadening it. Governing basis: G8-full-prompt.md, “Assignment, Workspace, And Phase Controls.”

3. **G8-REVIEW-0001-R3 — P2: make the immutable source metadata resolve to the preserved file.** `PACKAGE_IDENTITY.json:3` records only `source_filename: complete-handoff.md`, while the candidate preserves `handoff/Club_Arena_All_In_One_Start_Here.rev4.txt`. There is no `complete-handoff.md` in the candidate and no separate source-path field. Preserve the original acquisition name/path as provenance and add the actual archived relative path. The source and payload hashes are correct; do not modify the immutable text. Recording the payload byte range above will also make extraction unambiguous: a naive split at boundary lines includes one additional framing newline. Governing basis: complete source preservation and recoverable revision/hash requirements.

4. **G8-REVIEW-0001-R4 — P2: repair evidence attribution and add reproducible provenance.** `EVIDENCE_INDEX.md:15` attributes the 58 original controls, four verified IDs, and 54 open controls to `2026-09-10-phase3-independent-release-review.json`. That file contains a separate source/runtime/release review and does not contain those counts. The correct count source at the baseline is `docs/audits/2026-09-10-phase3-independent-acceptance.json:9–12`, SHA256 `855c194017826fa520b7c5d1133cdefc01b514bcf463fa93b6892bbefca2f080`. Keep the release-review file for its separate function witness. Also attach exact artifact references to the unnamed narrow Phase 2 acceptance, Spin receipt/applicability manifest, and Stage-B candidate; a bare hash or directory is insufficient to recover the intended evidence. `VERIFICATION.md:5–7` needs original evidence IDs/paths, observation time, method, revision/environment and limitations for its Passed preflight claims. Raw GitHub arrays have update times for individual items but no recorded acquisition command/time; record acquisition provenance rather than treating those update times as the snapshot time. Existing frontend/engine JSON observations are timestamped and should remain narrow identity evidence. Do not rerun financial suites to fill metadata fields. Governing basis: G8-full-prompt.md, “Prevent Duplicate Audits And Unnecessary Tests.”

5. **G8-REVIEW-0001-R5 — P2: version ownership acknowledgments and reconcile the stale accounting revision.** `OWNERSHIP.md:10` identifies local commit `bfa696cb54`. I could independently retrieve the accounting owner's acknowledgment to G8, tool item `exec-7349698c-e0dd-49ac-ab14-3235435ef3f9`, and its later completed message to the release owner, item `exec-600d5d8a-c40c-4e19-a7bf-35e7b2a0010a`, explicitly stating that `e1ee0a1a47` supersedes transient `bfa696`. Preserve the first acknowledgment, append the newer owner-reported revision with message provenance, and obtain/record the full SHA through the owner or read-only object resolution before treating it as an exact artifact. This is not a new write-ownership transfer. Other acknowledgment claims in `OWNERSHIP.md:9,13–15` and `DECISIONS.md:11–13` need durable message/notice IDs and times. Release reports corroborate the lane and named owners, but the direct release-owner reply was not exposed by the accessible current-turn read; its precise permission wording remains coordinator-reported to this reviewer. Preserve the restrictions while adding evidence. Do not infer release permission merely from PR ordering or elapsed time. Governing basis: acknowledgment, liveness, assignment generation, and safe transfer controls.

6. **G8-REVIEW-0001-R6 — P2: record all seven projects' known and unknown state explicitly.** `PROGRAM_INDEX.md:9–11,23–28` gives a correct global warning but only four aggregate worker rows. It does not track the required per-project fields: lead/session or queued status, phase state, baseline version, coverage totals/exclusions, open/blocked/completed counts, current task/owned paths, dependency, latest evidence-backed update, delivery target, PR/deployment state, and next action. Add seven project entries with `Unknown`, `Not Started`, or null values where appropriate and links to the owning records. Keep full G1–G4/G6/G7 work queued; this does not require launching them or inventing their phase counts. Timestamp capability observations as well: `CAPABILITIES.md:7` says no separate task was created, while line 11 records four later creations. Make that chronology explicit and link the observed status snapshot. I independently observed the three sibling support tasks active through `wait_threads(timeoutMs:0)`; the main task's four-internal-agent count is coordinator-reported, not independently queried from this separate task. Governing basis: G8-full-prompt.md, “Authoritative Program Records” and “Session Topology And Messaging.”

**Additional clarifications for the same revision**

`DEPENDENCIES.md:5` should say “reuse applicable coverage and review missing, previously unreviewed, or invalidated scope,” rather than the potentially narrowing “focused deltas only.” The adjacent full-coverage requirement is correct; the old 109/216 inventories are not a maximum scope.

`PROGRAM_ISSUES.json` correctly declares itself a reference index and leaves total unresolved required findings null. Its 17 `GH-CA-*` entries should be clearly labeled external references or pending aliases until the backlog support task establishes their canonical mappings. Do not treat the field name `canonical_id` as an accepted root-cause deduplication decision. The pending mapping is legitimate recovery work and does not justify a second technical repair register.

**Limits and next scoped assignment**

Evidence and tool excerpts supporting this review are in [the evidence file](G8-REVIEW-0001-generation1-evidence.json). No technical source audit, financial test, database mutation, credential access/change, shared-file/index write, deployment, reservation, or child delegation was performed. No sensitive values were copied into the deliverables. A limited indicator scan found no private-key, token, JWT, or email indicators in the candidate; this is not a comprehensive privacy certificate. I did not independently verify GitHub ADMIN permission, the original metadata SQL execution, internal worker slot-release semantics, maximum nesting depth, or all direct owner messages. Current-turn visibility limitations are explicit, not assumed passes.

The next assignment is a **focused independent delta review of R1–R6 and their metadata/link consequences** after G8 revises its own records. Supply the revised candidate hash list, exact change description, and versioned owner/evidence references. Reuse the unchanged immutable handoff read and existing metadata checks. A document Accepted verdict would apply only to the exact revised document snapshot; it would not approve publication, any technical phase, foundation, or final program. G8 must continue its own independent technical evidence acceptance and the existing operator's release checkpoints.
