# Bounded live-table health evidence

The live continuity tests required per-table progress after the default health endpoint stopped returning its entire fleet array. That mismatch failed four production tests before table observation began. Restoring the unbounded array would restore a measured event-loop rendering cost.

Explicit health requests now select up to32 table UUIDs, or up to32 tournament tables in a required MTT/Spin/SNG and club scope. The selected progress records come from the same snapshot as the health verdict and served release identity. Invalid, duplicate-key, mixed, missing and oversized scopes fail before rendering. Ordinary health probes keep their compact summary. Only public category, club, occupancy counts and progress facts are returned; no ownership authority or private hand data is exposed.

The four continuity tests request exact cash-table batches and scoped tournament candidates, then follow those same IDs. All existing hand-advance, inactivity, readiness, maintenance, release and WebSocket assertions remain. Returned counters, response size, unique IDs and requested scope are also checked explicitly.

Verification ona0a56d8a4565713e0633409fa7666ccbfda62d06: server build and client type check passed.32focused tests include the real router and GameServer status path.46existing ownership/routing tests passed after updating method locators for the optional scope argument. The full server suite passed11,064tests with145existing skips (760passing files and one skipped file). The first full run's six old-source-locator failures are retained in the evidence.

A frozen native comparison against4a737fdcdae832e8635cc2a3e40073bda0fbc2af reproduced the missing old array. From2,000 source table records, the current scoped response returned32 rows/7,297bytes, preserved the default compact probe and health/source fields, selected one exact requested table and exposed its next progress increment. Runtime and E2E source hashes match the final tested source. This is offline software proof; four WebKit cases were parsed/listed, not executed against unpublished code.

Protected publication and final production continuity tests remain pending. No deployment guard, maintenance control or production table is changed by this repair.
