# Final Table background floor correction

## What changed

- Removed the curved lower-stage structure from the Final Table broadcast background because it read as a second poker table on mobile.
- Replaced it with an uninterrupted matte studio floor so the actual Club Arena Final Table skin remains the only table in the scene.
- Preserved the existing event-only `final_table` overlay, live seats, and avatar-library player presentation.

## Mobile composition

The portrait background now supplies only the premium broadcast environment. The real gameplay table continues to occupy the dominant central layer, with the arena visible around its perimeter instead of competing with it.
