# Global Footer Lock

- Welds the approved Club Arena navigation footer to every viewport edge where it is enabled.
- Explicitly disables transforms, translations, transitions, and animations on the footer container.
- Extends the footer law so future changes cannot make the shared footer move or slide.
- Keeps the Club Arena root lobby footerless as required; this invariant applies wherever the footer is rendered.
