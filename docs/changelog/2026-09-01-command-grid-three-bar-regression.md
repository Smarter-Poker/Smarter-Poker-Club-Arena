# Six-Tile Command Grid Restores The Three-Bar Ban

- Reverses the navigation-art regression that restored retired three-bar
  binaries, glyphs, SVG geometry, and an opposing regression law.
- Replaces every visible drawer trigger with one shared premium six-tile
  command-grid component; Club Buttons retains its four-tile command grid.
- Restores cache tombstones for all six retired stable image URLs so installed
  clients purge stale copies instead of continuing to display them.
- Expands the production law across source and generated output, including
  glyphs, exact path geometry, three-rectangle geometry, asset names, runtime
  references, wiring, cache retirement, and the approved header hash.
