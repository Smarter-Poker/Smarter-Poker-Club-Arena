# 2026-09-05 - chip standard: the Phase 5 gate (deep dive before Phase 6)

**Branch** `fix/union-to-club-money-declares-itself`. Dan's gate: verify everything built in Phase 5 is fully built, wired and tested; find every bug, gap, stub, regression or wiring issue; make sure it is pushed and published. Dan also handed back every item the earlier notes had left "for Dan" or "for others": rake wallet club route, `SPIN_TIERS`, the 5.3 unregister/cancel ruling, #3065's file versions, `fn_redrive_unbanked_rake`, the unregistered cash-seat doors. All read from production between 10:13 and 20:20 UTC (the session paused between 10:45 and 19:44).

## Pushed and published

Every chip-standard migration applied since 09-04 19:00 is on `main` byte-exact against `schema_migrations.statements` (22 checked, mine all OK); PRs #3068 and #3096 merged and live; PR #3115 (Phase 5.2 close) refreshed by autopilot at 09:40 and on auto-merge. Seven gate migrations below applied and mirrored byte-exact; pushed on this branch.

## Live health (Phase 5 machinery)

1,866 enforced events closed since 02:55, 0 with prize or bounty left, 0 with fee unsettled; 0 `escrow_short` refusals; 0 live enforced banks negative; 0 live events with money rows and no escrow row; 7 `zz_ca_escrow_*` triggers present and declared, `fn_undeclared_money_triggers()` 0 for mine; supply meter flat every hour from 09:05 (-6.13) to 19:05 (+5.10), Mint card difference 3.88; 0 freeze journal refusals since 07:58 (three :55 breaks passed); `fn_ca_escrow_balance_drift(2)` 1,096 checked, 0 disagree; every wiring of Phase 5 and the promo/freeze fixes still in place in `pg_proc` (`fn_settle_tournament_obligation` reads `fn_ca_escrow_can_pay`, the meter carries the 5.2 text, the freeze guard carries the journal exemption, every declared door still declares).

## Found and closed (seven migrations, each probed rolled back first)

1. **`spin_entry_threw:9cd44071` (critical, 09:05): a deadlock in `fn_spin_book_entry`.** A registration holds the spin's escrow row and waits for the spin_entry advisory lock; the unbooked sweep holds the advisory lock and reaches the escrow row through the reserve leg. One to three a day since 09-02 (older than the escrow, which put the escrow row into the cycle). The entry was booked on the retry nine seconds later and the spin closed exact. `fn_spin_book_entry` now takes the escrow row before the advisory lock: one order on every path (`20260905101732`).
2. **Four unregistered money doors** filed by `fn_ca_money_rpc_drift` (`fn_cash_seat_move_execute`, `fn_cash_seat_swap_execute`, `fn_cash_cluster_tick`, `fn_union_clawback_promo_from_club`): each read, not assumed (the seat moves keep the felt total unchanged and touch no wallet; the tick cashes out through the declared `table_cashout` path; the clawback declares and keys). Registered with what they do; two stale rpc-drift incidents from 09-02 closed; `fn_redrive_unbanked_rake` returns empty while frozen (`20260905102140`). Drift is zero.
3. **#3065's mirror.** The files on `main` carried versions the MCP never applied (`...030103`, `...031715` against applied `...030738`, `...031852`), so a replay tool would re-run a money reroute. Renamed to the applied versions; content equivalent modulo comments (the MCP strips them) and one ACL restatement already true live.
4. **Spins enforced.** Measured over 13,346 spins since 09-04: none paid before its draw, none paid beyond its bank, 0 negative live banks after three hours of reserve-aware banks. Every spin now opens enforced and every spin row is enforced; the `reserve_out` that moves the filled pool to `spin_reserve` inside the registration that filled it is never refused (probed: a payment one cent beyond the bank refused, a reserve move of 999 not). 93 spins paid under enforcement in the first 25 minutes, 0 refusals (`20260905194420`, inside the 19:55 freeze).
5. **The seat reads the money that arrived.** The target escrow's `satellite_in` came from the seat's fee row (the nominal); the money moves on the satellite's `pool_transfer` leg. Equal on all 12 targets today, but a target with no fee would have been fed nothing and a short satellite pool the nominal. The target is now fed by the leg through `zz_ca_escrow_seat_transfer_leg`, the fee row carries the fee only, the shadow reads the same (same migration).
6. **`escrow-close:956383d2` (R5, 19:22): 200.00 left in a satellite's escrow.** Read: three seats of 200 owed to places 1-3 in Sunday $200 Deep Stack; places 1 and 3 already held the seat and were paid 200 cash; place 2 (railbirdd) had BOUGHT the target seat with 200 of own chips at 10:08 and the award reported the origin unknown, so the engine paid nothing. Under 10.9: railbirdd paid 200.00 through `fn_settle_tournament_obligation` under the place-2 key; the escrow reads zero; `fn_award_satellite_seat` now reports a cash entrant (is_satellite_qualifier false) as `held_from_this_satellite = false` so the engine pays the cash itself; NULL origin is unknown only on a seat a satellite awarded before 08-30 (`20260905195011`; the guard test's pins restated forward in `20260905201720`).
7. **5.3 ruled and built: a satellite seat is ordinary money in the target's escrow.** Unregister already refunds it in cash. Cancel did not: `atomic_cancel_tournament` and the deferred cancel guard computed what is owed from the entrant's own wallet debits, which a qualifier has none of, so a cancel refunded them nothing and committed with their seat value stranded. And the probe of that fix found a second gap in Phase 5.1: a cancel's fee reversal rows (attribution) were counted as fee leaving the escrow on top of the fee slice the refund already returned, so any fee-bearing cancel would have driven the fee bank negative and been refused (the 274 cancels since 02:55 were fee-less spins, so none was blocked yet). Both read the seat's leg now, the escrow and the shadow ignore a cancel's reversal rows, and an event with no cash entry apportions a refund by its own entry structure. Probed rolled back on a live target holding 43 qualifiers and 4 cash entries: 47 refunds of 50.00, escrow 0.00 / 0.00 / 0.00, the deferred guard satisfied (`20260905200041`).

## Rulings written into `docs/LAWS.md`

- The rake treasury has no club route: it is held in trust for the clubs and leaves only through the weekly close; the modal's refusal stands.
- A satellite seat is ordinary money in the target's escrow; unregister and cancel refund it in cash; a winner who already holds the seat is paid its value in cash; no ticket liability.

## Named, with their owner

- `SPIN_TIERS`: the rebalance to E[m] = 2.76 exactly (2x 4,809,776, 3x 3,930,716, denominator 10,000,000) is already in `spin_tier_spec` in production (`20260905171120`) and in PR #3146 (engine, client, tests) on auto-merge. Until #3146 publishes and the engine deploys, the engine draws the 2.7638 table against a 2.76 spec and the fairness gauge may read the 0.13% gap; self-resolving.
- `guard-def-drift:fn_ca_diamond_snapshot` is the diamond programme's (no mirrored migration matches its live body); the other four guard-def-drift incidents were matched to their mirrored migrations and closed.
- The felt's boundary noise (in-flight settlements at the snapshot instant, |gap| < 7 all day) is Phase 6's snapshot-at-one-instant.

## The incident board, closed to what is real

43 open incidents whose causes this programme closed were resolved with the migration that closed each: 5 `escrow-balance-drift` from the 07:35 run minutes before the reserve terms landed; 7 `bbj-meter` hours of freeze-refused legs; 6 `spin_entry_threw` deadlocks (09-02 and 09-03); 24 `supply-unexplained` hours from the era before the meter was exact (felt erasure, then two liability redefinitions), re-based at 09:05; 4 `guard-def-drift` whose live bodies match their mirrored migrations; 2 stale `rpc-drift`. What stays open is real and named: 30 `escrow:<id>` R5 findings from 09-02 to 09-04 (events that closed with residue before the balance existed, 2,544.11 in total) are the epoch reset gate's list, which is Dan's; one `spin_entry_threw` from 09-03 is a lock timeout (55P03), a different cause, left for its owner; `guard-def-drift:fn_ca_diamond_snapshot` is the diamond programme's.
