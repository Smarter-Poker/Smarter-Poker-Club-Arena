# Phase 5 Of 6: Human-Path Recertification

## Scope

Phase 5 was first built on 2026-09-01. This pass re-read that implementation
against the current application, the current post-deploy workflow, and the live
production state instead of treating the old completion note as proof.

The protected Club Bank, Deep Stack Society balances, funded horses, existing
players, existing memberships, and chip balances were not changed. The
financial-decision browser path is deterministic and has no writer. The live
route contract uses only a reserved zero-balance certification identity and
the existing guarded hard-delete sweep proves that its temporary membership
leaves no residue.

## Gaps Closed

- The exact production route from the audit, `/clubs/shark-club/members`, had
  broad route and mobile sweeps but no dedicated human-path release verdict.
  `production-club-members.spec.ts` now proves the authenticated World Hub
  handoff, both roster RPCs, hero decode, summary telemetry, server search,
  saved views, sort URL continuity, member navigation, 375px fit, 44px primary
  controls, 16px iOS-safe search text, title-cased control copy, the em-dash
  prohibition, and serious/critical accessibility findings.
- The post-deploy identity was a plain player while Player Command is
  intentionally staff-only, so a newly wired route test would certify only the
  permission gate. The reserved, disposable account now receives a verified
  zero-balance admin membership before browsers start. The helper refuses any
  existing membership or identity outside the certification namespace, and
  the existing hard-delete sweep removes the complete fixture.
- The post-deploy workflow now runs that Players contract after every successful
  publication, records it in its own JSON report, and feeds that report to the
  honesty checker. A missing, skipped, or empty Players verdict can no longer be
  reported as a green production run.
- The 60-second waitlist offer had unit coverage for its deadline arithmetic,
  but no real-browser proof that the actual banner counted down, fit at 375px,
  preserved its 44px dismiss target, and handed the player to
  `/table/:id?buyin=1`. The no-money Phase 5 component harness now proves that
  complete interaction through the real `MasterBus` and `WaitlistBanner`.
- The waitlist banner carried duplicate dead keyframes and an incomplete dismiss
  name while its table-name read was still resolving. The dead rules are gone,
  the accessible name now has a complete fallback, and the decorative close
  mark no longer uses an emoji glyph.

## Production Baseline

The read-only production check at the start of this pass found 1,301 active
seats, all occupied by horses and zero by humans; zero waiting rows and zero
live seat holds; 7,482 recorded insurance offers but only three completed
insurance transactions; two Rabbit Hunt reveals; and zero unresolved deploy
truth alerts. That is why deterministic browser coverage remains required:
the horse fleet continuously proves the server loop, but cannot prove a human
browser or its interaction surfaces.
