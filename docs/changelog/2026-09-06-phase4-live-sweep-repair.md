# Phase 4 live-sweep certification repair

The full production post-deploy matrix exposed two browser-harness races after
the Cashier and every stateful Club Arena journey had passed:

- The 375px route audit could begin its geometry evaluation while an auth or
  role guard replaced the document. It now retries only Playwright's bounded
  document-replacement errors and measures the destination page. All geometry
  and application errors still fail immediately.
- The real-pointer river squeeze check could inspect the animated host after a
  successful release had already settled the fifth board card and unmounted the
  host. It now accepts either the visible 180-degree terminal transform or the
  stronger completed state: the released host is gone and all five community
  cards are settled.

The document-replacement path has executable regression coverage, and the
repaired river journey was rerun directly against production in Chromium.
