# Chicken moving sequence

Replace the legacy landing-only chicken with four approved premium poses: pop, downward peck, head-up bobs, wide-beak cluck and half-open ending bobs. Animations are finite, use the shared speed control, and present one static expressive pose under reduced motion. Original source pixels remain outside public assets; runtime WebP preserves visible RGB and alpha exactly.

Visual validation: 25 focused checks, 11 chicken beat/scale captures, plus the four newest sequences together at 60 normalized-speed samples with no browser errors or reduced-motion animations. Organic cluck/bawk audio is still pending. The new rig uses the existing landing pop and does not pretend synthesized effects are recorded animal sounds. No frontend publication.
