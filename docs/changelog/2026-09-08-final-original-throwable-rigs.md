# Every original non-glove item has a rig

Skull now chatters its jaw, lights its eyes, cracks vertically and leaves a sculpted gravestone. Lightning forms a storm, strikes, and leaves a smoking avatar copy with sculpted eyes and raised hair. The eight ball rattles, turns and reveals a deterministic answer. Bear gains its missing sculpted anger mark. All effect art uses reviewed local atlases; avatar copies only reuse the target already visible in the same table.

The successful consumption request ID now travels from the selector to the local event and through optional CHAT payload metadata to receivers. The legacy message string stays unchanged, preserving old-client recognition. UUID metadata is validated and normalized. This supplies deterministic identity, not authorization: receiver-side server receipt verification remains pending.

The darkroom removes obsolete generated screenshots only after a successful capture, preventing stale beat lists from being mixed with a new run. Eight-ball answer text is visible on its named first frame. The receive-wiring law now pins the metadata-bearing call. Static-only artwork readiness is tested with a controlled unrigged manifest fixture, since no original non-glove item remains unrigged.

Verification before release: full TypeScript and 165 focused checks passed. Four rigs have 44 captured beat/size frames and 60 normalized speed samples, with zero reduced-motion animations or page errors. Remaining overall scope is the 32 additional named items, bespoke audio acceptance, item ownership/purchase gates, authenticated receipt delivery, legacy-engine retirement and real-device flows. The original catalogue is not overall 100 percent completion.
