# Rat card performance prepared for premium activation

The rat performance has a wink, a mouth-held blue card that slides out, chewing, an edge-on flip to an ace, and a laughing head bob. Genuine RGBA art replaces the rejected checkerboard still attempts. Reduced motion keeps a static ace. Fourteen frames were reviewed and 15 normalized speed samples passed, with no page errors or remaining reduced-motion animations.

This premium item is deliberately not added to the live registry or picker until server purchase and ownership gates are ready. The default registry/catalogue consistency law stays intact. The darkroom gains an explicitly selected --drafts mode to review authored rigs without modifying the production registry. Overall coverage does not count this as an integrated live rig.
