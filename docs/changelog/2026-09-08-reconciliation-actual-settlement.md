Follow-up integration: merged current main while retaining every source-retention assertion. CI exposed an older guard requiring a direct fn_credit_and_log reference in the reconciler. The reconciler now delegates to the existing obligation writer; the guard verifies the executable delegation and that writer's executable credit-and-log call, and rejects bare wallet credits in both definitions. All 54 source-retention checks, all 13 money-path guards, server TypeScript, and the full 6,486-test server suite (457 files) pass. Different branch totals reflect independently pending fixes, not removal of tests.
