# Unconfirmed Seat Outcomes Reach Financial Alerts

F45: the satellite award caller stopped on failed or ambiguous receipts but only produced an engine error. The caller now awaits the existing critical financial-alert path before throwing. Its message says the outcome is unconfirmed, never that a transport-failed transaction certainly rolled back. Context identifies the event, target, player, place and ticket value. No cash fallback or confirmed prize stamp is made.

Six adverse cases reproduced the missing alert. All 19 actual-method receipt cases, server TypeScript and the full local server suite pass: 6,653 tests across 473 files. Tests mock the notification boundary; no live message was sent.

This is a prerequisite for the unpushed F40 atomic funding candidate. Source merge is not engine deployment. Original guarantee funding, full award resumption and concurrent financial-session verification remain open.
