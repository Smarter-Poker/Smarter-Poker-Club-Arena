# Final Deal CSS Dependency And Terminal Contracts

The standalone review referenced six class uses from the overview stylesheet without declaring that dependency. Import the existing stylesheet so independent rendering and the CSS reference scanner resolve the same styles.

The terminal tests now pin parked roster validation, current engine ownership, bounded review expiry, exact proposal and revision, and the same proposal request through completion and recovery.

Verification: 81 client tests across four files and 75 server tests across four files passed. Existing CSS baselines are unchanged. No database migration or production acceptance is implied by these source checks.
