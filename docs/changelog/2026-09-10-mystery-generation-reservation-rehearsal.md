# Mystery Generation Reservation Rehearsal

Before: the accounting Phase 3 controls CA-03-02, CA-03-03 and CA-03-10 still require operation evidence. Existing mystery unit tests exercise inventory arithmetic, and existing full terminal probes need the coordinated Stage 1 release. No bounded PostgreSQL runner independently exercised current seed, generation-bound reserve and actor-bound reveal together.

Planned change: capture the exact installed function bodies and reservation table constraints in a small disposable PostgreSQL 17 fixture. Run funded inventory, retry identity, exact recipient binding, reveal authorization, rollback and observed overlapping reservation checks without a wallet payer stub or a production connection. Reuse the existing eight-group satellite origin-wallet refund probe. This evidence cannot certify payout completion or the pending bounty rebuy generation migration.

Verification: thirteen PostgreSQL mystery reservation/reveal groups and eight existing satellite refund groups passed. They include observed concurrent transactions and rollback after a late write fault. Exact fixture limitations and each open control gate are recorded in scripts/dev/fixtures/mystery-reservation/README.md and docs/audits/2026-09-10-phase-three-bounty-mystery-satellite-evidence.json. No production mutations or economic changes. Re-read: yes. Python syntax and diff checks pass. TypeScript and focused server tests did not complete because the disk filled during isolated dependency provisioning; no pass is claimed. Integration must run its normal gates.
