# Native funded Spin launch recovery

The compact Spin runner did not establish composition with the real journal, reserve receipts and escrow triggers. A new rollback-only native probe now calls the exact immutable funded-draw wrapper with current entry and settlement bodies, then completes an already-played launch through the actual lease-bound completion RPC.

The probe verifies a failure after the final immutable receipt erases all preceding money writes; retry succeeds once with exact reserve totals and fixed rake. Changed rules, stale lease ownership and attempted receipt deletion cannot alter committed evidence. After one busted seat vacates, the original paid field and persisted hand permit identical replay. Missing hand evidence or a short chip total refuses recovery. Valid recovery sets RUNNING and completes the launch receipt without moving money.

Four complete tracked migrations provide the missing local prerequisites. Every source guard is retained. The final immutable wrapper fingerprint matches `1c911e3ada50ffe0493b9b375e3fa9ae`; no financial or RNG function is stubbed. PostgreSQL 17.11 passes the native probe. The outer rollback restores all original function hashes and leaves no new receipt table, event, lease or injected fault trigger.

This is sequential native draw/launch evidence. Full native concurrency, final winner settlement, production adoption and the complete Phase 3 programme remain open. Opening payment and accepted-hand facts are fixture inputs. No runtime or production database change. Exact source hashes and boundaries: `docs/audits/2026-09-10-spin-native-immutable-launch-evidence.json`.

Re-read: yes. Native PostgreSQL proof and `git diff --check` pass. No TypeScript changed.
