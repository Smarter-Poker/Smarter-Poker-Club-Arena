# Funded satellite seats retain each paid component

One pre-start satellite award could count twice, its version 2 fee could reduce prize escrow twice, and an empty bounty ledger could invent a bounty balance. The correction keeps roster counting with its existing pre-start trigger, retains the full funded version 2 prize component, and requires an actual direct-ledger row before counting bounty.

The migration preserves the current ticket-redemption reader and global settlement-lock helper, validates their exact authority envelopes, and rewrites no historical financial row. Native 24-check evidence includes real paid registration, bounty splits, replay/contention, actual SQL roles and complete late-failure rollback. Production application remains pending explicit deployment approval.

This introduces no new runtime caller and does not activate held exact-K qualification or its absent Stage-B prerequisite.
