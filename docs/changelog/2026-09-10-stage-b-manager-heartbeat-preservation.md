# Stage B Preserves Manager Heartbeat Renewal and Player Reveal

The prepared Stage B request hook still took `FOR SHARE` on a tournament lease. Replacing the current hook with it would undo the deployed busy-manager fix: heartbeat v4 uses `FOR NO KEY UPDATE ... SKIP LOCKED`, so an admitted manager request would make its own heartbeat report `busy`.

The candidate preserves `FOR KEY SHARE`, pins the exact takeover function that locks `FOR UPDATE`, and includes the current heartbeat-v4 route. It writes the manager proof only after exact admission. Both Stage B and the standalone two-function prerequisite accept the current hook or their own exact postimage, preserving existing binding, owner, configuration and grants. The standalone bundle includes no financial cutover or deal activation.

Source review of live build `f1992eeb825918d6614d72a10c66988d0cdd292c` and main `d6a032d8d3de237f7371e4ea28101e7824569e7a` found one additional player route: TablePage invokes mystery reveal directly. The corrected hook explicitly admits an unmarked authenticated reveal, with browser authority and no manager proof. Its preflight pins the existing hardened reveal body and ACL: browser identity comes from `auth.uid()`, supplied actor cannot impersonate another player, and only service callers can use auto reveal. Ordinary service reveal still requires the exact manager lease. The main bounty-ledger and lifecycle corrections do not change this reveal authority or actor-header plumbing.

The native PostgreSQL 17 rehearsal passed 48 assertions. The previous prepared hook reproduced a skipped heartbeat; the corrected hook admitted two genuine renewals while the request remained open. A competing actual takeover visibly waited, changed generation only after release, and fenced the old owner. Scope and proposal-route refusals passed. Authenticated rebuy, decline and reveal retained browser authority without manager proof. The real reveal body admitted the designated player, refused forged actor and auto flags from an outsider without mutation, replayed the stored amount, and admitted the exact manager auto reveal. It refused anon, marked browser and ordinary service callers.

The reveal cases use valid rollback-only awards and exact live grants on one existing owned synthetic event. No payment function runs and no trigger is disabled. All reveal rows and ACL changes roll back; all function and trigger fingerprints and the twelve-event fixture count match after restoration. Separately, six isolated-bundle checks passed, including unknown-hook and unknown-reveal refusals, all five function postconditions, exact replay, and complete rollback of the pristine root database. No production DDL or full Stage B activation is claimed by this evidence.

The subsequent live source `6aee0b67da8b0b8c4e1cee9c6223aac4d3867e04` retains the audited actor/client, manager entry binding, lease helpers and frontend callers byte-for-byte. The limited manager/dealer delta changes metrics, refused-bust retry and horse recovery policy without changing admission authority. A single health response does not prove every live owner uses that build; mixed-build topology is unverified. The isolated hook remains prepared and unapplied because the deployed hook already preserves heartbeat renewal and extra route restrictions belong with the deferred Stage B cutover.
