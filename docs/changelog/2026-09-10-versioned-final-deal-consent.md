# Versioned Final Deal Consent

Before: generic votes named a tournament and voter, while cash shares were derived from later stacks. Collecting exact votes before parking also made a completed hand invalidate every reviewed amount.

Prepared correction: an explicit actor-bound request creates one durable review epoch. The manager parks first, then creates the immutable post-hand proposal. The server owns separate request and consent deadlines (central policy defaults: 120 seconds each). Repeated calls preserve the deadline; a consumed hand fingerprint requires another committed hand before another pause. GET requests never initiate a pause. Cancellation and expiry close the named review before the manager may resume play; completing or unknown money outcomes remain fenced.

Proposals bind the review epoch, tournament financial scope, full roster, seat generations, committed hands, fixed prize liabilities and exact integer-cent shares. Each vote additionally binds the displayed actor to auth.uid before any write. Only unanimous consent for that unchanged proposal can enter the existing cash authority. The prepared terminal wrapper binds its durable execution and lost-response receipt to the same proposal, review and manager generation.

Current lane compatibility: the tracked 20260910035435 migration moves hand settlement from the old global key to a separate hand barrier. The old key still serializes financial authorities, but acquiring tournament rows under that key before entering the current native terminal helper could deadlock a hand holding the barrier. All eight prepared locking entries and the separate activation now call the exact current global lane helper, taking authority key G then hand barrier B before any rows or relation locks. The expansion requires this helper to be available first.

Verification: 68 native PostgreSQL assertions passed in each of paid, unpaid and partly paid fixed-tail fixtures, with explicit rollback. Six two-session assertions passed across terminal-helper and shared-hand-helper holders. The old prepared G-only statements fail the shared-hand regression immediately. Holder helpers use transaction-local pg_temp identities with byte-identical, fingerprint-checked tracked bodies, preserving search_path and invoker semantics. The cash fixture authenticates its original local baseline, then applies exactly one tracked lane transform to the cash payer and verifies the current live body hash 141c723b5225bcec588b8957cf039184 before and after installation. The 68 assertions in each variant therefore exercise the current cash payer with unchanged native guards. They do not certify whole-terminal composition or hand production.

Cash fixtures exercise actual credits, journals, escrow and obligations, including failure after an earlier credit, full-pool totals, exact payout cents and payment replay. Roles, wrong/null actors, requester elimination, deadline replay, cancellation, newer review epochs and post-park pause admission are covered. Existing scope guards were preserved and rejected all three hostile scope changes. To reproduce the probes before the lane migration reaches the checkout, supply --lane-migration with its exact tracked source path; the composer checks its whole-file SHA256 before extracting the two rollback-only helpers.

Authority provenance is recovered: terminal, outcome resolver and rake live body hashes match the tracked M5 bodies from main c064df4a50b5a09a0bdd3f18a52953139f195b58 after the exact transform in 20260910035435, committed in d7f6f0fd02b0a8fb353e792ef675bb8bd813cd14. The edited M5 file in the external Stage B checkout is not this baseline. The live rake matches transformed M5, rather than the later explicit attribution-retry redeclaration. This finding closes the three source-hash discrepancies; it does not certify release readiness.

Rollout remains gated. No production DDL or engine deployment is part of this work. Whole-terminal completion and lost-response acceptance still require the missing local non-satellite receipt table, guards and matched authorities, followed by coordinated current production prerequisites. No M5 or satellite restoration was installed for these helper tests. See docs/audits/2026-09-10-versioned-final-deal-native-acceptance.json.

Cash leaf provenance gate: the native composer now requires exact tracked definition hashes and existing catalog body, signature, owner, language, security mode and search path for both raw payers, the shared obligation wrapper and its renamed core. The 68 assertions were rerun successfully in all three fixed-tail variants with these gates active, and all runs rolled back. Four altered source definitions, four altered catalog body expectations, a wrong search path and a missing function each refused before fixture writes. No leaf definition or ACL was changed; local leaf ACLs remain outside production permission acceptance.
