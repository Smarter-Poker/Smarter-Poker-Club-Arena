# A source-derived, funded house make-good is one durable item

Previously, `ca_manual_adjustments` recorded proposals but had no reusable server authority that derived their entitlement, locked shared debt and house custody, paid one item, and returned its immutable receipt. The ordinary credit writer also omitted its key from the receiving-wallet ledger context; ordinary bounty credits retained only aggregate recipient debt identity.

Migration `20260911164014` adds private append-only debt revisions, items, funding evidence and receipts. Server-derived total entitlement is distinct from an imported net adjustment. The payer follows the existing member-then-treasury lock and balanced ledger contract, rechecks actual ordinary payments, preserves frozen and terminal accounting, and returns the original receipt on retry. Two pinned ordinary bounty call sites retain individual source provenance, and a pinned credit-context change gives future ordinary receiving-wallet legs their actual idempotency identity. The true-place recipe calls the shared private validator from `20260911163920` on exact persisted rows, then rejects later positive play or entry/purchases and requires the positive terminal survivor. Existing table/engine decisions and amount logic are unchanged.

Native PG17 verification covers actual ordinary/house races in both lock orders, shared insufficient funding, exact retries, 20/35/15, 10→12 incremental correction, immutable/unauthorized data, terminal balanced permits and forged transaction/backend/leg cases, actual refund/mystery/knockout competition, and error/context rollback. The separate real D10 fixture verifies six historical paid transaction tuples and the five true-place make-goods totaling 121.45 while preserving the original 180 and all terminal financial state. Fixtures use explicit synthetic house custody; production is untouched.

The no-band-aids policy mapping and outstanding named legacy cohort evidence are documented in `docs/accounting/house-makegood-authority.md`. No schedule, payment backfill, allowlist change, credential, production funding, proposal import or payment is included. Re-read: yes. TypeScript is not changed; the broad check was attempted but the borrowed installed dependencies lack the unchanged native Capacitor, RevenueCat and Capgo modules. The registry/no-band-aids/allocator laws passed 381 tests. The exact native receipts and source hashes accompany the handoff; production execution is separately gated on current funded-source and cohort evidence.
