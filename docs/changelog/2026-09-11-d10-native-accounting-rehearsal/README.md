# D10: preserve the paid record and complete the event

The native PostgreSQL 17 rehearsal of Breakfast Turbo
`c1f15c30-33c4-4a64-85ac-44037519ca5b` completes through the original whole ruling
file and the real terminal authority. Six existing payouts remain 180.00, no
new place money moves, and the event's remaining 20.00 fee credits its actual
union rake wallet. The 121.45 true-order make-good remains owed through D8.

The exact chosen migration `20260911110000` runs first. The ruling restores the
six-place version 3 ladder as version 5, retains all 40 positions, status, chips,
elimination timestamps and sequences, closes the winner's one chair and four
tables, and writes the immutable terminal receipt. Existing ledger, wallet
credit identities, accepted hand receipts and contract history remain unchanged.
Payout, wallet and obligation financial fields remain unchanged; ordinary
terminal closure adds closure timestamps and updates obligation `updated_at`.
The winner's display prize is corrected from zero to the already-paid 58.55.

## Run the native proof

```sh
python3 scripts/ci/rehearse-d10-tournament-ruling.py
python3 scripts/ci/rehearse-d10-tournament-ruling.py --d12-migration sealed
```

Run outside :50-:03 UTC. The runner accepts no database URL or credential and
starts its own socket-only PG17 cluster. It verifies the sealed archive and
current financial source composition, restores actual event evidence, runs
native refusal probes and the exact ruling, verifies financial preservation,
replays the terminal authority and stops its cluster. `--pg-bin` may select a
different local PG17 binary directory.

The optional second command applies the exact archived D12 review draft only
after the D10 terminal transaction has committed. It restores the observed
function ACLs omitted by the schema donor, enables all seven guards through the
reviewed migration and proves 20 application-table hashes unchanged across
both guard installation and terminal replay. This certifies this preserved
paid-history sequence only; it does not certify unrelated D12 lifecycle paths
or authorize installing a later draft without its own proof.

The fixture holds 40 players, 36 historical seats, four tables, 237 original
settlement receipts, six paid obligations, six payouts, six wallet credit keys,
46 ledger entries, 46 wallet receipts, 40 refund entitlements, 40 rake records
and four contract versions. It restores the real stamped member wallets across
two clubs plus event club and union. There are no knockout candidates or atomic
commit rows for this older event. All 521 reused financial function bodies were
freshly compared to production and matched; 293 original trigger definitions
and states match the installed composition. The seven existing guards remain
disabled until the optional post-terminal D12 step.

The protected wallet key cannot be deleted: the native append-only trigger
refuses before the ruling can run. Repeating the whole ruling also refuses on
its exact starting-state preflight. Both denied transactions leave all 20
observed application tables unchanged. An initial incomplete fixture correctly
failed the settlement's strict wallet-key linkage check; the final fixture
includes all six genuine production keys without changing them.

## True-order debt evidence

`extract-bust-evidence.py` independently reads the accepted historical
`settlement_idempotency_keys.result.request.stacks` and `result.written` data.
It requires a succeeded receipt, a completed timestamp, matching table/hand
identity and a positive starting stack written to zero. All 39 eliminated
players have a last accepted zero-stack witness. Descending commit time and
same-hand starting stacks reproduce the five named hands for true places 2-6
and the 121.45 total. The extractor changes no standings or money.

`makegood_optional.sql` is preserved as the exact historical proposal source.
Its old filename does not waive the debt. It was not executed by this rehearsal;
D8 owns the audited import/payment authority and reconciliation with any
proposals that appear before application.

## Source and application boundary

- Current cached base: `5a550b6343f613a7134011b41d69cdad613642f7`.
- Shared migration source: `3aaf4ce0c4`, SHA256
  `bfdab47f58c5c1c5e74fa9fd6bc068bd4deb34185cb6846d9a7715014f9c2a57`.
- Whole ruling MD5: `e688d1621e65bf09dcf3165c94bbf9d3`; SHA256
  `d6242e74f3772a66c80d7a2938ff65fd855696dc4e6bd5f67efde039e14a613e`.
- Fixture manifest SHA256:
  `7fdf38d52c73ca4a4e6bb05ce10bdf65ddbe6f1076cb296045b8b62bc76dd4e0`.
- Archive SHA256:
  `9ccc3b7fcc4527167ed583fa56bc32a6ac87b5025efd2e0301f7dda1c00b525b`.
- Optional D12 preview SHA256:
  `d935beb5af03d5fe327e02c4965280b662fbda5d80a6fc3bd244fe23c96a5b62`.

No production migration, ruling, proposal, payment or wake was performed.
The sole lead must bind the exact source hashes, event, winner, starting state
and expected effects to its durable operation before applying production SQL.
Refresh current source and event pins, keep the original file whole, execute
outside the installed restricted window and reconcile its terminal/financial
receipts before retrying an uncertain response. D10 completion precedes D12
activation for this historical paid event; no future verifier may rerank it.
