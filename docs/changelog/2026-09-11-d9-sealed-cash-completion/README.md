# Club Arena D9 source-sealed cash completion

Twenty-one eligible historical cash/Spin tournaments complete through the existing ordinary terminal and financial functions. Native payouts total **1035.10**, rake totals **71.86**, and all seven D12 tournament guards stay enabled. The original440 hand receipts, historical ledger/wallet amounts and NULL elimination sequence bytes remain unchanged. This does not claim D9's entire cohort is complete.

The 28 witnessed-survivor cases remain accounted for as21 completed in rehearsal,3 satellites needing a separately audited historical-economic/entry-close source boundary,3 revived histories refused, and1 winner chip-mirror mismatch refused. The broader40-case inventory additionally retains11 truly unfinished events and the paid-history f370585d case; none is silently removed or reranked.

The entry point accepts only an event ID and the expected read-only preview fingerprint. It holds the existing global settlement lane, maintenance boundary, event, roster, tables and exact source-row locks, rechecks the proof, then inserts owner-only append-only evidence. That evidence stores full source rows, an exact SHA256 source digest, the original tournament before-image and the accepted standings. No caller provides a winner or ranking. Historical status repair is recognized by the existing launch guard only for an exact status-only before-image and the same transaction's validated seal. A deferred constraint requires fully certified ordinary completion before that seal can commit.

The normal place setter and canonical verifier consume sealed legacy evidence only when all old sequence bytes remain NULL and the recorded standings exactly match accepted hand order. Their ordinary financial calculations and payers are unchanged. There is no historical position rewrite. The existing ordinary winner promotion and terminal closure own the resulting lifecycle changes.

Required order: exact shared migration20260911110000, D12 migration20260911161539 (SHA256030afa6eb8db12ea18bf2cfc6175c40d8601dd3becc3816e978d0f1dbee73a3f), private witness migration20260911163920, then this allocator-reserved migration20260911170040. The shared status fix preserves finalized ladder bytes; the adapter does not relax their comparison. Existing function metadata is preserved while the source seal and completion entry remain postgres-owned with ordinary-role execution/DML revoked.

Run `python3 scripts/ci/rehearse-d9-cash-completion.py`. The sealed runner creates and stops its own Unix-socket-only PostgreSQL17 cluster. It verifies521 current preserved function bodies/293 trigger definitions and states, plus the separately scoped entry-close table schema. It restores genuine observed fixtures, activates the exact ordered migrations, then executes business calls with origin triggers. No production destination or credentials are accepted.

Twenty native refusal/closure probes pass, including concurrent source mutation between preview and lock acquisition, numeric JSON representation changes detected by the stored source digest, stale/forged previews, current revived/mismatched/satellite refusals, partial repair/seal commit refusal, and immutable seal update/delete/truncate. All25 tracked tables are byte-identical on exact terminal replay. Existing ledger/wallet/entitlement/rake records retain every original field except the existing terminal marker when required.

Modern regression: the genuine E2 fixture with its original11 hypothetical future busts still completes through the unmodified ordinary path, pays304.00 in17 places, matches all167 expected ranks, preserves155 historical elimination status/time/sequence/chip records, and replays without changes. It creates no legacy seal. This is a local future-finish rehearsal, not a claim that the live E2 tournament has finished.

Satellite scope remains distinct:3 source/target contract histories prove a20 entry promise plus8.50 remainder from each28.50 pool, but all3 lack original economic and entry-close receipts. Current target terms or a late start-trigger snapshot are not used as substitutes. Their completion is refused before any seal, status repair or payout while the separate reconstruction boundary is developed.

Migration SHA256 `61ecf75531c8848c312397f5905ef5a46c640cf8190aff3bd10ca3e4250eda50`. Archive SHA256 `9e22f74e5b4a408a58afa6bbaaceff73bc466ca2130978326fc52c5b6f8c4c55`. Manifest SHA256 `3d8a9e8c2617a9487b322f1482d52ced77178074c9b2a2942f5dd13df96e8a8d`.

No production writes, wakes, pushes or World Hub edits were performed.
