# Rakeback Discovery And Refresh Accuracy

A payable club could be hidden behind the twelve most recent periods, and an overlapping refresh could drop the post-claim or newly signed-in account read. The rakeback page now discovers an eligible closed period separately, refreshes at UTC midnight and coalesces a follow-up read. Account and claim ownership prevent stale results or retries from crossing users.

Recent totals, the latest period rate and the next ready estimate now describe their actual scope. The existing Supabase-aware retry utility handles retryable database responses, while uncertain transport outcomes trigger authoritative refreshes without an unsupported payout assertion.

The focused actual component suite passed 44 cases. This remains a coordinated release candidate requiring the standalone backend repair and runtime readback before UI publication.
