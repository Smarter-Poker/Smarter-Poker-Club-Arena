# Restore Seven Tournament Guards With Current Terminal Receipts

Normal terminal completion now obtains its existing immutable finish claim in the same transaction as settlement. The seven tournament guards can then be restored against a versioned exact-payment batch and the installed bust-witness ordering, without changing historical payments or standings.

Validation: sealed native PostgreSQL 17 E2/lifecycle rehearsal, D1 and D10 ruling compositions, exact funded refund composition, active-guard and function-ACL readback. Production activation remains outstanding.
