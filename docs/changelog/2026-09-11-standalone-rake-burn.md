# Standalone club rake burns chips

Requested by Dan on 2026-09-11: rake from a standalone club is burned; union rake may remain in the union wallet.

The guarded migration replaces cash and tournament standalone destinations with chip_retirement, which the native deferred journal trigger registers in ca_mint_ledger. No club treasury or wallet receives or loses another copy of the rake. Tournament fee escrow still leaves through its original receipt trigger. Public union games retain their rake and the separate BBJ drop remains intact. Tournament routing now follows the same ownership rule as cash: private games cannot credit the union, and a stamped union survives club membership changes. Unstamped historical public events retain their existing club fallback.

Historical destination claims are retained, so a previously credited hand or event is not retroactively burned. Cash receipt identity also survives hand-history ID arrival and club membership changes. A destination receipt with the wrong club, a missing union for a union leg, or a union on a club leg is refused before another destination can be written. The unkeyed legacy treasury-credit function refuses use. Both terminal validation consumers accept chip_retirement in the same migration transaction while preserving their other guards.

Validation: 40 isolated PG17 scenarios/check groups passed, including reproduced private-tournament leakage and malformed-receipt failures before their fixes, the previous treasury credits, three actual terminal destination predicates, native register and escrow bodies, five journal failure types, deferred register rollback, short escrow, and two concurrent sessions. These use synthetic schema and isolated attribution. Separate funded standalone and union SQL rehearsals passed on the preceding candidate; the ownership update must pass those exact composed paths again. The changed helper and adjacent insurance tests passed 18 checks; server TypeScript passed using existing shared packages. No package installation or copying occurred.

Release status: candidate only. CI, full terminal integration, live definition readback and production behavior proof remain required. No historical financial rewrite or production application is claimed.

Current-schema composition, 2026-09-14: retain the installed Diamond tournament fee and custody branch exactly, and accept the new Diamond bounty reads in both terminal consumers. This unapplied migration still refuses unknown preimages. Its isolated PostgreSQL probe now reads back all three compiled functions: the fee branch must remain byte-identical, and reversing only the three retirement destination predicates must recover both entire current terminal bodies. All 43 probe groups passed with owned database cleanup. A counterexample that omits the Diamond fee branch fails the new assertion. These checks establish branch preservation and chip routing; they do not execute the full Diamond custody or current funded tournament lifecycle. The older funded rehearsal cannot certify this successor.
