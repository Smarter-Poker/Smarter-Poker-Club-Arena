# Blocked releases retain their diagnostics

CI run 34671124841 failed a Table Studio interaction and left no browser artifact. The isolated CSS/Studio/financial-decision job now retains its existing failure screenshots, traces and error context for seven days. Its cleanup, retry counts and required outcome are unchanged; authentication storage and environment files are excluded.

The live engine reported hand 9972923 as blocked after its durable post-commit obligations had completed. The bounded blocked-settlement health list now retains each sampled engine's already-captured loop phase, so an operator can distinguish the reported wait without restoring the full fleet payload. The oldest twenty sample limit, settlement clock and liveness verdict are unchanged. This adds diagnostics; it does not claim to recover that live incident or authorize replaying its completed financial work.
