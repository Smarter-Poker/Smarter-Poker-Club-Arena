The durable engine observer could report a terminal failure before reaching its journal reader. Run 34668951867 rejected an obsolete target and retained the sealed engine, but Actions exposed only status 1. The concrete freshness refusal required a separate host journal read.

After validating a durable failure receipt, the observer now prints at most 200 lines from that receipt's exact systemd invocation. The read has a five-second timeout and a one-second forced termination allowance. A missing or timed-out journal still ends with the original failure verdict. No dispatch, retry, recovery, freshness or sealing authority changes.

The executable observer regression exercises the stale-target cause, exact invocation selection, journal failure and timeout while retaining status 1 and proving that no systemd operation or new request occurs.
