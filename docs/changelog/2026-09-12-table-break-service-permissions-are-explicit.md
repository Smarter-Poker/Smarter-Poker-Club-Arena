# Table Break Service Permissions Are Explicit

The original table-break installation used guarded dynamic grants. The static migration check could not derive those permissions and reported eleven service-only functions as browser-accessible. This additive installed migration records explicit grants for their exact signatures.

Full definition, owner, ACL and effective-role checks before and after the declarations verified that function bodies and access stayed unchanged. The original installed SQL remains immutable. Independent review accepted the source, and seven native PostgreSQL checks covered success plus definition, owner, ACL, overload, inherited-permission and postflight refusal with rollback. Native business function bodies were not invoked.

Actual migration version `20260912103123` contains the exact accepted 10,804-byte SQL statement. Root verified all eleven installed definitions and permissions unchanged and the history byte-identical. Protected source publication and actual funded tournament movement remain separate open gates.
