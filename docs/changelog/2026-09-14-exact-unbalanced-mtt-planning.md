# Complete exact MTT plans within the existing proof budget

Some large fields need only one or a few moves but exhausted the opt-in planner's search budget while allocating whole-field targets or enumerating dominated assignments. Exact cardinality quotas replace quadratic target calculation. Proven one-move fields enumerate all legal donor, player, receiver and chair combinations directly.

When one donor supplies one destination, the planner enumerates every free-chair subset and requires a unique inverse from original blind distance to source player. Only a complete zero-displacement set excludes positive-displacement alternatives in that same target vector. Movement count, natural big-blind sum/maximum, prior-move history and unbiased ties remain the objectives. Other geometry keeps exhaustive fallback. The 100,000-step budget and 2,000-table cap are unchanged; exhaustion returns pending without a partial plan.

TableBalancer is byte-identical to accepted G8 source7f18aed405, following the earlier exact target and one-move change. No runtime caller, feature activation, roster observation or retained assignment changes. Existing retained assignments must not be recalculated with a new draw-enumeration order. Full manager/SQL relocation, next-hand behavior, reconnect and all18 MTT lifecycle boundaries remain separate acceptance work.

The existing twelve large-field cases from accepted source5285d0d400 remain unchanged. Fifteen further tests from source1924592aa1 compare four-move plans with exhaustive ordered assignments at12,90,364 and2,000 tables, including blind history and reserved chairs. They also reach all70 tied winners, reject biased sampling residues and refuse missing randomness. Both suites verify full objective, winner membership, player/stack conservation and input immutability. These are source tests, not live activation or production performance guarantees.
