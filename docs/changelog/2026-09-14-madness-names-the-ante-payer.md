# Madness names the player who pays the ante

The game setup described Madness as collecting one big blind from every dealt player. Its configured big-blind-ante mode instead collects one big blind from the player in the big blind. The rules readout now names that payer; the engine, ante amount, and template configuration are unchanged.

The old sentence fails the corrected vocabulary check. All 82 vocabulary and rendered creation-flow checks pass. Before/after previews exercise the actual creation component at 375×812 and 1280×900, with no horizontal overflow or page errors.
