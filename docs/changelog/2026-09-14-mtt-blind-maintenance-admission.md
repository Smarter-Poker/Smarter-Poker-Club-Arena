# Blind clocks wait for maintenance without submitting rejected writes

A tournament can have `on_break=false` while the whole platform is frozen, particularly a heads-up satellite or Spin. Its blind timer still called the atomic publisher every second. The database correctly refused the request as paused, but the manager repeatedly submitted the same write and reported a failed transition. Live maintenance logs reproduced that traffic. This does not establish that the traffic caused every stalled tournament or expired lease.

The manager now keeps a single lifecycle-owned local wake during the known freeze, without reading or writing the database. After thaw it reads the exact tournament's durable level and shifted clock. It preserves any remaining playable time without rewriting that anchor. A possible lost publication still replays the identical request through the existing lease-fenced atomic publisher before changing the local level or announcing success. A freeze beginning before a successful response also preserves the committed clock. Ordinary tournament breaks, stale-manager refusal and physical in-flight ownership remain unchanged.

Eight new actual-method cases failed before the repair. The final focused suite passes24tests; the tournament and maintenance suites pass2,211tests in167files, with no skips or unhandled errors. Server TypeScript passes. The first broad run exposed three extracted-method/source-shape harness dependencies; they now include the real timer helper and maintenance input and assert both paused and live branches. Required CI and served runtime verification remain separate.

Only the blind-clock methods changed. No maintenance protocol, GameServer admission, manager lease, retirement, financial authority or database function was replaced.

The accompanying prospective configuration follow-through is recorded separately: at10:00:37UTC the two existing chip-club Turbo Tuesday PKO schedule templates were updated under an all-or-nothing exact-config/owner comparison. Their future spawns now select the existing4-minute TURBO opening and6.75bounty, half the13.50entry contribution within the unchanged15total. The22,000stack,500capacity, minimum4,9late-registration levels,600guarantee and schedule times remain. R20 was verified in served engine d28993d9 before applying fractional bounty terms; actual engine payload projections and exact database readback passed. No booked tournament or financial row was updated. Evidence is retained in the task's MTT-Future-Schedule-Correction-Readback.json and matching SQL/projection artifacts.

At10:06UTC the live reader still reports93stalled running MTTs,0overdue breaks and0overdue starts. Six recent heads-up satellites have dealt hands but have no terminal award receipt yet. Full MTT, bounty, satellite and financial-provider acceptance remains open.
