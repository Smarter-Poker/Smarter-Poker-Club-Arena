# A stale discovery result cannot resume an ended tournament

Satellite `5d0da5b1-6b3f-4cb2-8239-bae224528ffc` completed at 2026-09-14T10:24:45.658365Z. The served d28993d9 engine certified its target-entry award at10:24:46.562, then a stale decided-event scan resumed it at10:24:51.278. Entry closure refused `tournament_not_running`, but that new manager still reported zero tables and armed its blind clock. It attempted level17 about once per second through the observed10:42 window while the database correctly retained level16.

`TournamentManagerBase.resumeLifecycle` originally4578 discarded the current-row query error and checked only whether data existed. It now rejects an errored or misidentified row and permits gameplay restoration only when that exact current row is RUNNING. All other states return a non-running manager to the existing `GameServer.finishTournamentManagerAdmission` cleanup. That existing path still owns physical teardown and exact lease release. There is no new retirement, polling, retry, payout or status-writing authority, and no completion receipt is inferred from a status value. COMPLETING retains its separate financial recovery path.

Ten actual-method counterexamples failed before this change. The new suite covers ended, cancelled, completing, registering, missing/unrecognized status, errors with plausible data, wrong identity and completion racing discovery. It also exercises successful current-RUNNING dealer/clock restoration and the actual GameServer return path into the existing exact-manager cleanup method. The old blind-recovery fixture now includes the event identity and RUNNING state that a real successful current-row read carries; its partial-publication recovery assertions are unchanged.

All **5,709 service/tournament tests across366files** pass with zero failures/skips. Server TypeScript passes. These are local engine tests with mocked transport; they do not prove deployment, all production races or whole-fleet recovery. No SQL or live event mutation is included.

G8's separately owned Manager0059 source at317d4b83 was inspected and retains the same missing row gate. This change is confined to the row-adoption decision and preserves the existing diagnostic, epoch, pending-operation and lease machinery. Its independent publication/integration status must be retained in the program handoff.

Current-main composition with merged R27: all5,735service/tournament tests across367files and serverTypeScript pass. The only manual merge conflict was the appended audit register; both R27 and R28 evidence were retained. This does not establish serving or whole-fleet recovery.
