# Isolate publication decisions and release test evidence

Publication now resolves its exact protected-main target and runs all four client test shards on fresh hosted runners. Historical PR checks no longer skip the release suite. The installed test-dependency cache has a new publication-only namespace, so earlier entries from persistent PR runners are not restored. Build and tests still run in parallel; ordinary PR CI retains estate routing. Skipped, cancelled or failed release tests block both web and native publication.

CI cancellation is scoped to each PR and immutable head. An out-of-order old event cannot cancel the current required checks before the current-head admission step has a chance to reject it. Admission still rejects superseded work before dependency setup. Its runner assertions now match the merged CI routing while preserving read-only permissions and every admission/aggregate check.

The workflow predicates and group expressions are exercised directly. Eight regression cases fail on the previous workflow source. This is source verification; Actions execution, merge and production publication remain separate acceptance steps. No evidence of runner compromise is claimed.

The estate CI workflow explicitly limits Vitest to two workers per job, instead of relying on absent host service drop-ins. Server shards previously spawned about fifteen workers per job on shared sixteen-core hosts. An executable regression starts six real Vitest files and measures concurrent work under the committed workflow environment; shard counts, assertions and timeouts remain intact.

Accounting selects the independently qualified EU PostgreSQL pool through ACCOUNTING_RUNNER. Package installation runs only on fresh hosted fallback jobs; every job verifies all five PostgreSQL17 tools before dependency setup and tests. The existing23 accounting probes retain their own data/socket directories and disabled TCP. All three EU hosts already supplied root-owned PostgreSQL17.11 binaries; two concurrent disposable clusters per host passed with complete cleanup. The production origin's three runners are excluded from this qualification. The inert, read-only branch-proposal signal also uses the ordinary CI pool; privileged control remains separate.

The optional sharp loader now returns its established unavailable result on Mac before it creates a temp prefix or starts npm. Existing imports remain usable and Linux behavior remains unchanged. Six executable loader tests cover both platforms. This fixes one automatic local install path; World Hub's separate workspace helper repair and execution-wide dependency protection remain separate work.
