# Browser Fixtures Load Complete Styles Once

The reduced-motion animation check inherited a full stylesheet load and table mount, then repeated both in a second context within one 30-second budget. Set its media preference on the nested fixture before setup and use that page. The media assertion and one-millisecond animation limit remain intact.

The shared loader also started the application behind each synthetic layout and installed each stylesheet through a separate browser call. A failing replay-card trace contains 146 installations taking 10.4 seconds in those calls alone, plus application startup and transport overhead. The loader now reads the real manifest and styles, navigates to a temporary blank document at the same origin, and installs separate stylesheet elements in manifest order in one browser call. It waits for every load event, including imports, and removes its navigation interception. No application scripts execute during fixture setup.

A missing declared stylesheet could previously leave the other 145 accepted as a complete bundle. Every declared sheet must now be present and nonempty. Unreadable evidence fails required CI; an explicitly local unknown result retains its reported skip. Browser specifications, shared fixtures and Playwright configuration also trigger the browser build and CSS gate, so a test-only change cannot skip its own qualification.

The loader contract fixture is explicitly invoked by both PR-time CSS checks and the post-deploy sweep. It validates the loader itself against a local server; the neighboring browser specifications continue to measure the application's complete styles. The existing coverage declaration rule remains unchanged.

Validation: 26 classifier checks pass. A real Chromium transport fixture verifies 146 separately ordered styles, an imported rule, changed-bundle reads and navigation cleanup. The exact old loader accepts 145 sheets when one returns404; the corrected loader refuses it. A delayed-CSS probe proves the original reduced-motion test times out on its duplicate load while the single-fixture test passes; ordinary deal/winner effects retain350/600ms. Complete CI against the application's built CSS remains required. No timeout, retry, product CSS or animation assertion is relaxed.
