# Bounty residuals require complete knockout coverage

Tournament completion checked obligations that existed but could miss an
accepted knockout for which no obligation had been created. The champion could
then receive that head as residual, even though its exact claimant was known.
The private reproduction assigns all10units to the champion while the accepted
knockout has no obligation. Historical read-only witnesses show the same gap;
their residual totals do not establish individual compensation amounts.

Both ordinary and mystery terminal roots now check the complete candidate to
obligation anti-join under their existing locks, after immutable receipt replay
and winner validation. An exactly attributable missing generation returns
`unclaimed_bounty_candidate`. Unavailable or malformed accepted evidence returns
`bounty_candidate_evidence_unknown`; it cannot prove champion entitlement. The
mystery check precedes award reveal, pay and void operations. Candidate status,
current head, rebuy label or current winner status cannot exempt an old head.

The shared unsettled predicate remains unchanged, preserving activation and
satellite replay behavior. It still verifies existing pending obligations and
complete markers. Genuine champion own heads and unearned chest inventory may
close after all recorded generations are covered. The new helper returns only
the first refusal witness; absence is proved by the database over the complete
event. A10,002-row native case verifies both complete coverage and a missing
final candidate, measured at roughly4ms locally, not a production SLO.

Validation includes100 private native assertions, actual concurrent database
sessions, exact migration replay, source/metadata drift and seven role refusals.
The former structural assertion requiring an inline advisory lock is updated
to prove the actual global lock helper acquires both canonical locks in order
before the root's event read. No lock or financial authority is weakened.

No historical payment, pending amount, receipt or recipient is rewritten.
Stranded old generations require separate reconstruction and exact correction
authority. Full provider, Diamond/rebuy, top-level native terminal, protected CI,
installed readback and natural recovery are separate acceptance facts.

Installed as history20260914140327 after the database's protected break window
ended; three exact postimages and all five pinned dependency definitions and
metadata read back at14:03:40UTC. Both existing historical witnesses and the
active PLO8 event produce explicit missing/unknown coverage reports without
calling a financial authority. The full tournament suite passes2209cases in
164files, with app and server TypeScript passing. No natural complete terminal
journey or historical correction is claimed.
