# Zero-Stack Seat Closing During Concurrent Hands

An accepted tournament hand first writes a busted participant to zero, then closes that exact seat generation. The F06 custody trigger promoted the hand's shared tournament lock to exclusive at the close. A hand at another table in the same tournament could therefore make this hand return `rolled_back: F06_RETRY_CANONICAL_LANE` even though no table move was active. Original operational cases 37326 and 37327 preserve the hand failure and its separate drift mirror; root investigation is 37422.

The trigger now shares the existing global and tournament locks only for an already-zero row transitioning from occupied to left, with the exact same row, table, user, seat, join time and club. It still executes the original operation and receipt checks. A parked source needs the existing accepted-hand dispatch, and a begun source needs its canonical move dispatch. Funded exits, changing identities or seat generations, insert/delete, and roster custody changes keep their original exclusive path. No runtime flags, new authorizations, retries, financial data rewrites or alert closures are introduced.

Validation:

- The required native PostgreSQL 17 gate includes all 44 preceding lane checks and 34 fixture, predecessor, zero-seat, receipt, exclusivity and rollback checks, 78 total. The original guard refuses the concurrent zero-seat close. The candidate accepts it while positive stack exits, simultaneous stack clearing, changed identity/row/generation/club/table/seat, wrong status, and invalid park receipts remain refused. Global and tournament exclusive holders still exclude the close.
- A private copy-on-write clone of the cold full financial fixture executes the actual twelve-argument accepted-hand authority. The original accepts a 10+10 to 0+20 hand alone and refuses under a concurrent shared tournament holder without leaking chips, time banks, seat closure or an atomic receipt. The guarded candidate accepts and replays the same hand with exact seat and roster balances and an atomic receipt.
- A second full financial test commits the real park operation and reserved hand permit before the concurrent holder enters. The accepted hand closes the zero seat, and the actual post-commit obligation processor consumes that exact permit, carries both time banks and leaves no dispatch residue while the other table's holder remains active.
- The unchanged complete F06 custody and hand probes pass. All private clusters were stopped and removed; the original cold fixture was never started or changed. Final full evidence is `evidence/f06-bust-full-schema-qualification/20260914T002705Z/RESULTS.json` in the operational-alert task workspace. Original cold control SHA256: `58ee98e6cbf1a5f22799fa04e30376b16e39401bc4fd792ba355970652209ae2`.

The migration checks exact predecessor functions, owner/ACL and both enabled trigger bindings, verifies the resulting definition, and is replay-safe. Expected guard MD5: `d89dc77965f1abea47e366ad6f6a426f`. Release status and natural production evidence remain separately tracked in the operational-alert task. A recovered source alert alone is not proof of repair; the historical competing lock holder has not been recovered.
