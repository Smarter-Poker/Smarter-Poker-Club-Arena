# Keep the booked tournament start through recovery

A prepared timed tournament could resume its blind clock after an earlier break, or admit a replacement dealer without restoring the launch receipt's future start. Slow initial setup also delayed the first blind level by adding the original lead again after setup. The retained September 17 event evidence proves early cards and levels; it does not establish which dealer replacement produced the first early hand.

The manager now restores the receipt-backed `started_at` hold before every dealer start and replacement admission. Every blind-clock admission waits until that same timestamp. The existing first-level timer has one cancellable owner, so a pause, rearm or stale callback cannot reset the active level. Initial setup uses the absolute admitted deadline. Existing immediate SNG admission, Spin reveal, break and lifecycle fences remain in place. No engine physical-boundary, maintenance, database or release mechanism changes.

Validation: original source failed four directly targeted checks; the final connected actual-deal regressions fail six cases against original source and retain fourteen passing cases. The final candidate passes 184 tests in fourteen related existing files and the server TypeScript check. MTT and timed satellite cases execute actual manager start/resume, dealer admission, dealing loop, HandController and first action timer with controlled persistence/transport boundaries. Clock tests cover full first-level duration, earlier breaks, replacement recovery and stale callbacks. No production financial activity, deployment or post-deployment behavior is claimed by these local checks.
