# Immediate recovery inside the existing engine release transaction

A failed release could consume the small admission interval at the start of the hourly break and wait for the next hour. The existing 285-second candidate/rollback requirement stays intact.

The host transaction can now reserve one immutable announcement timestamp per run after a durable failed ancestor that has not shipped, or after directly observing a missed certificate. It reads existing seal failure receipts; no GitHub request fields, frozen v1 wrappers, scheduler, separate publisher or release observer gain write authority. A durable countdown that runs below the required budget qualifies as missed even when a hand straggler never allowed restart certification; missing health or unconfirmed maintenance cannot qualify. This observation grants no cutover authority. An already serving target still completes by exact certification without another pause.

The running engine receives the fixed timestamp through an internally authenticated, loopback-only request. Its existing maintenance owner checks the installed database capability, startup recovery, current break/thaw and separation from the next hourly announcement. Accepted requests use the same two-minute lead, five-minute countdown, table parking, ownership token, absolute end and v3 thaw as the regular break. Duplicate or expired requests cannot extend it. The maintenance health snapshot withholds recovery readiness until prior thaw and every resume wave finish, so the transaction does not spend its single request on a still-resuming fleet. The endpoint rechecks the same state after the database read. An unknown HTTP outcome retains the original reservation. A failed extra attempt does not allocate another timestamp.

The database guard now covers the durable announcement, countdown and release boundary outside the hourly clock window. Governed DDL takes the existing maintenance boundary lock shared; an exclusive announcement cannot race migration admission. Existing provider-role exclusions and audited emergency overrides remain. The service-only capability requires both expected guard bodies and both enabled event-trigger bindings. Preimage assertions prevent overwriting concurrent guard changes.

Qualification uses the existing server suite, release-seal suite and required PostgreSQL fixture job. Cases cover authentication, malformed input, duplicates, process replacement, lost acknowledgment, stragglers, expired replay, fixed duration, original hourly behavior, receipt eligibility, unknown request outcome, rollback budgets, real PostgreSQL roles, DDL rollback and concurrency. The isolated PostgreSQL test demonstrates the old outside-hour admission gap before applying the candidate.

Installation order: qualify and install the exact migration outside protected maintenance, read back its history and capability, then publish through the existing protected engine route. The first runtime installation may still use an hourly window because an older serving engine has no recovery endpoint. Source qualification is not production installation or an observed production recovery event; record those separately in the task evidence.

Database prerequisite installed September17 at19:06 UTC as Supabase migration20260917190616. Readback returned `engine-recovery-window-v1`, both enabled DDL event bindings, service-role execution allowed and anon/authenticated execution denied. The reserved source filename was aligned to that actual installed version without replaying or changing the SQL. Runtime publication remains a separate verification step.

The current pull-request event also retires obsolete CI attempts of that same PR in the existing change-classification job. Ordinary cancellation left `always()` validation jobs consuming workers after newer heads existed. The bounded correction verifies live PR/run identities before each cancellation, uses force only after ordinary cancellation remains nonterminal, and confirms the terminal result. Current-head checks, other PRs, production workflows, fork authority and required failure-reporting conditions remain protected. No timer, watcher, new publisher or recurring cleanup is added.

The client/engine routing audit executes the actual staging shell against isolated Git histories: client pages, styles, assets, instructions, server tests and simulations do not dispatch an engine release; runtime, release-control and mixed changes do. A 310-file client change also remains outside engine activation. The existing client publisher depends only on its own target, build and test results and has a separate concurrency group. The CI classifier excludes ordinary client paths from engine/native-fixture checks while preserving explicitly connected financial validation. This is retained regression protection for the existing separation, not a claim that a new hourly client gate was removed. A real client publish (35261914997) activated at19:12:18–19:12:25 UTC and verified at19:12:27; its same-commit engine detector explicitly skipped dispatch. Its repeated client tests were required because protected main's resulting tree differed from the tested PR tree.
