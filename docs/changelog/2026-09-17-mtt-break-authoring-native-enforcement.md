# New MTT ladders cannot author ignored break rows

The manual creator and schedule authoring RPCs previously accepted `isBreak: true` levels even though the tournament engine skips those rows. The client prevention shipped separately in PR4801. This change rejects unsupported new MTT ladders at the two authenticated authoring boundaries, preserving authorization, fees, payout terms and the supported synchronized-break opt-out.

The schedule guard compares the submitted config with the locked stored row. An unchanged historical configuration can still be enabled, disabled or renamed. A new or changed configuration cannot introduce another ignored break. A target-linked `sng` or `spin` schedule is also a satellite in the compatible scheduled writer, so its target keys use the existing prepared `fn_ca_is_new_mtt(jsonb)` classifier. Genuine fixed formats and empty target objects retain their existing classification. This pure classifier does not activate unlimited entry admission or reinterpret funded events.

The migration validates both RPC preimages and the prepared classifier's exact body, definition, owner, grants and configuration before replacement, and validates both RPC postimages before commit. An unknown successor refuses atomically. The migration is not installed by this change's local checks; installation and readback remain separate.

The existing MTT preparation driver now invokes the maintained authoring module after all eight preparation stages. It clones the actual prepared template, loads only absent captured dependencies, proves existing authorities were preserved, and runs the actual RPCs as authenticated and anonymous sessions. No governed creator is overwritten and no alternate runner or workflow is added. Module, fixture and probe edits select the existing accounting job.

Retained local evidence: the original manual defect is red; the former fixed-label schedule guard separately fails the target-linked satellite counterexample. The corrected composed-current PostgreSQL17.11 probe passes25 assertions, four exact classifier/body/ACL/postimage drift refusals, and full data/catalog rollback. The owned cluster was stopped and removed. Existing routing tests pass288 assertions after three selector failures; existing runner tests pass29 after four newly added checks detected missing wiring/preflight. Their execution seam is explicitly mocked and proves order, failure propagation and source binding, not native SQL behavior.

This qualifies the exercised authoring path under the prepared legacy admission ABI. It does not certify unlimited activation, concurrent schedule edits, engine break timing, a funded tournament journey, production installation or hosted enforcement before the required checks execute.

Production installation: root applied the qualified migration once at 2026-09-17T21:41:47Z as provider version20260917214147. The independent21:41:59Z readback matches both RPC definitions, owners, grants, search paths and the unchanged classifier; admission remains legacy-capacity-v1. No production tournament was submitted as a test. Required protected source integration and hosted checks remain separate.
