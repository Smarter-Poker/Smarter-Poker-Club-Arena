# MTT preparation runs through the existing accounting check

The retained R46 preparation inputs now run as one source-bound chain in the
existing required PostgreSQL accounting job. Eight real native probes retain
their direct refusal, authorization, business-field and rollback assertions.
Three admission-lock specifications also retain their removed-barrier
negative controls. The final prepared catalog runs six synthetic-future and
two legacy creator/restart concurrency cases with the existing strict stock
PostgreSQL output consumer and a fresh disposable database for every case.

The integration preserves exact captured function, ownership, ACL, trigger
and constraint checks. It supplies the real `pg_trgm` dependency used by the
read projections and requires precisely the foundation probe's intended
normalizer-refusal warning. Other warnings, missing markers and partial
assertion results remain failures. The final read probe uses a valid minimal
row to reach the intended recorded-capacity CHECK and requires that exact
constraint to reject its unqualified NULL capacity. It no longer removes a
constraint or seeds an impossible positive row.

The existing accounting job supplies its already-built matching isolation
tool. New fixture, probe and driver paths select that same job, and attempted
native results are retained as artifacts. No new CI job, release mechanism,
scheduler or production operation is introduced.

Local workflow routing was red before wiring (14 new failures, 219 existing
passes) and the current expanded routing check passes 234 cases. Runner and transcript regressions
passed 25 and 22 cases. The current local PostgreSQL 17 composition passed all 249
native assertions, the original-seat refusal, twelve exact drift
refusals, three admission lock specifications with removed-barrier controls,
and all eight creator/restart races. Every case database was removed and the
owned cluster was stopped and removed; executable hashes matched at completion.

The foundation keeps schema locks separate from finite historical format
qualification. Its database trigger requires the original creation, contract,
entry and launch witnesses and unchanged business fields. The shared SQL renderer
accepts at most 128 explicit event IDs, retains three-second lock and five-second
statement bounds, and cannot run under a tournament schema lock. Native checks
exercise partial commit, replay and rollback with all origin guards enabled.
Strict launch consumers refuse installation while any active parent remains
unqualified. Preparation preserves legacy admission throughout.

One satellite authority had advanced since the historical capture. The fixture
now requires its exact historical body, definition, owner and ACL before
installing the captured current successor and checking those identities again.
Preparation 061 changes only its expected current pre/post hashes and retains
the current service-role permission; its four admission edits are unchanged.
Wrong predecessor bodies and permissions are tested as rollback refusals.

The inherited fixture does not include the newer satellite finish-lane and
weekly-accounting dependencies, and preparation does not invoke funded
satellite settlement. The exact current terminal body/ACL is checked, but
current funded terminal behavior is a separate qualification requirement.

These changes do not activate unlimited entry. Real activation concurrency,
installation, compatible application publication and live behavior remain
separate acceptance requirements. The historical unrestricted R46 migration
is not run by this preparation command.

The same existing native owner also executes the exact three-source historical
Free Buy proof using a separate clone of the original captured template. It
retains original cutoff refusal, independently expected 0.20/0.10 fee plans,
unchanged financial evidence, exact replay, nine drift refusals, and the
single-call self-aborting production probe. The one-time append preserves the
original earning terms and current capture cutoff; it changes no wallet or
ledger balance. Preparation triggers declare their purpose at creation.

Production installed the eight preparation stages with legacy admission intact
and exact function/permissions readback. A separate three-row registry migration
completes declarations for the already installed stages without replaying DDL.
After the rollback probe and exact historical proof installation, both affected
Free Buy events completed through their ordinary terminal authority. Forty
prize receipts match forty posted ledger credits totaling 692.70 chips; all64
backing tables are closed, no live seats remain, and both escrows are zero.
The inherited parent counter retains1 and is not used as proof of a live seat.
Compatible engine publication and unlimited admission activation remain pending.
