# Must Move Application Recovery

The September 13 application restoration removed later Must Move fixes. This package replays the missing owned application changes from PRs 4615, 4619, 4625, 4628, 4632, 4641, 4651, 4666 and 4671, the cash-seeding shutdown patch, and the four final Must Move patches. It retains the arrival-notification and React 19 regression corrections qualified in engine release `57653ba0f3429cb4b8db7f1785a95250fe6fb94d`.

Correct behavior: lobby replies belong to the current viewer, game and opening; game queue entry and exit use authorized database doors; holds and presence belong to the exact seat occupancy; confirmed earlier batch outcomes survive a later failure; turn clocks and controller continuations retain their owner. Stopped cash-seeding work cannot start another page read. Existing business polling is preserved.

PR 4664 restart-count behavior already survived restoration, so its source and tests are not duplicated. The PLO card uses its approved versioned image and the exact previously published bytes. The original permanent image is unchanged. The prior buy-in reference collision repair is not required by the restored buy-in component, which does not consume that asset.

## Database Custody

Read-only production verification on September 17 confirmed installed versions `20260914101745`, `20260914110751` and `20260916044342`. The last is the installation identity of the original source candidate `20260915134400`; its source filename and fixture reference now use the installed identity. Its retained statement SHA-256 is `eb7a8bff8c81fc7963d348f46e4a300693b55440bde549264957cfabbaef5015`. Arrival proof remains service-role-only, uses a pinned search path, and joins the exact active destination occupancy. Its destination-occupancy index is valid and ready. The game/table waitlist cancellation definitions and pending-move occupancy field remain installed. No production migration or data mutation is needed for this replay.

## Delivery Checklist

- Preserve the restored provider workflows and external telemetry removal.
- Run existing required GitHub-hosted client, server, build and isolated PostgreSQL checks on the final integration.
- Coordinate actual shared engine dependencies and preserve provider concurrency safeguards.
- Complete protected merge, client publication and engine release through the restored routes.
- Verify both client build identities, the engine component identity and owning release proof, then bounded affected UI behavior.

Historical checks are source-recovery evidence. They do not qualify this new integration. This package does not start Phase 2 or claim whole-application, full shutdown-cohort or financial-program certification.

The final recovery retains the qualified roster-wake, controller-continuation and regression-registry corrections from `57653ba0`. The move-arrival fixture uses the restored time-bank interface (remaining seconds and remaining uses); it does not introduce the later unlimited-activation feature. Existing required checks exposed the missing registry header, two historical source assertions and that fixture-interface mismatch before this correction.
