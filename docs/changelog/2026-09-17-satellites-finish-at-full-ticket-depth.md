# Satellites finish at full-ticket depth

The manager previously continued a multi-ticket satellite until one player remained. The current settlement receipt also required a single winner, so changing the playing-count threshold alone could not fix it. Two connected finish-stage counterexamples failed against the original source.

Recorded `mtt-v2` satellites with an ordinary unlimited MTT target now use a version-3 terminal contract once their entry terms are final. The authority distinguishes full tickets from a separate cash remainder, proves the positive-stack qualifier cohort and the eliminated tail, and refuses pending hand writers or causal eliminations. Qualifiers remain unranked. If the last accepted elimination crosses the threshold, already recorded eliminated positions determine the additional ticket and remainder recipients. Financial award slots are stable identities, not invented finishing positions.

The engine holds the existing next-deal boundary on a bust and on adoption, verifies every current engine is physically parked, then re-reads eligibility before invoking the atomic settlement owner. It preserves manager generation, maintenance, ownership and abort guards. The existing financial lane serializes settlement and ambiguous-outcome resolution; a lost response does not authorize another payment. Existing version-2 receipts, funded legacy formats and the single-ticket path retain their original authority and economics.

Real-time law: triggered by the `satellite_qualifiers` discrete WS event, no snapshot diff. The table exit also reads the signed-in player's immutable receipt on initial load and reconnect. Result cards, the archive and standings show equal survivors as “Qualified” without champion or ordinal labels. Ordinary tournament winners remain unchanged.

Local qualification in the owned PostgreSQL 17 fixture passed 67 cohort assertions, 20 current version-2 financial controls, four installation refusals (including the real money-DDL registry guard), and actual two-session commit/rollback races with two removed-lock negative controls. Full data/catalog rollback and owned database cleanup passed. The fixture preserves the captured production state, including seven disabled historical Stage-B triggers; it does not claim those proposed guards are active. Opening balances and accepted-hand witnesses are explicit private fixtures, not production activity.

The connected engine checks passed 126 assertions. Table exit/result-card checks passed 51, and archive/standings checks passed 46. Both compilers passed. Original-source controls failed for the new engine, table-exit and archive behavior. The native race-output reader additionally refuses missing evidence, echoed SQL, unexpected diagnostics and the wrong financial outcome.

The migration was installed as provider version `20260917231519`. Exact function bodies, owners, execution permissions, receipt shape and approved money-writer registration were read back. The initial unregistered-writer attempt aborted completely; the retained native negative control now exercises that same production event guard. This source requires the maintained R46 format/admission preparation and a qualified unlimited-MTT activation before its new-format path can be used. Protected CI, integration with newer manager changes, installation/readback, publication and affected live behavior remain separate requirements. No historical event, payout formula, production balance, publisher, timer or repair service changed in this work.

The maintained caller composes all eight installed preparation migrations and the synchronized-break authoring authority. Its parser and native transaction/race checks execute in the existing required accounting job, with retained receipts on failure. The manager integration preserves the newer booked-start hold before arming the independent qualifier boundary.

The final submission states the ranker's already-required service-only grants explicitly and documents that the quoted trigger DDL is an unchanged catalog precondition. The original installed source SHA is `167b98bd1840cfa7b75664dac65226150fd4a2e1c57990258ce965dfe3cc94e5`; all eight function bodies remain identical. Fresh installed ranker body, owner, grants and enabled attachment readback matches. The migration was not replayed. This resolves the local static authorization/declaration findings without bypassing or changing either checker.

The final native rerun exposed a local test transport deadlock while the expected unregistered-writer rejection returned a large SQL context. Both process stack samples showed blocked writes and PostgreSQL was idle in an aborted transaction. The negative candidate now uses the same runner's file input path, retaining the exact refusal, catalog/data rollback, deadline and cleanup checks. The interrupted private cluster was stopped and removed before requalification.
