# Breakfast keeps its original rank and paid contract

On September 8 the original engine requested rank 21 and zero prize for the
remaining zero-stack player. That request failed its live-seat roster guard;
later attempts recomputed different ranks. The other 32 recorded ranks and four
paid prizes already agree with the original five-place payout contract. A later
pending entry-close record contains a conflicting four-place snapshot.

The bounded continuation admits that one original failed-request witness in its
own immutable receipt, preserves the 32 recorded ranks, releases the exact
zero-stack seat through existing custody authority, and restores only the
pending entry contract. The existing cash owner derives the lone survivor and
uses its original idempotent payers. Its legacy sequence branch accepts only the
exact private witness; ordinary tournaments retain existing sequence checks.
There is no synthetic old hand, knockout candidate, historical sequence, new
fee rule, chip rebasing, scheduler, or paid-record rewrite.

The operation checks the full recorded roster, paid rows, obligations, wallet
keys, original fee charges, physical seat identities, current manager identity,
and remaining funding before changing state. A later hand, permit, candidate,
lease or financial contradiction refuses. Three unchanged unfinished snapshots
on closed tables are preserved as historical evidence. Any downstream terminal
or fee refusal rolls back the rank, contract, receipt and attempted payment.

The unexplained 22,255 chip excess remains recorded unchanged. The 17 chips of
historical fee charges are not proof of earning terms. The prerequisite version 3
terminal keeps those fees in the original escrow with an immutable unresolved
custody obligation, no bank credit, and incomplete accounting. Final player
results and financial certification remain distinct.

The directly invoked private PostgreSQL verification uses the retained full
34-player roster and original paid records with the exact frozen fee-custody
dependency. It requires only the remaining 53.12 prize once, all 32 earlier
places and 99.88 paid unchanged, and the original 17 fees held. It also checks
full rollback, committed replay, private permissions, immutable receipts,
dependency drift and actual concurrent requests through the existing lane.

Validation and installation status are retained with the exact native receipt;
source authoring alone does not certify a completed tournament or publication.
