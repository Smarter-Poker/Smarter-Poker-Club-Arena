# Diamond Spins controls and authenticated spin repair

The wheel now fills the available page width, with entry selection, Daily Bonus choices, balances and action buttons below it at every screen size. The approved wheel assembly and control artwork remain intact. The page no longer caps its measured wheel width at 1,000 pixels.

Reported spins reached the authoritative diamond ledger writer but were rejected by the profile balance guard because its approved caller list omitted the new wheel transaction. The correction and authenticated database qualification are recorded with this delivery; recovery continues to use the original request identity and never invents a new debit after an uncertain response.

Validation: the existing real-page width regression failed at 1,248 pixels before the change and passes afterward; its control-location assertion now keeps entry navigation below play. The existing browser reveal cases also check full-width geometry and control clearance. Local desktop and phone-width previews were inspected, the complete client compiler passed, and 80 focused wheel cases plus 62 applicable UI/source contracts passed. Required provider checks, installed database verification and publication evidence are recorded separately in the task checkpoint.
