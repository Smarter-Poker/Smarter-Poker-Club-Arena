# Drained tournament owners retain exact custody through replacement

A physically drained manager could remain registered after its premanifest park refused ordinary stop, preventing a genuine replacement lease. A separate idle check could then retire a new owner merely because no retained hand history existed.

The existing stop/admission transition now retains the exact original manager and engines before removing activation slots, checks the complete private state and both maps after the awaited database assertion, and claims the successor through normal protocol-2 admission. Reserved originals keep that owner in a non-dealing recovery mode with normal lease renewal. Existing original submission recovery remains reachable; no payload, hand outcome or lease is fabricated.

The new service-only RPC reads and locks the existing authoritative custody. Terminal admission requires original accepted postcommit/history, immutable preparation cancellation, or an exact fenced abort receipt. After all originals are terminal, references and immutable evidence move to the retained archive before normal startup. The authenticated diagnostics endpoint includes both custody collections with bounded, explicit coverage and no financial proof payload.

Install the qualified read/assert migration before this runtime. Database installation, the original physical predecessor identity, business disposition and certified runtime publication remain separate acceptance gates. Existing GitHub accounting qualification runs the directly affected PostgreSQL cases and retains their evidence.
