# EarlyBird retains its original projected witness

EarlyBird's completed, unaccepted hand snapshot was eligible for ordinary pruning while its original F06 permit remained reserved. The preventive retention change preserves future unresolved witnesses. For this already missing row, the original 13:04 database query retained every snapshot field used by the existing disposition transaction, the original whole-row MD5, and the exact permit. The complete row and `state_json` do not survive.

Migration `20260918232101` extends the existing mixed disposition authority for this one exact event and original permit. An immutable source constant contains the original projection and capture hashes. The transaction refuses altered evidence, another permit, or a present replacement snapshot. It consumes the observed fields directly without constructing or inserting a snapshot row. The durable receipt labels the case `pruned_completed_unaccepted_mtt` and retains the original snapshot identity/hash as historical provenance, whose whole-row hash cannot be recomputed from the projection.

The same transaction still validates and locks the current service actor, platform freeze, protocol-2 lease, whole reserved set, generations, roster, original paid custody, cards, earlier financial receipts, and retained-submission fence. Its outcome remains `aborted_unsettled` with zero credit. The prior 2,500-chip payment is not repeated, and no hand result, winner, payout, new lease, or deal is invented. Normal present-snapshot and prior-commit paths remain supported.

The existing native PostgreSQL F06 qualification invokes the new boundary directly. It checks old refusal/new acceptance, immutable historical evidence, current guard refusals, both ordering directions against accepted settlement and retained submission, financial writer locking, durable outcome readback, identical/changed replay, and unchanged payment and 53 prior receipt pairs. The fixture uses the exact historical event/permit/projection identities with isolated synthetic dependency records; it does not certify historical provenance or physical process retirement. Existing CI routing and source bindings include these inputs.

Installation changes only the function definition and performs no business disposition. Production use requires the owning task's separate current-state and physical retirement proof, protected installation, actual self-aborting probe, and unchanged operation readback. Source/native evidence alone is not production recovery.
