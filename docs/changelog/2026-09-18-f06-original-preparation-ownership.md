# F06 original hand preparation retains its owner

A tournament sweep could read a reserved original permit while `dealHand` was awaiting time-bank allowances, classify that ordinary preparation as recovery, and request a whole-table break. Its later custody acquisition could then stop a controller that had started during the park request.

The original dealer now retains an exact preparation token until its preparation exits through the existing `finally`. Manager admission recovery and durable-break binding consult that owner before selecting the permit. The ordinary retained-permit view remains unchanged for release safeguards. Failed preparation, a pre-start pause return, and genuinely retained unknown outcomes remain recoverable through the same original custody path; no permit, history, or financial evidence is discarded.

Verification: the direct Manager/dealer race fails against the previous source by issuing a park request during a pending preparation. The existing Manager flow suite passes 42 tests after the repair, including delayed BEGIN, delayed allowances, real controller start, failure and pause exits. Five connected F06 suites pass 47 tests; server TypeScript passes. The existing required server CI runs these test files. Production installation and disposition of previously stalled original objects require separate owning-operation evidence; missing hand history does not establish non-actuation.

Current-main composition preserves the newer bank allowance, satellite qualifier, and rule-refusal behavior. Its accounting admission initially rejected a stale hash after PR4820 tightened the existing TournamentTimerService discarded-error allowance from two to zero. Only that repository fixture binding is refreshed, with the prior hash and protected source recorded; captured accounting fixtures, financial bodies, native assertions, and historical provenance remain unchanged. The existing ratchet, clock, and CI-routing suites pass 437 checks. The full maintained PostgreSQL accounting packet also passed locally, including rejection rollback, concurrent authority, native scheduler adoption, and original P&L/basis qualification. Its owned database processes stopped; final protected CI remains required.

The same hosted run exposed duplicate setup in the reduced-motion CSS test: its common hook loaded and mounted a page for about 19 seconds, then the test opened another context and loaded the bundle again until the unchanged 30-second deadline expired. The test now applies reduced motion to the already prepared page before the same measured animation beat. Every duration assertion remains unchanged. This removes the demonstrated duplicate work; it does not establish a root cause for the separate first-attempt local-preview socket error.

The exact-head CI classifier omitted the maintained live-animation spec and skipped its browser job. The existing selector now routes that exact spec to the browser/build checks, with a direct failed-before/passed-after path regression and a lookalike refusal. Only the two changed selector inputs in the Cash hosted manifest are rebound; no Cash SQL, runner, or captured authority changes.
