# Last-table positive no-start continuation

The original recovery path stopped the exact dealer and durably resolved its original permit as `never_started`, then required a destination table to finish the park. A last physical table has no destination. After a manager replacement, the same source exclusion also prevented normal startup from reaching a continuation owner.

The current-owner transaction now accepts only the immutable original terminal no-start permit and exact premanifest custody. It preserves the current protocol-2 lease, whole roster, prior accepted stack receipt, and exact canonical zero-stack departures. It records one permanent continuation receipt and withdraws that exact park. It never changes a permit outcome, seat, stack, registration, lease, payout, or hand history. Replay returns the same receipt; active private/snapshot/card/dispatch or movement evidence refuses. The existing five raw hand-writer bindings also fence that exact terminal hand after withdrawal. Receipt updates, deletes and truncation refuse.

The original manager retains unknown continuation identity under its exact stopped-dealer custody. Successful continuation releases only that park and uses existing registry compare-and-swap to admit a fresh engine. Successor startup can use the same authority only after the current candidate stops and joins. A read-only multi-table hint leaves ordinary movement admission untouched; if eligibility changes after the stop, the terminal candidate enters existing exact-owner recovery instead of falling through to movement startup.

## Changed paths and verification

- `TournamentManager.ts`, `TournamentManagerBase.ts`, and `tournamentTableBreakRpc.ts`: actual original recovery and excluded startup wiring, strict receipt parsing, typed noneligibility, and exact-owner replacement. Read before and after editing.
- Migration `20260918094425`: permanent receipt, narrow continuation authority, additive immutable transition and raw-writer fence. Required function/owner/ACL and all five live writer bindings are pinned; finite full-set relation admission precedes catalog changes.
- Existing F06 native runner: original and successor continuation, whole prior-three/current-two canonical elimination, identity/refusal, replay, zero financial effects, rollback, concurrent and late writers, immutable receipt/permit/park and ACL checks. Existing required accounting job owns execution; classifier tests include every added native input.
- Existing connected manager and RPC tests: the old source fails the actual last-table placement and lost-reply counterexamples. Current original and successor paths pass with real manager, registry, custody and permit objects; the database transport remains isolated and the actual transaction is covered separately by native PostgreSQL.
- The temporary composition with G8 movement admission passed 65 connected cases and server TypeScript. Its exact inputs are retained outside the source tree. Other authors' source remains separately owned.

Final local verification: 73 connected manager/RPC cases, 449 classifier cases, and server TypeScript passed. Native qualification: 586 cases passed, owned cluster removed, exact migration SHA `57bdce8ed60155806193cfc728bf5e92e97964ffff48106c70c064ca968ba608`. This source record does not claim production installation or business actuation. Root owns installation and the seven existing events' live readback.

Real-time law: no new player-facing signal; fresh dealing and existing named game events remain owned by the normal admitted engine, with no snapshot-diff trigger.
