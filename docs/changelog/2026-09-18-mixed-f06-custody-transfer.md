# Mixed F06 custody transfer and original-operation recovery

The original manager could retain a mixture of begun moves, winning receipts,
park requests and unresolved hand preparations after its lease expired. The
existing handoff admitted only untouched premanifest parks. This follow-up adds
an immutable complete custody receipt at that owning stop/handoff boundary and a
finite successor continuation of the original operation identities.

Root owns protected integration, migration installation, publisher operation and
production verification. This helper's scope is source and isolated qualification;
no production mutation or inspector attachment has occurred.

## Contract and connected behavior

Every exact dealer and manager continuation must positively drain. The complete
physical map, original hand bindings, allocator epochs, retained source map,
pending request vectors and inactive retirement reservations are captured without
clearing them. Lease-first preparation uses nonblocking earlier settlement and
player lanes, locks canonical operations/members/attempts/receipts, and appends
only an immutable transfer. Missing/changed physical, bank or canonical evidence
refuses. One preselected successor generation is persisted before registry CAS or
old-lease release; receipt discovery survives replacement of the process.

Admission requires a real claim by that selected generation, binds the actual
process/acquisition identity, and requires positive terminal evidence for every
original hand. It never rebinds an old dealer. All canonical tables are reserved
while the real successor restores pending park, begin, amendment, move, custody
and cleanup identities and invokes their existing RPCs. A first-send begin,
amendment or custody intent persists before dispatch. Unknown outcomes remain
attached. Pending no-start work without its existing immutable continuation
receipt refuses transfer; this path never invents no-start evidence.

The movement guard retains the full previous guard and adds only an exact live
admitted-transfer capability for original operation membership and source roster.
Old generation/operation/attempt/winner records are preserved. New dealer permits
remain fenced until canonical recovery completes. Completion validates original
outcomes, adopts proven native bank/presence custody and appends an immutable
receipt before local reservations are released and ordinary resume is admitted.
Completed history no longer participates in discovery; an ordinary later restart
gets a new generation. A different process cannot reuse a partially admitted
generation: it needs a new positively qualified ownership transfer.

Bank values require the complete same-hand, same-occupancy native snapshot,
including unlimited activations. Metadata alone never reconstructs balances.
An unrelated historic empty snapshot is allowed only when actual local metadata,
parked banks and live banks are empty. Nonempty or initialized evidence retains
all original checks. Completed native snapshots retain the oldest contributing
original parked timestamp, preserving the native disconnect expiry instead of
renewing it. Bank restoration itself has no age expiry.

Original engine objects and flags remain intact. Exact terminal receipt adoption
accounts for their preparations; unresolved source operations keep the native
maintenance and drain barrier until durable completion. No watcher, repair loop,
new scheduler or alternate publisher is added.

## Activation and remaining evidence

Serving source 8825 does not contain this successor implementation. The separately
owned, source-maintained legacy checkpoint extension captures actual original
objects, prepares the same durable receipt under the owning frozen release token,
and performs exact native unregister CAS only after original hand disposition.
Preparation validates the actual maintenance row and leader identity, including
finite timestamps and the full 285-second reserve; a caller GUC is insufficient.
It neither thaws nor writes leases, hands, seats or financial rows.

The two original-hand dispositions are separately owned by PR 4905 and preserve
the old lease and all mixed operation evidence. SQL disposition alone does not
retire a local preparation. Exact installed contracts, source publication and all
23 live original bank captures remain unqualified here. Root's readback shows
old empty rows for Noon and one old nonempty Afternoon row; actual retained local
metadata is still unknown and no balance is inferred from that readback.

## Ownership and validation record

Owned branch: work/mtt-mixed-custody-transfer-20260918. Worktree:
/Volumes/SmarterWork/agent-work/mtt-mixed-custody-transfer-20260918.
Initial base: bf1d632db65435f6ef1ac86b12268ab9167f405a.
PR 4901's prevention is preserved separately (protected merge 03669b1341).

Fresh resumption reading included all four canonical policies, the full portable
policy output with truncated portions supplemented from source, repository
AGENTS/AGENT-PLAYBOOK/CLAUDE/PUBLISHING, applicable .agents rules, deploy-paths,
changelog README, both required engine programme references, the existing
checkpoint and full mixed-recovery-analysis. Reader emitted
2026-09-19T02:32:09.877Z at 01eee6f2f7, policy version 2.9, manifest SHA256
7663cc909626f7e9966931d27166ad8774addc801f7ad1898a2d7564bc13c378.

Evidence resides in
/Volumes/SmarterArchives/agent-evidence/mtt-mixed-custody-transfer-20260918.
Native qualification uses isolated PostgreSQL 17.11 with the maintained actual
financial foundation, canonical movement/hand authorities and native presence
schema. It never uses active production games.

- native11 passed 59 mixed assertions, 28 existing drained assertions, both
  existing lock races and complete rollback. A separate disposable clone commits
  receipt, claim/admission and first original move in distinct sessions, loses
  replies, then proves one effect after the exact original UUID replay.
- connected11 passed 94 tests across the real GameServer/Manager custody path,
  drained handoff and native parked bank startup/roster/re-park. Seven interrupted
  ownership edges keep custody and block ordinary dealing. Server typecheck passed.
- native16 passed the expanded 65 mixed assertions, 28 strict custody assertions,
  committed restart chain, all four mixed settlement/player-lane lock-order
  permutations and both existing custody/admission races. Full rollback and
  local database cleanup passed. The earlier native13 lexer failure was fixed.
  The real publisher run-attempt ID 35405450271-1 is covered. Qualified installed
  function definitions, ACLs and hashes are captured as mixed_postimages in RESULTS.
- Final integrated source passed server build, 584 CI routing assertions and
  2134 related server tests (166 pre-existing gated skips). After checkpoint
  integration, 155 focused tests passed across custody, bank restoration, actual
  old-checkpoint semantics, diagnostics and lease boundaries. Two diagnostic-only
  test harnesses were updated to represent the added mixed capture entry point.
- The generator asserts every composed movement-guard replacement occurs exactly
  once; source drift cannot silently omit part of the original guard.

Normal commit hooks passed. Protected main a8ba0d4245 (including PR4901 and
PR4905) and e56f1f115a (PR4906) are integrated; the classifier conflict preserves
both sets of required inputs. Root authorized normal push after final checks.
The initial normal push refused stale exact fingerprints for the CI classifier
and routing regression in the existing Cash native manifest. Those two pins were
refreshed with their old values retained; Cash SQL and fixture inputs are
unchanged. All six maintained manifest/oracle preflight checks pass.

Native17 also passes the full native16 recovery suite and nine catalogue checks.
The existing publisher can read fn_f06_mixed_custody_contract() before creating
its one-shot intent or attaching to the process. This fixed service-only STABLE
catalogue returns hashes and authority metadata for fourteen exact functions,
including itself; no business rows are read or changed. The actual service role
READ ONLY result equals independent installed metadata. Browser roles and a
wrong JWT role refuse, while missing functions and altered ACL/config remain
visible. The schema fragment declares every new public/private function and
receipt relation; schema inventory and definer checks pass. The standalone live
telemetry exposure reader requires provider credentials absent locally; its
configured hosted check and installed readback remain separate evidence.

Normal pre-push validation, protected submission and provider CI remain pending.
No installed, published or live-recovered result is claimed here.
