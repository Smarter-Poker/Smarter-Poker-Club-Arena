# Mixed original and current generation disposition

The current MTT and Spin release blockers retain reserved hands from prior lease owners. One MTT hand has no snapshot: absence cannot establish that it never started. A separate explicit mixed-generation RPC binds the complete current event inventory and the exact current lease, then records one immutable event disposition and one fence per distinct original/current generation.

Snapshot-backed hands preserve each saved stack plus total invested, with dead investment already included. A missing-snapshot hand requires the exact last canonical atomic receipt, its distinct nested stack receipt, completed and sealed post-commit result, matching seat/user/joined-at stack lineage and no later authoritative hand writes. It is recorded as `aborted_unsettled`, with no invented snapshot, history, refund or credit. Other-table parks and accepted sibling outcomes remain unchanged.

The existing HU, original-successor and homogeneous-generation RPCs remain intact. Three existing shared guards additionally recognize the new immutable receipt family. The native F06 verification path exercises the real writer admission, lease and hand locks, takeover and settlement races, prior-receipt drift, conservation, rollback, late generations and exact replay.

This source change and isolated native qualification do not establish production installation or operation. The MTT operation owner must read a fresh complete inventory after the latest takeover and preserve the existing production installation and self-aborting probe requirements.

The no-snapshot proof checks absence of active custody through the existing unique live-table snapshot index. Whether completed historical snapshots exist is unknown and unnecessary: those records cannot establish accepted settlement or never-started status. Exact accepted, dispatch, private-state and prior financial boundaries still refuse conflicting outcomes. Historical snapshots remain unchanged.

The explicit HU subcase admits only `sng-v1`, a two-seat event with one open two-seat table, two exact live playing occupants, one prior-backed unresolved hand and no open park. It reuses the same complete prior canonical and post-commit proof, or a separately typed original zero-credit abort receipt with its unchanged terminal permit and withdrawn park. The latter binds every original roster identity and stack, including the immutable occupancy and registration, and refuses later accepted or private writes. The installer binds the actual occupancy stamp and unique index; current `joined_at` is captured without inventing it in the older receipt. A prior abort UUID has its own child column and can never impersonate an atomic hand UUID. Both original and current unresolved generations are fenced atomically, and the original terminal records remain unchanged.

Native controls retain the old format refusal and exercise both actual HU transactions, replay, wrong format/capacity, extra table/chair, missing prior proof, recreated occupancy/registration, changed stacks, late writes and open or started park refusal. Installation composes the separately qualified two-relation SHARE drain, preserving its finite budget and partial-lock rollback while allowing unrelated lease and history readers.
