# MTT activation retains original funding evidence

The installed activation guard correctly refused two registration and purchase
definitions changed by the original-funding migration. A separate successor
guard preserves every existing activation check and adds the exact installed
human, horse, receipt, inventory and transaction-frame authorities. It checks
the new private tables' columns, constraints, indexes, permissions, RLS and
immutable trigger attachments. No event, payment, historical receipt or ABI row
is rewritten by guard preparation.

The previous installed guard migration remains byte-for-byte unchanged. The
previous uninstalled activation transaction is retained as historical source;
the new row-only transaction requires the successor guard's exact body. It
still requires compatible runtime adoption and retirement of old writers,
including the actual rollback image. Source or local SQL cannot certify those
remote release prerequisites.

The existing activation runner composes the complete installed human and horse
migrations over its real financial foundation. Its bounded supplemental graph
uses read-only captured current inventory/frame/ledger definitions and table
metadata. It excludes unrelated weekly payers and cash-hand acceptance. It
does not replace an existing function unless the installed migration's exact
preimage accepts it. The reached refund-tranche frame column is included.

All prior activation races, refusals and funded-HU continuity cases remain.
Additional cases cover original human and horse funding before/after the ABI
boundary, exact 200-chip debits and receipt/frame identities, duplicate requests,
and whole-transaction rollback when the real old debit-capture function cannot
supply the new funding evidence. Six additional drift cases refuse weakened
funding permissions, identities, immutable/frame triggers and column defaults.

The first composed native run refused the missing horse-registration predecessor
before the horse migration could change anything. The fixture now restores the
four missing real registration-chain functions from the qualified accounting
packet. It verifies the five already-present public/capacity/settlement helpers
without replacing them, then checks that no existing catalog or data changed.
The horse migration retains its original exact preimage refusal.

The isolated admission fixture uses the actual legacy Chips house-board identity
accepted by the captured automation policy, and includes the real journal
fallback actor as a synthetic fixture identity. Production identity and platform
constraints remain enabled; no Diamond-only platform flag is fabricated.

PostgreSQL 17 qualification passed 14 races, 23 exact refusals, funded legacy HU
continuity and human/horse original-funding debit, duplicate and rollback cases.
All 112 execution inputs still match their recorded hashes; the private cluster
was stopped and removed. The existing classifier passed 339 checks and the
transcript parser passed five checks.

Guard preparation was installed once as provider migration 20260918014651 and
read back at 2026-09-18 01:47:01 UTC with exact body, owner, permissions and
configuration. The ABI remains legacy-capacity-v1. Row activation and dependent
client release still require the compatible sealed engine and rollback proof.
