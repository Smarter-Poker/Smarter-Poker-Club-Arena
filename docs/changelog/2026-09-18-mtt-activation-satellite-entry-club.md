# Preserve satellite entry-club repair at the MTT activation boundary

The installed original-funding activation guard pins 149 exact function definitions, owners and permissions. Two of those definitions are the terminal satellite seat producers. Their independently owned repair makes the transfer journal use the registration's actual admitted member club, which is required for union targets. The existing guard correctly refuses those changed definitions.

This change adds a successor guard from the exact installed original-funding predecessor and a separate row-only activation transaction. Only the two expected producer hashes change within the guard body. All other authority, trigger, schema, immutable-funding, active-format, maintenance and one-way transition checks remain unchanged. Installed migrations and historical captures remain unchanged.

The existing isolated PG17 activation path composes the actual producer migration, demonstrates the preceding guard refuses it, installs the additive successor without changing business rows or other catalog authority, and runs the actual activation/funded human/horse/HU and contention cases. Publication, installation and production ABI activation remain root-owned and separate from local qualification.

Root completed the native qualification: 4 connected probes, 14 contention cases and 27 refusal controls passed, with all source bindings exact and the private database stopped and removed. This includes predecessor refusal after the two producer changes, refusal of unrelated authority drift, real funded human/horse entries and historical HU continuity. No production mutation was performed during this qualification.
