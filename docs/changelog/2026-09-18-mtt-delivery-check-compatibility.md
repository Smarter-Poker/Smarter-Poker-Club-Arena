# Pending MTT delivery checks read their actual candidate

The first executing hosted check attempt for the pending MTT integration exposed
three stale verification assumptions. Four tournament SQL readers required a
closing dollar tag immediately followed by a semicolon, rejecting a valid
qualified function with intervening whitespace. They now accept that whitespace
while still requiring the matching tag and semicolon. Financial SQL is unchanged.

The lifecycle source guard prohibited every direct manager-map deletion. The
new drained-custody transfer deliberately removes the exact stopped manager only
after checking every captured identity and retaining its original custody packet.
The guard now checks that sole synchronous transfer and forbids direct deletion
elsewhere. Runtime transfer tests remain in place.

The full weekly accounting fixture retained four stale outer source hashes after
MTT routing and two nested qualification bindings were integrated. Refresh only
those reviewed successor hashes and record their prior identities and rationale.
All captured financial catalog fixtures, financial components and assertions stay
unchanged. The native accounting job must still execute and pass.

Validation: the five affected engine files pass all 54 tests. The unchanged
accounting binding guard reproduces the prior refusal and accepts the corrected
binding. Existing routing tests pass 552 assertions; the generated activation
binds all 37 components. Hosted checks, protected merge and production proof are
separate remaining delivery requirements.
