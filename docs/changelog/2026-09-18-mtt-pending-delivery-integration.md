# MTT pending delivery integration

Interrupted tournament managers retain their original stopped dealers and durable hand records while a genuine successor manager holds recovery ownership. Gameplay remains held until the original hands have authoritative outcomes. The recovery database assertion was installed once as provider version `20260918164824`; its definition, permissions and fixed search path match the qualified migration. Engine publication remains pending.

This integration preserves the previously qualified original paid-entry protection, retained hand submissions, parked table movement, original never-started continuation, snapshot access path, recorded legacy fee finality, Breakfast settlement and five historical Spin custody changes. It retains each source migration and original financial capture unchanged. The required Accounting workflow executes every contributing suite.

Conflicts were limited to joined workflow steps, test-routing assertions, captured-file formatter exclusions and source-input fingerprints. Both sides were preserved. The shared F06 native runner now exercises the snapshot access path with the no-start and interrupted-hand suites in the same isolated cluster; an already installed continuation is reused only after all five exact catalog postimages pass.

Validation is recorded in the task checkpoint against the final candidate. Independent financial qualifications with unchanged functional inputs are retained; the changed combined runner and required exact-candidate pre-push checks execute again. Protected checks, merge, publication and live behavior remain separate and are not claimed by this source integration.
