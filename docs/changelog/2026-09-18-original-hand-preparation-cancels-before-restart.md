# An original unstarted hand is durably cancelled before restart

A pause could arrive while `dealHand` awaited its F06 reservation. The controller was correctly discarded, but its reserved permit survived process replacement and refused the next dealer. Retaining that permit in memory did not make a restart safe.

The original preparation now irreversibly fences `permit.start` and awaits an exact durable cancellation before releasing its local permit. A lost reply retains the original identity and blocks restart readiness, including for a stopped dealer. The existing next-deal entry can replay only that same cancellation. Maintenance readiness and the shutdown drain both observe this unresolved state. Attempted controllers, unknown reservations and a changed original owner cannot claim no-start.

Migration `20260918071546` adds a private immutable receipt and a service-only cancellation RPC. It keeps the current protocol-2 Manager admission, lease, canonical lane and row locking, exact tournament/generation/table/lifecycle/hand/permit/custody binding, and existing terminal permit immutability. Duplicate requests return the same receipt. No park operation is fabricated and no signing key is invented. Existing BEGIN and settlement dispatch continue to refuse the terminal hand; the next distinct hand can reserve normally.

The proof is the retained original permit object and its positive synchronous non-actuation fence. Database witnesses are contradiction vetoes, not an inference that missing records mean a hand never started. Active snapshots use the maintained partial index; completed historical snapshots are neither scanned nor claimed absent. Startup orphan completion can mark a snapshot complete without settlement, so this API must never be used to reconstruct an original owner from persisted rows. Actual Manager admission still refuses an unresolved persisted permit.

Additive raw-writer triggers share the existing nonblocking tournament lane and reject a cancelled preparation. Tournament identity comes from the physical table, including before BEGIN becomes visible. This serializes original BEGIN, cancellation and late snapshot/private/card/history/atomic writers without taking an earlier blocking lock from a row writer. All five trigger-bearing relations are admitted before DDL: the first has the bounded lock timeout, the remainder use NOWAIT, and refusal rolls back the full transaction.

Validation: the connected preimage regression failed with a durable reserved permit after the real dealer stopped (14 existing cases passed). The repaired connected engine/permit/maintenance and focused existing tests passed (175 cases, plus the final 43-case Manager run including replacement refusal). Both TypeScript projects compiled. The existing native F06 runner passed 194 cases against captured current authority, including exact receipt identity, duplicate effects, rollback, lease/actor/identity refusal, attempted evidence, both writer orders, pre-BEGIN serialization, late writers, immutable history, maintenance freeze and busy installer rollback. The existing CI input classifier passed 424 cases. The migration remains uninstalled at this source checkpoint; protected checks, installation, engine publication and natural runtime verification are separate delivery evidence.

No player balance, prize, bounty, roster, seat or settled hand is modified by cancellation. This is prevention for original preparations in the new process; previously interrupted hands remain under their separately qualified disposition authorities.

Normal pre-push related tests exposed two constructor-bypassing fixtures that omitted the running state and tournament move pause set now read by the real preparation finally path. Both fixtures now initialize those existing fields; their original failure and boundary assertions remain unchanged (24 cases passed). The Cash native manifest updates only the shared classifier test file fingerprint after its two added input-path cases; all six existing metadata checks passed. Runtime and SQL behavior are unchanged by these submission corrections.

The first production installer attempt refused a concurrent private-state writer before DDL and rolled back fully. The installer now uses the maintained three-second bounded admission transaction: each later NOWAIT refusal releases every partial relation lock before retrying, while the five trigger-bearing relations retain SHARE ROW EXCLUSIVE admission. The direct native counterexample fails on the prior one-attempt installer; runtime authority and every SQL statement after admission are byte-identical. No external migration retry loop is added.

The corrected existing native runner passed 197 cases, including transient private-writer completion, bounded exhaustion, full rollback and release of earlier relation locks. The disposable cluster was removed and all 19 distinct source input pins matched.

Installation checkpoint: provider migration 20260918074842 installed the exact corrected candidate (SHA256 c8fe53556d98e5238bf2428b77a8a935d54f98ab21d004a14ea40cbb462ad2e4). Root read back all three function bodies and permissions, six enabled trigger bindings and the private RLS table. This branch independently captured its exact catalog and history at 07:49:11 UTC for the scoped schema manifest. No cancellation was invoked as a production test; engine publication and natural behavior remain pending.
