# Original interrupted hands preserve their retained custody

The mixed F06 disposition can now qualify two additional demonstrated forms of retained original evidence. Both remain zero-credit `aborted_unsettled` outcomes. Neither creates a hand, reconstructs a snapshot, changes stacks, or asserts that an interrupted hand never started.

- A three-player Spin with no target snapshot must retain its exact committed and inactive original dispatch, original semantic refusal and request identity, complete prior canonical stack and post-commit proof, and a bijection between three original private-card rows and the exact current user/chair roster. The immutable expected input binds every original card row hash without exposing card values. Later cards, snapshots, outcomes and submissions still refuse. The existing complete canonical financial receipt proof is unchanged.
- A completed, unaccepted MTT snapshot may coexist with earlier recorded final stack receipts whose canonical hand records are absent. The exact original paid-custody and snapshot proof remains required. Each earlier succeeded key must match a unique final settlement, identity, reference, delta/conservation result and totals, with finite ordered timestamps before that paid custody and an earlier numeric hand number. Present canonical receipts still require exact results. Other financial checks are unchanged. This does not claim the missing records were pruned or reconstruct accepted outcomes.

Both paths retain the whole-event lease and roster comparison, settlement writer drain, original/current generation fences, immutable operation identity and replay protection. Original card, snapshot, refusal, dispatch, paid-custody and financial rows remain unchanged. Actual production invocation additionally requires the operation owner's fresh physical custody and predecessor-process evidence; an unavailable current engine object does not establish an old process stopped.

The existing directly invoked native F06 qualification includes the original refusals, successful rollback and committed replay, malformed and later custody, row-hash drift, competing writer drain, accepted-settlement priority, late generation refusal, original-row preservation and compatibility with active/completed predecessors. A demonstrated baseline harness broken pipe now feeds its unchanged expected-deadlock suffix from an owned file, retaining the original deadlock, barrier and rollback assertions.

Source/native qualification, protected delivery, database installation and the actual original operations are separate evidence. MTT owns installation and production operations.
