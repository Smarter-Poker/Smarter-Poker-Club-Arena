# Paid Spin entry through current terminal authority

The prior six-image qualifier proves genuine paid entry and a separately seeded
mixed-history terminal. It does not connect a paid entry to the current launch,
draw and terminal path. The seventh image in the same finite owner addresses
that specific compatibility gap before candidate database installation.

Captured functions restore only missing current launch dependencies under exact
private preimage checks. Existing candidate components retain their guards and
ordinary modern entries must not receive mixed-history admission. Financial
operations run in the disposable database through the real authority. Synthetic
finish input is explicit and never represented as original hand history.

Local PostgreSQL 17.11 execution `7424712a-cf9d-469d-85c2-e0705f10f823`
passed genuine entry, lease/launch, funded draw, explicitly modeled finish input,
real terminal payment and exact replay. Independently checked cash conservation
is 100 = 97 treasury + 2 winner + 0.76 reserve + 0.24 Union fee wallet. Three
fee sources each recognize 0.08. All 50 altered-evidence controls refuse;
115 wrapper, 377 routing and 6 enclosing-manifest tests and root TypeScript pass.
The native run retained 136 exact inputs, 40 stages and 82 evidence leaves,
with original 240/30-second bounds and verified cleanup/disposal.

The independent verifier originally omitted the draw authority's fingerprint
augmentation and actual Union fee custody. It now validates their exact
provenance, numerical types, original financial identities and complete fee
recognition. A duplicate wallet record accepted by its initial dictionary
projection was reproduced and refused by the corrected cardinality check.
Original failed attempts remain retained; no success is retroactively assigned.

Required hosted checks, protected merge and publication of this extension remain
pending. No production database change, recovered historical hands, full financial
qualification or incident closure is asserted by this source record.
