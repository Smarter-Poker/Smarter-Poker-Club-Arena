# Production browser observation boundaries

Run 35310320354 reached an empty remembered MTT tab while the sticky-sort assertion expected populated game cards. The production spec now selects All through the real tab and verifies a rendered card before measuring the unchanged sticky geometry.

The wallet assertion passed its first count read, then exhausted the case's 30-second ceiling before the immediate second read; the retained failure page still displayed two balances. The case deadline now contains its existing navigation, readiness, greeting, count and persistence budgets. Count persistence, optical centering, clipping and footer geometry checks remain intact.

The live Club Data UI reported 199 players for one pair of pages and 200 after switching sorts; the saved report did not retain those response identities. Each pagination attempt now observes its own exact request and returned identities, validates within-page uniqueness, and requires the UI to show the unique first-page/continuation union. A later query cannot reuse an earlier cardinality as its oracle. The explicit refresh still must retain the latest expanded count.

No application, database, engine, profile-gate or publication safeguard is changed. The earlier profile-gate failure contained repeated cache-busting navigations without the failing import evidence; it remains separately unqualified.

Qualification: retained production failure supplied the before reproduction. Focused local results and the final protected/live browser result are recorded in the owning G8 acceptance checkpoint.
