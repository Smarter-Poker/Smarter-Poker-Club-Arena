# Satellite transfers follow the admitted registration club

The union-hosted satellite `46b07f4c-de04-43b3-bd21-10592b247c09` could not finish for winner `669a0232-3b6c-43bb-b6f4-c5e37f1ddfbf`. Its terminal producer wrote a target pool-transfer ledger entry using the host club while the admission trigger selected the player’s member club. The unchanged fee capture correctly refused the disagreement as `tournament_fee_charge_evidence_mismatch`. Ordinary ticket admission already records the member club on the same kind of transfer.

The additive migration changes only `fn_ca_settle_satellite_cohort` and `fn_settle_satellite_tournament_pre_money_path_gate`. Each consumes the exact inserted registration, validates its target/player/satellite provenance, and uses its admitted club for the funding transfer. Escrow routing remains keyed to the target tournament. All entitlement, fee, deferred commit, refund, rake and receipt authorities remain unchanged. No historical financial record is rewritten.

The version was reserved through the maintained migration tool. A single transaction checks exact predecessor definitions, owners and ACLs, replaces three unique source fragments in each producer, and checks exact successor identities. The separately qualified activation successor changes only these two expected function hashes; it preserves the other 147 function pins and all schema, maintenance and funding checks.

The existing directly invoked PostgreSQL 17 satellite qualifier runs genuine funded v2/v3 cross-club awards, actual target cancellation/refund and replay, whole-transaction rollback, and forged identity refusals. Its result validator refuses missing or false evidence. No production money operation is used as a test. Final native evidence is retained in the task’s existing satellite-historical-fee-proof directory; installation, protected merge and live receipt proof are separate delivery steps.

Final local qualification passed in `producer-native-root-07`: the original cross-club fee failure was reproduced; actual v2 and v3 awards, seven forged identity refusals, exact 100-chip member-wallet cancellation refund (200 total), and unchanged replay passed. The existing 67 cohort assertions and eight concurrency cases passed with complete data/catalog rollback and private-cluster cleanup. All recorded source hashes match. The isolated cash-accrual relation is a captured empty dependency of cancellation’s unchanged terminal-evidence trigger, not a claim of cash accounting qualification.
