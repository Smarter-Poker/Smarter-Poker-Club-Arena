# Current receipt-lane composition, source only

The original receipt-lane component accepts the old terminal wrapper `480be313`.
The held current terminal component requires `c64e0499` and restores that version
on rollback. Installing the old lane before replacing the fixture catalog did not
prove that the lane could be installed and reversed on the current cohort.

Separate `spin-mixed-basis-current-receipt-lane` forward/reverse components now
bind the captured current wrapper, finish/global helpers, doctrine, receipt reader,
pre-seat guard and place settler. They also bind the captured legacy share helper,
RPC and compactor. Owner, ACL, configuration, security, volatility and full source
must match. The reverse additionally checks the exact private statement handler.
The old lane files, financial function bodies, captures and terminal components
remain unchanged.

The existing two mixed images restore and read back current catalog/recognition
authority before lane installation. They then install the held terminal component,
run their original completion or source-change cases, reverse terminal consumption
and finally reverse the current lane. Temporary snapshots compare the original
current authority and all bounded business rows across the appropriate boundaries.
Terminal rollback preserves committed evidence; it does not undo payment.

Private source adds nine forward and twelve reverse owner/ACL/configuration fault
cases. Each requires the exact refusal, then checks catalog and business equality
after its subtransaction rolls back. The source controls reject missing/reordered,
wrong-role, failed or changed-stream stages, changed authority, lost business rows
and incomplete refusal receipts. The original statement/compactor schedules and all
six image scopes remain required. Allocation budgets remain 240 + 30 seconds and
business-race budgets remain 20 + 5 seconds.

Validation at this source checkpoint: 110 existing wrapper controls, six cash
manifest controls, and direct execution of the maintained routing array's 132
positive and 35 negative paths pass. The frozen previous module fails the new
stage-order contract; the new module passes for both mixed images. These are source
and protocol results. No PostgreSQL, full Vitest suite, installation, push or
publication ran for this change. Root review and native execution remain pending.

Scope stays synthetic zero-fee future-consumer qualification. The separate genuine
paid-entry image remains unchanged. This source does not recover the 415 histories,
establish original-22 membership, qualify positive-fee completion or close Inbox 5.
