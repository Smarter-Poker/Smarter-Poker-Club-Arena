# Mixed-history Spin terminal admission and immutable replay

The held mixed-history terminal candidate reached a PostgreSQL record-variable collision when checking its initial lock. Receipt replay also serialized the same settlement instant differently under another session time zone. The candidate now uses an unambiguous lock alias and fixes the canonical receipt function to UTC, preserving its body, permissions and the caller's time zone.

The current terminal wrapper acquires its required exclusive barriers before finish/seat authority for an unsealed mixed candidate. Admission refuses an unsupported late lock upgrade. Completed replay retains its narrower lane. Forward and rollback installation require exact captured function definitions and metadata; rollback restores the original four public authorities while preserving committed basis, completion and payment evidence.

The existing required Spin PostgreSQL qualification gains two separate finite allocations: completion/replay and a committed source change while completion waits. Every executed fixture and consumer is staged from the exact committed candidate and pinned before and after execution. The maintained driver uses the existing private Session implementation, the original 240-second work/30-second cleanup limits and 20-second race/5-second cleanup limits. Default CI runs all five images, retaining the original three. No scheduler, release worker or production test is added.

The declared input estate is explicitly synthetic and zero-fee. Its independent oracle requires 300 cash chips conserved, a single 20-chip payout/credit/ledger movement, unchanged repeated execution, exact source-change refusal, fresh backends and zero remaining sessions/locks. Eight rollback owner/ACL refusals preserve full committed state. Local PostgreSQL diagnostics reproduced the alias error and cross-timezone mismatch before these corrections; maintained final-source execution is recorded separately in the task evidence.

This source component is not a migration installation. Positive-fee settlement, all other receipt callers, complete financial qualification, authentic missing histories and oldest-incident closure remain unqualified. No amount, outcome or historical record is invented to close those gaps. Hosted enforcement, protected merge, publication and any later qualified database installation each require their own retained evidence.
