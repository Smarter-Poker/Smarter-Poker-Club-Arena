# Diamond Spins: Run Recovery And Game Finish

Auto Spin could reuse a consumed ticket or the previous availability quote when the next read was slow. The wheel now retires that ticket before preparing the next spin and waits for both fresh availability and a new sealed ticket. Preparation failures stop the run with a visible retry path. Refresh Wheel updates availability without placing a wager, preserves any uncertain request for Recover Spin, and keeps real maintenance/funding restrictions intact. The pause reason stays beside the controls even after a prior prize. A lower funded stake can obtain the ticket missing from an initially paused page.

Each bonus game displays its confirmed chip amount in the existing animated prize popup and then returns to the same club wheel. Completion follows the last Plinko drop, remaining Mines reveal, Donkey collision/remaining route or Crash flight reveal. Unknown outcomes do not navigate away. Donkey Cross starts on its highway shoulder; a historical loss is not a new collision. The city buildings and pavements are removed.

Focused real-component regressions reproduce the old ticket/quote races and cover five-spin completion, Stop, maintenance refresh, failed ticket preparation, durable receipt recovery and bonus terminal-frame sequencing. Local browser preview completed a five-spin run with fixture receipts; responsive recovery and scene/popup checks use no live wallet transactions. Required provider checks and production proof are recorded separately in the task delivery checkpoint. The companion financial migration and receipt changes are qualified separately and install only after this compatible client is published. There is no engine runtime replacement.

Crash now prepares its ticket from the funded wheel award state even when direct-entry quoting is closed. A failed preparation shows Retry Game without resubmitting any entry. Exact clicked cash-outs and uncertain-request recovery remain intact. The reproduced unavailable-direct-entry regression failed before and passes after the correction.
