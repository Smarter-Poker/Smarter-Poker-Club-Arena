# Diamond Spins Fits One Play Screen

Diamond Spins now shares the available screen height between the wheel and its attached black control area. The amount input, five denominations, balances, paid/free entry choices, automatic-spin control and Spin button remain beneath the wheel on phones and desktop. The wheel aperture follows its available width and height; rotating or resizing the screen does not reset a spin.

The tall standalone control chassis is replaced by the approved action-plate artwork at its native proportions. Prizes, ready bonus entries, daily rewards, fairness verification and history remain available through Prizes & More, using the existing accessible dialog. Refresh and receipt recovery remain reachable in the bottom controls. Spin animation no longer scrolls this viewport away from its controls.

The maintained browser regression mounts the actual page with global CSS and isolated wallet boundaries at 320x568, 390x844, 1280x720 and 844x390. It verifies no page scrolling, wheel/control separation, visible controls, denominations, automatic-spin selection, claimed bonus selection and the details dialog. Existing wheel reveal and receipt-routing checks remain enforced. No payout, database or engine contract changes.
