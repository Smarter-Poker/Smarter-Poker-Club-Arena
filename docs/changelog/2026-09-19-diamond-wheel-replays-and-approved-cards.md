# Diamond Spins: Approved Cards, Replay And Daily Statements

The wheel now uses the owner's approved twelve illustrated prize cards and eight upper cards, fitted to the curved wheel sectors. Chip cards print the selected entry's actual chip amount; the upper tiers are MINI 5x, MINOR 10x, MAJOR 25x and GRAND 100x. The matte selector holder follows the rim, and its hinged pointer responds to the passing dividers. Only an upgrade activates the upper selector. The idle upper ring uses the exact separate blue title-only artwork selected in the owner's 11:12 screenshot. When UPGRADE lands, the front wheel recedes around the shared axle and reveals the approved complete upper cards.

The global header and one framed control panel fit the wheel page at phone, desktop and landscape sizes. Club Chips reads only the current club membership balance. A visible 30-second countdown can start the selected spin once, with Hold available; hidden pages, open details, loading and unavailable play pause it. A completed spin does not silently arm another countdown.

Completed normal and Super bonus games have private replay history and explicit share actions. Public random-token links expose only the finished game, and posting to the social feed uses the canonical duplicate-safe social path. Replay animation preserves the recorded outcome and shows the terminal crash or collision before completion. Future Crash rounds use a slower curve and a 100x ceiling; existing round snapshots remain unchanged.

Daily Diamond accounting keeps entry receipts and Diamond or inventory expenses in durable server custody. It settles one owner wallet net transfer after the Chicago business day, with an itemized statement and one notification. Promo-first and Main-Bank chip funding are unchanged. Claimed Daily Bonus entries remain Mint funded, and their transfer does not consume the player's purchased Diamond lots.

Verification includes real isolated PostgreSQL accounting and all-row rollback, concurrent duplicate settlement, forced notification failure, privacy and canonical social sharing; component, service, pointer and responsive wheel reveal tests are retained in the existing checks. Production wagering and social posting are not performed by these tests. Supabase installation timestamps replace the initially reserved filenames; the qualified SQL and guard-declaration identifiers remain unchanged.

The curved artwork is rasterized once into a bounded shared cache, preserving the original painted pixels and all spin animation timing. This removes thousands of per-frame clipped atlas draws that exhausted the required software browser tests. All sixteen wheel browser cases pass with four workers, unchanged thirty-second deadlines and zero retries. Three active accounting source bindings were refreshed for this PR's additive CI classifier routes; historical receipts and financial fixture bodies remain unchanged.

Cold artwork loading leaves the existing sector chassis visible until each cached image is ready, instead of mounting thousands of temporary mesh images. The original mesh remains an explicit renderer-failure fallback. A four-times CPU-throttled capture loads all64sections in3.02seconds. Control groups use bounded flexible tracks so fallback fonts cannot wrap the landscape entry buttons onto another row; the real-page browser regression covers that font fallback.

The reveal fixture records transient rendered upgrade states inside the browser. Hosted traces showed that sequential remote assertions could arrive after the secondary spin had already completed. The test now requires every original card, selector, scale, visibility and navigation condition together in one recorded state; animation start/end order remains independently required. All six upgrade cases pass with four workers, CPU throttling, video and tracing enabled, without retries or changed production timing.

Owner follow-up: eight standalone playable test links now use the shipping game scenes with fictional local rounds. The separate HTML entry does not boot account/authentication or import financial services. The entry is published by a second Vite pass into dist/diamond-test/ rather than as a second Rollup input: a second input hoisted every shared module into one chunk whose stylesheet loaded ahead of the application's own, which reordered the global cascade and turned the Table Studio pixel baselines red on a page the test entry never touches. The application bundle is byte-for-byte a single-input build again. The service worker treats diamond-test.html as a standalone document, never the cached arena shell, so opening a test link cannot boot the wrong page in either direction. The test page's Plinko tables are the three production plinko_tables rows exactly, read back on September 19 and pinned by the browser regression. Normal and Super entries, Plinko denominations and risk selection, Double Down, next-choice prizes, exact Crash cashout and the five-second result reveal are available without a funded account award.

Won bonuses open automatically instead of appearing in a saved-game list. Live spins and unfinished bonuses block in-app links, router navigation and browser back while keeping the mounted game alive; tab closure gets the browser's unload confirmation. Existing durable receipts still recover interrupted play. New server admission serializes each player's club spins and refuses a new entry while an award is pending or its game is open; replay of the original receipt remains valid. Migration 20260919172312 is installed and read back. Native qualification exercised real normal/Super settlements, pending/live refusal, replay and complete rollback without production wagers.
