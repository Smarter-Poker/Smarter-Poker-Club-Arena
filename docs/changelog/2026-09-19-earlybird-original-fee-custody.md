# Early Bird completion retains its original unresolved fees

Early Bird's existing terminal transaction rolls back because its original27
rebuy fee rows are absent from the explicitly bounded legacy custody cohort.
The rows total2.70 and match the retained fingerprint. The repair adds exactly
that original tuple; the terminal payer, fee hold, earning evidence and bank
requirements remain unchanged. There is no fee distribution or production
financial mutation in this source delivery.

The native qualification retains the actual100 registration rows, including
nine prize caches totaling71.45, plus original funding and credit evidence.
Those cache values have no corresponding prize receipts in the capture.
The canonical payer produces10 awards totaling99.30, pays27.85 to the winner,
and retains2.70 in the original fee escrow. Late failure rolls back the entire
payment, and duplicate completion returns the same receipt without new effects.
The fixture uses synthetic account balances and table support; production
credit history and the original runtime boundary require independent readback.

This protected containing delivery also carries the already-qualified F06
lease-reaper protection from PR4910. A demonstrated shard4 test failure is
repaired by using the policy's existing injected clock for the private-state
refusal assertion. Its separate advancing-clock budget assertion and every
production timing constant remain unchanged.

Verification: native before/after transaction and regression checks pass;
server TypeScript and focused PLO4 tests pass. Final hooks, hosted checks,
protected merge and installed/live financial evidence are separate gates.
