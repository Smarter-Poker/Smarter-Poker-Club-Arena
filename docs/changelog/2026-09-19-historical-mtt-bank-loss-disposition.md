# Recorded historical bank loss uses one ordinary Lifetime session

Root authorized this bounded source repair after read-only comparison. It covers
only the two original 8825 managers and 23 positively matched current occupancies
in the retained witness. The extra current Noon table 66b1cb1d is outside this
cohort and has no inferred bank or process proof. Root owns production actions.

Owned tree: `/Volumes/SmarterWork/agent-work/mtt-historical-bank-loss-20260919`.
Branch: `work/mtt-historical-bank-loss-20260919`, based on reviewed PR4914 at
3908e4230d. The separately owned forward stopped-bank source is copied unchanged
for composition until its normal integration. Its actual capture path remains
distinct from this recorded historical loss. The old stopped objects are never
reanimated, their unknown final amounts never asserted, and old debit requests
never repeated or reversed.

The intended transition appends `historical_loss_normal_session_v1` to the
existing immutable mixed transfer. Preparation proves the original terminal hand
receipt, complete stopped owner, full operation custody and exact cohort, then
reads current ordinary Lifetime eligibility with no missing-read fallback. It
stores a normal session plan, not a preserved original bank. Only the existing
admitted successor, after every original movement completes, may initialize that
plan through the existing atomic completion and parked-presence transaction.
The existing completion identity prevents repeat initialization after spending
or restart. No additional financial grant, table, RPC or per-use ledger is planned.

The legacy checkpoint must recognize and validate this named disposition before
successor code can run. It retains the original raw metadata and pending/unknown
outcomes, commits both exact transfers and uses the existing synchronous identity
CAS. It cannot classify an absent old bank as uninitialized or native-preserved.

Current read-only evidence, observed 2026-09-19 03:28:39 UTC:
`/Volumes/SmarterArchives/agent-evidence/retained-event-recovery-options-20260919-0327`.
All 23 exact occupancy joins have current Lifetime VIP and zero purchased seconds.
The historical total 940 seconds remains only the last known observation. A
literal 40-second new base is below one 120-second witness. Actual component
qualification instead proves identical 20-second, two-per-street, exhausted-clock
guards: old 120/6 grants [40,40,40,0] over four streets; ordinary 40 plus Lifetime
grants [40,40,40,40]. Node20.20.2 passed the retained direct component test.

Canonical owner policies2.9 and repository AGENTS/AGENT-PLAYBOOK/CLAUDE plus the
existing retired-original checkpoint were freshly reread for this continuation.
Source, native qualification, protected integration, installation and live
recovery remain separate. Implementation and connected native checks are pending.
