# Exact original MTT custody at the existing release checkpoint

The serving `8825af51` engine retains stopped original tournament objects whose local hand permits remain reserved even after an authoritative zero-credit interruption. Its native maintenance gate correctly refuses restart. The older checkpoint profiles cannot transfer those mixed movement owners.

The existing provider release transaction gains one closed compatibility profile for the measured original source, image, container, process start and instance. Fourteen compiled module hashes bind the native implementation. Its existing fsynced, nonretryable intent now retains the two transfer IDs and preselected successor generations before inspector access. No second publisher, schedule or repair loop is added.

The checkpoint captures actual manager, table, permit, allocator, movement and reservation objects; joins their real successful stop boundaries; and fences object identity across every asynchronous boundary. Complete local movement and original bank evidence is sealed and read back through the mixed-custody transaction only after the exact original zero-credit hand disposition. Both continuing custody receipts must exist before the existing synchronous same-object unregister operation retires either event's global table entries. Original local objects, permits, movements and claimed reservations remain unchanged.

A cleared accepted permit is bound through the stopped object's original allocator epoch and nonempty historical permit rows with the same event, generation, table, custody and unique lifecycle. Missing or conflicting evidence refuses. Only the captured receipt gains the resolved lifecycle; the engine is never edited. An initialized bank requires its complete matching durable native snapshot, including occupancy and unlimited-use state. Metadata is never treated as reconstruction evidence for lost time.

Healthy tables retain their native bank checkpoint and exact readback. Inspector cleanup, the native maintenance certificate, freeze ownership and the full 285-second reserve remain mandatory. A lost custody-commit response is explicitly unconfirmed and retains the original intent; it never authorizes retry.

## Validation

- Seventeen cases execute the actual archived `8825` modules on Node 22.23.2; all fourteen compiled hashes match independently measured production bytes. Native stop, accepted-hand permit clearing, bank snapshot capture, both owning indexes, readiness and same-object retirement execute. Database I/O is isolated; this is not production qualification.
- The existing guard, shell admission, real isolated inspector transport and inherited-lock checks pass 106 cases. Connected source contracts pass 330 cases across 17 files after selecting the installed supported Python runtime for the builder fixture.
- Regression reproduction caught same-value replacement of a pending object before the identity fence, and a committed-but-unacknowledged receipt reported as not started before the outcome repair. Both are pinned by native tests.
- Shell/JavaScript syntax, policy integrity and final typechecks are required before submission. The existing required client and server CI suites execute these maintained tests.

The corresponding qualified database disposition/custody installation and successor replay are separate dependencies. Actual completeness of the 23 retained live originals, production actuation, protected merge and publication remain unverified here. No production inspector attachment, hand disposition, payment or restart was performed by this source change.
