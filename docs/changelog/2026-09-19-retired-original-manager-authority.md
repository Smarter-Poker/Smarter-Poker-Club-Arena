# Original manager custody survives native lease-row loss

The native stale-lease reaper deleted two expired tournament leases while their
same original managers still retained stopped dealers, unresolved hand permits
and mixed movement operations. The retained-hand disposition and mixed transfer
correctly refused a missing lease, leaving no supported original-owner boundary.
Reaper prevention is delivered separately. This change preserves positive original
custody without recreating a lease or granting the retired generation play rights.

## Exact scope and contract

Only the two recorded 8825 manager generations and their complete 14/9 table-to-
engine maps qualify. The fixed source, instance, container, image, process, manager,
original permit and already reserved disposition UUIDs are bound in the maintained
cohort input. Fresh positive stop completion must account for every original and
all tracked work. Unavailable diagnostics are not empty state. The exact 8825 stop
implementation's joined continuation fixed point supplies the explicit continuation
witness; a null DTO does not supply it.

`fn_f06_attest_retired_manager_origin` observes and then appends an immutable
`f06_retired_manager_origins` record under the native absent-row claim fence.
It invokes the complete existing retained-hand proof, including original snapshot
or sealed prior-plus-inbound evidence, paid continuity, accepted zero outcomes,
all seats/registrations and all movement members, attempts and winning receipts.
It refuses a competing lease, changed process, freeze or changed canonical proof.
The original disposition UUID is retained. Lost replies return that exact record.

The existing retained disposition may use this witness only after revalidating
its complete original proof. Its original lease-present path is preserved. It
terminalizes only the original interrupted hand through the existing immutable
receipt path; it does not move a player or write a payout, wallet or lease.

The existing future checkpoint order is preserved: original disposition precedes
mixed transfer. Frozen preparation requires the actual maintenance token, leader,
source, process and full reserve, the same complete physical map, an exact linked
terminal receipt, unchanged original mixed/seat vectors, and all existing bank
proof. Preparation appends custody only. The already consumed publisher invocation
35415689014 is neither changed nor replayed by this source.

The native claim trigger and absent-owner observation share one nonblocking
transaction lock. After the witness, the old generation remains fenced. Before
completion, only the transfer's selected successor can claim. An admitted partial
successor cannot change process/acquisition, renew an expired identity or recreate
an absent row. Completion allows a later ordinary generation; original receipts
remain immutable. No repair loop, watcher, new scheduler or alternate publisher
is introduced.

## Qualification and delivery ownership

Owned checkout: `/Volumes/SmarterWork/agent-work/mtt-mixed-custody-transfer-20260918`.
Owned follow-up branch: `work/mtt-retired-origin-20260919`. PR4907 head b273bb5b30,
including the root-owned finite admission continuation, is integrated unchanged.
Reviewed PR4911 head 9552509e7b325282d370bfca66371bf93097882e is integrated at
ddf61bce87. Its pre-intent, read-only contract comparison now pins the exact
24-function catalogue qualified below; equality and refusal behavior are unchanged.
Current protected main 56c722f8ec is integrated at 7bca6733e5; the maintenance
changes compose with the existing finite mixed-admission continuation. All 542
accounting input bindings still match after integration.
Root owns protected merge, installation, physical attestation, publication and
all production mutations. This helper has made no production write or inspector
attachment.

The four canonical owner policies, full portable policy content (supplemented
where output was truncated), repository AGENTS/AGENT-PLAYBOOK/CLAUDE/PUBLISHING,
applicable rules, deploy-paths, changelog instructions and existing custody
checkpoint were freshly read on resumption. Policy reader time:
2026-09-19T03:10:13.666Z at ddf61bce87; policy 2.9; manifest SHA256
7663cc909626f7e9966931d27166ad8774addc801f7ad1898a2d7564bc13c378.

Evidence: `/Volumes/SmarterArchives/agent-evidence/mtt-retired-origin-20260919`.

- Full native PostgreSQL 17 chain: native-5 passed 1,196 checks, including 95
  retired-origin cases. All four production source inputs still match that run.
- Focused-2 restored that retained native predecessor into a fresh isolated cluster
  and passed 105 checks, including exact whole-row lost-reply effects, unchanged
  original move UUIDs, byte-preserved business vectors, both claim-lock orders,
  competing/missing/expired/changed identity refusals, immutable receipts, actual
  original close/custody/ACK RPCs, completion and a later ordinary restart.
- Restoring each actual prior authority in a rolled-back native transaction proves
  the red-before absent-lease refusal; the final authorities pass the original
  chain. No production financial probe is used.
- The actual service role reads the complete 24-function fixed catalogue inside a
  READ ONLY transaction. The prior fourteen have identical metadata except the
  intentionally changed prepare and catalogue functions. The qualified catalogue
  is retained as `focused-2/qualified-service-contract.json`.
- CI routing passed 579 assertions. Exact affected current manifest fingerprints
  are refreshed; historical captures remain unchanged. Independent checks confirm
  all 542 current accounting inputs; cash manifest tests passed six cases.
- Supported Node 20.20.2 passes all 69 checkpoint admission and actual isolated
  transport tests after the 24-entry preflight integration. The initial run used
  unsupported Node 26 and correctly refused all twelve transport cases; no runtime
  assertion was changed. The publisher pin and fixture exactly equal the native
  service-role result.
- Root TypeScript, migration declaration/authorization checks, generator exactness,
  policy integrity, whitespace checks and the first normal commit hooks passed.
  Final normal submission and hosted checks remain pending.

## Production limit

The real retained bank amounts remain unqualified. This source does not reconstruct
balances from metadata or treat disposed/missing banks as zero. Initialized or
nonempty custody still requires the exact native same-hand/same-occupancy durable
snapshot, including unlimited activations. Source qualification, protected merge,
installed authority, a successful checkpoint, and live recovery are separate results.
