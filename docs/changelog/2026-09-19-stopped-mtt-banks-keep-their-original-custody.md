# Stopped MTT banks keep their original custody

An MTT table stop disposed the native time-bank engine before its owner had retained the player's actual remaining balance. Physical timer cleanup then allowed the manager and global table indexes to discard the original. An older parked row or initialization metadata cannot prove the final remaining seconds.

The owning stop now joins existing bank accounting and presence writers, cancels active allocations through their existing accounting event, and captures the actual bank and presence state before disposal. Unknown debit outcomes retain the original owner. Same-process replacement adopts that immutable capture inside the existing table identity CAS, before fresh allowance initialization. It retains unlimited activations and accounting metadata, filters the captured presence state by the original occupancy, and preserves it through another park or stop before the first roster read. Confirmed terminal table cleanup uses its existing closed-table receipt before ending the session.

A stopped manager with banks but no pending move now uses the existing mixed-custody transaction. Four narrowly extended functions validate the actual new-runtime capture against its original engine, process, generation, lifecycle, hand and occupancies. The immutable receipt precedes original-owner release. The selected successor consumes that receipt through the existing admission and completion path, writes the native parked state, and can subsequently release normally. Lost replies retain the same transfer identity. The legacy 8825 source cannot use this new captured-bank branch. No new storage system, unfenced write, background repair, historical reconstruction or balance grant is introduced.

Validation: the maintained PostgreSQL 17 shared-hand qualification passed 1,242 cases, including old-source refusal/new-source progress, both lease-lock orders, committed prepare/completion lost replies, exact duplicate readback, unchanged original seats, native owner release, selected successor admission, and later ordinary restart. The restored bank remained 7 seconds, one use, unlimited activations, and 33 previously consumed seconds. Actual service-role read-only catalog output matches the publisher's complete 24-function contract. Connected engine tests cover bank-only manager progress, unknown replies, actual map ownership, maintenance retirement, terminal receipts, same-process presence and occupancy changes. Root and server typechecks pass.

These are isolated source and database-fixture results. They do not restore or certify the missing final banks on the historical 8825 process. Protected integration, installed catalog verification, provider publication and affected live behavior remain separate delivery requirements. Historical loss disposition is a separately scoped containing change.
