# Paid terminal qualification retains live source bindings

The seven-image qualification adds terminal routing and lossless fixture exclusions. Existing accounting source bindings also pin those shared files; their stale hashes stopped PR5350 before financial execution. Update only the reviewed live pin maps and enclosing binding hashes, appending explicit audit entries while preserving historical records. No SQL, assertion, deadline or frozen retention seal changes. The protected source-binding verifier covers all 762 pins across 15 binding files.
