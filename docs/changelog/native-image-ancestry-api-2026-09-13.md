# Admit the exact ancestry reads used by image qualification

Native run 34781340011 completed the bounded engine producer, the successful import and all three intended import refusals. Actual namespace observations passed and the host Docker bridge identity remained unchanged. The consumer then refused its first Git commit lookup with ENGINE_CI_IMAGE_GITHUB_ENDPOINT: qualification had added commit and ancestry reads without adding those exact routes to the shared API allowlist.

The read-only helper now admits full lowercase commit IDs and the precise one-result ancestry comparison for Smarter-Poker/Smarter-Poker-Club-Arena. The fixed public GitHub host, GET method, timeout, response bound and other endpoint restrictions remain. Foreign repositories, partial IDs, branch names and extra query parameters are rejected before transport.

Validation: 29 bundle, 14 qualification and 13 producer checks passed, zero skips. Both new valid endpoints failed on the previous source. The corrected helper also executed both actual GitHub reads for the failed run and verified its two merge parents and recorded-base ancestry. This does not qualify the complete consumer or authorize a production import; a fresh native run must still pass those gates.
