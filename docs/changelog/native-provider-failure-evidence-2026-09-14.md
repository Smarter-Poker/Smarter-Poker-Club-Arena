# Retain bounded provider failure evidence

Native run 34781718014 advanced beyond the active-query role cleanup and stopped during five-provider qualification. The controller retained only that outer phase even though the provider emitted its failed proof. All five external fixture cleanup checks passed, but the provider stage and observations were discarded.

Failed provider proofs now retain only an enumerated error type and a five-character SQLSTATE, preserving the first semantic failure across cleanup failure. The controller validates a single bounded, duplicate-free record with exact fields, known stages, pinned versions, hashes and Boolean observations. It retains a separate failed diagnostic that cannot satisfy the unchanged positive provider admission contract. Arbitrary messages, SQL, keys and service output are excluded.

Validation: 70 focused provider lifecycle, smoke controller, native diagnostic and cleanup checks passed with zero skips. The actual previous controller fails the new end-to-end failed-receipt test by dropping the provider diagnostic. Controlled provider failures exercise first-failure preservation, physical client closure and private-data exclusion. No SQL input, role installer, provider assertion, timeout or production setting changed. Complete native provider and full-schema qualification remain open.
