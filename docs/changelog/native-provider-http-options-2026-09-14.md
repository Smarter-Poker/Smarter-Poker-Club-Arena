# Use the actual HTTP extension option contract

Native run 34791356153 reached private HTTP with SQLSTATE XX000 after genuine symbol, ACL, denial, spatial, Vault and PL/pgSQL observations passed. Its private listener and database clients closed, and all external fixture resources were removed. The pinned pgsql-http source rejects CURLOPT_FOLLOWLOCATION as a runtime option; the probe requested it unconditionally before its HTTP request.

The probe now retains both supported timeouts and explicitly disables inherited proxies using CURLOPT_PROXY with an empty value. It no longer claims that the extension disables redirects: upstream GET behavior remains unchanged, and the exact owned loopback peer supplies a fixed 200 response with no Location header. Caller-supplied destinations remain impossible.

The controlled driver now checks option names against a fixture extracted from the exact upstream C allowlist, instead of returning success for arbitrary options. Both the prior successful-protocol test and new contract check fail on the previous SQL. The corrected source passes 55 focused provider/controller checks with zero skips; the actual peer check rejects redirects and verifies the absent Location header. A new native run remains required.

Primary contracts: [pinned pgsql-http source](https://github.com/pramsey/pgsql-http/blob/5e2bd270a9ce2b0e8e1fdf8e46b85396bd4125cd/http.c) and [libcurl proxy option](https://curl.se/libcurl/c/CURLOPT_PROXY.html). No extension implementation, role installer, phase deadline, financial behavior or production setting changed.
