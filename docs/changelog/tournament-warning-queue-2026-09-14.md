# Keep every current tournament warning reachable

Several simultaneous away seats previously overwrote one another's warning. A displayed notice could also retain an old stack or survive a returned terminal table. The watcher now queues each away table once, refreshes positive observations without reordering the queue, and dismisses or retires only the matching notice. A continuous dismissed episode stays dismissed until a positive non-away observation rearms it.

Returned terminal or explicit non-tournament records retire that table's pending opening and notice. Missing rows or missing joined data do not prove departure. Account/session replacement, superseded reads, hidden-page replies and duplicate opening acknowledgements remain fenced. A successful correlated opening advances only its own warning.

Integrated the exact two independently reviewed postimages from 0f57d85cbcc706630aa431922432bfb591975d81. All 111 normal project checks across eight files passed with zero skips after reclaiming this task's own duplicate storage; the earlier attempt ran zero tests because the Mac ran out of disk space. Formatting passed. Full client compilation and the existing whole-app bundle ceiling still require CI; the prior source exceeded that ceiling by 1 kB. No limit was raised and no real tournament movement, next hand or served-state qualification is claimed.
