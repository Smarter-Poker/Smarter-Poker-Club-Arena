# Workflow evidence: deploy-rescue-audits

- run: wf_c850a19b-ca5
- finished: 2026-09-10T20:06:48.149Z
- status: completed
- agents: 2
- summary: Two read-only audits in parallel: residual horse-worker crash paths on main, and the exact honest-status change list for the deploy workflow

## key

crash

## r

```json
{
  "summary": "Q1: the engine kills itself. Production 7732b971 treats the queue time of the job currently on the worker as if it were compute time. The job deadline is 8000ms counted from enqueue. When it expires while the job is the one posted to the worker, LiveHorseDecisionWorkerClient.onJobDeadline calls fail() (7732b971 client.ts:666-676). fail() queues onFatal (client.ts:647). GameServer's onFatal re-throws (GameServer.ts:1874-1877). The throw lands in index.ts's uncaughtException handler (index.ts:464-473), which calls fatal() and then shutdown(). gameServer.stop() never finishes within the 40s hard deadline, so the process calls exit(1) (index.ts:372-378). Docker's restart policy \"always\" brings it back. sp-autoheal is not involved, the worker did not die and it is not an unhandledRejection.\n\nHost evidence: this container crashed 5 times between 19:13 and 19:53 UTC (jobs 18156, 14520, 13398, 11596, 3917), each time about 8-40s after roughly 720 RUNNING tournaments were re-adopted in one burst. At the three crashes where Prometheus had a sample: worker last_compute was 0.20-0.34ms, active job age 0.32-0.45s, oldest queued job 4.3-7.6s, queue depth 45-56, main event-loop p99 0.43-0.74s. The 8s budget was spent waiting in the queue, not computing. Each crash cost about 95s: 40s shutdown, then about 27s waiting for the stale lease, then about 24s worker hydration.\n\nQ2: No. c4881fea3b (#4190) removes the first link. The enqueue deadline now only rejects with HorseDecisionExpiredError (origin/main client.ts:735-763). A separate execution timer is armed at dispatch (client.ts:510). 0cd8255f14 (#4194) makes worker-side validation errors non-fatal. But fail() -> onFatal -> throw -> uncaughtException -> 40s -> exit(1) is intact (client.ts:688-717, GameServer.ts:1881-1885, index.ts unchanged). The chain can still be entered through the execution deadline and 9 other paths listed below.\n\nThe new build 70a6a75f contains both fixes and went live at 19:55:52. By 20:00 it had only seen re-adoption bursts during the maintenance-break freeze: 0 expired jobs, RestartCount 0. It has not yet been tested by a burst after the thaw.\n\nQ4: I propose moving supervision into the process: a worker lost after READY is replaced by a fresh client instead of restarting the whole engine. It is not needed tonight if 70a6a75f survives about 3 bursts after the thaw. Land it immediately if any horse-worker fatal appears on the new build.",
  "findings": [
    {
      "title": "Q1 causal chain (7732b971): the client's own deadline check starts a fatal process shutdown",
      "detail": "1) enqueue arms an 8000ms timer from ENQUEUE (client.ts:418, DEFAULT_JOB_TIMEOUT_MS at :142). 2) onJobDeadline: if the job is the active one, it calls fail(new Error('...exceeded its 8000ms queue-plus-compute deadline')) (client.ts:666-676). 3) fail() rejects the active and queued jobs, terminates the worker, sets phase to failed, then calls queueMicrotask(() => this.onFatal(error)) (client.ts:619-648). 4) GameServer's onFatal calls revokeDealerPrerequisites and then `throw error` (GameServer.ts:1873-1878). 5) A throw from a microtask surfaces as 'uncaughtException', which calls fatal() and then shutdown() (index.ts:464-473). 6) performShutdown's hard deadline: after 40s it calls process.exit(1) (index.ts:372-378). 7) Docker restart policy always. After fail(), every enqueue returns Promise.reject(new Error(this.lastError)) (client.ts:401-402). That is why the flood of horse_decision_worker_failed / horse_mind_observation_failed lines carries an 'at LiveHorseDecisionWorkerClient.enqueue (client.js:271)' stack. Those lines are consequences, not causes. The causal Error carries the onJobDeadline stack (dist client.js:513 via the timer at :287).",
      "evidence": "docker logs club-arena-engine: 19:26:02.399 first horse_decision_effect_commit_failed (job 14520); 19:26:02.4037 'Received shutdown signal'; 19:26:02.4039 '[GameServer.Uncaught_exception] Error: live horse decision worker job 14520 (DECIDE_FAST) exceeded its 8000ms queue-plus-compute deadline'; 19:26:02.4039 'FATAL (GameServer.Uncaught_exception) - draining for supervisor restart'; 19:26:02.405 'Shutting down...'; 19:26:42.405 '[GameServer.shutdown_deadline] graceful shutdown exceeded 40000ms'. Same sequence at 19:13:30.86->19:14:10.86 (job 18156), 19:37:32.86->19:38:12.86 (13398), 19:48:31.90->19:49:11.90 (11596), 19:53:16.19->19:53:56.19 (3917). docker events: die exit=1 at 19:53:56 (the container's earlier 'die' events had rotated out of the buffer).",
      "severity": "critical"
    },
    {
      "title": "Not sp-autoheal, not worker death, not unhandledRejection",
      "detail": "The log kind is GameServer.Uncaught_exception, not Unhandled_rejection. The worker was healthy: its compute was sub-millisecond, and it was terminated by the client inside fail(). Autoheal cannot fire on this: the Docker healthcheck reads only running and liveness (after a 300s grace), and the liveness verdict has no horse-worker input (GameServer.ts:2821-2828). /health's 503 gate includes the worker phase (handlers/health.ts:84-89), but the live Caddyfile is a plain 'reverse_proxy localhost:8080' with no active health check.",
      "evidence": "sp-autoheal logs since 12:00 contain only its own startup lines ('Monitoring containers for unhealthy status in 120 second(s)' at ~:57 hourly), with no restart actions. poker_engine_liveness=1 at 19:25:00-19:26:10. Healthcheck config: node probe exits 1 only if !(running===true && liveness in ok|standby). /etc/caddy/Caddyfile: engine.smarter.poker { reverse_proxy localhost:8080 }.",
      "severity": "info"
    },
    {
      "title": "Proximate trigger: re-adopting about 720 tournaments at once floods the single horse FIFO, and queue wait is charged to the active job",
      "detail": "Each crash follows a discovery pass that resumes about 720 RUNNING tournaments and creates about 1000 table engines within about 3s. That saturates the main loop. The FIFO posts one job per main-loop round trip (client.ts maybeDispatch), so the queue backs up and the 8s enqueue budget runs out while the worker computes in under 1ms. The same set of tournaments is re-admitted on every pass (710/710 overlap 18:59->19:13; 736/736 overlap 19:56->19:59 on the new build). At boot 18:59 the burst was followed at 19:00 by 1016 'Stopped. Dealt 0 hands'. This is a separate root cause for another agent. Code: origin/main GameServer.ts:5314-5330 fans out ensureTournamentManagerAdmission(...,'resume') for every RUNNING id not in tournamentEngines, with no concurrency cap.",
      "evidence": "Resume+start lines per 10s: 18:59:4x 1735, 19:13:2x 1659, 19:25:5x 1721 (718 distinct tournaments, 1005 engine starts in 19:25:54-57), 19:37:2x 1735 (724 distinct), 19:47:5x 1725, 19:53:0x 1745. Prometheus at the crashes: 19:26:00 main p99 744ms / sampler_late 172 / queue 56 / oldest queued 5688ms / active 324ms / last_compute 0.20ms; 19:37:30 p99 432 / queue 53 / oldest 4288 / active 439 / compute 0.25; 19:53:15 p99 527 / queue 45 / oldest 7632 / active 452 / compute 0.34; 19:13:30 p99 365. Worker event-loop p99 stayed at 21-70ms.",
      "severity": "high"
    },
    {
      "title": "The fatal shutdown cannot finish in 40s: exit(1) without releasing ownership adds a stale-lease wait",
      "detail": "None of the 5 shutdowns reached the table-drain log ('Drained all' / 'Drain window elapsed', GameServer.ts:2447+), so performStop stalled in an earlier await. The most likely one is drainOwnedLifecycleJobs() at origin/main GameServer.ts:2420, joining about 720 in-flight resume admissions against a slow DB (unverified). The worker is not what hangs: a failed client's stop() only terminates. Result per crash: about 95s of fleet outage and hands reported by CRASH RECOVERY. This is not specific to horses: any fatal during a burst behaves the same way.",
      "evidence": "19:26:42.4 exit, 19:26:43.97 HTTP listening, 19:27:09.9 '[leadership] ... LEADER' + 'Boot claim granted on retry - the previous lease went stale', 19:27:33.4 GtoPostflopLoader loaded (worker hydration about 23.5s), 19:27:37.4 'CRASH RECOVERY: Found incomplete hand #9257662'. During the shutdowns: supabase_timeout, 'canceling statement due to statement timeout', 218 'tournament_lease_proof_expired' (19:37-19:38).",
      "severity": "high"
    },
    {
      "title": "Q2: #4190 and #4194 remove the observed entry point but not the chain (answer: NO)",
      "detail": "REMOVED: queue wait charged to the active job. origin/main client.ts:735-763: onJobDeadline now rejects with HorseDecisionExpiredError, posts CANCEL, keeps the job as FIFO owner and never calls fail(). The execution timer is armed only at dispatch (client.ts:505-511), with a one-poll-turn setImmediate grace (client.ts:765-781). REMOVED by #4194: validation errors marked recoverable:true in the worker (workerRuntime.ts:282-296) are handled without fail() (client.ts:600-625). The pineapple cause is fixed in HandController.ts:914 (performAction refused during pineapple_discard), HandController.ts:1513 (currentPlayerSeat=-1) and ServerTableEngineRunout.ts:708 (clearTurnTimer). STILL PRESENT: fail() -> queueMicrotask(onFatal) (client.ts:688-717, :716); onFatal throws (GameServer.ts:1881-1885); uncaughtException -> fatal -> shutdown (index.ts:464-473); 40s -> exit(1) (index.ts:369-379); exit code 1 even on a clean fatal drain (index.ts:414). index.ts and these GameServer lines are identical in 7732b971, origin/main and the deployed 70a6a75f. Under today's measured numbers #4190 would have prevented every crash: active age 0.45s or less, far below the 8s execution budget.",
      "evidence": "git diff 7732b971 origin/main -- server/src/index.ts shows no changes. git merge-base --is-ancestor: c4881fea3b and 0cd8255f14 are both in 70a6a75f32. 70a6a75f32 GameServer.ts:1881-1885 is unchanged, and client.ts:716 and :765 are identical to origin/main.",
      "severity": "high"
    },
    {
      "title": "Remaining path P1: a posted job with no reply for 8s -> process fatal",
      "detail": "Trigger: a job gets no reply within 8000ms of dispatch plus one poll turn. Causes: pathological HorseLogic/DECIDE_DEEP compute, worker CPU starvation or GC, synchronous work inside the worker (solver-loader refresh, persistence), or a real wedge. Consequence: the full chain above (fleet restart of about 95s). Likelihood: LOW, given today's compute of 0.2-16ms and worker loop p99 of 70ms or less.",
      "evidence": "origin/main client.ts:166 (8_000), :510 (executionTimer armed at dispatch), :765-781 (setImmediate recheck -> fail), :716, GameServer.ts:1884 `throw error;`, index.ts:472.",
      "severity": "medium"
    },
    {
      "title": "Remaining path P2: any exception thrown during execution in the worker -> process fatal",
      "detail": "Trigger: HorseLogic.decide, HorseMind capture/apply, observation, status or discard throwing after envelope validation. The worker catches it and sends ERROR without recoverable, and the client calls fail(). One odd game state at one table restarts the whole fleet. This is the same failure class #4194 hit through validation. Likelihood: LOW-MEDIUM (6,600+ line HorseLogic, many variants).",
      "evidence": "origin/main workerRuntime.ts:314-321 (catch -> ERROR without recoverable); client.ts:604-608 (`if (message.recoverable !== true) { this.fail(...) }`).",
      "severity": "high"
    },
    {
      "title": "Remaining path P3: worker thread 'error' or unexpected 'exit' -> process fatal",
      "detail": "Trigger: an uncaught exception or unhandled rejection in worker-owned timers (solver loaders, HorseMind persistence, telemetry), ERR_WORKER_OUT_OF_MEMORY, or a native crash. The worker entry installs no process-level handlers and the Worker is created with no resourceLimits. Likelihood: LOW.",
      "evidence": "origin/main client.ts:228-235 ('error' -> fail; 'exit' without shutdown ack -> fail); worker.ts:8-13 (no handlers); client.ts:148-153 (new Worker(url) with no options).",
      "severity": "medium"
    },
    {
      "title": "Remaining path P4: postMessage throwing on the main thread (DataCloneError from one bad snapshot) -> process fatal",
      "detail": "Structured clone runs before anything reaches the worker, so worker state is intact. The failure still escalates the whole fleet: #4194's recoverable classification covers only worker-side validation. Likelihood: LOW.",
      "evidence": "origin/main client.ts:512 (safePost(job.request)), :725-733 (catch -> fail). Pinned as fatal for a generic Error by client.test.ts:522-535.",
      "severity": "medium"
    },
    {
      "title": "Remaining path P5: protocol-integrity faults -> process fatal",
      "detail": "Unexpected READY or invalid solver identity, STOPPED before the FIFO drained, ERROR with requestId null (hydration or SHUTDOWN failure), a message with no active job, FIFO or fence mismatch, wrong result type, ACK operation mismatch, STATUS losing solver identity. These are genuine corruption signals, but every one of them costs the whole fleet. Likelihood: VERY LOW.",
      "evidence": "origin/main client.ts:522-530, 548-551, 570-572, 575-578, 581-599, 626-648, 663-666.",
      "severity": "low"
    },
    {
      "title": "Remaining path P6: boot hydration past 90s -> boot fatal -> restart loop",
      "detail": "The READY timeout is 90s, and a worker that fails before READY re-throws out of performStart. Measured hydration today was about 23.5s under a slow DB (DB pair hydration 6842ms). Likelihood: LOW-MEDIUM only if the DB degrades further.",
      "evidence": "origin/main client.ts:165 (90_000), :237-240; GameServer.ts:1886-1897; log 19:27:09.9 -> 19:27:33.4.",
      "severity": "low"
    },
    {
      "title": "Remaining path P7: graceful worker stop has no deadline, so a slow final HorseMind flush can hold every SIGTERM (including the hourly :55 restart) past 40s -> exit(1) without releasing ownership",
      "detail": "stop() waits for STOPPED with no timer. The worker's shutdown awaits stopOwnedServices, whose flush retries a failed chunk row by row at up to 15s per RPC. GameServer awaits all of this before distributed release, with no race of its own. HorseMindPersistence claims to be 'bounded by caller's race', but no caller races it. Likelihood: MEDIUM on slow-DB days. Not observed directly: logs of earlier containers are gone.",
      "evidence": "origin/main client.ts:373-430; workerRuntime.ts:1080-1087, :138-147; HorseMindPersistence.ts:496-515, :199-229; supabase/client.ts:63 (15_000); GameServer.ts:2559-2569; index.ts:369-379.",
      "severity": "medium"
    },
    {
      "title": "Remaining path P8 (table stall, not process): a Pineapple all-in runout parks the hand on a discard failure or expiry",
      "detail": "requestPineappleDiscard is called without its own deadline in the runout path, so under bursts DECIDE_DISCARD waits out the 8s enqueue deadline. The runout then returns with settlement 'parked behind the two-card invariant' until the table's own stall chain recovers it (up to 5 min). The forced-runout variant restarts the table instead. Likelihood: MEDIUM during bursts on pineapple tables with all-ins. Blast radius: one table.",
      "evidence": "origin/main ServerTableEngineRunout.ts:644-653, :1272-1285 (pineapple_runout_worker_failed -> return), :3019-3029, :1397-1406 (killForRestart).",
      "severity": "medium"
    },
    {
      "title": "Remaining path P9 (degradation, not a crash): FIFO throughput is tied to main-loop latency, so bursts turn horse decisions into instant fold/check",
      "detail": "On the new build, each burst expires most queued DECIDE_FAST requests at 8s. Each expired request takes the fail-safe action with thinkTime 0. This is visible to players (10.5: timing is part of treatment) and leaves one reportError per decision. It goes away only by removing the burst (see the trigger finding).",
      "evidence": "origin/main client.ts:496-518 (one job per round trip), ServerTableEngineTurns.ts:2786-2815 (safeDecision fold/check, thinkTime 0); queue 45-56 with main p99 0.43-0.74s at the crashes.",
      "severity": "medium"
    },
    {
      "title": "Remaining path P10: getLiveHorseDecisionWorker() throws synchronously inside timer callbacks once the singleton is nulled -> uncaughtException",
      "detail": "Reachable only after stopLiveHorseDecisionWorker has run and a table timer survived its own fence. The practical effect is shutdownMustFail, i.e. exit 1 instead of 0. Likelihood: VERY LOW.",
      "evidence": "origin/main client.ts:851-854, :860-868; ServerTableEngineTurns.ts:2965 (secondLook timer), :3172 (horseActionTimer).",
      "severity": "low"
    },
    {
      "title": "The design contract being reversed, and precedent for doing it",
      "detail": "'No replacement' is a written 2026-09-08 design decision. Its rationale is obsolete. Decision RNG is derived per request from decisionKey/fence and is 'identical across worker restarts'. The current fatal path already terminates the worker without a final HorseMind flush, and the successor rehydrates from the DB with a replay tail. The equity worker pool in the same repo already runs a respawn budget. Any supervisor change must update the guard pin in the same commit.",
      "evidence": "GameServer.ts:1870-1879 comment ('Unexpected worker loss is process-fatal because silently replacing it would reset RNG...'); LiveHorseDecisionWorkerWiring.guard.test.ts:89-95 ('has no synchronous fallback or replacement-worker path', not.toContain('respawn')); workerRuntime.ts:1047-1068 (requestRngSeed); boot log 19:27:18 'DB hydration ... (replay tail since 2026-09-10T19:24:38)'; EquityWorkerPool.ts:112-155, :431-444 (respawnBudget).",
      "severity": "info"
    }
  ],
  "proposal": "WHEN: Not tonight. The live build 70a6a75f32 (up since 19:55:52) contains #4190 and #4194, which remove the only entry point observed today. The supervisor below reverses a written design contract, so it should land in daylight.\n\nGate for tonight, checked once per burst (bursts recur every 2.5-14 min, and the first after-thaw burst is the real test):\n- docker logs --since 2026-09-10T20:00:00Z club-arena-engine 2>&1 | grep -c -E 'GameServer.Uncaught_exception|execution deadline after dispatch|shutdown_deadline\\] Error' must be 0.\n- docker inspect -f '{{.RestartCount}}' club-arena-engine must stay 0.\n- poker_horse_decision_worker_expired_jobs should rise during bursts. That proves the non-fatal branch absorbed them.\n- If any horse-worker fatal appears on 70a6a75f, land the change below immediately.\n- The separate root cause (about 736 tournaments re-admitted every discovery pass, GameServer.ts:5314-5330) should go to another agent. It still causes P8 and P9 on the new build.\n\nCHANGE (smallest one that stops any horse-worker condition from taking the engine down). The class stays untouched: one client never rebuilds its own thread. The module replaces a client that was lost after READY.\n\n1) server/src/engine/horseDecision/client.ts. Replace the module block at origin/main lines 841-868 with the following. The word \"respawn\" must not appear in this file.\n\nexport interface LiveHorseDecisionWorkerSupervisorOptions extends LiveHorseDecisionWorkerClientOptions {\n  /** False once the owning lifecycle is fenced; nothing is replaced then. */\n  canReplace?: () => boolean;\n  /** One call per replacement scheduled for a worker lost after READY. */\n  onReplace?: (error: Error, replacement: { attempt: number; delayMs: number }) => void;\n}\nexport interface SupervisedLiveHorseDecisionWorkerStatus extends LiveHorseDecisionWorkerStatus {\n  replacements: number;\n  lastReplacementError: string | null;\n}\n/**\n * A LOST WORKER IS REPLACED, NOT THE FLEET (2026-09-10). A worker that served READY and then fails\n * (wedge, crash, protocol fault) is replaced by a fresh client while tables keep dealing; every\n * rejected/queued decision already takes its caller's legal fail-safe action. Decision RNG is derived\n * per request (workerRuntime requestRngSeed, identical across worker restarts) and HorseMind rehydrates\n * from the durable store exactly as a process restart does. Only a boot worker that never reached READY\n * stays fatal. Every replacement is an incident (CLAUDE.md 10.12), reported by the owner's onReplace.\n */\nexport const LIVE_HORSE_WORKER_REPLACEMENT_BACKOFF_MS = [0, 5_000, 30_000, 120_000, 300_000] as const;\nconst LIVE_HORSE_WORKER_REPLACEMENT_WINDOW_MS = 10 * 60_000;\ninterface LiveHorseWorkerSupervisor {\n  readonly options: LiveHorseDecisionWorkerSupervisorOptions;\n  stopped: boolean;\n  timer: ReturnType<typeof setTimeout> | null;\n  recentFailures: number[];\n  replacements: number;\n  lastReplacementError: string | null;\n}\nlet singleton: LiveHorseDecisionWorkerClient | null = null;\nlet supervisor: LiveHorseWorkerSupervisor | null = null;\n\nfunction spawnSupervisedClient(owner: LiveHorseWorkerSupervisor, boot: boolean): LiveHorseDecisionWorkerClient {\n  const { workerFactory, readyTimeoutMs, jobTimeoutMs } = owner.options;\n  const client: LiveHorseDecisionWorkerClient = new LiveHorseDecisionWorkerClient({\n    workerFactory, readyTimeoutMs, jobTimeoutMs,\n    onFatal: (error) => onSupervisedWorkerLoss(owner, client, error, boot),\n  });\n  return client;\n}\n\nfunction onSupervisedWorkerLoss(owner: LiveHorseWorkerSupervisor, failed: LiveHorseDecisionWorkerClient, error: Error, boot: boolean): void {\n  if (boot && failed.status().readyAt === null) { owner.options.onFatal?.(error); return; }\n  if (owner.stopped || supervisor !== owner || singleton !== failed) return;\n  if (owner.options.canReplace && !owner.options.canReplace()) return;\n  const now = Date.now();\n  owner.recentFailures = owner.recentFailures.filter((at) => now - at < LIVE_HORSE_WORKER_REPLACEMENT_WINDOW_MS);\n  const backoff = LIVE_HORSE_WORKER_REPLACEMENT_BACKOFF_MS;\n  const delayMs = backoff[Math.min(owner.recentFailures.length, backoff.length - 1)];\n  owner.recentFailures.push(now);\n  owner.lastReplacementError = error.message;\n  owner.timer = setTimeout(() => {\n    owner.timer = null;\n    // fail() already began terminate(); join it so two threads never own HorseMind persistence.\n    void failed.stop().catch(() => undefined).then(() => {\n      if (owner.stopped || supervisor !== owner || singleton !== failed) return;\n      if (owner.options.canReplace && !owner.options.canReplace()) return;\n      owner.replacements += 1;\n      singleton = spawnSupervisedClient(owner, false);\n    });\n  }, delayMs);\n  owner.timer.unref?.();\n  try { owner.options.onReplace?.(error, { attempt: owner.recentFailures.length, delayMs }); }\n  catch { /* reporting can never re-enter the process-fatal path */ }\n}\n\nexport async function startLiveHorseDecisionWorker(options: LiveHorseDecisionWorkerSupervisorOptions = {}): Promise<LiveHorseDecisionWorkerClient> {\n  if (!singleton) {\n    supervisor = { options, stopped: false, timer: null, recentFailures: [], replacements: 0, lastReplacementError: null };\n    singleton = spawnSupervisedClient(supervisor, true);\n  }\n  await singleton.ready();\n  return singleton;\n}\nexport function getLiveHorseDecisionWorker(): LiveHorseDecisionWorkerClient { /* unchanged */ }\nexport function liveHorseDecisionWorkerStatus(): SupervisedLiveHorseDecisionWorkerStatus {\n  return { ...(singleton?.status() ?? stoppedStatus()), replacements: supervisor?.replacements ?? 0, lastReplacementError: supervisor?.lastReplacementError ?? null };\n}\nexport async function stopLiveHorseDecisionWorker(): Promise<void> {\n  const owner = supervisor;\n  if (owner) { owner.stopped = true; if (owner.timer) clearTimeout(owner.timer); owner.timer = null; }\n  const owned = singleton;\n  try { if (owned) await owned.stop(); }\n  finally { if (singleton === owned) singleton = null; if (supervisor === owner) supervisor = null; }\n}\n\n2) server/src/GameServer.ts:1881-1885. Rewrite the 1870-1879 comment to state the new contract. In the call:\n      await startLiveHorseDecisionWorker({\n        onFatal: (error) => { this.revokeDealerPrerequisites(generation); throw error; }, // boot only: never reached READY\n        canReplace: () => this.directAdmissionIsCurrent(generation),\n        onReplace: (error, replacement) => reportError(error, 'GameServer.horse_decision_worker_replaced', replacement),\n      });\nThe existing pins in LiveHorseDecisionWorkerLifecycle.guard.test.ts:30-31 ('onFatal: (error) =>', 'throw error;') stay green.\n\n3) Metric. In engineInstruments.ts after :288, add gauge 'poker_horse_decision_worker_replacements' (\"Live horse decision workers replaced in-process after a post-READY loss since boot; every increment is an incident\"). In GameServer getPrometheusMetrics, after `horseDecisionWorkerExpiredJobs.set(worker.expiredJobs);`, add `horseDecisionWorkerReplacements.set(worker.replacements);`. Add the metric name to the list in LiveHorseDecisionWorkerHealth.guard.test.ts:31-45.\n\n4) Tests in server/src/engine/horseDecision/client.test.ts. Import afterEach, LIVE_HORSE_WORKER_REPLACEMENT_BACKOFF_MS, getLiveHorseDecisionWorker, liveHorseDecisionWorkerStatus, startLiveHorseDecisionWorker and stopLiveHorseDecisionWorker. Reuse FakeWorker, ready, snapshot and fastResult. Add this block:\n\ndescribe('live horse decision worker supervision', () => {\n  const workers: FakeWorker[] = [];\n  const spawn = vi.fn((): FakeWorker => { const w = new FakeWorker(); workers.push(w); return w; });\n  afterEach(async () => {\n    const stopping = stopLiveHorseDecisionWorker();\n    for (const w of workers.splice(0)) w.emitMessage({ type: 'STOPPED' }); // a READY client drains via SHUTDOWN->STOPPED\n    await stopping; spawn.mockClear(); vi.useRealTimers();\n  });\n\n  it('replaces a worker lost after READY instead of escalating to the process', async () => {\n    const onFatal = vi.fn(); const onReplace = vi.fn();\n    const starting = startLiveHorseDecisionWorker({ workerFactory: spawn, onFatal, onReplace });\n    workers[0].emitMessage(ready); const original = await starting;\n    const inFlight = original.decideFast(snapshot('in-flight'));\n    workers[0].emitError(new Error('worker core lost'));\n    await expect(inFlight).rejects.toThrow('worker core lost');\n    await vi.waitFor(() => expect(spawn).toHaveBeenCalledTimes(2));\n    expect(onFatal).not.toHaveBeenCalled();\n    expect(onReplace).toHaveBeenCalledWith(expect.objectContaining({ message: 'worker core lost' }), { attempt: 1, delayMs: LIVE_HORSE_WORKER_REPLACEMENT_BACKOFF_MS[0] });\n    expect(workers[0].terminateCalls).toBe(1);\n    const replacement = getLiveHorseDecisionWorker(); expect(replacement).not.toBe(original);\n    workers[1].emitMessage(ready);\n    const next = replacement.decideFast(snapshot('after-replacement'));\n    expect(workers[1].sent).toEqual([expect.objectContaining({ type: 'DECIDE_FAST', requestId: 1 })]);\n    workers[1].emitMessage(fastResult(1, 'after-replacement'));\n    await expect(next).resolves.toMatchObject({ fence: 'after-replacement' });\n    expect(liveHorseDecisionWorkerStatus()).toMatchObject({ phase: 'ready', replacements: 1, lastReplacementError: 'worker core lost' });\n  });\n\n  it('keeps a boot worker that never reaches READY fatal to boot', async () => {\n    const onFatal = vi.fn(); const onReplace = vi.fn();\n    const starting = startLiveHorseDecisionWorker({ workerFactory: spawn, onFatal, onReplace });\n    workers[0].emitError(new Error('hydration failed'));\n    await expect(starting).rejects.toThrow('hydration failed');\n    await vi.waitFor(() => expect(onFatal).toHaveBeenCalledTimes(1));\n    expect(onReplace).not.toHaveBeenCalled(); expect(spawn).toHaveBeenCalledTimes(1);\n  });\n\n  it('replaces a wedged worker whose posted job outlives its execution budget', async () => {\n    vi.useFakeTimers();\n    const onFatal = vi.fn();\n    const starting = startLiveHorseDecisionWorker({ workerFactory: spawn, onFatal, jobTimeoutMs: 5 });\n    workers[0].emitMessage(ready); const original = await starting;\n    const expired = expect(original.decideFast(snapshot('wedged'))).rejects.toBeInstanceOf(HorseDecisionExpiredError);\n    await vi.advanceTimersByTimeAsync(5); await vi.runOnlyPendingTimersAsync(); await expired;\n    await vi.waitFor(() => expect(spawn).toHaveBeenCalledTimes(2));\n    expect(onFatal).not.toHaveBeenCalled(); expect(workers[0].terminateCalls).toBe(1);\n    expect(liveHorseDecisionWorkerStatus()).toMatchObject({ phase: 'starting', replacements: 1 });\n  });\n\n  it('backs off repeated losses and never spawns once shutdown has begun', async () => {\n    vi.useFakeTimers();\n    const onReplace = vi.fn();\n    const starting = startLiveHorseDecisionWorker({ workerFactory: spawn, onReplace });\n    workers[0].emitMessage(ready); await starting;\n    workers[0].emitExit(1);\n    await vi.waitFor(() => expect(spawn).toHaveBeenCalledTimes(2));\n    workers[1].emitMessage(ready);\n    workers[1].emitExit(1);\n    await new Promise<void>((resolve) => queueMicrotask(resolve));\n    expect(onReplace).toHaveBeenLastCalledWith(expect.any(Error), { attempt: 2, delayMs: LIVE_HORSE_WORKER_REPLACEMENT_BACKOFF_MS[1] });\n    await vi.advanceTimersByTimeAsync(LIVE_HORSE_WORKER_REPLACEMENT_BACKOFF_MS[1] - 1);\n    expect(spawn).toHaveBeenCalledTimes(2);\n    await stopLiveHorseDecisionWorker();\n    await vi.advanceTimersByTimeAsync(LIVE_HORSE_WORKER_REPLACEMENT_BACKOFF_MS[1]);\n    expect(spawn).toHaveBeenCalledTimes(2);\n    expect(liveHorseDecisionWorkerStatus().phase).toBe('stopped');\n  });\n\n  it('does not replace a worker lost after its owner fenced the lifecycle', async () => {\n    let current = true; const onFatal = vi.fn();\n    const starting = startLiveHorseDecisionWorker({ workerFactory: spawn, onFatal, canReplace: () => current });\n    workers[0].emitMessage(ready); await starting;\n    current = false; workers[0].emitExit(1);\n    await new Promise<void>((resolve) => setTimeout(resolve, 10));\n    expect(spawn).toHaveBeenCalledTimes(1); expect(onFatal).not.toHaveBeenCalled();\n  });\n});\n\n5) Update the guard in the same commit (repo rule 5.8). LiveHorseDecisionWorkerWiring.guard.test.ts:89-95 becomes 'has no synchronous fallback, and only the module supervisor replaces a lost worker'. It keeps the new Worker( count of 1, not HorseLogic.decide(, \"this.phase = 'failed'\" and 'this.terminateWorker()'. It replaces not.toContain('respawn') with:\n- expect(sliceMethod(client,'export class LiveHorseDecisionWorkerClient')).not.toContain('new LiveHorseDecisionWorkerClient(')\n- expect(client.match(/new LiveHorseDecisionWorkerClient\\(/g) ?? []).toHaveLength(1)\n- expect(sliceMethod(client,'function spawnSupervisedClient(')).toContain('new LiveHorseDecisionWorkerClient(')\n\nAdd to LiveHorseDecisionWorkerLifecycle.guard.test.ts:\n  it('replaces a worker lost after READY in-process and reports every replacement', () => {\n    const start = sliceMethod(gameServerSource, 'private async performStart('); const code = blankNonCode(start);\n    const options = start.slice(code.indexOf('await startLiveHorseDecisionWorker('), code.indexOf('await this.cleanupStaleData('));\n    expect(options).toContain('canReplace: () => this.directAdmissionIsCurrent(generation)');\n    expect(options).toContain(\"'GameServer.horse_decision_worker_replaced'\");\n  });\n\n6) Write docs/changelog/2026-09-10-a-lost-horse-worker-is-replaced-not-the-fleet.md. Record the 5 crashes, the metrics, and why the RNG/HorseMind rationale no longer holds.\n\nOPTIONAL follow-ups (not needed for the goal):\n(a) In maybeDispatch (client.ts:510-512), post before arming. On an error with name === 'DataCloneError', reject only that job, count it as recoverable and continue, so P4 stops touching the worker. Generic post errors stay fatal, which keeps client.test.ts:522 valid.\n(b) Bound stopLiveHorseDecisionWorker at shutdown (race owned.stop() with about 15s, then terminate and report) so P7 cannot spend the 40s process budget. The HorseMind replay tail makes a skipped final flush cheap.",
  "unverified": [
    "Which await held the fatal shutdowns for 40s. None reached 'Drained all' or 'Drain window elapsed'. drainOwnedLifecycleJobs() at GameServer.ts:2420 joining about 720 resume admissions is a suspicion, not proven.",
    "Why the same about 720-736 RUNNING tournaments are re-admitted on every discovery pass (710/710 and 736/736 overlap), and why at boot 18:59 about 1016 engines then 'Stopped. Dealt 0 hands'. Not root-caused.",
    "Whether 70a6a75f survives a burst after the thaw. At 20:00:03 its only two bursts (19:56:5x with 736 resumes, 19:59:2x with 734) happened during the maintenance freeze; expired_jobs=0, RestartCount=0.",
    "That under extreme main-thread saturation a worker reply already posted is always handled in the poll phase before #4190's setImmediate recheck. This rests on libuv/Node semantics and #4190's own unit test; not measured in production.",
    "P7 (the final HorseMind flush exceeding 40s on a :55 SIGTERM) was not observed. Logs of containers before 18:59 are gone.",
    "The proposed code and tests were not compiled or run (read-only mirror). Timer and microtask sequencing in the fake-timer cases was reasoned from the existing #4190 tests.",
    "The container's earlier restarts (the reported RestartCount 13+ before 18:53) were not examined. That container's logs no longer exist.",
    "Disclosure: one comparison command wrote two temporary tournament-id lists to /dev/shm on engine-01 and deleted them in the same command. No other writes anywhere."
  ]
}
```

---

## key

honest

## r

````json
{
  "summary": "State as of origin/main f311f78854 (#4208, merged 2026-09-10T19:48:27Z, same session). The core is already in: timeout-minutes 110 (auto-deploy-hetzner.yml:223), JOB_TIMEOUT_S 110*60 (:841), a wall-clock GATE_DEADLINE on the break gate (:871), INFLIGHT_STALE_MIN 120 (engine-watchdog.sh:379), and a new last step 'Verdict \u2014 green only if production serves this commit' (:1471-1499). The Verdict runs after Cleanup, after every step keyed on failure() or success(), and copies the ledger's SHIPPED expression byte for byte (pinned). It exits 1 only when job.status==success and SHIPPED!=true and dedupe skip!=true, which means both break-gate declines ('IMAGE STAGED, CUTOVER DEFERRED' and 'BREAK NEVER OPENED'). Every other case exits 0. No run has executed it yet: the only in-flight run, 34519356271 (created 19:15:50Z, sha 70a6a75f), runs the old YAML.\n\nMeasured 2026-09-10, 60 runs from 00:45 to 19:15Z:\n- 26 green. 10 shipped. 16 shipped nothing: 12 image-staged-deferred, 1 break-never-opened (34445622542), 3 already-live (f1992eeb).\n- 8 red. Every red run since #4083 failed at 'Stage the current release proof control', meaning main had moved: a supersession, not a ship failure.\n- 25 cancelled.\n\nUnder #4208 the 13 gate declines turn red and the already-live runs stay green.\n\nResidual defects in the status design:\n1. Supersession is still RED. It already opened a false train alarm, #4109 (05:20:45Z). With runs now holding the deploy-hetzner group for up to 110 min, it will get more common.\n2. There is no machine-readable kind for a break-gate decline, so a future deliberate 'lease held elsewhere' deferral would be painted red.\n3. The dedupe cannot tell already-live from coalesced. The DID NOT DEPLOY step prints '<sha> is NOT on production' on already-live runs (verified on 34472588025).\n4. engine-watchdog train_check ignores timed_out, names 'Verdict' as the failing gate without the reason, comments on every sweep, and never names the workflow's display name. Because of the last point, check-main-is-green would also file its own issue after 6h of red verdicts.\n5. Text is now stale in engine-watchdog.sh, deploy-ship-rate.mjs, workflow comments and laws.d.\n6. A dead start-marker step warns on every run.\n\nConsumers of the conclusion: engine-watchdog train_check and check-main-is-green, nothing else.",
  "findings": [
    {
      "title": "#4208 already implemented the timeout/budget change and a final Verdict step",
      "detail": "timeout-minutes 110 and JOB_TIMEOUT_S=$(( 110 * 60 )) are in step, GATE_DEADLINE bounds the poll, and the engine-watchdog stale window is 120. The Verdict (:1471) is the last step, after Cleanup (:1447). ROLLBACK (:1167 'failure() && ...'), Retention (:1286 'success() && ...') and DID NOT DEPLOY (:1315, implicit success()) all come before it, as do the always() steps Revoke (:1254), Post-deploy summary (:1388) and Record deploy truth (:1418). Its exit 1 therefore changes only the run colour, and Cleanup does not have to be last. Verdict logic: JOB_STATUS!=success exits 0; SHIPPED==true exits 0; DEDUPE_SKIP==true exits 0; otherwise '::error title=DID NOT SHIP::' and exit 1. Pins: tests/unit/deployAndPublishAreHonest.test.ts:320 (no step after the verdict), :329 (SHIPPED identical to the ledger's), :333-346 (branch regexes; after lastIndexOf('fi') the text must hold DID NOT SHIP and end 'exit 1'), :354 (ROLLBACK before the verdict). Also tests/the-deploy-can-always-ship.law.test.ts:187-218 (every start minute reaches the break; INFLIGHT_STALE_MIN > timeout) and :377 ('waits in the break gate'); tests/engine-watchdog-asks-production.test.ts:164-170 (stale > deploy timeout).",
      "evidence": "git show f311f78854 --stat (6 files); auto-deploy-hetzner.yml@f311f78854 lines 223, 841, 871, 1471-1499",
      "severity": "info"
    },
    {
      "title": "Every step from the drain step to the end: id, if, whether it can fail, non-ship behaviour",
      "detail": "Line numbers are in auto-deploy-hetzner.yml at f311f78854.\n- drain :792 (id drain; if dedupe skip!='true'). Cannot fail: all three paths exit 0. Budget refusal :848-860 emits ::notice 'IMAGE STAGED, CUTOVER DEFERRED' plus skip=true and gate_reason. Never-opened :897-902 emits ::warning 'BREAK NEVER OPENED' plus skip=true and gate_reason.\n- seal :906 (id seal; if dedupe!=true && drain skip!=true). Can fail: rollback misuse, bad token, or a seal prepare 'die' such as 'another audited cutover is still active' or 'normal deploy would move behind'. Skipped on non-ship.\n- cutover :943 (id cutover; same if). Can fail. Outputs attempted, candidate_cid, candidate_started_at, success. Skipped on non-ship.\n- health :1005 (id health). Can fail (24 container / 12 public attempts). Output verified=true. Skipped on non-ship.\n- Promote :1069 (no id). Can fail. Skipped on non-ship.\n- prove :1093 (id prove; STRICT, 240s). Can fail. Skipped on non-ship.\n- seal_commit :1109. Can fail. Skipped on non-ship.\n- rollback :1160 (if failure() && cutover.attempted=='true' && seal_commit!=success). Can fail. Outputs target_committed and recovered_sha. Skipped on non-ship.\n- Revoke/GUARANTEE :1250 (if always() && seal success && rollback!=success). Can fail (exit ABORT_RC). Skipped on non-ship.\n- Retention :1285 (success() && both skips!='true'). Cannot fail ('|| true').\n- DID NOT DEPLOY :1314 (if dedupe skip=='true' || drain skip=='true'). Cannot fail; prints a warning and a summary.\n- Post-deploy summary :1387 (always). Effectively cannot fail.\n- Record deploy truth :1417 (always). Cannot fail: the recorder always exits 0 (pinned in deployTruthIsAnsweredByTheDatabase.test.ts:50-55). Writes shipped=false plus REASON on every non-ship, including cancellations.\n- Cleanup :1447 (always). Cannot fail.\n- Verdict :1471 (always). Described above.\nDedupe :332 (id dedupe, no if) outputs:\n- skip=true at :339 when /health.version == target[0:8] (already live). Exits before anything else is evaluated.\n- unplanned_restart=true at :398, only when uptime < seconds since our last shipped ledger row (>0). Otherwise absent; the summary prints 'no'.\n- skip=true at :433: coalesced. Requires event != workflow_dispatch (:430), a healthy engine, and 0 <= since-last-shipped < 1200s.\n- skip=false at :443.\n- '' when the step never ran.\nThere is no coalesced or reason output, so no consumer can tell already-live from coalesced.",
      "evidence": "grep of '- name:|id:|if:' in auto-deploy-hetzner.yml at f311f78854; job logs of runs 34516171966, 34472588025, 34445622542",
      "severity": "info"
    },
    {
      "title": "A superseded run is still RED, and 110-minute holds make it the dominant red",
      "detail": "The Stage step compares the run's control SHA to the current tip of main (:262-263) and exits 1 on any mismatch. That covers runs that waited in the concurrency queue, and runs that started while a merge train advanced main. Every red run since #4083 is this case, 8 of 8 on 2026-09-10. None was a ship failure. The Verdict cannot help: JOB_STATUS=failure, so it exits 0. The ledger records these runs as 'run ended without a verified cutover'. engine-watchdog train_check (two consecutive non-cancelled failures) opened the false alarm #4109 at 05:20:45Z naming this step. Now that a run may hold the group for up to 110 min, a pending run (the :35 tick, agent dispatches, an extra dispatch when the watchdog treats an in-progress run as stale) almost always starts after main has moved.",
      "evidence": "auto-deploy-hetzner.yml:253-278 (:262 '[ \"$CONTROL_SHA\" = \"$REMOTE_MAIN\" ]'); failed runs 34440149516 34440541284 34440665782 34442107723 34478102468 34493989808 34494907616 34518081218, each with failing step 'Stage the current release proof control' (jobs API); issue #4109 'Engine watchdog: the hourly deploy train is failing', body 'latest failing step: Stage the current release proof control', open 05:20:45Z to 07:04:58Z; tests/engine-release-seal.law.test.ts:962 pins the equality literal",
      "severity": "high"
    },
    {
      "title": "Any future deliberate deferral would be painted red: the gate has no machine-readable kind",
      "detail": "The Verdict treats every drain skip as 'should have shipped' (:1496-1499). The two declines carry only prose gate_reason (:858, :898). An explicit 'deferred: production lease held by another owner' path would therefore end RED unless the drain step exports a kind that the Verdict allowlists. The seal prepare already fails closed and red on 'another audited cutover is still active (run X)' (engine-release-seal.py cmd_prepare). If that is the lease case, it would have to be detected in the gate rather than die in the seal step.",
      "evidence": "auto-deploy-hetzner.yml:857-858, 897-898, 1492-1499; server/scripts/engine-release-seal.py:286-325",
      "severity": "medium"
    },
    {
      "title": "Already-live runs announce that the commit is NOT on production; the reasons are indistinguishable",
      "detail": "In DID NOT DEPLOY, every dedupe skip falls into the coalescing text (:1354-1355). The step then ends with '::warning title=DID NOT DEPLOY::$REASON \u2014 $SHA is NOT on production.' (:1385), which is false when production already serves the commit. The summary's else branch (:1374-1376), the Verdict summary, and the ledger REASON ('coalesced or already serving this commit', :1438) all merge the two cases. The phrase 'a restart inside this same break was deliberately coalesced' is also wrong. Spacing is 1200s since our last shipped row, and only non-dispatch events can coalesce (:430). With cutovers confined to the :55 break, that is practically unreachable at the :35 tick.",
      "evidence": "run 34472588025 log: 'Production already serves f1992eeb \u2014 nothing to deploy.' then '##[warning]coalescing \u2014 we restarted the engine too recently, or it is already on this commit \u2014 f1992eeb825918d6614d72a10c66988d0cdd292c is NOT on production.' and 'deploy truth recorded: attempt 359 ... shipped=false reason=coalesced or already serving this commit'",
      "severity": "medium"
    },
    {
      "title": "engine-watchdog train_check will misreport red Verdict runs and duplicate the main-is-green alarm",
      "detail": "engine-watchdog.sh has four problems in train_check:\n(a) :152 counts only 'failure' and :173 recovers only on 'success'. timed_out and startup_failure are invisible to it, unlike workflowVerdicts BAD_CONCLUSIONS.\n(b) :155 names the first failing step. For a red Verdict run that is 'Verdict \u2014 green only if production serves this commit', and the body says 'it is the gate that is refusing ... (fix main, never the gate)' (:169). The actual reason lives only in the step's ::error annotation (the publish-watchdog engine job has no checks: read) and in the ledger.\n(c) :159-160 comments 'Still failing' on every sweep: every Publish Club Arena completion plus the :19/:42 cron, with no rate limit.\n(d) The issue never contains the workflow display name. check-main-is-green treats a failing workflow as loud only when an open issue contains run.name. So after MAIN_RED_HOURS=6 of consecutive red verdicts it reports 'Auto-Deploy Hetzner Engine (on server/ changes)' as SILENT and raises or comments on 'A workflow has been failing on main with nobody watching'. #4160 is open with 46 comments, for other workflows.\nCancelled runs are stepped over by both detectors, and a single green (already live) resets both.",
      "evidence": "engine-watchdog.sh:144-181 (jq :148 '.conclusion!=\"cancelled\"'); scripts/ci/check-main-is-green.mjs:61 (HOURS=6), :162-170 (hasOpenAlarm needle=name), :188; scripts/ci/lib/workflowVerdicts.mjs:54; publish-watchdog.yml:57 cron '19,42', :137 engine job, :375 main_is_green; issue #4160 open since 13:13:07Z",
      "severity": "medium"
    },
    {
      "title": "Stale 'a green deploy means nothing' text contradicts #4208",
      "detail": "The following text is now false or misleading:\n- engine-watchdog.sh:8-21 header: 'both of which report SUCCESS'.\n- The staleness-issue body at :437-442: 'Why a green deploy is not an answer ... Both ... report success ... A run of 11-20s shipped nothing'.\n- .github/scripts/deploy-ship-rate.mjs:107-109: '${t.skipped} reported success having deployed nothing ... a green tick ... does not mean a deploy'. shipped=false rows now include red and cancelled runs.\n- Workflow comment :1301-1302: 'A coalesced or drained run finishes GREEN'.\n- Drain comment :849-855 ('NOT shipped nothing ... why this is a notice'), the ::notice at :856, and gate_reason '... so the next window cuts over' at :858. Nothing guarantees a next window, and the run is red.\n- docs/laws.d/tests-the-deploy-can-always-ship.md, first sentence: 'A green deploy run must either ship the commit or say truthfully why it did not'.\n- scripts/ci/record-engine-deploy-attempt.mjs:4 comment.",
      "evidence": "git show origin/main:<file> at the cited lines",
      "severity": "low"
    },
    {
      "title": "The dead start-marker step warns on every run",
      "detail": "'Record that this deploy run started' (:304-315, always, continue-on-error) calls fn_ca_record_engine_deploy_start. Production no longer has that function. #4208's message says the DB dispatcher, the step's only reader, was retired by 20260910183316; that migration file is not on origin/main. Every run now carries a pg error annotation.",
      "evidence": "runs 34516171966, 34518081218, 34518297221: '##[warning]function public.fn_ca_record_engine_deploy_start(unknown, unknown, unknown) does not exist'; scripts/ci/record-engine-deploy-start.mjs:33",
      "severity": "low"
    },
    {
      "title": "Verdict edge cases: it never looks at production; SHIPPED is hidden on red runs; cancellations pollute the ledger",
      "detail": "(a) The Verdict paints a gate decline red without checking /health. If the dedupe read /health as unreadable (run 34505100894: 'Engine health is unreadable \u2014 spacing gate bypassed') while production already served the target, the run is red although 'production serves the target'.\n(b) When JOB_STATUS!=success it exits before reporting SHIPPED. A run that sealed the release and then failed the Revoke abort-audit (:1250-1283) gets no SHIPPED verdict line.\n(c) Cancelled runs still write a ledger row, shipped=false, reason 'run ended without a verified cutover'. For example, 34512893031 was cancelled in the gate at 18:32:47 and 34518297221 in tests; both are in-progress API cancellations, since cancel-in-progress is false.",
      "evidence": "auto-deploy-hetzner.yml:1480-1499, 1432-1442; jobs API steps for 34512893031 and 34518297221 (step 31 'Record deploy truth' ran)",
      "severity": "low"
    },
    {
      "title": "Other effects of 110-minute holds",
      "detail": "- engine-watchdog INFLIGHT (:380-386) measures age from createdAt, which includes pending time (run_started_at equals created_at for every run observed). A run that pended more than 10 min and then ran is treated as stale before it ends, so one extra dispatch is made. That dispatch only queues a pending run and never cancels an in-progress one.\n- The agent-autopilot reaper cancels any run in status=queued that is at least 45 minutes old (agent-autopilot.yml:429-434). It is only harmless if runs waiting on the concurrency group report 'pending'.\n- schedule-liveness isBusy (:506) will block wedge-cycling of the deploy workflow while a run is in flight. That is safe.\n- CUTOVER_RESERVE_S=540 (:842) does not cover ROLLBACK (up to 360s of proxy polling plus a 120s strict proof) or the always() tail. At the worst start minute the margin is about 19 min against about 24 min worst case.\n- Not consumers of this conclusion: post-deploy-e2e.yml (workflow_run on 'Publish Club Arena', :43/:90), publish-watchdog.sh (publisher only), cron-health.yml, estate-digest.mjs (schedule created_at), and fn_ca_engine_deploy_truth_watch (c_silent_after 6h).\n- CI gap: ci.yml:170/195-197 do not classify .github/scripts/** as test-relevant, so a PR editing only engine-watchdog.sh skips the unit tests that pin it.",
      "evidence": "engine-watchdog.sh:379-386; agent-autopilot.yml:429-434; schedule-liveness.mjs:506-525; post-deploy-e2e.yml:43,77,90; supabase/migrations/20260902103000_deploy_truth_cannot_mistake_zero_for_healthy.sql:40; ci.yml:170,197",
      "severity": "low"
    }
  ],
  "proposal": "Baseline is origin/main f311f78854. Ship everything below as ONE PR that also touches tests/. ci.yml does not treat .github/scripts/** as test-relevant, so the tests/ change is what makes the unit suite run.\n\n1. auto-deploy-hetzner.yml, Stage step (:253-278). A superseded run ends CANCELLED, not red. Add the job-level block `permissions: { contents: read, actions: write, issues: read }`. The default token here is already read-only: the financial gate's `gh issue create` fails on every run today.\n```yaml\n      - name: Stage the current release proof control\n        id: control\n        shell: bash\n        env:\n          GH_TOKEN: ${{ github.token }}\n        run: |\n          set -euo pipefail\n          CONTROL_SHA='${{ github.sha }}'\n          [[ \"$CONTROL_SHA\" =~ ^[0-9a-f]{40}$ ]] \\\n            || { echo \"::error::workflow control SHA is not one full commit\"; exit 1; }\n          git fetch --no-tags --depth=200 origin main\n          REMOTE_MAIN=\"$(git rev-parse FETCH_HEAD)\"\n          SUPERSEDED=false\n          if [ \"$CONTROL_SHA\" = \"$REMOTE_MAIN\" ]; then\n            :\n          elif git merge-base --is-ancestor \"$CONTROL_SHA\" \"$REMOTE_MAIN\"; then\n            SUPERSEDED=true\n            echo \"::notice title=SUPERSEDED::main is ${REMOTE_MAIN:0:8}, past this run's ${CONTROL_SHA:0:8}; stale control-plane code refused, the run on the newer main ships this commit. Cancelling this run.\"\n            gh run cancel \"$GITHUB_RUN_ID\" --repo \"$GITHUB_REPOSITORY\" >/dev/null 2>&1 \\\n              || echo \"::warning::could not self-cancel; the dedupe step stands this run down\"\n          else\n            echo \"::error::this deployment workflow was not dispatched from current main; refusing rollbackable control-plane code\"\n            exit 1\n          fi\n          echo \"superseded=$SUPERSEDED\" >> \"$GITHUB_OUTPUT\"\n          # (existing CONTROL_DIR staging unchanged)\n```\nA cancelled conclusion is what GitHub already gives superseded pending runs, and both detectors step over it (engine-watchdog.sh:148, workflowVerdicts.mjs:54-78). Fallback, if actions: write is not wanted: leave the run green with reason=superseded, and make train_check and check-main-is-green treat that run as no evidence.\n\n2. Dedupe step (:332-445).\n- Add as the first lines of the step:\n```bash\nif [ \"${{ steps.control.outputs.superseded }}\" = \"true\" ]; then\n  echo \"skip=true\" >> $GITHUB_OUTPUT; echo \"reason=superseded\" >> $GITHUB_OUTPUT; exit 0\nfi\n```\n- After :339 add `echo \"reason=already_live\" >> $GITHUB_OUTPUT`.\n- After :433 add `echo \"reason=coalesced\" >> $GITHUB_OUTPUT`.\n- Optionally delete coalescing altogether. It only fires on schedule events (:430), and cutovers confined to the :55 break make it practically unreachable.\n\n3. Drain step (:792-904).\n- After :857 add `echo \"gate_kind=staged_deferred\" >> $GITHUB_OUTPUT`.\n- After :897 add `echo \"gate_kind=no_certificate\" >> $GITHUB_OUTPUT`.\n- Contract for any deliberate deferral, such as a production lease held by another owner: a path INSIDE this step that writes skip=true, gate_reason=<prose>, and gate_kind=lease_held_elsewhere, then exits 0.\n- Text: turn the ::notice at :856 into ::warning. In gate_reason at :858, replace 'so the next window cuts over' with 'a later run must cut it over'. Update the comments at :849-855 and :1301-1302.\n\n4. DID NOT DEPLOY step (:1314-1385).\n- In the elif at :1354-1355, switch on `\"${{ steps.dedupe.outputs.reason }}\"`:\n  - already_live: REASON=\"already serving this commit\"\n  - superseded: REASON=\"superseded: main moved past this run; the run on the newer main ships it\"\n  - anything else: keep the existing coalescing text.\n- Make the else branch of the summary (:1374-1376) say the specific reason.\n- Replace :1385 with:\n```bash\nif [ \"${{ steps.dedupe.outputs.reason }}\" = \"already_live\" ]; then\n  echo \"::notice title=ALREADY LIVE::production already serves $SHA; nothing to deploy.\"\nelse\n  echo \"::warning title=DID NOT DEPLOY::$REASON \u2014 $SHA is NOT on production.\"\nfi\n```\nThis keeps the '::warning title=DID NOT DEPLOY::' pin in theCopyRules and the pins at deployAndPublishAreHonest:139-141.\n\n5. Record deploy truth (:1433-1442). Change only REASON; the SHIPPED line must stay byte-identical to the Verdict's (:329 pin). Prepend:\n```\n${{ steps.control.outputs.superseded == 'true' && 'superseded: main moved past this run before it started; the run on the newer main ships this commit'\n || steps.dedupe.outputs.reason == 'already_live' && 'already serving this commit'\n || steps.dedupe.outputs.skip == 'true' && 'coalesced: we restarted production under 1200s ago'\n```\nKeep the existing drain, prove and health alternatives. Insert `|| job.status == 'cancelled' && 'cancelled before a verified cutover'` immediately before the final 'run ended without a verified cutover'.\n\n6. Verdict (:1471-1499). Replace it with the version below, keeping the name and last position. It exits 1 only when the run should have shipped AND production does not serve the target.\n```yaml\n      - name: 'Verdict \u2014 green only if production serves this commit'\n        if: always()\n        env:\n          DEDUPE_SKIP: ${{ steps.dedupe.outputs.skip }}\n          DEDUPE_REASON: ${{ steps.dedupe.outputs.reason }}\n          DRAIN_SKIP: ${{ steps.drain.outputs.skip }}\n          GATE_KIND: ${{ steps.drain.outputs.gate_kind }}\n          GATE_REASON: ${{ steps.drain.outputs.gate_reason }}\n          JOB_STATUS: ${{ job.status }}\n          SHIPPED: <unchanged: byte-identical copy of :1425>\n        run: |\n          SHORT=\"${SHA:0:8}\"\n          if [ \"$JOB_STATUS\" != \"success\" ]; then\n            echo \"An earlier step already failed or cancelled this run (shipped=$SHIPPED); its error is the verdict.\"\n            exit 0\n          fi\n          if [ \"$SHIPPED\" = \"true\" ]; then\n            echo \"SHIPPED: production serves $SHORT.\"\n            echo \"### Verdict: SHIPPED \\`$SHORT\\`\" >> \"$GITHUB_STEP_SUMMARY\"\n            exit 0\n          fi\n          if [ \"$DEDUPE_SKIP\" = \"true\" ]; then\n            echo \"Nothing to ship for $SHORT: ${DEDUPE_REASON:-dedupe reason not recorded}.\"\n            echo \"### Verdict: nothing to ship (${DEDUPE_REASON:-not recorded})\" >> \"$GITHUB_STEP_SUMMARY\"\n            exit 0\n          fi\n          if [ \"$DRAIN_SKIP\" = \"true\" ] && [ \"$GATE_KIND\" = \"lease_held_elsewhere\" ]; then\n            echo \"Deliberately deferred: $GATE_REASON\"\n            echo \"### Verdict: deferred \\`$SHORT\\` - $GATE_REASON\" >> \"$GITHUB_STEP_SUMMARY\"\n            exit 0\n          fi\n          REASON=\"${GATE_REASON:-the cutover did not verify}\"\n          [ \"$DRAIN_SKIP\" = \"true\" ] || REASON=\"the cutover did not verify\"\n          SERVED=$(curl -sf --max-time 10 \"$ENGINE_URL/health?nocache=$(date +%s%N)\" 2>/dev/null \\\n            | python3 -c 'import json,sys; print(json.load(sys.stdin).get(\"version\") or \"\")' 2>/dev/null || echo \"\")\n          if [ -n \"$SERVED\" ] && [ \"$SERVED\" = \"$SHORT\" ]; then\n            echo \"::warning title=SERVING WITHOUT A CUTOVER::$REASON, yet production serves $SHORT.\"\n            echo \"### Verdict: production serves \\`$SHORT\\` although this run did not cut over ($REASON)\" >> \"$GITHUB_STEP_SUMMARY\"\n            exit 0\n          fi\n          [ -n \"$SERVED\" ] || REASON=\"$REASON; COULD NOT TELL what production serves, /health unreadable\"\n          echo \"### Verdict: DID NOT SHIP \\`$SHORT\\` - $REASON\" >> \"$GITHUB_STEP_SUMMARY\"\n          echo \"::error title=DID NOT SHIP::$REASON. $SHA is not on production, and this run should have put it there.\"\n          exit 1\n```\nThis satisfies every existing pin at :320-354. After the last literal \"fi\", the script contains no word with \"fi\" in it, because the test uses lastIndexOf('fi').\n\nTruth sources, in order:\n- SHIPPED: the ledger's own expression, with container, proxy, engine_leader and seal proofs.\n- Deliberate kinds: step outputs only.\n- One cache-busted public /health read, used only before painting red; unreadable stays red, labelled COULD NOT TELL.\n\n7. engine-watchdog.sh train_check (:144-181).\n- Use `--json conclusion,status,url,databaseId,workflowName`.\n- Treat failure|timed_out|startup_failure as red.\n- When $step starts with 'Verdict', add `why=$(gh run view \"$rid\" --repo \"$REPO\" --log 2>/dev/null | grep -m1 'this run should have put it there' | sed -E 's/.*##\\[error\\]//')` as a '| why |' row, and drop 'it is the gate that is refusing ... never the gate' for that case.\n- Add a row '| workflow | <workflowName> (`auto-deploy-hetzner.yml`) |' so check-main-is-green.mjs:162-170 sees the deploy as loud.\n- Comment only when $url is not already in the issue's last comment.\n- Rewrite the header at :8-21 and the body at :437-442: green = shipped / already live / coalesced; red = should have shipped; cancelled = superseded.\n\n8. .github/scripts/deploy-ship-rate.mjs:107-109: stop saying the non-shipped rows 'reported success'. Say that break-gate declines are red runs since #4208, and that cancelled rows are supersessions.\n\n9. Delete 'Record that this deploy run started' (:304-315) and scripts/ci/record-engine-deploy-start.mjs, or restore the function. Either way, not one half without the other.\n\n10. Update the first sentence of docs/laws.d/tests-the-deploy-can-always-ship.md to: 'A deploy run is green only when production serves its commit or not shipping was deliberate (already live, coalesced, lease held elsewhere); a run that should have shipped and did not is red; a superseded run cancels itself.'\n\n11. Optional:\n- agent-autopilot.yml:429: have the reaper skip 'Auto-Deploy Hetzner Engine (on server/ changes)', or raise STUCK_MINUTES above 110 for it.\n- engine-watchdog.sh:379: raise INFLIGHT_STALE_MIN to at least 2\u00d7timeout+10, because createdAt includes pending time.\n- Widen CUTOVER_RESERVE_S (:842) to cover ROLLBACK, about 480s more.\n\nTests:\nA. tests/unit/deployAndPublishAreHonest.test.ts, in the describe 'a green engine deploy means production serves the commit (2026-09-10)', add:\n```ts\nit('a deliberate deferral names itself; declines that should have shipped never do', () => {\n  const script = verdict.slice(verdict.indexOf('run: |'));\n  expect(script).toMatch(/if \\[ \"\\$DRAIN_SKIP\" = \"true\" \\] && \\[ \"\\$GATE_KIND\" = \"lease_held_elsewhere\" \\]; then[\\s\\S]{0,300}?exit 0/);\n  expect(script).not.toMatch(/\"(staged_deferred|no_certificate)\"/);\n  const gate = sliceYamlBlock(HETZNER, '      - name: Wait for the maintenance break to park every table');\n  expect((gate.match(/echo \"gate_kind=[a-z_]+\" >> \\$GITHUB_OUTPUT/g) ?? []).length)\n    .toBe((gate.match(/echo \"skip=true\" >> \\$GITHUB_OUTPUT/g) ?? []).length);\n});\nit('every dedupe skip says which one it was, and already-live never claims the commit is missing', () => {\n  const d = sliceYamlBlock(HETZNER, '      - name: Skip if production already serves this commit');\n  expect((d.match(/echo \"reason=(already_live|coalesced|superseded)\" >> \\$GITHUB_OUTPUT/g) ?? []).length)\n    .toBe((d.match(/echo \"skip=true\" >> \\$GITHUB_OUTPUT/g) ?? []).length);\n  expect(sliceYamlEntry(HETZNER, \"name: 'DID NOT DEPLOY\")).toMatch(/\"already_live\" \\]; then\\s*\\n\\s*echo \"::notice title=ALREADY LIVE::/);\n});\nit('the verdict reads production before it paints a run red', () => {\n  const s = verdict.slice(verdict.indexOf('run: |'));\n  expect(s.indexOf('/health?nocache=')).toBeGreaterThan(-1);\n  expect(s.indexOf('/health?nocache=')).toBeLessThan(s.lastIndexOf('::error title=DID NOT SHIP::'));\n});\n```\nAlso add an executable table test. Dedent the Verdict's run body and run it with bash. Point ENGINE_URL at a node http server (the pattern is in theDeployProvesItself.test.ts) and set GITHUB_STEP_SUMMARY to a temp file. Expected exits:\n\n| JOB_STATUS | SHIPPED | Other env / server | Exit | Output check |\n| --- | --- | --- | --- | --- |\n| success | true | | 0 | |\n| success | false | DEDUPE_SKIP=true, reason already_live, coalesced or superseded | 0 | |\n| success | false | DRAIN_SKIP=true, GATE_KIND=lease_held_elsewhere | 0 | |\n| success | false | DRAIN_SKIP=true, staged_deferred, server returns another version | 1 | |\n| success | false | DRAIN_SKIP=true, no_certificate, server returns 500 | 1 | stdout has 'COULD NOT TELL' |\n| success | false | DRAIN_SKIP=true, no_certificate, server returns the target | 0 | 'SERVING WITHOUT A CUTOVER' |\n| failure or cancelled | any | | 0 | |\n\nB. tests/engine-release-seal.law.test.ts, after :962:\n```ts\nexpect(stageControl).toContain('git merge-base --is-ancestor \"$CONTROL_SHA\" \"$REMOTE_MAIN\"');\nexpect(stageControl).toContain('gh run cancel \"$GITHUB_RUN_ID\"');\nexpect(stageControl).toMatch(/not dispatched from current main; refusing rollbackable control-plane code\"\\s*\\n\\s*exit 1/);\n```\nIn deployAndPublishAreHonest, add `expect(sliceYamlBlock(HETZNER,'      - name: Skip if production already serves this commit')).toMatch(/steps\\.control\\.outputs\\.superseded \\}\\}\" = \"true\" \\]; then[\\s\\S]{0,120}?echo \"skip=true\"/)`.\n\nC. tests/engine-watchdog-asks-production.test.ts: slice train_check out of SH_CODE and assert:\n- it matches /failure\\|timed_out\\|startup_failure/;\n- it contains 'workflowName' and '| workflow |';\n- the 'Still failing' comment is guarded by a check for $url in the last comment.\n\nD. Keep the-deploy-can-always-ship.law.test.ts:307-313 and deployAndPublishAreHonest:329. The SHIPPED lines must stay identical and REASON must still reference steps.drain.outputs.gate_reason. Do not write 'timeout-minutes: <n>' in any comment above :223, because theDeployProvesItself.test.ts:65 uses an unanchored first-match regex.",
  "unverified": [
    "What conclusion GitHub gives a run whose job exceeds timeout-minutes (cancelled, failure or timed_out), and whether the always() steps (Record deploy truth, Verdict) still run after that kill. No deploy run in the last 500 (since 2026-09-02) lasted 54 minutes or more.",
    "The REST status of a run waiting on the deploy-hetzner concurrency group ('pending' vs 'queued'). This decides whether agent-autopilot's 45-minute status=queued reaper can cancel a legitimately waiting deploy run.",
    "Who cancelled today's in-progress deploy runs (34501790172, 34508883367, 34512893031, 34518297221). cancel-in-progress is false, so these were API or UI cancellations, not the concurrency group.",
    "That production lacks fn_ca_record_engine_deploy_start is inferred only from run-log warnings. The migration 20260910183316 named in #4208 is not on origin/main, and no database read was done.",
    "The proposed Stage-step changes were reasoned, not executed: the self-cancel (gh run cancel needing actions: write), and git merge-base --is-ancestor inside the depth-1 checkout after git fetch --depth=200.",
    "The #4208 Verdict has not run yet. The only in-flight run, 34519356271 (created 19:15:50Z, sha 70a6a75f), predates f311f78854 and executes the old YAML (timeout 55, no Verdict).",
    "The breakdown of today's no-ship reasons comes from job logs. The annotations API returned 403 'Resource not accessible by personal access token' with the local token.",
    "origin/main in ~/Documents/club-arena moved from f9bcd036e5 to f311f78854 during this task because another process fetched. All line numbers above are for f311f78854."
  ]
}
````
