# Workflow evidence: club-arena-night-audit

- run: wf_b89b25d1-a4d
- finished: 2026-09-11T08:06:24.470Z
- status: completed
- agents: 14
- summary: Line-by-line review of tonight's engine + deploy changes, plus three read-only root-cause investigations

## reviewed

````json
[
  {
    "changeset": "resume-budget",
    "summary": "Reviewed fdd789d337 (#4212) and 23f00c1bef (#4218) line by line against the read-only worktree at 404948b354, with production /health, logs and read-only SQL.\n\n**No defect found in:**\n- **In-flight accounting:** tournamentResumesInFlight is set before launch and deleted by compare-and-delete in .finally. The same id cannot relaunch while its admission is in the registry, because the lane re-checks the registry synchronously and its chain settles in microtasks before the next timer-driven iteration. The 60s horizon is applied correctly.\n- **Distress chain:** a thrown resume counts 1 via .catch, a scheduled retry counts 1 via .then, and there is no double count on reject.\n- **Verdict:** exactly one nextEngineStartBudget call per lane pass, in a finally, with no early continue in the lane's try.\n- **Races with other admission paths:** no double admission against the REGISTERING start, the seat-first fast lane, the decided and never-dealt sweeps, the seat-first finish sweep or retry timers. All go through ensureTournamentManagerAdmission's registry and a synchronous tournamentEngines check. tournamentEngines is set before manager.resume() flips running=true synchronously.\n- **Shutdown:** the lane is launched through launchDiscoveryJob, so drainOwnedLifecycleJobs joins it, and it exits on the generation fence. The DirectEngineRecovery pin counts 4 discovery jobs.\n- **RUNNING board read failure:** reports, counts as distress, launches nothing. The read is an index scan (idx_tournaments_status_start_time), so the 5s cadence is cheap, and the client has a bounded fetch timeout.\n- **What the move left in discoverTournaments:** a diff of the method from 8fc27a1d73 to now shows only the removed RUNNING block plus a comment. The only leftover is a stale doc comment in the never-dealt sweep ('RUNNING discovery path above').\n- **Test windows:** sliceStatement and sliceEnclosingBlock anchor on the lane's read (first occurrence at :5927, not the finish sweep at :7168).\n\n**Production now (version 404948b3):**\n- tournamentResumeBudget 25, tournamentResumesInFlight 0, activeTournaments 368 against 368 RUNNING.\n- All 369 RUNNING tournaments hold a lease owned by 1-4c617df9.\n- No resume_failed or stop-failure lines in the retained logs.\n\n**The findings:**\n- **Completions counted as distress:** verified in production logs. All three completions in the window were followed 4-7s later by GameServer.tournament_lease_lost, which is the line emitted just before the distress increment. Its impact is quantified from the DB completion rates plus a simulation of the budget law.\n- **Head-of-line starvation:** the code path is verified; the trigger is latent and not occurring now.\n- **Per-pass re-charging of a quarantined manager:** the code path is verified; the trigger is latent.",
    "verified": [
      {
        "title": "Normal tournament completions are charged as re-adoption distress and halve tournamentResumeBudget",
        "severity": "medium",
        "file": "server/src/GameServer.ts",
        "line": 708,
        "description": "performOwnedEngineLeaseProofRenewal adds every entry of lostManagers to tournamentResumeDistress. renewVerifiedTournamentManagerLeaseProofs (GameServer.ts:586-640) walks every manager still in tournamentEngines, and TournamentManagerBase.renewTournamentLeaseProof (TournamentManagerBase.ts:598-606) refuses renewal for any manager with stopFenceApplied or !running. When a tournament finishes, its manager stops itself (TournamentManagerEliminations.ts:3889, `void this.stop()`) but stays in GameServer.tournamentEngines until this renewal pass retires it. So each normal completion is classified as lost, reported as GameServer.tournament_lease_lost ('...to another engine instance'), and counted as distress, and the lane's next pass halves the budget. I checked this in production. The retained engine logs hold three completions, and each one was followed 4-7s later by exactly that line: 22a4e408 COMPLETE 04:22:34.41 then lease_lost 04:22:39.97; 2ebb7a41 04:25:31.07 then 04:25:34.95; 95bc456e settled 04:26:22.90 then 04:26:29.96. That message is emitted only at GameServer.ts:698-700, right before the += at :708. The DB shows 409-501 COMPLETED tournaments per hour on 2026-09-10 05:00-15:00 UTC and 690 at 2026-09-11 01:00. The test at tournamentResumeBudget.test.ts:215 ('a manager that lost or outlived its lease is distress too') pins this over-broad count.",
        "failure_scenario": "At a normal busy-hour rate of about 450 completions/h (one every ~8s), roughly half of the 5-6s lane passes see distress from completions alone. Running nextEngineStartBudget against Poisson completions gives a mean resume budget of 10-12 out of 25, with 27-36% of passes at the floor of 5. At 690/h the mean is 7.7-8.7, with 49-59% of passes at the floor. After an hourly deploy restart, or a burst of real lease losses, RUNNING re-adoption therefore runs at about half to a fifth of the designed rate for reasons unrelated to lease health, and the tables of events not yet adopted deal nothing in the meantime. It also gets worse as adoption succeeds: resumed decided or terminal events finish and add more distress. /health's 'pinned at the floor means managers keep losing their leases' becomes a false alarm.",
        "suggested_fix": "Charge only real lease losses. Add a side-effect-free accessor to TournamentManagerBase, e.g. `hasStoodDown(): boolean { return (this.stopFenceApplied || !this.running) && !this.tournamentLeaseAuthorityExpired; }`. Note that isRunning() and hasCurrentTournamentLeaseAuthority() have side effects, so do not use them here. In performOwnedEngineLeaseProofRenewal, evaluate it for each lost manager BEFORE calling manager.fenceForTournamentLeaseLoss(), because the fence sets tournamentLeaseAuthorityExpired. Keep fencing and retiring every lost manager, but only increment tournamentResumeDistress (and report GameServer.tournament_lease_lost) when !stoodDown. Storm-expired managers (tournamentLeaseAuthorityExpired=true) and managers fenced by the DB or taken over while running are still counted. Update the pin at tournamentResumeBudget.test.ts:215 to require that stood-down or completed managers are excluded.",
        "changeset": "resume-budget",
        "verdict": {
          "real": true,
          "confidence": "high",
          "evidence": "I could not refute it. The code path is deterministic, and production shows it on every completion I could check.\n\nCODE (worktree 404948b354, which is also the running image's revision label):\n- A finished tournament stops its own manager: TournamentManagerEliminations.ts:3889 `void this.stop()`. stop() calls applyStopFence (TournamentManagerBase.ts:4775-4781), which sets stopFenceApplied=true. It then runs applyManagerMutationFence(true) (4711-4717), which sets running=false and clears the lease expiry timer. tournamentLeaseAuthorityExpired stays false.\n- The manager never removes itself from GameServer.tournamentEngines. The only removal is stopOwnedTournamentManager (TournamentManagerOwnership.ts:82-100). The one caller that cleans up completed managers is in discoverTournaments (GameServer.ts:5338-5345), and #4218 itself measured that loop at minutes per pass.\n- The next lease pass runs within 5s (the cadence is floor(20000/4), GameServer.ts:200-202). The proof deadline is still valid, so the manager is a candidate (GameServer.ts:586-597).\n- heartbeat_tournament_leases_v4 (read from pg_proc) ignores tournament status and answers 'kept'. renewTournamentLeaseProof then refuses because stopFenceApplied is set or running is false (TournamentManagerBase.ts:598-606), which puts the manager in lostManagers (GameServer.ts:613-619).\n- GameServer.ts:696-703 then reports 'GameServer.tournament_lease_lost ... to another engine instance' and fences the manager. Line 708 adds the distress. git blame puts line 708 in fdd789d337; the classification and the report are older (adf1a2c35f, 2026-09-08).\n- The lane added by #4218 (GameServer.ts:5891-6004) reads the distress at the start of each pass, and its finally block applies max(5, floor(b/2)) (engineStartBudget.ts:32-36). A single completion takes the budget 25\u219212\u219217\u219222\u219225.\n- The pin at tournamentResumeBudget.test.ts:215-218 locks in this behavior.\n\nPRODUCTION (one instance, 1-4c617df9, tournamentLease.conflictCount 0):\n- The retained logs contain exactly three tournament_lease_lost lines. Each came 2.4-5.1s after this engine's own manager finished normally:\n  - 605fcde6: COMPLETE 04:40:01.437, lease_lost 04:40:05.611\n  - a3c54b2c: COMPLETE 05:04:21.247, lease_lost 05:04:26.356\n  - a1df113e: satellite settlement certified 05:37:34.981, lease_lost 05:37:37.400\n- None of the three logged a '[tournament-lease] no longer proves' warning, so the database said 'kept' and the refusal was local. All three are COMPLETED in the DB, and their lease rows are gone.\n- The retained logs have no lease_proof_expired or manager_fenced_by_database lines at all.\n- The reviewer's three IDs match DB ended_at values (04:22:33.96, 04:25:30.65, 04:26:22.16). Their log lines have since rotated out (json-file, 5x50m), so I did not re-verify those.\n- Hourly completion counts on 09-10 from 05:00 to 15:00 were 476, 501, 320, 335, 434, 455, 432, 409, 450, 490, 469, so 07:00 and 08:00 fall below the claimed 409-501. On 09-11 01:00 the count was 690, bunched into 01:32-01:48.\n- I simulated the law myself with 5s lease passes, 5.2-6.5s lane passes and Poisson completions:\n  - 450/h: mean budget 10.0-11.5, with 28-37% of passes at the floor.\n  - 690/h: mean budget 7.5-8.5, with 50-60% of passes at the floor.\n  - This matches the claim.\n\nIMPACT CAVEATS (the claim overstates this part):\n- The budget only matters while there is a backlog of managerless RUNNING tournaments.\n  - Right now RUNNING = 431 = lease rows = activeTournaments.\n  - 1032 /health samples from 05:41 to 06:22 all read 25 with 0 in flight. No completion fell in that window, so I did not see a live dip.\n- Restarts happen inside the :55 freeze. Across the 11 hours on 09-10, each of the minute-of-hour slots :53 to :59 held 5-12 completions in total, against 114-134 for each of :00 to :03.\n  - So re-adoption that finishes before the :00 thaw is not affected.\n  - The throttle applies when re-adoption spills past the thaw, which is exactly when completions are densest, or when re-adoption follows real lease losses mid-hour.\n- Separately, every completion also sends a false lease-lost error report. That predates this change.\n- Process note: I ran `git fetch -q origin main` in ~/Documents/club-arena. It updated only the remote-tracking refs; the worktree and branches were not touched. My poller script and its log are still in /tmp/claude-audit-skeptic-resume-budget.*.",
          "corrected_fix": "The direction is right: charge only real lease losses. The proposed version needs two corrections.\n\n1) Mirror renewTournamentLeaseProof, including its shutdown-drain exception. beginServerShutdownDrain sets running=false without setting stopFenceApplied (TournamentManagerBase.ts:4762-4766). The shutdown lease loop runs this same performOwnedEngineLeaseProofRenewal (GameServer.ts:763-772). With the proposed predicate, a drained manager that really is taken over by another instance would count as 'stood down', and its lease_lost report would be silenced. Add to TournamentManagerBase:\n\n```ts\n/** Side-effect-free. This generation stopped itself while its lease authority was still intact. */\nhasStoodDownWithLeaseIntact(): boolean {\n  return !this.tournamentLeaseAuthorityExpired &&\n    (this.stopFenceApplied || (!this.running && !this.shutdownDrainFenceApplied));\n}\n```\n\nDo not use isRunning() or hasCurrentTournamentLeaseAuthority() here; both have side effects.\n\n2) Read it before the fence, and do not count the same manager again on a later pass. fenceForTournamentLeaseLoss sets tournamentLeaseAuthorityExpired=true (TournamentManagerBase.ts:643). If a stood-down manager's teardown lasts longer than one 5s pass, the next pass finds it again through hasCurrentTournamentLeaseAuthority() (GameServer.ts:589-592). By then expired=true, so the proposed check would count it and report it after all. In performOwnedEngineLeaseProofRenewal:\n\n```ts\nlet leaseLosses = 0;\nfor (const [tournamentId, manager] of lostTournamentIds) {\n  if (this.tournamentEngines.get(tournamentId) !== manager) continue;\n  const alreadyRetiring = this.tournamentManagerRetirementOperations.has(manager); // WeakMap, GameServer.ts:368\n  const stoodDown = manager.hasStoodDownWithLeaseIntact(); // must run BEFORE the fence\n  if (!stoodDown && !alreadyRetiring) {\n    reportError(new Error(`Lost the tournament lease on ${tournamentId} to another engine instance`), 'GameServer.tournament_lease_lost');\n    leaseLosses++;\n  }\n  manager.fenceForTournamentLeaseLoss();\n  lostManagers.push([tournamentId, manager]);\n}\nthis.tournamentResumeDistress += leaseLosses;\n```\n\nKeep retiring every entry in lostManagers exactly as today.\n\nTests:\n- Replace the source pin at tournamentResumeBudget.test.ts:215-218.\n- Add a behavioral test: a manager that has called stop() and whose heartbeat says 'kept' adds 0 distress.\n- In the same test, a manager whose proof expired, or that was fenced by the DB or taken over while running, adds 1.\n- A manager found again on the next pass while its retirement is in flight adds 0.\n\nOptional: the predicate also excludes a resume that failed internally. resumeLifecycle swallows the error and sets running=false (TournamentManagerBase.ts:4697-4707), and admission then stops it without scheduling a retry. Today that case is counted only when the lease pass happens to catch its retirement. If 'failing to resume' should stay distress, as the /health comment at GameServer.ts:2959-2962 says, count it explicitly in the lane rather than relying on the lease pass."
        }
      },
      {
        "title": "Failing tournaments at the head of the RUNNING board can starve every younger tournament: no per-id backoff, and pending retries are not treated as in flight",
        "severity": "medium",
        "file": "server/src/tournamentResumeBudget.ts",
        "line": 86,
        "description": "selectRunningResumes always gives the room to the first unowned, non-admitting rows in started_at order, and nothing records that an id's last resume came back without a manager. Several lane resumes settle without a manager and without a retry timer. One case: manager.resume() swallows its error, reports resume_failed and sets running=false (TournamentManagerBase.ts:4698-4707); finishTournamentManagerAdmission then stops the manager and returns normally (GameServer.ts:1408-1418). Another case: the claim returns owned_elsewhere, which clears the retry and returns (GameServer.ts:1356-1358). Either way the .finally frees the slot, and the .then adds no distress, because GameServer.ts:5977-5982 counts only a pending retry timer. The tournament stays RUNNING and managerless, so the next pass picks the same ids first again. Separately, admissionInFlight reads only tournamentManagerAdmissionOperations, so ids waiting on a retry timer are also relaunched every pass ahead of the tail. Before #4212, every managerless RUNNING id was launched on every pass, so a failing head could not block the tail. Since #4218, the same failing ids are retried every ~5s (claim, manager, table engines, teardown) instead of once per multi-minute pass. The seat-first lane already had to add parking for exactly this hot-loop shape (GameServer.ts:6935-6945). The law test at tournamentResumeBudget.test.ts:107 only models picks that succeed.",
        "failure_scenario": "Say the oldest R RUNNING events each fail to resume deterministically. R is budget minus in flight: 25, or 5 when the budget is at the floor, which the completion-distress finding makes common. Every pass then launches exactly those R, they fail within the pass, the next pass picks them again, and no younger RUNNING tournament is ever adopted. Meanwhile /health reads tournamentResumeBudget 25 and tournamentResumesInFlight ~0, which looks healthy. This is not happening in production now: all 369 RUNNING tournaments hold a lease owned by instance 1-4c617df9, and activeTournaments equals the RUNNING count. It is a latent regression that the budget introduced.",
        "suggested_fix": "Give the lane a memory of failed ids. In the lane's .then, when the admission settled and `!this.tournamentEngines.has(tournamentId)`, put the id on a per-id doubling cooldown, e.g. `Map<string,{until:number;attempts:number}>` going 5s -> 10s -> ... -> 5min. Clear it when the id gets a manager or leaves the RUNNING board. Add a `coolingDown(id)` predicate to RunningResumePass that selectRunningResumes skips without spending budget. Also extend admissionInFlight to `(id) => this.tournamentManagerAdmissionOperations.has(id) || this.tournamentManagerAdmissionRetryTimers.has(id)`. Add a law test with at least a budget's worth of permanently failing head rows, asserting the tail is still adopted.",
        "changeset": "resume-budget",
        "verdict": {
          "real": true,
          "confidence": "medium",
          "evidence": "The mechanism is real, and every line the reviewer cites checks out. But it is latent, nothing triggers it today, and some of the claim is overstated.\n\nHOW IT WORKS (read in the worktree at 404948b354, which is also the production version per /health)\n- The selection has no memory of failures. tournamentResumeBudget.ts:83-91 computes room = budget - resumes still in flight, walks the board oldest started_at first, and skips only ids that have a manager or an admission in flight. At GameServer.ts:5953-5954, admissionInFlight means tournamentManagerAdmissionOperations only.\n- Some resumes finish with no manager and no retry timer:\n  - TournamentManagerBase.ts:4698-4707: a resume error is caught, reported as resume_failed, and running is set to false.\n  - GameServer.ts:1408-1418: a manager that is not running is stopped and the function returns normally. stopOwnedTournamentManager removes it from the map (TournamentManagerOwnership.ts:98-99).\n  - GameServer.ts:1356-1358: owned_elsewhere clears the retry and returns.\n  - In the lane, .then counts distress only when a retry timer exists (5979-5981), and .finally frees the slot (5987-5990). The tournament stays RUNNING with no manager, so the next pass (5s, GameServer.ts:190) picks it first again.\n- Ids waiting on a retry timer are not treated as in flight, so the lane relaunches them. That spends a slot, and scheduleTournamentManagerAdmissionRetry returns early at 1226 because a timer already exists.\n- Before #4212 this could not happen. `git show fdd789d337` shows the old `for (const tournament of running || [])` loop launched every id with no manager on every pass.\n- The test at tournamentResumeBudget.test.ts:107-125 adds every picked id to owned, so it only models resumes that succeed.\n- I ran the verbatim selection logic against a 442-row board, with failing resumes finishing inside the pass. Results by number of failing rows at the head (F):\n  - F=0: 18 passes to adopt everything.\n  - F=10: 29 passes.\n  - F=24: 418 passes, about 35 minutes.\n  - F=25 at budget 25: nothing adopted after 1000 passes.\n  - F=5 at budget 5: nothing adopted after 1000 passes.\n\nSKEPTIC CAVEATS (verified)\n1. It is not happening now. SQL shows 442 RUNNING tournaments, all 442 leased by 1-4c617df9 with a heartbeat under 30s, and none unleased. /health showed activeTournaments 442, tournamentResumeBudget 25 and tournamentResumesInFlight 0 in all 8 samples over about 35s.\n2. owned_elsewhere is not a lasting trigger here:\n   - Only one engine may run on this host (auto-deploy-hetzner.yml:756).\n   - TOURNAMENT_LEASE_STALE_SECONDS is 30 (tournamentLease.ts:47), so after a crash the conflicts clear within 30s and hit every id the same way.\n3. resume_failed would need a resume that throws every time, and I found no such throw in the current resume path. It is defensive:\n   - Errors reading tables are ignored, and the table rebuild is non-fatal.\n   - restoreDrawnFirstButtons, reconcileTournamentEntryWindow and clearPersistedBreak all catch their errors.\n   - The main remaining throw is the blind-recovery UPDATE. No trigger or check constraint on public.tables rejects a blind-only update. The retired-club guard cannot fire either, because fn_retire_settled_club refuses to retire a club with active tournaments, and all 5 clubs are active.\n   - The other throws, a table-registry conflict (TournamentManagerBase.ts:728-739) and \"Tournament not found\" after a failed read, are transient.\n   So full starvation needs a future bug that fails at least a room's worth of the oldest resumes every time. There is precedent: the 2026-08-25 `No players` resume throw, documented at TournamentManagerBase.ts:4348-4360.\n4. \"The budget is commonly at the floor\" was not observed. The budget was 25 in every sample, and I saw one tournament_lease_lost in about 20 minutes of logs. So R is normally 25, and fewer than 25 failing rows slow the tail down rather than stop it.\n5. It is not invisible: every failure reports resume_failed to the console and Sentry on each pass, even though /health would look healthy.",
          "corrected_fix": "The fix direction is right, with these corrections:\n1. Record the cooldown in the lane's .finally, not only in .then. The condition is: the admission has settled, `!this.tournamentEngines.has(id)` and `!this.tournamentManagerAdmissionRetryTimers.has(id)`. A rejected admission can also leave an id with no manager and no timer. For example, the early `await this.awaitTournamentManagerLeaseRelease(tournamentId)` at GameServer.ts:1300 sits outside the try at 1378-1399 and schedules no retry. A cooldown set only in .then misses it.\n2. Add `coolingDown(id)` to RunningResumePass, and have selectRunningResumes skip it without spending budget. Change admissionInFlight to `(id) => this.tournamentManagerAdmissionOperations.has(id) || this.tournamentManagerAdmissionRetryTimers.has(id)`. Mirror both in the recheck inside the loop at GameServer.ts:5965.\n3. For owned_elsewhere, derive the cooldown from the lease instead of the doubling ladder: max(5s, TOURNAMENT_LEASE_STALE_SECONDS - conflict.holderAgeSeconds). The lease already says when the id can be claimed.\n4. Cap the doubling at about 60s (RESUME_IN_FLIGHT_HORIZON_MS), not 5 minutes. Skipping ids that are cooling down already guarantees the tail gets adopted. A 5-minute cap only keeps the oldest events dark longer after a transient fault clears.\n5. Prune entries each pass for ids that have a manager or are no longer on the RUNNING board read. Clear the map at teardown next to the retry timers (GameServer.ts:2293-2294).\n6. Do not count these failures as distress. One event that always fails would then hold the budget at the floor of 5.\n7. Add a law test: at least ENGINE_START_BUDGET_MAX rows at the head that always fail, with cooldown applied between passes. Assert the whole tail is adopted and each failing row is retried at most once per cooldown.\nAside, older than this change set: after a successful claim, a throw retries every 250ms forever. clearTournamentManagerAdmissionRetry at GameServer.ts:1364 resets the attempt counter, so the delay at 1228-1230 is always the first step. Keeping the failure memory in the admission code itself, rather than only in the lane, would cover this and the other resume callers (GameServer.ts:5726, 7228)."
        }
      }
    ],
    "low": [
      {
        "title": "A lost manager is charged on every renewal pass until it leaves tournamentEngines, so a quarantined manager pins the budget at the floor forever",
        "severity": "low",
        "file": "server/src/GameServer.ts",
        "line": 708,
        "description": "Distress is not deduplicated per manager. After the first detection, the manager is fenced (tournamentLeaseAuthorityExpired=true) but stays in tournamentEngines until stopOwnedTournamentManager completes. TournamentManagerOwnership.ts:88-99 deliberately keeps it in the map when stop() rejects ('quarantined for the next cleanup pass'). Each renewal pass (OWNERSHIP_LEASE_RENEWAL_CADENCE_MS = 20000/4 = 5s) finds it again at GameServer.ts:586-591 through !hasCurrentTournamentLeaseAuthority(), passes the identity check at :697, reports GameServer.tournament_lease_lost again, and adds 1 again at :708. A teardown that takes longer than 5s is also charged several times.",
        "failure_scenario": "TournamentManagerBase.stop() throws an AggregateError when a table engine fails to release ownership or the seat-move quarantine stays unresolved. If one manager's stop() keeps doing that, it produces distress on every 5s renewal pass. Every lane pass is then distressed, and tournamentResumeBudget stays at ENGINE_START_BUDGET_MIN (5) for the life of the process with no storm at all. Any re-adoption backlog (a restart, a burst of lease losses) then runs at a fifth of the designed rate. I saw no stop failures and no repeated lease_lost ids in the retained ~3h of production logs, so this is latent.",
        "suggested_fix": "Charge each manager object at most once. Keep `private readonly tournamentResumeDistressCharged = new WeakSet<TournamentManager>()`, and in performOwnedEngineLeaseProofRenewal only increment tournamentResumeDistress for lost managers not already in the set, adding them as you go. Optionally, also skip re-reporting GameServer.tournament_lease_lost for a manager that is already fenced. This can go in the same edit as the completion fix at line 708."
      }
    ]
  },
  {
    "changeset": "scheduler-recursion",
    "summary": "The #4214 change to pump() is correct and I found no regressions. It changed only pump() (TournamentEliminationScheduler.ts L404-442): a comment, the pumping and pumpRequestedWhilePumping fields, and the do/while with try/finally. Main at 404948b354 has that file byte-identical to the fix commit.\n\nThe two findings are both low severity and both pre-existing, in paths the brief asked me to check: (1) the wake-timer batch can still pump mid-batch and invert urgent-over-routine priority; (2) a stale routine reference in shiftValid can let a tournament jump the queue.\n\nHow I tested: I copied the old and new scheduler to /tmp on the Mac with stubbed imports. At 5,000 queued managers the old code overflows the stack after roughly 1,900 of them in all three of the new test's cases: the finish() and live-behind-dead paths as unhandled rejections, the delayed-wake path as an uncaught exception. The new code drains flat, and the live manager queued behind the dead ones is dispatched. I also ran an isolated vitest (cache kept in /tmp) of TournamentEliminationScheduler.test.ts plus the new test: 13/13 pass. The worktree's git status is clean afterwards.\n\nEvery path into pump:\n- **register \u2192 enqueue:** L166-168 and L303.\n- **Unregister closure:** L170-174.\n- **finish():** L472 and L474. It only ever runs from promise reactions (L512-521), never synchronously inside a pump.\n- **Wake timer:** L345-348.\n- **isActive() inside scheduleWake and enqueue:** L201 and L285 can reach the unregister closure and pump. This is flat, because the resulting pump folds any deeper dead managers.\n- **remove():** never pumps.\n- **dispatch():** makes no synchronous callouts; run is deferred to a microtask.\n\nCan a nested request be lost? No. The only synchronous callout inside the loop is isActive() (L434), and the loop decides to exit (capacity full, or next() returns null) after the last callout. So the do/while re-check can never find new work; it is redundant but harmless.\n\nInfinite loop? No. The flag is only set by callouts during a pass. In production, isActive() is lifecycleIsCurrent \u2192 expireTournamentLeaseAuthority \u2192 fenceForTournamentLeaseLoss \u2192 applyStopFence \u2192 applyManagerMutationFence \u2192 stop() (TournamentManagerBase.ts L651, L577, L642, L4774, L4711, L4783). None of these registers new work, and ServerTableEngineBase.stop() (L2734) returns a promise. So nothing can re-enqueue work without end.\n\nException safety: the finally block does reset pumping, and a stale pumpRequestedWhilePumping is cleared at the start of the next pass. If isActive() threw after unregistering, the folded request would be dropped; a harness shows one queued entry left stranded next to a free slot. But I found no production isActive() that can throw: reportError catches its Sentry errors (errorReporter.ts L212-245), recordRecoveryEvent is wrapped in try/catch, and stop() is async. On the finish() and timer paths any throw is fatal anyway (index.ts L472-473). So I did not report it.\n\nQueue invariants: isActive() runs after next()/shiftValid have returned, so no scan is in progress. remove() only flips flags, and a nested register() adds a new Entry that is picked up in the same pass. The over-dispatch race is also closed, because nothing dispatches during isActive().\n\nOther schedulers in server/src: none has this re-entrancy shape. I checked EquityWorkerPool.pump (L392), whose callouts are postMessage and async promise rejections, and horseDecision/client.maybeDispatch (L496), which dispatches one job at a time. DeadlineScheduler.tick (L434) re-reads the heap after each callback and is never re-entered. BombPotScheduler and the async drains in StableHandExecutor and HorseSessionRotator are fine too. A grep of unregister closures shows TournamentEliminationScheduler's is the only one that triggers processing.\n\nProduction (read-only):\n- The engine reports version 404948b3, which includes the fix.\n- There were 0 RangeError or 'Maximum call stack' lines in the last 3h of engine logs.\n- /metrics shows the scheduler healthy: 368 registered, queue_depth 0, dispatch_total failed 0.\n- The newest engine_recovery_events row with detail 'tournament_lease_lost' is from 2026-09-10 22:57:48 UTC, so a real lease storm has not yet tested the fix in production.\n\nReproduction scripts are on the Mac in /tmp/claude-audit-scheduler-recursion: t_repro.ts, t_repro23.ts, t_batch.ts, t_stale.ts, t_throw.ts and sched_fix.ts.",
    "verified": [],
    "low": [
      {
        "title": "Wake-timer due batch still pumps mid-batch when a manager dies at enqueue time; the new guard does not cover this path",
        "severity": "low",
        "file": "server/src/tournament/TournamentEliminationScheduler.ts",
        "line": 347,
        "description": "This is pre-existing: the code is identical on 35c96eadd7^, so it is not a regression. It is the one route from an isActive() side effect into pump() that the new pumping flag leaves unguarded. The comment on the wake-timer callback (L341-344) says to enqueue the whole due batch, then pump once, so that next() decides urgent-vs-routine priority. But enqueue(entry, kind, false) calls isActive() (L285). For a manager whose lease proof has lapsed, lifecycleIsCurrent() (TournamentManagerBase.ts:651) runs expireTournamentLeaseAuthority -> fenceForTournamentLeaseLoss -> applyStopFence -> applyManagerMutationFence -> unregisterEliminationScheduler. The unregister closure (L170-174) then calls pump() while pumping === false, because the timer callback is not a pump pass. A full pump therefore runs in the middle of the batch and dispatches whatever is queued so far, before later urgent entries from the same batch are enqueued. Case 3 of the new test never hits this path. Its managers answer true on their first isActive() call while dying (test L149), so they only die inside pump(), after the batch is already enqueued.",
        "failure_scenario": "Setup: maxConcurrent=1 and three wakes due in the same timer firing, sorted [R routine order0, D routine order1, U urgent order2]. D's lease proof has expired. I ran this against a copy of the current main file with its imports stubbed (/tmp/claude-audit-scheduler-recursion/t_batch.ts on the Mac). With D alive the dispatch order is U,R,D: urgent first, as designed. With D dead it is R,U. R's routine sweep takes the only slot before U's urgent bust/rebuy/bounty sweep is even enqueued, so correctness work waits a whole routine sweep. Due batches that contain dead managers happen during lease-loss storms, which is exactly when this matters.",
        "suggested_fix": "Make the batch enqueue part of a pump pass. In the armWakeTimer callback, replace L345-347 with `this.pumping = true; try { due.sort((a, b) => a.dueAt - b.dueAt || a.order - b.order).forEach(({ entry, kind }) => this.enqueue(entry, kind, false)); } finally { this.pumping = false; }` and keep the single `this.pump()` that follows. Any unregister-closure pump during the batch is then folded into pumpRequestedWhilePumping, and that one pump satisfies it. I checked this patch: the order becomes U,R with D dead, the three recursion cases stay flat, and all 13 tests pass (TournamentEliminationScheduler.test.ts plus the new test). Also add a test case where the manager dies on its first isActive() call inside the batch."
      },
      {
        "title": "Stale routine reference left by the urgent upgrade becomes valid again after a routine rerun, so a hot tournament jumps the queue",
        "severity": "low",
        "file": "server/src/tournament/TournamentEliminationScheduler.ts",
        "line": 292,
        "description": "This is pre-existing, not introduced by #4214. I am reporting it because the brief asked about next()/shiftValid invariants. isActive() side effects do not corrupt the queues: isActive() only runs after next() has returned, and remove() only flips flags. This hole is separate from that. The in-place upgrade in enqueue() (L291-296) leaves the entry's reference in routineQueue, and the comment calls it 'a harmless stale item'. shiftValid (L364-368) accepts any reference whose entry.queuedAs === kind. If the entry is re-enqueued as routine before that old reference reaches the head (by finish()'s dirty rerun at L469-472, or by a routine wake), the old reference passes the check again. The entry is then dispatched from its old position instead of the tail. That breaks the rule stated at L470-471: 'Tail insertion is the fairness rule: a hot tournament gets one coalesced rerun, after peers that were already waiting.'",
        "failure_scenario": "Setup: maxConcurrent=1; X holds the slot; A, B and C are queued as routine. wakeUrgentAfter('A', 0) upgrades A, leaving its old routine reference at the head. X finishes and A is dispatched via the urgent queue. While A runs, wakeAfter('A', 0) marks it dirty as routine. When A finishes, finish() pushes A to the routine tail, but the stale head reference now passes shiftValid. Reproduction (/tmp/claude-audit-scheduler-recursion/t_stale.ts, run against the current main code) gives dispatch order X,A,A,B,C; the fair order is X,A,B,C,A. In production, a tournament with many busts that also gets routine feature wakes (add-on retry, final-table deal polling) can get ahead of routine peers during a backlog, which raises their oldest_wait.",
        "suggested_fix": "Keep exactly one reference per entry. In the upgrade branch, before `this.urgentQueue.push(entry)`, add `const stale = this.routineQueue.indexOf(entry); if (stale >= 0) this.routineQueue.splice(stale, 1);`. Alternatively, store {entry, seq} references with a per-entry enqueue sequence and have shiftValid require ref.seq === entry.enqueueSeq. I checked the splice version: the order becomes X,A,B,C,A and all 13 scheduler tests pass."
      }
    ]
  },
  {
    "changeset": "deploy-train",
    "summary": "Read the whole of .github/workflows/auto-deploy-hetzner.yml and .github/scripts/engine-watchdog.sh at 404948b354. Read the diffs of ffd317c9b6 (#4216) and 24f9f05c75 (#4221), and the updated law tests. Checked the behaviour against live GitHub run data (gh, read-only), the production deploy ledger (read-only SELECT) and a shallow-clone reproduction.\n\nVerified correct:\n- A workflow_dispatch sent with GITHUB_TOKEN does create a run. At 04:57:37 today run 34562223669's gate declined (523 tables unparked at the 04:55 break), and its Verdict dispatched run 34564131192.\n- A newer run replaces the pending run. That same dispatch cancelled pending push run 34563496426.\n- On a cancel, the always() steps run, `job.status` in their env is 'cancelled' (ledger row for run 34543650303), and `steps.cutover.outcome` is 'skipped'.\n- The gh flags are valid: `run list --commit/--status failure`, `workflow run --ref`.\n- The job permissions cover checkout, the issue writes, run list and workflow run.\n- No bash -e / pipefail pitfalls in the control, dedupe, drain or Verdict scripts.\n- The depth-200 fetch supports the ancestor and diff checks.\n- The superseded dispatch loops only on control-plane changes.\n- Every engine commit since #4216 got a push run.\n- schedule-liveness no longer watches this workflow, and the watchdog no longer dispatches.\n- The law-test arithmetic (the 130-minute ceiling, stale window 270) is consistent.\n\nDefects found:\n- The high one: the new live-by-inclusion dedupe silently no-ops every audited rollback, and a law test pins that behaviour. Reproduced with a depth-1 checkout of 23f00c1b plus a depth-200 fetch of main, against production's 404948b3.\n- The Verdict's hand-on guard keys on the cutover step's outcome instead of its `attempted` output, so a cutover refused before any mutation strands the commit.\n- The retry bound resets on every merge, and the cancelled branch is unbounded even though a job timeout counts as a cancellation. Both can loop without limit.\n- hand_on discards rollback intent.\n- A fix to the control plane cannot restart a stopped train.\n- An old-SHA run that replaces a newer pending run strands newer engine commits.\n\nAlso noted, not reported as defects:\n- The comments at lines 100-102 and 1611-1614 and the watchdog issue body still say a failed run never hands on, which #4221 made false.\n- The Verdict comment says a runner that died is healed; it is not, because a dead runner never runs the Verdict.\n- Live right now: the engine did not park 523 tables at the 04:55 break, so the train is handing itself on every hour with red runs. That is the gate's designed fail-closed behaviour.\n\nThe worktree is unchanged, and the Mac temp files I made are removed.",
    "verified": [
      {
        "title": "Audited rollback is short-circuited as 'already live' by the new live-by-inclusion dedupe",
        "severity": "high",
        "file": ".github/workflows/auto-deploy-hetzner.yml",
        "line": 409,
        "description": "#4216 added a second dedupe skip: if the served /health version VER contains SHA (`git merge-base --is-ancestor \"$SHA\" \"$VER\"`), the run ends with skip=true / reason=already_live. Nothing exempts `github.event.inputs.rollback == 'true'`. A rollback target is by definition an ancestor of the build it rolls back from, so every audited rollback now stops at the dedupe. The seal step (lines 916-951), the only place MODE=rollback exists, is never reached. The Verdict then goes green ('nothing to ship (already_live)'), DID NOT DEPLOY prints '::notice title=ALREADY LIVE::', and the ledger records 'already serving this commit'. Before #4216 the only dedupe was exact-version equality, so rollback worked. tests/the-deploy-can-always-ship.law.test.ts:262-270 pins the unconditional form. I reproduced it on the Mac with the same git state the runner has: a depth-1 checkout of rollback target 23f00c1b, then `git fetch --depth=200` of main. With VER=404948b3 (what production serves now) the exact dedupe code printed skip=true and 'Production serves 404948b3, which already contains 23f00c1b'.",
        "failure_scenario": "Production serves a bad build, 404948b3. An operator runs `gh workflow run auto-deploy-hetzner.yml -f ref_sha=<23f00c1b full sha> -f rollback=true -f rollback_reason=...`. The control step passes (CONTROL_SHA is main). The dedupe finds 23f00c1b is an ancestor of 404948b3 and skips. Tests, gate, seal(rollback) and cutover are all skipped, and the run ends GREEN in about a minute. Production stays on the bad build, and every surface says the rollback target is already live.",
        "suggested_fix": "On the dedupe step add `env: ROLLBACK_REQUESTED: ${{ github.event.inputs.rollback || 'false' }}`. Guard only the inclusion branch: `if [ \"$ROLLBACK_REQUESTED\" != \"true\" ] && [ -n \"$VER\" ] && git cat-file -e \"${VER}^{commit}\" ... && git merge-base --is-ancestor \"$SHA\" \"$VER\"; then`. Keep the exact-version check. The forward-only seal already refuses non-rollback downgrades, so the inclusion skip is only an optimisation for normal runs. Extend the law test at tests/the-deploy-can-always-ship.law.test.ts:262 to require the rollback exemption.",
        "changeset": "deploy-train",
        "verdict": {
          "real": true,
          "confidence": "high",
          "evidence": "I tried to refute this and couldn't. The defect is real, in the worktree at 404948b354 and still on current origin/main 1579ac7c14 (#4235), where the same unguarded check sits at lines 444-450.\n\n1. Nothing exempts a rollback from the dedupe. `.github/workflows/auto-deploy-hetzner.yml` lines 385-415: the dedupe step has no `if:` and no `env:`. After the exact-version check (397-402), the inclusion branch at 409-410 (`git cat-file -e \"${VER}^{commit}\" && git merge-base --is-ancestor \"$SHA\" \"$VER\"`) writes skip=true and reason=already_live. `SHA` is the dispatched ref_sha: it is set at job level and re-resolved by the target step into GITHUB_ENV.\n   - Rollback is read in only one place, the seal step (916-935, `ROLLBACK_REQUESTED` at 920, `MODE=rollback` at 931). That step is gated `if: steps.dedupe.outputs.skip != 'true'` (918), and so are the tests, gate and cutover.\n   - `git show ffd317c9b6` (#4216) is the commit that added the LIVE BY INCLUSION block.\n   - Before #4216 the only other dedupe skip was coalescing, and it was bypassed for `workflow_dispatch`, so a rollback dispatch hit only the exact-version check.\n\n2. Every rollback the seal accepts is one this check catches. `server/scripts/engine-release-seal.py`:\n   - Lines 311-316: rollback mode requires the target to be an ancestor of `highWaterSha`.\n   - Lines 460-461: a normal deploy commit sets `highWaterSha = target`.\n   - So while production serves its last normal deploy (VER is the high-water commit), any accepted rollback target is an ancestor of VER.\n\n3. The platform side behaves as claimed. `actions/checkout@v4` with a 40-hex ref does a depth-1 fetch of that SHA, and the control step then runs `git fetch --no-tags --depth=200 origin main` (line 305). I reproduced this myself in a throwaway clone under /tmp from the local repo over `file://`:\n   - depth-1 fetch and checkout of 23f00c1befb8; shallow = 23f00c1b;\n   - depth-200 fetch of origin/main (1579ac7c); shallow became 14107e8b, so 23f00c1b was unshallowed;\n   - the exact dedupe code with VER=404948b3 printed `skip=true reason=already_live :: Production serves 404948b3, which already contains 23f00c1b`.\n   - Log: `/tmp/claude-audit-skeptic.log`. The scratch clone has been deleted.\n\n4. The scenario is realistic today:\n   - Live /health returns `version: 404948b3`.\n   - `ca_engine_deploy_attempts` row 385 shows 23f00c1b shipped at 00:57 UTC, and row 386 shows 404948b3 shipped at 01:55 UTC. 23f00c1b is exactly the previous release.\n   - The #4235 commit message calls 404948b3 a bad build (it freezes tournament tables).\n   - No rollback run has been recorded, so this is established from code and the reproduction, not observed in production.\n\n5. The downstream effects are as claimed:\n   - DID NOT DEPLOY step (`if` at 1326) prints `::notice title=ALREADY LIVE::` (1396-1397).\n   - Ledger REASON is 'already serving this commit' (1453).\n   - Verdict: JOB_STATUS is success, DEDUPE_SKIP is true, so it prints 'nothing to ship (already_live)' and exits 0, green (1564-1568).\n\nLimits on the claim (they narrow it but don't refute it):\n- The skip fires only if VER and the target are both inside the depth-200 main window. Main takes 92 commits a day (measured), so the window reaches back about 1.5 days; my reproduction's boundary was 2026-09-09 17:58 UTC.\n- If /health is unreadable (VER empty), the rollback proceeds.\n- The law test at `tests/the-deploy-can-always-ship.law.test.ts:262-270` only matches the tail of the `if` line. It doesn't strictly pin the unconditional form, but no test requires a rollback exemption.\n\nAdjacent, separate issue (verified, not part of this claim): both hand-on dispatches (lines 318 and 1510) call `gh workflow run ... --ref main` with no `-f` inputs. A rollback run that is superseded, cancelled or fails before cutover is silently turned into a forward deploy of main.",
          "corrected_fix": "The proposed fix is correct. Apply it as written:\n\n1. On the dedupe step (line 386) add:\n   `env: ROLLBACK_REQUESTED: ${{ github.event.inputs.rollback || 'false' }}`\n   This is the same expression the seal step uses at line 920. It is the string 'true' only on a dispatch, and 'false' on a push.\n\n2. Guard only the inclusion branch:\n   `if [ \"$ROLLBACK_REQUESTED\" != \"true\" ] && [ -n \"$VER\" ] && git cat-file -e \"${VER}^{commit}\" 2>/dev/null && git merge-base --is-ancestor \"$SHA\" \"$VER\" 2>/dev/null; then`\n\n3. Keep the exact-version check. A rollback to the build already being served is a genuine no-op.\n\nExisting pins still pass: the regex at `tests/the-deploy-can-always-ship.law.test.ts:267-269` and the reason/notice checks at `tests/unit/deployAndPublishAreHonest.test.ts:472-482`.\n\nAdd a law assertion that the dedupe step defines ROLLBACK_REQUESTED from `github.event.inputs.rollback` and that the inclusion `if` contains `\"$ROLLBACK_REQUESTED\" != \"true\"`.\n\nNothing else in the workflow blocks an ancestor target once the dedupe lets it through: there is no other `is-ancestor` on SHA, and the seal enforces rollback authority itself (seal step at 926-932, `engine-release-seal.py` at 311-320).\n\nRecommended follow-up, separate from this fix: forward `-f ref_sha`, `-f rollback` and `-f rollback_reason` in the hand-on dispatches at lines 318 and 1510, or refuse to hand on when rollback=true, so a pending rollback can't be silently turned into a forward deploy."
        }
      },
      {
        "title": "Verdict treats a cutover refused before any mutation as 'cutover ran', so the train stops with no successor",
        "severity": "medium",
        "file": ".github/workflows/auto-deploy-hetzner.yml",
        "line": 1540,
        "description": "The #4221 guard hands on only when `steps.cutover.outcome` is empty or 'skipped'. The cutover step deliberately separates 'refused before touching anything' from 'mutation authorized': it writes `attempted=true` (line 996) only after the locked certificate re-check prints __SP_CUTOVER_MUTATION_AUTHORIZED__. The ROLLBACK step (line 1177) keys on that output. Several failures happen before the marker, leave production untouched and do not roll back: the locked re-check exits 75 (line 976), `flock -w 30` times out, localhost curl fails, or the ssh connection fails. In all of these the step outcome is 'failure' and `attempted` is unset. The Verdict reads 'failure' as 'cutover RAN', skips the whole hand-on block and exits 0. That is exactly the 'run that never touched production' #4221 says heals itself.",
        "failure_scenario": "Tables finish parking late in the :55 break. The runner gate polls every ~15.5s (see run 34562223669 at 04:55-04:57, remaining counting down with tables unparked) and first sees READY with remainingMs \u2248 181000. After the seal-prepare ssh and cutover ssh/flock (1-30s), the locked re-check sees < 180000 and exits 75 before the marker. Other triggers are a supervisor holding /var/lock/club-arena-engine-up.lock for more than 30s, or a failed ssh connect. ROLLBACK is skipped and GUARANTEE aborts the unused token. Verdict: JOB_STATUS=failure, CUTOVER_OUTCOME=failure, so no hand_on. Nothing is queued, the watchdog only reports, and the commit waits for the next engine push.",
        "suggested_fix": "Add `CUTOVER_ATTEMPTED: ${{ steps.cutover.outputs.attempted }}` to the Verdict env. Change line 1540 to `if [ -z \"$CUTOVER_OUTCOME\" ] || [ \"$CUTOVER_OUTCOME\" = \"skipped\" ] || { [ \"$CUTOVER_OUTCOME\" = \"failure\" ] && [ \"$CUTOVER_ATTEMPTED\" != \"true\" ]; }; then`. On 'failure' the step always writes `attempted` before it exits, so the output is reliable; leave 'cancelled' out, because a cancelled step may be killed before writing it. Update the pin in tests/unit/deployAndPublishAreHonest.test.ts:271-273.",
        "changeset": "deploy-train",
        "verdict": {
          "real": true,
          "confidence": "high",
          "evidence": "I tried to refute this and couldn't. I checked it against worktree 404948b354 (it contains #4221), the Actions logs and the ledger.\n\n1. The step behaves as claimed (.github/workflows/auto-deploy-hetzner.yml). The remote script runs `set -euo pipefail`, then `flock -w 30` (974), then `curl -sf` to localhost (975), then the certificate check, which exits 75 if remainingMs < 180000 or if running is not true (976). Only after that does it print the marker (977). Nothing that changes production (stopping sp-autoheal, engine-up.sh) runs before 981. `attempted=true` is written only when the marker is in the captured output (995-997). Every failing exit of the step comes after that check (998 onward). ~/hssh is a plain `exec ssh ... ConnectTimeout=20` with no retry (line ~587), so a failed connection also leaves no marker. The step has no continue-on-error, so its outcome is 'failure' and job.status is 'failure'.\n\n2. What runs next. Verify, promote, prove and seal_commit have no status function, so they skip. ROLLBACK needs `attempted == 'true'` (1177), so it skips. GUARANTEE (1264) aborts the token and runs engine-supervisor.sh, which is a no-op on the untouched sealed container. The Verdict guard (1540) accepts only an empty or 'skipped' outcome, so it prints the error and exits 0 (1563) without calling hand_on.\n\n3. I ran the Verdict block as extracted from the file, with stubbed gh and curl:\n   - failure/failure/attempted unset: no dispatch, rc 0.\n   - Same inputs with the proposed guard: \"Retrying\", then a dispatch.\n   - failure/failure/attempted=true: still no dispatch.\n\n4. Nothing else starts a successor.\n   - The only `gh workflow run` calls are at line 318 (control step, at run start) and 1510 (hand_on).\n   - The workflow has no schedule.\n   - engine-watchdog.sh only reports (lines 32-36, 330-368).\n   - None of the 136 cron.job rows mentions deploy, dispatch, engine, github or http.\n   - A stall needs no newer engine push to be pending. A pending run would ship this commit by inclusion.\n\n5. The timing is realistic.\n   - The break is 5 minutes and the threshold is 3 minutes left (MaintenanceBreak.ts:275, 319). Tables that park late keep the gate shut until they land (~1200-1206).\n   - The runner gate uses the same >=180000 threshold, so it has no margin.\n   - Polls land every 15.2-17.4s. Run 34564131192 printed remaining=181s at 05:56:58.8, with 620 tables unparked. Run 34562223669 had 523 unparked through its whole break.\n   - The seal prepare between the gate's READY and the cutover took 0.65-1.6s in five shipped runs (34519356271, 34527130413, 34541424053, 34546078412, 34549573490), plus the cutover's ssh and flock.\n   - The supervisor holds the same lock for its whole tick, including a /health probe of up to 15s (engine-supervisor.sh:49, 210-212, 499). That can widen the gap.\n   - So a READY first seen at about 180-183s is refused by exit 75 before the marker.\n\n6. It hasn't happened yet. None of the 85 \"run ended without a verified cutover\" rows in the last 30 days failed at the cutover step (I checked each run's steps), against 133 verified cutovers. It is reachable but rare.\n\n7. It contradicts the design's own reasoning. The comment at 1537-1539 says a failed cutover \"rolled production back\", but for these failures ROLLBACK is skipped. The law test at tests/engine-release-seal.law.test.ts:1373 is titled \"a rejected locked certificate cannot mark mutation or trigger a blind restart\". The mutation marker has existed since #4083 (2026-09-09), before #4221.",
          "corrected_fix": "The proposed fix is right. One correction to its reasoning: the step does not \"always write attempted\". It writes `attempted=true` only when the marker was received (lines 995-997) and writes nothing otherwise. The output can still be trusted on 'failure', because that check comes before every failing exit (line 998 onward). So compare with `!= \"true\"`, which treats unset as not attempted, and keep 'cancelled' excluded.\n\nChanges:\n1. Verdict env: add `CUTOVER_ATTEMPTED: ${{ steps.cutover.outputs.attempted }}`.\n2. Line 1540: `if [ -z \"$CUTOVER_OUTCOME\" ] || [ \"$CUTOVER_OUTCOME\" = \"skipped\" ] || { [ \"$CUTOVER_OUTCOME\" = \"failure\" ] && [ \"$CUTOVER_ATTEMPTED\" != \"true\" ]; }; then`\n3. Rewrite the comments at lines 1498 and 1528-1539 to say the boundary is \"mutation authorized\" (attempted=true), not \"the cutover step ran\". Optionally change \"Failed before any cutover\" to \"Failed before the cutover changed production\".\n4. Tests:\n   - tests/unit/deployAndPublishAreHonest.test.ts:271-273: pin the new guard.\n   - Add `expect(verdict).toMatch(/CUTOVER_ATTEMPTED: \\$\\{\\{ steps\\.cutover\\.outputs\\.attempted \\}\\}/)`.\n   - The guardAt check at line 291 and the count of 4 hand_on calls still hold.\n\nCaveats and optional refinements:\n- An exit 75 is really a gate decline, but with this fix it uses up DEPLOY_RETRY_LIMIT. After three late-park refusals on one commit the run prints \"GAVE UP ... broken, not unlucky\", which is the wrong label. A more precise fix: when the remote exits 75 with no marker, the cutover step records a no_certificate-style decline and the Verdict hands it on like the runner gate's no_certificate, with no retry cap. That needs extra `if:` conditions on the steps that follow.\n- Separately, the runner gate could require a margin, for example remainingMs >= 180000 plus the seal and cutover latency, so the locked re-check stops rejecting a certificate the gate accepted a moment earlier.\n- Known residual: if ssh drops after the remote printed the marker, `attempted` stays unset even though engine-up.sh may have run. ROLLBACK already treats that case as not attempted, and GUARANTEE's supervisor puts the sealed image back. With the fix, that run would also hand on, still capped by the retry limit."
        }
      },
      {
        "title": "Retry bound is counted per commit but the retry dispatches main tip, so any merge resets it (unbounded retry loop)",
        "severity": "medium",
        "file": ".github/workflows/auto-deploy-hetzner.yml",
        "line": 1545,
        "description": "FAILED_BEFORE counts failed runs with head_sha == this run's SHA (`gh run list --commit \"$SHA\" --status failure`). hand_on() (line 1510) does not retry that commit: it dispatches `--ref main`, whose github.sha is main's tip at dispatch time. The successor's count therefore starts from zero whenever any commit, including a client-only merge, has landed since. GAVE UP ON <sha> is reached only if main stays still for three consecutive failing runs. On this repo main moves several times an hour: engine commits alone landed at 04:26, 04:36 and 04:47 today. In practice the claimed bound 'the third failure says GAVE UP and stops' does not hold for any persistent failure that spans commits.",
        "failure_scenario": "A failure that persists across commits: a broken server test on main, a Docker build break, an expired deploy key, or less than 5G free on the host. This workflow's own history shows such failures span commits: on 2026-08-21 16:11-17:19 eight consecutive push runs on eight different SHAs failed 'Server tests' (2474d936, 9f6de161, 8109b734, 71364afb, 77980479, 1e15f563, 1cfee027, 01dfb076). Under #4221 each failing run sees FAILED_BEFORE=0 for its own SHA and dispatches the next main tip. The loop runs all day, one run every ~12-30 min. Each run repeats the 9-12 min suite, and for build failures a full docker build on the production engine host. The loop never raises GAVE UP.",
        "suggested_fix": "Bound consecutive pre-cutover failures of the train, not of one commit. For example, count leading failures among recent completed runs of the workflow: `gh run list --workflow auto-deploy-hetzner.yml --status completed --limit 20 --json conclusion --jq '[.[]|select(.conclusion!=\"cancelled\")] | (map(.conclusion==\"failure\")|index(false)) // length'`. Optionally ignore runs whose only failing step is the Verdict (gate declines). Stop at DEPLOY_RETRY_LIMIT. An alternative is to key the count on the engine tree (`git rev-parse \"$SHA:server\"`) rather than the commit. The push trigger still restarts the train on the next engine push. Update the pin in tests/unit/deployAndPublishAreHonest.test.ts:277-279.",
        "changeset": "deploy-train",
        "verdict": {
          "real": true,
          "confidence": "high",
          "evidence": "I could not refute the mechanism. It shows up in the code, in the platform's behaviour and in live runs. Two parts of the claim go too far (\"never raises GAVE UP\", \"runs all day\"), but the stated bound of 3 failures does not hold.\n\nCODE (worktree 404948b3):\n- auto-deploy-hetzner.yml:238 sets `SHA: ${{ github.event.inputs.ref_sha || github.sha }}` and :251 resolves it to HEAD, so SHA is the run's head_sha.\n- :1545-1547 computes FAILED_BEFORE with `gh run list --workflow auto-deploy-hetzner.yml --commit \"$SHA\" --status failure`.\n- :1509-1510 hand_on() runs `gh workflow run auto-deploy-hetzner.yml --ref main` and passes no ref_sha.\n- :1500 and :1533-1535 describe the bound as per commit (\"fails the same way three times ... the fix's own push starts the next run\").\n- No circuit breaker exists anywhere else in .github; engine-watchdog.sh only reports.\n\nPLATFORM, CHECKED LIVE:\n- `GH_DEBUG=api gh run list --commit <sha> --status failure` (gh 2.86) sends `.../actions/workflows/278928300/runs?status=failure&head_sha=<sha>`. The count is per head_sha.\n- hand_on starts the next run on main's tip, not on the failing run's commit:\n  - Run 34562223669 (SHA 999b5775, a no_certificate decline) called hand_on at 04:57:37. The new run, 34564131192, ran on a04495f6.\n  - That run called hand_on at 05:57:48. Its log shows `SHA: a04495f6...` and \"Handed on\". The new run, 34567969674, runs on c58dfafd.\n  - c58dfafd changes only ci.yml, docs, scripts/art and package.json. It touches no file under server/, so a client-only merge became the next run's commit.\n- The dispatch also replaces a pending push run: push run 34563496426 on a04495f6 was cancelled at 04:57:39. A cancelled run never adds a failure to the new tip's count.\n- So the next run's FAILED_BEFORE is 0 whenever main has moved. GAVE UP needs 3 failed runs on one SHA, which means main must stay still for about 2 run cycles.\n\nHOW LONG A CYCLE IS:\n- The Server tests step took 11.5, 8.4 and 11.3 minutes in runs 34562223669, 34564131192 and 34567969674.\n- The image build runs on the Hetzner host itself: `~/hssh build-engine-image.sh` at :717-737 runs `docker build` there. A failing build step is re-run on that host every cycle.\n\nHOW OFTEN MAIN MOVES:\n- origin/main has 990 first-parent commits since 2026-09-04, 297 of them in engine paths.\n- The median gap between commits is 4.3 minutes. Only 7.7% of gaps are 24 minutes or longer.\n- Engine commits landed today at 04:26:43, 04:36:42 and 04:47:05.\n\nFAILURES DO SPAN COMMITS:\n- On 2026-08-21, all 8 SHAs cited in the claim failed \"Server tests must pass before anything is deployed\".\n- 56d5a89d and c770f59d failed the same step afterwards, at 17:55 and 17:59.\n\nREPLAY (my model, not an observation): I replayed the real 09-04 to 09-10 commit timeline with a failure that never clears, starting at each of 255 engine commits.\n- With a 12-minute cycle, the train ran a median of 21 runs before it first fully stopped (p90 59, max 79), against the documented 3. Only 5% of starts stopped at exactly 3.\n- It ran 87 runs in 24 hours, against 40 for push runs alone.\n- With a 25-minute cycle: a median of 22 runs before stopping, and 47 against 31 runs per 24 hours.\n- GAVE UP does fire when main goes quiet, and every start stopped within 24 hours. So \"never\" and \"all day\" overstate it. The bound actually comes from quiet periods on main, not from DEPLOY_RETRY_LIMIT.\n\nNOT YET HAPPENED: since #4221 merged at 00:43Z, no run has failed before its cutover. The ca_engine_deploy_attempts ledger shows every completed run since was either shipped or a no_certificate decline. The loop is a latent defect; it has not been observed.\n\nWHAT IS WRONG WITH THE PROPOSED FIX: as written, its jq counts every run with conclusion failure. Break-gate declines have been red since #4208 because the Verdict step exits 1.\n- Run against live data now, the proposed jq returns 2, because runs 34564131192 and 34562223669 were both no_certificate declines.\n- So if the run in flight now hit a flaky test, it would say GAVE UP with zero retries, which defeats #4221.\n- Leaving out gate declines is therefore required, not optional. A gate decline should end the streak, because it proves tests, build and pre-flight all passed.\n- The jobs API tells the two apart: on both decline runs the Verdict step's conclusion is failure. A run that failed before its cutover leaves the Verdict step green, because that branch exits 0.\n- The server-tree alternative is weaker. It still counts declines, and it resets on every engine push (about 41 a day), so infrastructure failures such as low disk or a bad key would still loop.",
          "corrected_fix": "Count consecutive failures of the train as a whole, across commits, and count only runs that failed before their cutover. Replace the FAILED_BEFORE assignment at .github/workflows/auto-deploy-hetzner.yml:1545-1547 with:\n\n```bash\n# Consecutive pre-cutover failures of the TRAIN (any commit), newest first.\n# Cancelled runs are skipped. A green run, or a red run whose OWN Verdict step\n# failed (a gate decline / unverified cutover - it passed tests, build and\n# pre-flight), ends the streak. Any API error fails closed (TRAIN STOPPED).\nFAILED_BEFORE=0\nLIST=$(gh run list --repo \"$GITHUB_REPOSITORY\" --workflow auto-deploy-hetzner.yml \\\n         --branch main --status completed --limit 30 --json databaseId,conclusion \\\n         --jq '.[] | select(.conclusion != \"cancelled\") | \"\\(.databaseId) \\(.conclusion)\"' 2>/dev/null) || FAILED_BEFORE=\"\"\nif [ -n \"$FAILED_BEFORE\" ]; then\n  while read -r ID CONCL; do\n    [ -n \"$ID\" ] && [ \"$CONCL\" = failure ] || break\n    V=$(gh api \"repos/$GITHUB_REPOSITORY/actions/runs/$ID/jobs\" \\\n          --jq '[.jobs[].steps[] | select(.name|startswith(\"Verdict\")) | .conclusion][0] // \"\"' 2>/dev/null) \\\n      || { FAILED_BEFORE=\"\"; break; }\n    [ \"$V\" = success ] || break\n    FAILED_BEFORE=$((FAILED_BEFORE + 1))\n    [ \"$FAILED_BEFORE\" -lt \"$DEPLOY_RETRY_LIMIT\" ] || break\n  done <<< \"$LIST\"\nfi\n```\n\nKeep the existing case block:\n- an empty count stops with TRAIN STOPPED;\n- a count below DEPLOY_RETRY_LIMIT calls hand_on;\n- anything else raises GAVE UP. Reword that message from \"on this commit\" to \"in a row on the deploy train\".\n\nHow the bound then behaves:\n- Push runs still start on every engine push, but they do not retry while the streak stands.\n- The first run that gets past its pre-cutover steps resets the streak, whether it ships or is declined at the gate.\n- `--branch main` keeps stray dispatches on other refs out of the count.\n- A failed cutover that rolled back leaves the Verdict green, so it counts toward the streak. That errs on the safe side.\n\nAlso update:\n- the \"per commit\" wording at :1500 and :1533-1535;\n- the pin at tests/unit/deployAndPublishAreHonest.test.ts:277-279, and the \"per commit\" comment at 261-263.\n\nDo not use a retry counter passed as a dispatch input instead. A push run starts at 0, so each engine push that starts a run would earn fresh retries, and on this repo the train would still run almost non-stop during busy hours."
        }
      },
      {
        "title": "Cancelled branch hands on without any bound, and GitHub reports a job timeout as a cancellation, so a stalled pre-cutover step loops forever",
        "severity": "medium",
        "file": ".github/workflows/auto-deploy-hetzner.yml",
        "line": 1541,
        "description": "For JOB_STATUS=cancelled the Verdict calls `hand_on || true` with no count; only the failure branch is bounded, and `--status failure` never sees cancelled runs anyway. A job that exceeds timeout-minutes is cancelled by GitHub and handled like a user cancel. This workflow's history shows it: runs 33822776687 and 33827181901 hit the old 25-minute ceiling. The running step logged '##[error]The operation was canceled.' at exactly 25:00, the always() steps then ran, and the conclusion was 'cancelled'. The repo has zero runs with conclusion timed_out. The production ledger row for cancelled run 34543650303 reads 'cancelled before a verified cutover', which confirms `job.status` evaluates to 'cancelled' in the env of these always() steps. No step has timeout-minutes, and ~/hssh sets ConnectTimeout but no ServerAliveInterval (line 587), so a hang is only ended by the 130-minute job timeout.",
        "failure_scenario": "A commit makes the pre-cutover path hang deterministically, for example a vitest worker that never exits or a docker build/ssh session that stalls. At 130 minutes the job is cancelled. The Verdict sees JOB_STATUS=cancelled and CUTOVER_OUTCOME=skipped and hands on unconditionally. The successor stalls the same way, and the cycle repeats every 130 minutes indefinitely, holding the deploy group. engine-watchdog.sh train_check ignores cancelled runs (line 153), and its staleness path always finds a run 'in flight', so neither DEPLOY TRAIN FAILING nor TRAIN STOPPED is raised.",
        "suggested_fix": "Apply the same consecutive-run bound to the cancelled branch, counting cancelled runs that actually started a job. Also add step-level `timeout-minutes` to 'Server tests must pass\u2026' (e.g. 25), the image build (e.g. 30) and the other ssh steps, and add `-o ServerAliveInterval=15 -o ServerAliveCountMax=4` to ~/hssh. A hang then becomes a counted step failure instead of a job-timeout cancellation. Update the pin in tests/unit/deployAndPublishAreHonest.test.ts:274-276.",
        "changeset": "deploy-train",
        "verdict": {
          "real": true,
          "confidence": "high",
          "evidence": "I tried to refute this and could not. Every link in the chain checks out against the code, the runner source, the Actions API or the ledger.\n\n1. **The code.** In `.github/workflows/auto-deploy-hetzner.yml` (worktree 404948b354), lines 1540-1543 go: cutover outcome is empty or skipped, then `JOB_STATUS` is `cancelled`, then `hand_on || true`. There is no count. The only bound is the failure branch at 1545-1556 (`gh run list --commit \"$SHA\" --status failure`), which never sees cancelled runs. `timeout-minutes: 130` (line 219) is the only real timeout. The other match, at line 847, is a comment. Line 587 builds `~/hssh` with `ConnectTimeout=20` and no `ServerAliveInterval`.\n\n2. **A timeout arrives as a cancel.** I checked the runner source (actions/runner main). The runner has no job-level timer: a job timeout reaches it as the same cancel message a user cancel does (`Worker.cs:124-125`). That sets the job to Canceled, so `job.status` is `cancelled` (`StepsRunner.cs:146-155`). It only becomes Failed on a runner shutdown. A step's own `timeout-minutes` is different: it marks the step Failed with \"has timed out after N minutes\" (`StepsRunner.cs:322-329`).\n\n3. **The run history matches.**\n   - Runs 33822776687 and 33827181901 ran on commits whose workflow had `timeout-minutes: 25`. Each job was cancelled 25m13s after it started.\n   - The first was stuck in the image build at `RUN npm prune --production` from 00:48:06 to 01:06:18. The second was stuck in the gate wait.\n   - Both logged `##[error]The operation was canceled.`, then the always() steps ran and succeeded. Job and run conclusions were both `cancelled`.\n   - The API reports `total_count 0` runs with conclusion `timed_out` across the whole repo.\n   - Run 34543650303 was cancelled by hand. Its Verdict log shows `JOB_STATUS: cancelled` and `Cut over` skipped, and ledger row id 384 reads 'cancelled before a verified cutover'.\n\n4. **The hand-on works in production.** Run 34562223669's Verdict dispatched 34564131192 (`github-actions[bot]`), and that run handed on to 34567969674.\n\n5. **Nothing raises the two alarms.**\n   - `engine-watchdog.sh:153` drops cancelled runs. Line 154 maps `timed_out` to failure, a conclusion this repo never gets.\n   - `INFLIGHT_STALE_MIN=270` (line 355) always finds the current run in flight, so `TRAIN STOPPED` (line 368) never fires.\n   - No database-side watch exists: `fn_ca_engine_deploy_truth_watch` is absent and no pg_cron job mentions deploys.\n   - One correction: the staleness path does still raise `ENGINE BEHIND` and opens an issue. But that issue says the run \"waits in its break gate\", which points the reader the wrong way.\n\n6. **What the harm actually is.** The loop runs on each successor while main still carries the hang. It stops when a fix reaches main (the next hand-on picks it up) or someone disables the workflow.\n   - Production is never restarted, because the hang is before the cutover.\n   - A hung run holds the deploy group, so a fix waits up to 130 minutes.\n   - The repo is private, so each cycle bills about 130 runner minutes.\n   - The design's loud `GAVE UP` error never appears.\n   - A deterministic hang is plausible. For example, a synchronous infinite loop in a test is not stopped by vitest's `testTimeout`.\n\nDisclosure: I ran one `git fetch -q origin main` in `~/Documents/club-arena` by mistake. It only rewrote `.git/FETCH_HEAD`; the reflog shows no ref moved by it, and the working tree and branches were untouched.",
          "corrected_fix": "Keep two parts of the proposed fix and change the third.\n\n1. **Keep the step-level `timeout-minutes`.** This is the part that actually changes how a hang is classified. The runner turns a step's own timeout into a Failed step (`StepsRunner.cs:322-329`), so a hang ends red with `job.status=failure`. It then goes through the bounded branch, and `train_check` counts it.\n   - Put timeouts on the pre-cutover steps that can block: server tests (about 25), image build (about 30), and the pre/pre-flight/pull/install-supervisor/one-engine ssh steps (a few minutes each).\n   - Do not put a short one on the break-gate step. It legitimately waits up to about 60 minutes and already has its own deadline.\n   - Do not put one on the rollback step, which must never be cut off halfway through a restore.\n   - The existing tests read the first `timeout-minutes` match, which is the job-level one, so step timeouts do not break them.\n\n2. **Keep `-o ServerAliveInterval=15 -o ServerAliveCountMax=4` on `~/hssh`.** A silent remote command survives, because sshd answers the keepalives.\n\n3. **Do not copy the failure branch's bound onto cancels as-is.**\n   - The existing bound is not \"consecutive\". It counts failed runs on the exact commit (`--commit \"$SHA\"`).\n   - Every hand-on dispatches `--ref main`, so the successor's commit is main's tip. The count restarts whenever main moves. The live chain 999b5775 -> a04495f6 -> c58dfafd shows this, and main moves several times an hour here, so a per-commit bound would rarely trip across 130-minute cycles.\n   - `--status cancelled` also returns pending runs that a newer push replaced. Those have zero jobs (34562849545, 34563496426) and must be excluded.\n\n   Instead:\n   - In the Verdict, detect the ceiling directly. If `JOB_STATUS=cancelled` and `now - DEPLOY_STARTED_AT >= (130-3)*60`, treat it as a timeout: emit `::error title=TIMED OUT` and send it through the retry bound, not the unconditional hand-on.\n   - Key that bound, and the existing failure bound, on engine-relevant content rather than the exact commit. For example, count earlier failed or timed-out runs, with a started job, whose commit shows no diff from `$SHA` under `server`, `scripts/ci` and `.github/workflows/auto-deploy-hetzner.yml`.\n   - Also make `engine-watchdog.sh:153-154` count cancelled runs that ran to the ceiling as failures, because GitHub never reports `timed_out` for this workflow.\n\n4. **Update the pins.** Change `tests/unit/deployAndPublishAreHonest.test.ts:274-276` and keep the hand-on call-count pin at line 298 consistent."
        }
      }
    ],
    "low": [
      {
        "title": "A fix to the deploy control plane starts no run: the push paths leave out the workflow file and scripts/ci",
        "severity": "low",
        "file": ".github/workflows/auto-deploy-hetzner.yml",
        "line": 107,
        "description": "The only automatic starts are pushes matching server/** (minus tests and sim) and hand-ons from runs already in flight. The workflow's own CONTROL_PLANE (lines 297-300) includes .github/workflows/auto-deploy-hetzner.yml and scripts/ci, which match no push path. A run whose cutover ran and failed never hands on, by design. When such a failure comes from a control-plane defect, pushing the fix starts nothing. The cron and the watchdog dispatch that used to pick this up were both removed in #4216. The watchdog's TRAIN STOPPED text ('push the fix') is wrong for this case.",
        "failure_scenario": "A bug in scripts/ci/prove-engine-version-moved.mjs makes the PROVE step fail after a good cutover. The run rolls back and does not hand on. The fix touches only scripts/ci/** (or the workflow file), so no run starts. The engine stays behind until an unrelated server/** merge or a manual dispatch.",
        "suggested_fix": "Add `- '.github/workflows/auto-deploy-hetzner.yml'` and `- 'scripts/ci/**'` to `on.push.paths`; server/scripts is already covered by server/**. When production is current, such a run ends as already_live in about a minute."
      },
      {
        "title": "The 'MAIN MOVED, CONTROL PLANE DID NOT' branch assumes a newer run is queued; if the pending run is replaced or cancelled, newer engine commits are stranded",
        "severity": "low",
        "file": ".github/workflows/auto-deploy-hetzner.yml",
        "line": 313,
        "description": "When CONTROL_SHA is an ancestor of main and the control-plane diff is empty, the run ships its own commit and asserts 'any newer engine push has its own run queued behind this one'. It never checks this. The pending slot holds one run, and any later-created run cancels it, older SHA or not. The pending slot can lose its newer run in three ways. A hand_on dispatch whose ref resolved to main just before an engine push can be created after that push's run and replace it. The dedupe comment at lines 403-408 itself anticipates late push events. And an operator can cancel the pending run: it never executes, so 'a cancel is not a stop' does not apply.",
        "failure_scenario": "Engine pushes M1 then M2. M2's run is pending behind an in-flight run. An older-SHA run (M1) is then created and replaces it. That run starts with CONTROL_SHA=M1, REMOTE_MAIN=M2 and an identical control plane, so it ships M1 and reports the newer run as queued. Nothing ever dispatches for M2.",
        "suggested_fix": "In that branch, check whether engine paths changed: `git diff --quiet \"$CONTROL_SHA\" \"$REMOTE_MAIN\" -- server ':(exclude)server/**/*.test.ts' ':(exclude)server/sim'`. If they did, emit an output such as main_ahead=true, and have the Verdict call hand_on after a SHIPPED or already_live finish when it is set. A dispatch only replaces the pending run with an equal-or-newer main, so it is idempotent."
      }
    ]
  },
  {
    "changeset": "deadline-clock",
    "summary": "I reviewed commit 1dfbb9fdfe (origin/fix/the-deadline-clock-belongs-to-no-tournament, one commit on top of 404948b354): DeadlineScheduler.ts, HorseHandReview.ts, dataActorContext.ts and the new test.\n\n**Runtime and the core mechanism.** Production runs node:22-slim, NODE_VERSION 22.23.2, with only --dns-result-order in NODE_OPTIONS, so AsyncLocalStorage uses the async_hooks implementation. I rebuilt the ALS, AsyncResource and bind helpers from the commit on Node 22.22.2 and checked the behaviour directly:\n- The committed pattern is correct at the two sites. A tick started inside tournament A sees no authority. B's bound callback runs as B, including after an await and in timers it creates. A cash callback sees none.\n- The old code reproduces the production throw exactly.\n- A one-shot setTimeout armed inside A fires with A. That is what exposes the unfixed prune timer (finding 1).\n- One gap in the new helper: a nested synchronous call to a root-bound function inside a tournament run reads that tournament. It is not reachable from the two current call sites (finding 2).\n\n**Production evidence.** The logs had 45,588 \"cannot be rebound\" errors in 30 minutes, from DisconnectEngine, HandController, DeadlineScheduler, PreciseActionTimer and TurnFSM. 30,319 had DeadlineScheduler.tick in the stack. The rest were cut off by the 10-frame stack limit inside PreciseActionTimer and DisconnectEngine chains that the scheduler feeds; the largest groups and the samples I read never left those chains. No TOURNAMENT_MANAGER_FENCED in 3 hours.\n\n**DB actor sensitivity.** Only these production functions read the actor settings: fn_close_empty_tournament_table, fn_ca_paid_spin_launch_entitlements and fn_ca_tournament_launch_supply_version (need manager authority), fn_resolve_committed_tournament_seat_move (needs service), and fn_stage_a_bridge_legacy_capacity_receipt. The only one the engine calls that needs manager authority is fn_close_empty_tournament_table, from a bound TournamentManager method. The seat-move resolver uses raw fetch with service headers.\n\n**Did any callback rely on the authority it now loses?** No. I read the heartbeat, turn, disconnect, time bank, RIT, insurance and table-break callbacks. Authority-bearing work goes through bound engine methods or awaited promises. The unbound pieces run as service, which Stage A admits: refreshTournamentBrainContext reads, chipContinuity.sweepPresence and HandController's financial alerts. The sub-engines contain no direct DB calls.\n\n**Other process-wide timers, drains, workers and singletons.** I grepped every setInterval, setTimeout, setImmediate, queueMicrotask, new Worker, .channel and lazy singleton in server/src and traced who starts each one. The only unfixed instance is the HorseHandReview prune timer (finding 1). The rest are safe:\n- **Started at boot in root:** GameServer.start and index.ts services, maintenance break chain, lease and discovery loops, leadership, rakeSpecGuard, errorReporter, StatsHealth, DealRate, Rakeback, ScheduledTournament, TournamentRecurring, the horse fleet, lifecycle, rotator and executor, and the metrics loops.\n- **Already bound or guarded:** handProjection (AsyncResource.bind plus a throw guard), GameServer bounty and manager-wake subscriptions, realtime callbacks bound at registration (realtimeCallbackContext), TournamentEliminationScheduler (per-registration binding), and the index.ts shutdown.\n- **Harmless:** EquityWorkerPool workers respawned inside a tournament context only resolve promises. The horse decision worker is created at boot. BombRequestBus's shared channel handler only sets a flag, and no open tournament table has bomb pots (DB: 1,034 tournament tables, 0 bomb-enabled). ClusterController wakes skip tournament tables. bbjDrillRegistry is an inline single-flight read. GTO and HorseMind loaders run in the worker thread. Dynamic imports load no top-level timers.\n- **Per-table or per-manager, so bound to their own tournament:** every remaining timer in ServerTableEngine*, HandController, TournamentManagerBase and EngineTelemetry.\n\n**Tests.** The new tests would fail on the old code for the right reasons. The source-regex pins only cover the scheduler and the nets flush, not the prune timer.\n\nI did not edit, commit or push anything. All DB access was read-only SELECTs, and nothing on the engine host was changed.",
    "verified": [
      {
        "title": "The horse-review retention prune timer, in the same file, still inherits the first reviewed hand's tournament authority",
        "severity": "medium",
        "file": "server/src/services/HorseHandReview.ts",
        "line": 1117,
        "description": "The change binds netFlushTimer to the process root, but leaves the other process-wide timer in the same file unbound. recordHorseHandReviews (line 1070) arms a one-shot `setTimeout(..., 10 * 60 * 1000)` at lines 1115-1127, guarded by the module-level `pruneArmed` (line 1065). The timeout calls `supabase.rpc('sp_prune_horse_hand_reviews')`, a process-wide DELETE across horse_hand_reviews, horse_daily_nets, horse_daily_play and horse_tournament_daily. Nothing in the chain enters an authority of its own. The timer is armed in the continuation of whichever settled hand first produced a 20bb+ horse review. That path is ServerTableEngineSettlement.postHandTasks (line 1405, bound for tournament engines) -> logHandHistory (line 1969) -> `void recordHorseHandReviews(...)` (handHistory.ts:590), so the timer inherits that table's tournament ALS context. I confirmed this on Node 22.22.2 (production runs 22.23.2): a setTimeout armed inside runWithTournamentDataAuthority(A) fires with store A. The commit message says the nets flush is 'the one other process-wide timer started lazily from an engine path', and the new describe block is named 'every lazily-started process-wide timer runs as the process'. Both claims are wrong: the test only pins netFlushTimer and the scheduler.",
        "failure_scenario": "The process boots and tournament tables deal first. The commit itself says the resume lane (#4218) makes that order likely. The first flagged horse hand is at a table of tournament A, so pruneArmed is set and the timeout is created inside A's authority. Ten minutes later the prune goes out through the shared client with x-smarter-data-actor=tournament-manager plus A's id and lease generation. The pre-request hook is live in production: authenticator has pgrst.db_pre_request=smarter_private.fn_smarter_data_api_pre_request. One of two things then happens. (a) A's lease is still current: the DB admits the process-wide prune as A's manager work (app.smarter_data_actor='tournament-manager') and takes FOR SHARE on A's engine_tournament_leases row for the DELETE transaction. (b) A has finished or lost its lease in those 10 minutes, which Spins and SNGs routinely do: the prune is refused with 403 TOURNAMENT_MANAGER_FENCED. observeFenceInResponse routes the fence to A's handler and reportError('HorseHandReview.prune') fires. Because pruneArmed is never reset, the 30- and 180-day retention prune never runs again for the rest of the process lifetime. The race is live in production. In the current boot (started 01:55:26Z) the first review was a cash hand at 02:01:14; the first tournament review came at 02:04:46, and 16 tournament reviews were written by 02:19.",
        "suggested_fix": "Bind the timeout callback the same way as the nets flush: `setTimeout(bindToProcessRoot(() => { void (async () => { const { error: perr } = await supabase.rpc('sp_prune_horse_hand_reviews'); if (perr) reportError(new Error(perr.message), 'HorseHandReview.prune'); })().catch((err: unknown) => reportError(err, 'HorseHandReview.prune')); }), 10 * 60 * 1000);`. Add it to theDeadlineClockBelongsToNoTournament.test.ts. Best is a behavioural test: call recordHorseHandReviews inside runWithTournamentDataAuthority(A) with fake timers and a mocked supabase that records currentTournamentDataAuthority() when rpc('sp_prune_horse_hand_reviews') is called, then assert null. At minimum, add a source pin like /pruneArmed = true;\\s*setTimeout\\(\\s*bindToProcessRoot\\(/. Correct the commit and describe claims that only one other lazy timer exists.",
        "changeset": "deadline-clock",
        "verdict": {
          "real": true,
          "confidence": "high",
          "evidence": "I tried to refute this and could not. The defect is real, but its severity is low.\n\n**Code at 1dfbb9fdfe (unchanged on origin/main 3ff0503004, where this change-set landed as 999b57756e / #4225)**\n- `server/src/services/HorseHandReview.ts` has two timers.\n  - `netFlushTimer` (826-833) is now bound with `bindToProcessRoot`.\n  - The prune timer (1115-1127) is still a plain `setTimeout(() => {... supabase.rpc('sp_prune_horse_hand_reviews') ...}, 10*60*1000)`.\n- `pruneArmed` appears only at lines 1065, 1115 and 1116. It is never reset.\n- The call path runs in tournament A's authority:\n  - `TournamentManagerBase.createManagedTableEngine` (516) wraps every prototype-chain method of a leased tournament engine in `bindTournamentDataAuthorityMethods`. That covers `ServerTableEngineSettlement.postHandTasks` (1405).\n  - `postHandTasks` awaits `commitAuthoritativeHand()` (2115). That calls `logHandHistory` (1968).\n  - `logHandHistory` awaits `insertHandHistoryRow`, then calls `void recordHorseHandReviews` (`handHistory.ts:590`).\n  - Nothing on that path leaves the tournament context. The upsert and rollup awaits resume in the same context, so the timeout is created inside A's authority.\n- The shared client stamps authority headers per attempt with `dataActorHeaders(headers)` (`client.ts:115`), reading the ALS store. On a 403 it calls `observeFenceInResponse` (152), which notifies the handler of the current authority.\n\n**Platform check**\n- I ran a simulation of the timer chain against the real branch `dataActorContext.ts` (Node 24.15, 26.3). An unbound prune armed after two awaits inside `runWithTournamentDataAuthority(A)` sent `tournament-manager`, protocol 2, A's tournament id and A's lease generation. Wrapped with `bindToProcessRoot` it sent `service`, protocol 1, no tournament headers.\n- A plain-ALS version of the same chain gave the same store propagation on Node 20.20.2, which uses the async_hooks ALS like Node 22 (the Dockerfile is `node:22-slim`). Node 22 itself is not installed locally, so I did not run it there.\n\n**Database (read-only)**\n- The authenticator role has `pgrst.db_pre_request=smarter_private.fn_smarter_data_api_pre_request`.\n- For a `tournament-manager` POST, that hook takes a row lock on A's `engine_tournament_leases` row. The lock is **FOR KEY SHARE**, not FOR SHARE as the claim says. If the generation is not current (or the heartbeat is more than 30s old) it raises `TOURNAMENT_MANAGER_FENCED` (ERRCODE 42501, which reaches the client as a 403).\n- `sp_prune_horse_hand_reviews` is SECURITY DEFINER. It deletes from `horse_hand_reviews` (30 days) and from `horse_daily_nets`, `horse_daily_play` and `horse_tournament_daily` (180 days). None of those tables has a trigger, so a prune run under a current lease succeeds as A's manager work.\n\n**The race happens in production**\n- In the current boot (leader since 01:55:27Z) the first review was cash at 02:01:14 and the first tournament review was at 02:04:46. There were 16 tournament reviews by 02:19.\n- In the two previous boots the first review was a tournament one:\n  - The ~23:56 deploy (fdd789d337): tournament 0e9ca563 (an MTT, which ended at 01:47) at 00:01:32, with the first cash review at 00:08:49.\n  - The ~00:57 deploy (23f00c1bef): tournament 7d6f3d3b (an MTT, still RUNNING) at 01:05:41, with the first cash review at 01:19:50.\n- In those boots the prune was armed under that MTT's authority. The engine logs only go back to 05:42 because of rotation, so I could not see the outcome. I have no evidence that the fenced case (b) has happened yet.\n\n**Impact caveats**\n- The oldest `horse_hand_reviews` row is from 2026-08-26 and the daily tables start 08-26, 09-05 and 08-28. The prune therefore deletes nothing today; the 30-day part becomes active around 09-25.\n- A fenced prune is only skipped until the next restart. Deploys run roughly hourly.\n- A stand-down caused by the fence only hits a manager the database has already fenced.\n\n**Proposed fix**\n- Wrapping the prune callback in `bindToProcessRoot(...)` is correct.\n- The commit message (\"the one other process-wide timer\") and the describe block \"every lazily-started process-wide timer runs as the process\" (test lines 121-148, which pin only the nets flush and the scheduler) are inaccurate as written.\n- The suggested fake-timer behavioural test would pass on the old code. vitest 2.1.9's fake clock runs callbacks synchronously from the tick caller (`vitest/dist/chunks/vi.DgezovHB.js:2300`, `timer.func.apply(null, timer.args)`). So an `advanceTimersByTime` in the test body records null with or without the fix. See corrected_fix.",
          "corrected_fix": "1. In `server/src/services/HorseHandReview.ts:1115-1127`, bind the prune callback exactly as proposed:\n```ts\nif (!pruneArmed) {\n  pruneArmed = true;\n  setTimeout(\n    bindToProcessRoot(() => {\n      void (async () => {\n        const { error: perr } = await supabase.rpc('sp_prune_horse_hand_reviews');\n        if (perr) reportError(new Error(perr.message), 'HorseHandReview.prune');\n      })().catch((err: unknown) => reportError(err, 'HorseHandReview.prune'));\n    }),\n    10 * 60 * 1000\n  );\n}\n```\nPut any explanatory comment above `pruneArmed = true;` so a source pin still matches.\n\n2. Test (the one proposed, with fake timers, would pass without the fix). Make the behavioural test discriminating:\n   - Load HorseHandReview fresh with `vi.resetModules()` and a dynamic import, so `pruneArmed` starts false.\n   - Mock `./supabase/client.js` so that `from().upsert().select()` resolves `{data: [{horse_user_id: H}], error: null}` and `rpc(name)` records `currentTournamentDataAuthority()` when `name === 'sp_prune_horse_hand_reviews'`.\n   - Keep real timers. Spy `globalThis.setTimeout` to forward to the real `setTimeout` with a 1 ms delay (`const real = globalThis.setTimeout; vi.spyOn(globalThis, 'setTimeout').mockImplementation(((fn: () => void) => real(fn, 1)) as never)`). This creates a real Node Timeout at the call site inside A's authority.\n   - Call `recordHorseHandReviews(input with a horse net >= 20bb)` inside `runWithTournamentDataAuthority(A, ...)`, wait about 20 ms, and assert the recorded authority is null.\n   - Without the fix this test records A; with it, null.\n   - If you use fake timers instead, you must call `vi.advanceTimersByTime` inside `runWithTournamentDataAuthority(B, ...)` for the test to discriminate.\n   - The minimal fallback source pin `/pruneArmed = true;\\s*setTimeout\\(\\s*bindToProcessRoot\\(/` does match the fixed layout.\n\n3. Change the commit/PR text and the describe block so they no longer say only one other lazily-started timer exists. There are two in HorseHandReview.ts: the nets flush and the one-shot retention prune.\n\n4. Optional: state that under the current hook the lease lock is FOR KEY SHARE, not FOR SHARE."
        }
      }
    ],
    "low": [
      {
        "title": "bindToProcessRoot's 'whenever it is called' contract is false on Node 22 when the bound function is re-entered inside a tournament run",
        "severity": "low",
        "file": "server/src/services/supabase/dataActorContext.ts",
        "line": 58,
        "description": "bindToProcessRoot returns processRootScope.bind(work) over ONE shared AsyncResource. Its doc (line 54) says 'Run `work`, whenever it is called, outside every tournament authority', and the block comment invites reuse for any process-wide callback. Production runs Node 22 with the default async_hooks-based AsyncLocalStorage; NODE_OPTIONS only sets --dns-result-order and no async-context-frame flag. In that implementation, AsyncLocalStorage.run() writes the store onto the current execution resource object. Inside a root-bound callback, that object is the shared processRootScope. When such a callback enters a tournament's bound method (runWithNormalizedTournamentDataAuthority -> tournamentManagerActor.run(B)), processRootScope itself carries B for the synchronous duration. A root-bound function invoked synchronously there re-enters processRootScope and sees B, not null. I reproduced this on Node 22.22.2: inside a root-bound interval, bindTournamentDataAuthority(B)(() => bindToProcessRoot(fn)()) makes fn observe B, and a setTimeout created in fn also fires with B. The two current call sites are timer callbacks that only the timer queue invokes, so production is not affected today. The exported primitive is still wrong for the next caller.",
        "failure_scenario": "A future change applies bindToProcessRoot to something that can be invoked synchronously, for example a process-wide flush helper, an EventEmitter listener, or a per-entry DeadlineScheduler callback. Code running inside the scheduler tick calls a tournament B bound method that synchronously reaches that helper. The helper runs, and schedules timers or DB requests, under B's authority even though it is 'bound to the process root'. Its requests carry B's headers, and B's bound work can be invoked from it without error while other tournaments' bound work throws 'cannot be rebound'.",
        "suggested_fix": "Explicitly clear the store inside the wrapper, which is correct under both the async_hooks and the AsyncContextFrame implementations: `return processRootScope.bind((...args: Args) => tournamentManagerActor.run(undefined as unknown as TournamentManagerActorContext, () => work(...args)));`. On Node 22.22.2 this gave no authority in the nested case, left a nested run(C) intact, restored correctly afterwards, and gave child timers no authority. Do not use tournamentManagerActor.exit() instead: on Node 22 it leaks B back in after any nested run(); I checked that too. Add a nested-case assertion to the new test."
      }
    ]
  }
]
````

## investigations

````json
[
  {
    "key": "eliminations-and-hand-commits",
    "report": "The two loops have separate causes. Loop 1 is bad data left by a rebuy defect on Sep 8\u20139, and the engine and settlement code make it expensive. Loop 2 is a code defect that tonight's six-hour stall triggered, and the fix is merged but not deployed. The crashes at 20:13 and 20:57 left no bad rows for either loop. I checked everything below with read-only SELECTs, engine logs and code in worktree 404948b3 and on main. I changed nothing. Other people edited production data while I was measuring (07:04 and 07:23 UTC), so the \"state now\" figures are as of 07:25 UTC.\n\n## Loop 1: `unresolved_knockout_generation_chain` and `bust_blocked_player_skipped`\n\n**Root cause: stale bust records, not the crashes.**\n- From 2026-09-08 12:59:30 to 2026-09-09 07:12:26 (timestamps from `ca_ddl_events`), `process_tournament_rebuy` skipped the step that marks a player's bust record (`tournament_knockout_candidates`) as `rebought`. Migration 20260909071226 describes this defect.\n- The rebuys in that window went through and were charged, but the old bust record stayed `pending`.\n- The one-off repair on Sep 10 at 12:45 (20260910124524) only fixed players who had already busted a second time. Players who had not busted again kept a hidden stale record, which only causes trouble once they bust again.\n- When they bust again, the elimination function refuses them permanently: 20260910171911:166-175, and the live function body matches that file (md5 b4937067\u2026).\n- The stale records I found platform-wide:\n\n| Tournament | Player | Stale bust hand | Rebuy charged (1.00 each) | Busted again |\n|---|---|---|---|---|\n| 798866ae | hawk_82 | 8550341 | Sep 9 05:27:55 and 05:28:32 | Sep 10 15:02:20 |\n| 7aa16fa7 | f44d72f2 | 8260161 | Sep 8 18:05:31 and 18:05:34 | Sep 11 01:33:10 |\n| 7aa16fa7 | 3a7ad729 | 8260256 | Sep 8 18:05:52 and 18:05:55 | Sep 11 02:00:24 |\n| 7aa16fa7 | f8058099 | 8261342 | Sep 8 18:08:17 and 18:08:22 | Sep 11 01:32:29 |\n| a5aa6984 | river222 | 8569325 | Sep 9 06:28:26 | not yet, holds 2500 chips |\n| a5aa6984 | MIAJordan | 8579957 | Sep 9 06:30:08 | not yet, holds 320000 chips |\n\n- No stale record exists that was created after Sep 9 06:30. The current rebuy function marks the record `rebought` in the same transaction as the purchase and refuses unresolved chains, so the current code is not creating new ones.\n\n**Code defects that turn this into a costly loop:**\n1. **The refusal protects nothing.** A database rule requires every bust record to show chips before the hand (`stack_before > 0`), so a newer record already proves the player came back. For non-bounty events no money depends on resolving it.\n2. **The engine retries forever.** It retries every 5 seconds with no backoff (`TournamentManagerBase.ts:257`, `TournamentManagerEliminations.ts:1033-1035` and `:2298`).\n3. **The stale record corrupts bust order.** The engine orders busts by the earliest pending record (`TournamentManagerEliminations.ts:613-641`), so the blocked player is tried first on every pass.\n4. **Skipping a blocked player misranks paid places (this one moves money).** The comment at `TournamentManagerEliminations.ts:1019-1023` says a skipped player keeps their original elimination time. That is false:\n   - Both elimination functions stamp the elimination time as the moment they accept the player, not the bust time.\n   - The final payout function, `fn_settle_tournament_places`, ranks players by the order they were accepted (`elimination_sequence`, highest gets 2nd).\n   - The standings normaliser ranks by elimination time instead, so the two ranking functions disagree.\n   - Any player recorded late is ranked above players who busted after them. This has already paid out:\n\n| Settled events since Sep 10 13:00 | Players paid out of bust order | Place prizes affected |\n|---|---|---|\n| 12 | 52 | 1,003.70 |\n\n   - Example, Midweek Bounty 4366c4c0: TinyTurnKid busted Sep 10 05:39:05, was recorded at 15:45:37 and was paid 2nd (70.95). By bust order they finish about 17th.\n   - Example, Midweek Mystery 7d240805: 2nd place (132.78) went to a player who busted at 06:05:10, about eight hours before the final six.\n   - Small adjacent swaps in that list may be near-simultaneous busts on different tables; the large jumps are not.\n\n**Observed loop behaviour:**\n- 06:58:09\u201307:00:07, just after the 06:57 redeploy: 51 refusals, about one per blocked player every 6 seconds.\n- Earlier (05:51\u201306:57, the log window I could see on the old process): none.\n- After 07:00 the refusals stopped because the elimination scheduler is swamped. At 07:25 its queue was 575 deep and the oldest item had waited 1,059,993 ms (about 18 minutes).\n\n**State at 07:25 UTC:**\n- **798866ae:** repaired by someone else at 07:04:31\u201332:\n  - hawk_82's stale record was set to `rebought`.\n  - SlyAnteDoc's stray seat b39e7320 was closed. It had been moved to a new table with 0 chips 1.6 seconds after busting on Sep 9, and was refused as `knockout_generation_has_new_live_seat`.\n  - Both players were eliminated, and every position was re-ranked by bust time.\n  - The re-rank will not survive settlement. SlyAnteDoc (bust order 9th) and hawk_82 have the newest acceptance numbers (28562 and 28561), so the payout function will move SlyAnteDoc to 2nd (16.89 instead of 5.08).\n  - That would shift 35.67 of the 100.20 pool among 11 players.\n  - Only the separate terminal-settlement failure (last section) is stopping this right now. The engine tried to finish the event at 07:19:27.\n- **7aa16fa7:** someone set its three stale records to `rebought` at 07:23:25.\n  - The three players are still unrecorded, waiting for a sweep.\n  - They will be recorded 7th, 6th and 5th in bust order. I found no paid-place ranking conflicts among players already recorded there.\n- **a5aa6984:** MIAJordan's stale record was also fixed at 07:23:25.\n  - river222's record (d0ac2895, hand 8569325) is still stale. river222 holds 2500 chips with no seat.\n  - Both players still carry expired rebuy prompts from Sep 9.\n- **Money:** nothing has been paid in these three events. Escrow balances are 100.20 (798866ae), 204.40 (7aa16fa7) and 99.30 (a5aa6984).\n\n**Data repair for the one remaining row (shown, not executed):**\n```sql\nBEGIN;\nSET LOCAL lock_timeout='5s'; SET LOCAL statement_timeout='30s';\nSELECT 1 FROM public.tournaments WHERE id='a5aa6984-6c1c-4b59-aeb7-9e7878853bdd' FOR UPDATE;\nDO $fix$ DECLARE v int; BEGIN\n  IF NOT EXISTS (SELECT 1 FROM public.tournament_knockout_candidates c\n      JOIN public.tournament_players tp ON tp.tournament_id=c.tournament_id AND tp.user_id=c.eliminated_user_id\n     WHERE c.id='d0ac2895-ecb8-41dd-a89b-27ee58fa14d5' AND c.state='pending'\n       AND c.created_at >= '2026-09-08 12:59:30+00' AND c.created_at < '2026-09-09 07:12:26+00'\n       AND tp.status='playing' AND COALESCE(tp.chips,0)>0\n       AND EXISTS (SELECT 1 FROM public.chip_ledger l WHERE l.tournament_id=c.tournament_id\n                     AND l.from_entity_id=c.eliminated_user_id AND l.category IN ('rebuy','reentry')\n                     AND l.created_at > c.created_at AND l.created_at < '2026-09-09 07:12:26+00')) THEN\n    RAISE EXCEPTION 'board moved; re-measure'; END IF;\n  UPDATE public.tournament_knockout_candidates SET state='rebought', resolved_at=COALESCE(resolved_at,clock_timestamp())\n   WHERE id='d0ac2895-ecb8-41dd-a89b-27ee58fa14d5' AND state='pending';\n  GET DIAGNOSTICS v=ROW_COUNT; IF v<>1 THEN RAISE EXCEPTION 'resolved %, expected 1', v; END IF;\n  UPDATE public.tournament_players SET rebuy_prompt_until=NULL   -- optional: what the rebuy door writes\n   WHERE tournament_id='a5aa6984-6c1c-4b59-aeb7-9e7878853bdd'\n     AND user_id IN ('dca6c345-c2ab-456f-98d9-dfbca3a43f7d','4f7f8abb-ba34-464c-9ecb-0ab7a972a978')\n     AND rebuy_prompt_until < '2026-09-10';\n  IF EXISTS (SELECT 1 FROM public.tournament_knockout_candidates c WHERE c.state<>'rebought' AND EXISTS\n       (SELECT 1 FROM public.tournament_knockout_candidates n WHERE n.tournament_id=c.tournament_id\n          AND n.eliminated_user_id=c.eliminated_user_id AND n.hand_number>c.hand_number))\n     OR EXISTS (SELECT 1 FROM public.tournament_knockout_candidates c JOIN public.tournament_players tp\n          ON tp.tournament_id=c.tournament_id AND tp.user_id=c.eliminated_user_id\n         WHERE c.state='pending' AND tp.status='playing' AND COALESCE(tp.chips,0)>0) THEN\n    RAISE EXCEPTION 'an unresolved knockout generation remains'; END IF;\nEND $fix$;\nCOMMIT;\n```\nriver222 also needs a seat before a5aa6984 can finish; that is a separate problem.\n\n**Code fixes to stop the loop and pay places correctly:**\n- **Ranking, needed before 798866ae settles:** the payout function should rank by bust time, the same key the standings normaliser uses. Both elimination functions should stamp the elimination time from the bust record's time instead of the moment they accept the player. Once both are done, the skip logic no longer misranks.\n- **Elimination function (non-bounty):** resolve older pending bust records to `rebought` inside the same transaction instead of refusing.\n- **Engine:** order busts by each player's latest bust hand, and back off on permanent refusals instead of retrying every 5 seconds.\n\n## Loop 2: \"did not durably sync every final seat stack\"\n\n**Root cause: a code defect, triggered tonight by the stall.**\n- When a tournament runs past its planned blind levels, the engine invents new levels by multiplying up and does not round them (`server/src/tournament/blindEscalation.ts:244-247`). This produced blinds like 255.58/511.15.\n- Any hand that is not an all-in for whole stacks then ends with fractional stacks.\n- The hand-commit function writes stacks into `tournament_players.chips`, which is an integer column, then checks the stored value against the requested one (20260910054712:609-651). They differ, so it raises P0404 and rolls the whole hand back.\n- The engine then kills and restarts the table (`ServerTableEngineSettlement.ts:2120-2139`, `:2259-2263`, `:1289-1295`) from the state before the hand, and the next hand is refused the same way.\n\n**Evidence from the three named SNGs:**\n- They dealt nothing from Sep 10 18:00:21 to Sep 11 00:00:33 while their blind clock kept running past the 12 planned levels, reaching levels 12, 13 and 23.\n- 22 hands failed at 00:00:13\u201300:01:52: 11 on table 045ddd14 (4131e3e9), 9 on 3720f848 (27fac5e7), 2 on bc400685 (22a10610).\n- The only hands accepted at 255.58/511.15 were all-ins with whole-number pots (800, 1266, 1718).\n\n**No bad data was left.**\n- Rollbacks were complete, and all three SNGs finished and paid correctly (19.00, 1.90 and 3.80; escrow balance 0).\n- Platform-wide at 07:0x: no fractional stacks and no seat/roster chip mismatches across 1,181 live tournament seats.\n\n**Repair:**\n- Deploy main commit b93be238e4 (#4244), which rounds invented levels to whole chips.\n- It is not in the running image (c58dfafdc6, deployed 06:57:16). Fractional levels are still being created: 80 since 06:57, and 201 hands in 82 tournaments at 07:00\u201307:03. They only survive because each of those hands was a whole-stack all-in.\n- No data repair is needed.\n- A secondary oddity: after the kill and restart, those tables dealt at 50/100 even though the tournament was at level 13 or 23.\n\n## Adjacent blocker: no tournament has finished since 05:04\n\n- The last terminal settlement was at 05:04:20.887.\n- Migration 20260911050554 replaced `fn_complete_tournament_terminal` at 05:05:54. The new version:\n  - opens a seat-exit authorization for every live seat;\n  - requires all of them to be consumed when it closes.\n- The trigger that consumes them (`fn_tournament_live_seat_exit_requires_authority`, created in 20260909014545:2889-2968) does not exist in production. That migration is not in `schema_migrations`.\n- 38 of 40 finish attempts since 06:57 failed with \"tournament seat-exit authority left 1 live seat(s) unconsumed\".\n- 7aa16fa7 also cannot progress: its 4 remaining live players sit on 4 separate one-player tables."
  },
  {
    "key": "registering-walk",
    "report": "The REGISTERING walk is slow because every top-up re-reads the whole fleet before deciding it can add nobody. A fix for that part is already merged on main as #4256 (91a049ecfc, merged 07:22:29Z) but it is not deployed. Even with #4256, a pass stays at about 60\u201370 s because of a second sequential loop after the walk. Change C below fixes that; Change D is optional.\n\nProduction runs c58dfafd (container started 06:57:16Z). For these code paths it is the same as worktree 404948b3; the only difference is the embed name at TournamentRecurringService.ts:4453. Line numbers below are 404948b3 unless marked \"main\" (main = 53edbd4d10). TRS = server/src/services/TournamentRecurringService.ts, GS = server/src/GameServer.ts.\n\n**1. Why each top-up takes about 2.8 s**\n\nThe walk (GS:5046) awaits one top-up at a time for every row: past-start rows at GS:5118, ramp rows at GS:5086. A seat-first top-up that finds nobody makes 16\u201318 sequential PostgREST round trips.\n\nPer tournament (4\u20136 round trips):\n- tournament row, TRS:5176\n- `fn_tournament_primary_table`, TRS:5247\n- table seats, TRS:5267 (held-empty boards return here, TRS:5314)\n- roster, TRS:4854\n- a second read of the same seats, TRS:4878\n- profiles, TRS:4904, only when someone is unseated\n\nSame answer for every tournament in the pass (12\u201313 round trips), all via `pickFreeHorses` (TRS:5348):\n- horse load, TRS:4694: 4 paged joins at TRS:4452 (3,351 live seats, 1,000 per page) plus 1 registration page at TRS:4533\n- fleet, TRS:4737: always 2 pages, because the fleet is exactly 1,000 horses and a full first page forces an empty second one\n- club membership, TRS:4773: tournament read at TRS:4634, then clubs and `union_clubs` at TRS:4649\u20134650 (union only), then members at TRS:4668 (the union's 1,508 rows are 2 pages; Deep Stack Society's 418 are 1)\n- cash-room reserve, TRS:4818: cash tables at TRS:4334 plus one seat count at TRS:4355\n\nMTT top-ups (the ramp and past-start MTTs) go through `registerHorses` instead. Its reads are at TRS:5583, 5610, 5672 (2 pages), 5711, 5761, 5817, 5828 and 5870, plus one `fn_register_horse_for_tournament` call per horse (TRS:5950). That is about 18\u201320 round trips plus the RPCs.\n\nEvidence:\n- **Database time is small.** pg_stat_statements since 2026-09-10 02:34Z:\n  - load seat page: 107,466 calls at 28.5 ms mean\n  - registration page: 37,433 calls at 38.3 ms\n  - fleet: page 1 36,532 calls, empty page 2 36,522\n  - club members: page 1 37,006, page 2 22,438\n  - tournament read at the top of each top-up: 46,100 calls\n- **Round trips are the cost.** Each one costs about 0.1 s from the engine; the decided sweep does about 500 counts in about 50 s.\n- **Every one of those top-ups was wasted.**\n  - In the steady-state walk from 06:34:49 to 06:42:56 there were 190 \"holding N horse(s) back for the cash room; 0 of M claimable\" lines (146 from the walk, 44 from the seat-first fast lane) and zero fills.\n  - The database now shows 337 REGISTERING, 201 past start and short (199 seat-first; 53 partly filled, some 2 days old) and 14 MTTs with a ramp target.\n- **HorseFleetManager is not on this path.** TRS does not import it. The \"[HorseFleet] No available horses\" lines come from the cash seeder running dry on the same exhausted fleet.\n\n**2. How long a pass takes now (production log timestamps)**\n\nThe stuck-COMPLETING recovery of \"Breakfast Turbo\" logs once per pass, right after the walk.\n\n| Situation | Measured |\n|---|---|\n| Build 404948b3, steady state | Markers at 06:08:16, 06:16:16, 06:24:55, 06:33:34, 06:42:55: passes of 8:00, 8:39, 8:39, 9:21. Walk 8 min 07 s (06:34:49 \u2192 06:42:55), one top-up every 2.6\u20133.4 s. After the walk, about 75 s. |\n| Walk frozen (:53 to :00) | Passes 60\u201367 s apart (05:53:03 \u2192 05:59:06, and 06:58:20 \u2192 06:59:27). All of it is the decided-RUNNING sweep at GS:5703\u20135717, one count per RUNNING tournament. |\n| c58dfafd, first pass after the restart and thaw | Walk 07:00:38 \u2192 07:27:17.8, about 26 min 40 s (90 fills, 25 ramp registrations at 10\u201320 s each). Decided sweep 07:27:35 \u2192 about 07:32 (625 candidates, 263 \"is decided\"). Whole pass about 31.5 min. |\n\n**3. The fix**\n\n**(a) Top-up cost: already on main as #4256.** I built the same fix independently before finding it on main:\n- `HorseTopUpPass` reads the load, fleet, available pool, reserve and membership (per club scope) once per pass, kept at most 10 s. Failed or unknown reads are never kept, and the pass is cleared after any seat or registration.\n- Past-start top-ups run 4 at a time beside the walk, with a per-event backoff of 45 s, then 90/180/360 s, then 10 minutes.\n\nMy equivalent patch for 404948b3 is at `/tmp/claude-audit-registering-walk.patch`. It checks clean against 404948b3 and c58dfafd, type-checks, and passes the 101 test files that read these two files plus a new 7-test file. On the original code, a second `pickFreeHorses` call in the same tick repeats all 6 table reads; with the patch it reads only its tournament row. Do not apply it to main: 9 of 15 TRS hunks conflict with #4256.\n\n**(b) Still needed on main for a pass measured in seconds.**\n\n**C. The decided-RUNNING sweep, GS main:5881\u20135889 (404948b3: GS:5709\u20135717).** This is the 60\u201367 s floor; #4256's own commit message says the pass stays at about 50\u201370 s. Add imports for `fetchAllRows` (`./services/supabase/pagination.js`) and `IN_LIST_CHUNK` (`./services/supabase/chunkedIn.js`).\n\nBefore:\n```ts\nfor (const t of maybeDecided || []) {\n  const { count: playingCount, error: playingErr } = await supabase.from('tournament_players')\n    .select('*', { count: 'exact', head: true }).eq('tournament_id', t.id).eq('status', 'playing');\n  if (playingErr || playingCount === null || playingCount === undefined) continue;\n  if (playingCount > 1) continue;\n```\nAfter:\n```ts\nconst playingById = new Map<string, number>(); const playingUnknown = new Set<string>();\nconst decidedIds = (maybeDecided || []).map((t) => String(t.id));\nfor (let i = 0; i < decidedIds.length; i += IN_LIST_CHUNK) {\n  const chunk = decidedIds.slice(i, i + IN_LIST_CHUNK);\n  const page = await fetchAllRows<{ id: string; tournament_id: string }>((cursor, want) => {\n    let q = supabase.from('tournament_players').select('id, tournament_id').in('tournament_id', chunk)\n      .eq('status', 'playing').order('id', { ascending: true }).limit(want);\n    if (cursor) q = q.gt('id', cursor);\n    return q;\n  }, { label: 'GameServer.decidedSweepPlaying', maxRows: 100_000 });\n  if (!page.complete) { for (const id of chunk) playingUnknown.add(id); continue; }\n  for (const r of page.rows) { const tid = String(r.tournament_id); playingById.set(tid, (playingById.get(tid) ?? 0) + 1); }\n}\nfor (const t of maybeDecided || []) {\n  if (playingUnknown.has(String(t.id))) continue; // unreadable stays UNKNOWN, never 0\n  const playingCount = playingById.get(String(t.id)) ?? 0;\n  if (playingCount > 1) continue;\n```\nEXPLAIN ANALYZE on production for one 200-id chunk: index scan on (tournament_id, status), 513 rows, 5.4 ms.\n\n**D. Optional: decide every row first, then fill, GS main:5253\u20135291.** On main the walk still waits for a free top-up slot (`while (pastStartTopUps.size >= 4) await Promise.race(...)`) before it can decide the next row. After a thaw with about 200 due boards, every later start decision waits behind them.\n- Inside the loop, replace the launch block with `dueTopUps.push({ tournament, target, now, misses })`.\n- Move the unchanged slot wait, freeze/generation re-check, clock set and launch into a loop over `dueTopUps` placed just before the existing drain at main:5375.\n- All of `theWalkReadsTheFleetOnce.test.ts`'s patterns still hold.\n\nC and D together, on a copy of main: type-checks, and 103 test files (1,353 tests) pass, including `theWalkReadsTheFleetOnce.test.ts`. Patch: `/tmp/claude-audit-registering-walk-main.patch` (dry run applies clean on main).\n\n**4. Existing tests that pin this code**\n- `server/src/maintenance/theFreezeIsTotal.law.test.ts`: the freeze check sits before `topUpWithHorses(` in the walk loop; `topUpWithHorses` checks the freeze before its first await; every seat and registration RPC loop checks the freeze.\n- `server/src/services/pickFreeHorsesLimits.test.ts`: fleet and pool reads use `fetchAllRows`, `.order('id')`, the `.complete` checks, and the shuffle.\n- `server/src/services/HorsesStayInTheirClub.test.ts` and `aClubBoardFillsFromItsOwnMembers.test.ts`: the club-membership regexes (on main they include the pass argument).\n- `server/src/services/HorseConcurrency.test.ts`: the horse-load query.\n- `server/src/services/seatFirstFillOrder.test.ts`: home-first ordering, the single `break`, the null-load contract, and the `if (!load) return` guards.\n- `server/src/services/seatFirstSeatPrecheck.test.ts`: a runtime test asserting exactly 3 table-seat reads.\n- `server/src/services/seatFirstCountSync.test.ts`: the seat-first versus MTT branch in `topUpWithHorses`.\n- `server/src/services/anIdListHasACeiling.test.ts`: the cash-reserve chunking and its failure report.\n- `PagedReadsAreDeterministic.law.test.ts` and `PagedReadsCannotLieAboutBeingComplete.test.ts`: paging and ordering of the load reads.\n- `server/src/services/MttPrestartRamp.test.ts`: the ramp target and 45 s tick.\n- Walk structure: `server/src/engineStartBudget.test.ts` (no helper between `discoverCashTables` and `discoverTournaments`), `server/src/tournament/spinLaunchParking.test.ts`, `server/src/tournamentResumeBudget.test.ts`, `server/src/tournament/theSweepOnTheCoreIsANumber.test.ts`, `server/src/tournament/SpinStartsInOneSecondAndPlaysInFull.test.ts`, and `server/src/tournament/TournamentFixes.guard.test.ts` (the never-dealt sweep next to C).\n- On main: `server/src/services/theWalkReadsTheFleetOnce.test.ts`.\n- Nothing pins the decided sweep that C changes.\n\n**5. Risks and gaps to decide on**\n- **Decided-tournament recovery will fire more often.** With a pass of seconds, the decided sweep re-fires an elimination sweep or a resume admission for every decided tournament on every pass, with no throttle (263 in the post-restart pass). Today that cadence only happens during freezes. Add a per-tournament throttle along with C.\n- **The fast lane gets none of #4256's saving.** GS main:7590 calls `topUpWithHorses(tournamentId, seats)` without a pass, and its bursts are large (about 44 top-ups within one second at 06:42:57Z). The overlay guard, the scheduler and the opening seats in `createSpin` (TRS main:4346) also read per call. Passing a pass into the fast lane is the smallest follow-up.\n- **The pass is cleared often.** #4256 clears the pass after every seat or registration, and ramps (14 due per pass now) register almost every pass. This costs efficiency, not correctness. Ramps also still run inline at GS main:5206.\n- **One test header is out of date.** Migration 20260910173147 changed seat and registration RPCs to take the platform lane shared plus their own tournament's lane exclusively. The \"platform-wide EXCLUSIVE lock\" described in the header of `seatFirstSeatPrecheck.test.ts` no longer holds. Measured now: seat RPC mean 275 ms over 201 calls (07:18:44\u201307:23:04).\n\nFiles on your Mac:\n- /tmp/claude-audit-registering-walk.patch\n- /tmp/claude-audit-registering-walk-main.patch\n- /tmp/claude-audit-registering-walk-repo/server/src/services/aTopUpSharesTheFleetRead.test.ts"
  },
  {
    "key": "codex-authority",
    "report": "**Report: codex-authority on 1e98a98109, \"fix(deploy): serialize engine production authority\"**\n\n**Bottom line:** don't port 1e98a98109 as it stands. It is no longer on its own branch, and on current main it is semantically wrong. The problem it targets is real and still open, and main has a hook waiting for it. Port the idea through the branch's newer version (f74c7ffa28), with the changes in section 7.\n\n### 1. Branch state (the orchestrator may not know this)\n- 1e98a98109 was built on 353d17c83d (#4207) and committed 2026-09-10 19:45 UTC. It is not an ancestor of the branch HEAD any more.\n- `agent/codex-horse-phase7/fix/exclusive-deploy-authority` is now at f74c7ffa28 (\"fix(deploy): close production authority bypasses\"). That sits on 1fad69f0a1, which has the same title as 1e98a98109 and is 1,977 lines. The branch was rebased onto 718ae10877 (#4251) at 07:06 UTC today, after about 20 amend and rebase steps in the reflog.\n- It is not pushed (`ls-remote` finds nothing under `agent/codex-horse-phase7/*`), there is no PR, and the worktree is clean.\n- The host-side files are byte-identical in 1e98a98109 and f74c7ffa28: `engine-production-authority.py`, `engine-up.sh`, `install-engine-supervisor.sh` and `verify-recovery-stack.sh`. Only the workflow, tests and docs changed.\n\n### 2. What it solves and how\n- **Problem:** GitHub's `deploy-hetzner` concurrency only coordinates GitHub runs. People and agents working on production outside GitHub (direct SSH, the Stage-B stopped-engine database and engine cutover) had no way to reserve a :55 break.\n  - On 2026-09-10 the Stage-B coordinator cancelled deploy runs 34512893031 and 34515705774 to protect its 18:55 break (main `docs/changelog/2026-09-10-every-deploy-reaches-the-break.md:20-21`).\n- **Mechanism:** it is a durable lease with a TTL, not a lock around `docker start`/`stop`. That lock already exists as `engine-up.lock`.\n  - State lives in `/var/lib/club-arena/engine-production-authority.json`. Each read-modify-write takes a `flock` on a sibling `.lock` file.\n  - Each lease has an owner and a 32-hex lease id. TTL is 60\u20137200 s (`py:50`). Commands are claim, renew, assert-owner and release; exit code 75 means \"held elsewhere\".\n- **Workflow changes (1e98a98109 line numbers):**\n  - Claims the lease before tests with TTL 3600 (`:475-480`).\n  - Deletes the SSH key while tests run (`:515`) and restores it afterwards (`:614`).\n  - Renews with TTL 3600 before the break wait (`:834-840`).\n  - Inside the cutover, under `engine-up.lock`, requires at least 600 s left on the lease (`:1060`).\n  - `engine-up.sh` requires at least 300 s, but only for a pending candidate, i.e. when `ENGINE_RELEASE_TOKEN` is set (`engine-up.sh:142-157`).\n  - Releases in an `always()` step (`:1575`).\n  - If another owner holds the lease, every host step is skipped and the run records `shipped=false`.\n\n### 3. Does the problem still exist? Yes.\n- **No lease on main.** Current main is 82f13569f1; its deploy files are identical to 1efd952ecd. The only mention of the lease is the changelog's \"not yet merged\" note (`:71-77`).\n- **The hook is unused.** Main's Verdict step allows `gate_kind=lease_held_elsewhere` (`auto-deploy-hetzner.yml:1725-1728`; added by 3ed2c15941, `hand_on 0` added by fcde405674). Nothing emits it: the drain step only emits `staged_deferred` (`:977`) and `no_certificate` (`:1018`).\n- **Not on the host.** The installed control generation (`\u2026generations/34571396262-20260911T071158Z-266826`) has only `engine-release-seal.py`, `engine-supervisor.sh`, `engine-up.sh` and `verify-recovery-stack.sh`. There is no lease state file; `/var/lib/club-arena` is 700 root:root.\n- **The outside operator it was built for is still pending.**\n  - PR #4232 (OPEN, \"fix(realtime): seal Stage B authority cutover\") keeps six `.sql.pending` migrations waiting for a \"protected stopped-engine production apply\".\n  - Its runbook coordinates only through `engine-up.lock` (`flock -n`, held through stop, apply and restart) plus stopping `club-arena-supervisor.timer`, the supervisor service and `sp-autoheal`. It has no deploy coordination.\n- **The push train makes collisions the default.** Run 34571396262 was pending from 06:46:46, started at 06:58:21, ran tests 06:59:09\u201307:10:59, and has been in the drain since 07:12:07 waiting for 07:55. Run 34574492283 is pending behind it.\n- **What main already protects:** `engine-up.lock` serializes the actual mutation (cutover `flock -w 30` at `:1085`; `install-engine-supervisor.sh:43` uses `flock -w 180`).\n  - So a deploy that collides with an operator holding the lock fails before mutating. It then hands on, bounded to 2 retries in a row (`:1696`). That is safe but red and noisy, and three cancels in a row stop the train.\n  - Not protected by any lock: the host `git reset --hard` (`:788`) and the image build.\n  - The only sanctioned way to stop deploys is disabling the workflow (`:1666`).\n\n### 4. Is the design correct?\n**The lease helper works.** I ran it in a local sandbox using the `ENGINE_PRODUCTION_AUTHORITY_*` environment overrides:\n- 40 parallel claimants: exactly one got rc=0, 39 got rc=75.\n- Renew after expiry returns 75; it never brings an expired lease back.\n- Taking over an expired lease reports `replacedExpiredOwner`.\n- Release refuses another owner's active lease (75), and a same-owner release with a different lease id cannot free a newer lease.\n- Writes are atomic (mkstemp, fsync, `os.replace`, directory fsync). Ownership and mode checks fail closed.\n\n**Weak spots in the helper:**\n- Stale leases are handled by TTL only. There is no check whether the owning run is still alive and no audited force-release; the only escape is deleting the root-owned JSON by hand.\n- `fcntl.flock` `LOCK_EX` has no timeout (`py:159`) and is called while the cutover holds `engine-up.lock`. The lock order is always `engine-up.lock` then the lease lock, never the reverse, so it cannot deadlock. But a stuck helper call would stall the cutover, and supervisor ticks would keep skipping.\n\n**Supervisor timer and autoheal:**\n- By design the lease does not touch either. The supervisor runs on a 60 s timer (`install:127`) and skips its tick if it cannot get the lock (`supervisor:212`).\n- Recovery of the sealed desired image passes no token, so the lease can never block it. That is correct.\n- The flip side: the lease alone cannot protect a stopped-engine window.\n  - The supervisor starts an exited engine container (`supervisor:417-419`) and restarts autoheal for a desired release (`:278`), unless `engine-up.lock` is held and the timer is stopped.\n  - 1e98a98109's CLAUDE.md says `engine-up.lock` must never substitute for the lease. It does not say the reverse: the lease never substitutes for the lock and for stopping the timer and autoheal.\n- A real benefit: putting \"Install/refresh host supervisor\" behind the lease stops a deploy from running `systemctl enable --now club-arena-supervisor.timer` (`install:246`) on a timer an operator deliberately stopped.\n\n**Workflow design: coherent on its 55-minute base, broken on main's 130-minute ceiling.**\n- **Lease-length arithmetic:**\n  - The renew (TTL 3600) happens before a wait that on main can last up to 3690 s (`JOB_TIMEOUT_S=130*60` at `:960`).\n  - Runs that reach the gate between about :55 and :05 would fail the 600 s check at cutover.\n  - Its own law test (`tests/engine-production-authority.law.test.ts:613-631`, TTL \u2265 timeout + 300) needs at least 8100 s on main. That is above the helper's 7200 s cap, so it cannot be satisfied without redesign.\n- **SSH-key isolation is overstated.** Code from the checkout already runs before the key is written: the dedupe step runs `npm i pg` and `scripts/ci/last-shipped-engine-deploy.mjs`. A background process from `npm ci` could also read the restored key later. That is no worse than main, which sets up the key on the same runner after tests, but it is not the guarantee the commit claims.\n- **It releases the lease even when rollback or the guarantee step failed**, i.e. with the host in an unreconciled state.\n- **It starves outside actors.** The claim comes about 41 s into a run: in run 34571396262, the job started at 06:58:21 and dedupe finished at 06:59:02. When a run is already pending, the lease is free for well under a minute between train runs.\n\n### 5. How badly it conflicts with current main\nI used `git merge-tree --write-tree` with a throwaway `GIT_OBJECT_DIRECTORY`, so nothing was written to the repo. For the semantic checks I resolved conflicts conservatively (1e98a98109's `if:` lines plus main's new `timeout-minutes`; main's text everywhere else). The resolved workflow parses as valid YAML.\n\n- **Scale of drift:** since 1e98a98109's base, main changed the workflow by +546/\u2212182 across 8 deploy commits (f311f78854, 3ed2c15941, ffd317c9b6, 24f9f05c75, 1579ac7c14, fcde405674, 4b9ffd3b8b, 502cf83f8f). `server/scripts` is unchanged since the base, so the host files apply cleanly.\n- **Textual conflicts:** 9 hunks in `auto-deploy-hetzner.yml` and 1 in `tests/unit/deployAndPublishAreHonest.test.ts`. They are mostly `if:` lines against main's new per-step `timeout-minutes`, main's `ServerAlive` ssh options, and the DID NOT DEPLOY and ledger REASON text.\n- **Test failures:** main alone passes 255/255 across 13 deploy test files. Adding the port's own law test gives 14 files with 5 failures, in 2 files:\n  - `engine-production-authority` law, \"uses a bounded lease that outlives the workflow ceiling\": expected 3600 \u2265 8100.\n  - \"it decides from the SAME shipped expression the ledger records\": main's Verdict `SHIPPED` expression lacks the lease terms.\n  - \"a hang fails its step in minutes\": the relocated \"Setup SSH key\" step lacks `ServerAlive`, so the claim's ssh can hang until the job timeout, which GitHub reports as a cancel.\n  - \"reports the gate that actually held\" and \"every engine push starts its own run\u2026\": text pins on main's current shapes.\n- **Behaviour:** I ran main's Verdict script with the environment a 1e98a98109 deferral produces (`JOB_STATUS=success`, `SHIPPED=false`, `DRAIN_SKIP` empty).\n  - Result: `::error title=DID NOT SHIP::the cutover did not verify`, exit 1, and no dispatch, whether `MAIN_AHEAD` was true or false.\n  - So the run goes red and the train stops. That is the opposite of the commit's intended \"green, shipped=false\" and of main's hand-on contract for `lease_held_elsewhere`.\n  - Wiring it naively into main's existing `lease_held_elsewhere` branch (`hand_on 0`, no wait) would instead re-dispatch roughly every 1\u20132 minutes for as long as someone else holds the lease. That rate is my estimate from the 41 s timing above.\n\n### 6. The newer version, f74c7ffa28\nVersus 718ae10877 it is +2,291/\u2212310 across 15 files.\n\n**What it fixes:**\n- Tests move to a separate `validate` job with no secrets (`:163`), and the claim happens after validation (`:662`).\n- The renew uses TTL 7200 (`:711`).\n- Immediately before sealing it renews and requires at least 1800 s (`:1102-1125`), and checks again under `engine-up.lock` at cutover (`:1197`).\n- The lease is kept if reconciliation is unresolved (`:1709`).\n- The Verdict waits until the observed expiry and hands on once (`:1962`); a lease deferral is classed green.\n\n**Merge with current main:** 3 small hunks.\n- The Verdict env block: main's new `MAIN_AHEAD` against the branch's `SHIPPED`.\n- One assertion block in `deployAndPublishAreHonest`.\n- A formatting-only conflict in the release-seal law test from #4242.\n- Resolved (keep `MAIN_AHEAD` plus the branch's `SHIPPED`; take main's tests): 14 deploy test files, 276 of 277 pass. The one failure is #4262's pin of exactly 4 `hand_on` call sites, 3 of them with 0. The branch has 5, because it keeps both its new lease branch and the now-dead legacy `DRAIN_SKIP`/`lease_held_elsewhere` branch (`:2067`).\n\n**Issues still open:**\n- **(a) A validation failure silently stops the train.** The deploy job has `needs: validate` and no job-level `if` (`:205-207`). A failed, cancelled or timed-out validation skips the whole deploy job: no Verdict, no hand-on, no `ca_engine_deploy_attempts` row. Main hands on after any pre-cutover failure, including a flaky test (#4221; `:1696`), and always records the ledger (`:1543`).\n- **(b) Validation always runs.** Main skips tests when dedupe finds nothing to ship (`:563`). Now every already-live, superseded or hand-on run holds the queue for about 12 minutes first, and #4262's re-dispatch of a dropped rollback waits one validation longer.\n- **(c) The Verdict wait is blind.** SSH is already gone, so it cannot see an early release; it sleeps up to 125 minutes after the job started.\n- **(d) No audited force-release** for a stale or deliberately retained lease, which can last up to 7200 s.\n- **(e) Two deferral paths**, one of them dead.\n- **(f) #4232's runbook does not claim the lease**, so the lease does not yet protect against the one outside operator it was built for.\n\n### 7. Recommendation\nDrop 1e98a98109 itself; don't cherry-pick or rebase it. Port through f74c7ffa28 with these changes:\n1. Keep one deferral path: delete the legacy `lease_held_elsewhere` Verdict branch, or have the lease emit that gate kind and use the existing branch. This also satisfies #4262's 4-call-site pin.\n2. Keep #4221's behaviour and the ledger row when validation fails or is cancelled. Either make the deploy job `if: always()` and gate on `needs.validate.result`, or add a small `always()` reporter job that writes the row and hands on with retries+1.\n3. Gate `validate` on a cheap already-live/superseded pre-check, or document the roughly 12-minute cost per run that has nothing to ship.\n4. Poll the read-only `status` during the Verdict wait instead of sleeping blind.\n5. Add an audited `release --force --reason` for a dead holder, and document it.\n6. Document that the lease never replaces `engine-up.lock` plus stopping `club-arena-supervisor.timer` and `sp-autoheal` during a stopped-engine window. Land the #4232 runbook change (claim before scaling to zero, release after the proven restart) at the same time.\n7. Optionally, use a bounded `flock` (non-blocking with retry) in the helper.\n\nConsider splitting it in two: first the helper, the host scripts and claim/renew/assert/release inside the existing single job; then the `validate`-job hardening separately. Main's deploy workflow has taken 8 commits in about 30 hours, so a smaller first cut is lower risk.\n\nIf #4232 is abandoned and no outside production operations are planned, dropping the whole thing is defensible: `engine-up.lock` already keeps the mutation safe and disabling the workflow is the sanctioned stop. In that case also delete the dead `lease_held_elsewhere` hook from main's Verdict.\n\n### 8. What I touched\n- **Repo:** no writes. Merges were computed with a temporary object directory, and test runs used `/tmp` exports; both are deleted.\n- **One side effect:** Vite wrote and then removed a temporary config file in `~/Documents/club-arena/node_modules/.vite-temp`. That directory already existed and is empty again.\n- **Logs kept on the host:** `/tmp/claude-audit-codex-authority-*.log`.\n- **Engine host:** read-only ssh only (`stat`, `ls`, `systemctl is-active`, `docker ps`).\n- **Database:** one read-only SELECT on `ca_engine_deploy_attempts`.\n- **Moving target:** origin/main moved from 4b9ffd3b8b to 82f13569f1 while I worked. All conclusions above were re-checked against 1efd952ecd, whose deploy files are identical to 82f13569f1."
  }
]
````
