# Phase 13: joint ranges and board distributions

Implementation is in progress. This document is not a production closeout or strength claim. Phase12's protected merge a604063260e2388cd505657a4910828198973b3a is incorporated. Earlier engine/natural gates remain open; the user authorizes productive next-phase implementation during those operations.

The physical layout supports all nine variants, one to three independent boards and explicit shared-prefix runout layouts. Betting decisions use independent bomb boards; shared-prefix all-in runouts have no further betting action. Actual deck capacity determines the board/seat ceiling. Whole-chip tournaments and Diamond cash settlement are distinct from cent-unit chip cash. The controller supplies settlement units and the public dealt-seat census before stripping private cards. Folding or disconnecting never returns those dealt cards to the deck.

The versioned joint sampler uses a single physical deck for every opponent and board in each outcome. Folded deals consume their original card count. Hero's known Pineapple discard is removed; opponent discards remain unknown occupied cards. One public-line-conditioned heuristic prior belongs to each opponent, with explicit uncalibrated confidence and a bounded uniform escape. Unacted bomb ranges begin uniform. Conditional seat order is randomized, while all score arrays retain stable public identity order. Response strength sees current boards only. The local RNG does not advance the baseline strategy stream.

The joint return calculation constructs eligible pots after unmatched live refunds, preserves dead/individual-ante rights, divides each pot in actual board/high-low/button order and produces conserved resulting stacks. It retains per-pot returns, board means, full covariance, the joint return distribution, scoop/quarter outcomes and sampling uncertainty. Gross settlement evidence does not yet include cash rake or authorize any action. Identical board averages can produce different joint variance; independent fixtures verify that distinction and a locked board.

Independent evidence includes 324 actual-controller terminal settlement cases across nine variants and both chip units, plus 432 joint-sample award/refund comparisons. Sampling covers every street, one/two/three boards and actual physical seat ceilings. The current focused integration batch passes 242 tests. Initial sampler partial-board scoring failures and the dealt-census failure were retained, corrected and rechecked. No full final suite, frozen played-hand benchmark, promotion, push or publication of Phase13 is claimed.

Remaining first-round work: the complete launch/configuration matrix; bounded action-specific candidate/response calculation with cash rake; actual players-behind and cover effects; existing Phase7 final tournament utility ownership with joint outcome and integer settlement inputs; HorseLogic/worker/executor shadow receipts and rejection cleanup; frozen position-balanced controller benchmarks under load; final performance, protected checks/publication and natural per-path reconciliation. This remains the common Phase6/7-depth acceptance standard.
