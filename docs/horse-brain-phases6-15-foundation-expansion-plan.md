# Horse Brain Phases 6–15: foundation expansion plan

> **Current status — September 15, 2026 (source-only).** The reviewed blueprint requirements below remain in force. The owned Club Arena worktree contains 52 maintained runtime source/test/helper edits over `a8ce6b5697ba17cf92d81b8559e74c1d22f9f595`, with 1,547 focused cases prepared; no new tests have been executed and no installation or publication has occurred for these changes. Older counts and status statements below (including 651-case composition snapshots) are historical observations, not current acceptance claims. See [current Horse Brain acceptance record](/Users/smarter.poker/Documents/Codex/2026-09-11/ple/outputs/horse-brain-current-acceptance.md) for current evidence and remaining gates.

September 14, 2026. Apply the Phase 1–5 foundation blueprint **and** the Phase 6/7 depth standard. This is an implementation plan, not approval to activate an unqualified strategy. No phase is complete merely because its main function exists or its tests pass.

References: `horse-brain-first-five-gold-standard.md`, `horse-brain-round1-playbook.md`, the current acceptance record and each phase's existing implementation/evidence packet. Reuse the current code and evidence; do not rebuild already implemented components or reset another owner's work.

## Work order and shared prerequisites

1. **Foundation repairs and recertification.** Integrate the tested shared Horse candidate: Phase 1 trust-object/null-action repairs; Phase 2 malformed-policy/completeness, canonical physical-card identity, detached normalized records and full-request immutable cache; Phase 4 distinct checkpoint identity. Retain the committed Club validator and V31 immutable-store laws. Bind new pipeline bytes to a reviewed bundle and migrate any existing local checkpoint only after validating its full identity. Re-run cross-runtime malformed JSON, card-casing/duplicate-card and semantic mutation tests; exercise actual cache hits, independent key changes, expiry and caller mutation without bypassing the cache through dependency injection. Compare portable fixtures and accepted/rejected outcomes. Existing positive fixtures must keep the same policy bytes and decisions.
2. **Capability/evidence registry.** Extend the existing registry with phase, precise domain, policy/data versions, required fields, real entry point, execution owner, fallback, telemetry/ledger reader and promotion state. Distinguish live strategy, shadow evaluation, diagnostic evidence, offline tool, retired code and unavailable external input. Assert this registry against actual production launch configuration and executable callers.
3. **Common receipt contract.** Build on the existing Phase 15 witness/journal rather than another telemetry table. Bind full input key, sampling key, policy/data/read-frame identity, baseline distribution, phase changes, budget/effective work, intended action and authoritative outcome. Missing fields must be explicit, never reconstructed from present-day reads.
4. **Sequential domain audits.** Work Phase 6 through Phase 15 in the order below, one acceptance ledger at a time. Independent local work may continue while native delivery is pending. A pending delivery does not waive correctness, domain or natural-use gates.
5. **Final integrated qualification.** Freeze exact code/data/benchmark identities, run the required integrated checks once after final relevant changes, then publish through the qualified native path and observe natural production. No GitHub operation or unrelated deployment-infrastructure work is authorized by this plan.

The expanded foundation audit also requires restoring the installed Phase 3 event recorder's checksum and original-action metadata enforcement, sealing the range bytes actually read at solve time, and retaining the Club canonical-census position and truthful chart-benchmark repairs. A benchmark action outside the reference vocabulary must remain explicitly unavailable until a qualified receipt schema can represent it. Healthy rows and matching schema files do not substitute for installed-function or actual-consumer tests.

Phase 4 corpus construction can proceed independently under its existing source/input approval and licensed-host requirements. Its absence blocks a certified-solver claim; it does not excuse omission of explicitly heuristic core calculations in other phases.

## Phase 6: tournament preflop and complete tournament context

**Existing base:** cached canonical tournament context, format/objective metadata, table-local stacks/positions/blinds, M/effective depth, preflop atlas and node routing, baseline/fallback/source counters. Recent repairs distinguish away all-ins from voluntary responders and classify public aggression using actual action history.

**Actual availability at a8ce:** Cached canonical tournament context plus the public dealt census → `HorseLogic` reference owner → `tournamentPreflopPolicy` shifts/M → `HorsePreflop` → Phase7/legalization/controller acceptance. This reference path can affect decisions; chart/variant early returns can bypass atlas shifts. Generic witness evidence exists, while exact coordinate/interpolation and bypass attribution is prepared in runtime composition R1, not in the a8ce baseline.

**Next concrete first-round slice:** Qualify the composed Phase6 receipt across actual worker and accepted-hand paths, including absent capture, legacy data and chart bypass. Retaining a coordinate establishes lookup/forwarding, not causal action change or solver optimality. Produced covering-opponent M fields have no direct strategy reader and must remain identified as diagnostic.

**Build/audit next:**

- Reconcile every MTT/SNG/spin/HU/satellite/PKO/mystery format supported by the actual launch owner. Define unknown/missing/stale payout, bounty, entrant and blind-schedule states explicitly.
- Bind the context cache and atlas to immutable source versions and original decision-time metadata. Add failed/partial refresh, restart, stale schedule and changed-context tests analogous to Phase 2's atomic loader contract.
- Audit all preflop action nodes, actual actor/position pairs, open/limp/isolation/defense/overcall/squeeze/reraise/reshove/call-off and depth transitions. Preserve dealt-but-away forced contributions and showdown rights.
- Record atlas/chart/heuristic source and complete distribution, not only the branch label. Include rejected/retained decisions after Phase 7 and final legality.

**Acceptance:** contrasting real-worker fixtures for every critical format/node; exhaustive declared preflop coordinates plus boundary cases; truthful chart versus heuristic labeling; no loss of context between scheduler and worker; published natural receipts tying the used pack/context to accepted actions. Missing certified chart objectives remain disclosed.

## Phase 7: action-specific tournament utility

**Existing base:** a shared owner for payout/bounty/survival effects using candidate action outcomes; actual pot eligibility and opponent population; execution receipt accounting. Variant policies must feed this owner.

**Actual availability at a8ce:** Canonical payout/bounty/stack/pot inputs → `evaluateTournamentUtilityDetailed` → candidate-specific utility → retained legal action → controller/witness. This owner can affect current decisions; shadow variant comparisons are separate. The repaired responder contestable-pot and clockwise-order rules are part of the comparison floor.

**Next concrete first-round slice:** Complete exact-source economic/uncertainty and accepted-action evidence across the supported variant/format matrix. Preserve missing/approximate context refusals; neither a selected utility nor a positive aggregate proves GTO optimality.

**Build/audit next:**

- Version the utility model and bind exact payout/bounty/stack vectors, side pots, returned excess, blind/ante/rake conventions and error estimates to each decision.
- Independently construct candidate stack transitions and settlement; test fold/call/short-call/bet/full-raise/short-all-in and covered/covering relations. Include away all-ins, unequal side-pot eligibility, tied outcomes and bounty changes across hands.
- Preserve candidate distributions and measured versus estimated utility; report unavailable/approximate ICM rather than converting net-chip wins to prize EV.
- Assert that later persona/exploit/safety layers retain utility ownership and reconcile intended with coerced/fallback/accepted execution.

**Acceptance:** independent economics fixtures, same-input paired utility contrasts, real cash/tournament variant consumers, bounded work under load, complete ledger reconciliation and source-qualified natural use. Existing Phase 7 tests remain the minimum; aggregate favorable results cannot hide a critical wrong-price bucket.

## Phase 8: funded future-game continuation

**Existing base:** bounded future-hand transitions, funded chip movement, collision/dealt-seat handling and a continuation signal that changes Phase 7 candidate utility. The unchanged 4 ms work, 5 ms wall and 2 ms overhead limits and rejected promotion gates remain open.

**Actual availability at a8ce:** Phase7 input and continuation runner → funded future-hand/ICM evaluation → `tournamentPostflop` ledger → graph/witness/private capture and daily counters. The current worker permits shadow computation but rejects candidate activation. `completed` means useful computation survived final gates; baseline acceptance is not acceptance of that proposal.

**Next concrete first-round slice:** Implement the separately qualified promotion/withdrawal consumer without relaxing current gates. Qualify unchanged-budget useful completion and the zero-completion daily-reader candidate (24 planned cases, unexecuted); preserve unknown deployment/source attribution and the original positive-ratio behavior. Historical 14/29 cold and 25/29 warm completion belongs only to 27c82c4f.

**Build/audit next:**

- Freeze rollout depth, branch/sample budget, future blind timing, payout/bounty model, seed and effective work in the receipt. Snapshot the exact next-level timing instead of consulting a later wall clock during replay.
- Cover retained stacks, blind/ante rotation, all-in versus voluntary response, eliminated/away seats, current/next-hand boundaries, short raises and each relevant tournament objective.
- Prove every simulated transfer is funded and conserved and that inaccessible side pots do not improve the hero's forecast.
- Profile the current implementation against the unchanged strict budget. Optimize reusable intermediate results and allocation outside the action clock without silently shrinking effective work. Retain explicit deadline/unavailable outcomes.
- Run identical frozen baseline/candidate populations with all dealer-relative positions and report each predefined critical bucket and uncertainty, including the previously rejected runs.

**Acceptance:** funded transitions independently verified; result changes the actual Phase 7 calculation; required completion share within the original budget; reproducible results under load; qualified shadow publication and natural receipts. Promotion requires its original positive evidence; renaming a rejected candidate is forbidden.

## Phase 9: independent variant rules and exact-card facts

**Existing base:** independent Omaha reference, exact two-hole/three-board use, high/low separation, deck/blocker/nut/counterfeit facts, pot layers/refunds/odd-chip and physical-deck tests.

**Actual availability at a8ce:** Independent Omaha oracle → offline rule/equity/settlement comparisons; exact card facts → Phase10–12 shadow policy calculations. Actual settlement remains `HandController`/runout ownership. The offline oracle does not run on the live action clock or directly select Horse wagers.

**Next concrete first-round slice:** Keep the committed 80-case RIT proof separate from Phase9 full-seat-offer R2: three seven-seat PLO5 actual-betting/consent/runout cases are prepared, zero executed. R2 fixes only fixture configuration, not a demonstrated product defect. Qualify remaining enabled-domain intersections before broadening the rules claim.

**Build/audit next:**

- Reconcile the independent oracle's domain with every enabled variant/configuration, street, board count and seat ceiling; expose uncovered intersections.
- Keep rule facts separate from equity and strategy. A wrap count, percentile or category-best hand is not automatically nut equity or an unbeatable hand.
- Independently derive commitments, eligible pots and payouts from raw hand state instead of passing production-derived layers into the oracle.
- Cover impossible-card rejection, rank/suit ties, blockers that remove ties, low qualification/counterfeiting, quartered/sixthed pots, multiple board allocations, uncalled refunds and both integer/fractional chip rules.
- Bind oracle version and input seed/deck to downstream policy certificates; test malformed inputs and caller-owned object mutation.

**Acceptance:** independent differential cases and named laws per supported coordinate; every Phase 10–13 policy consumes the correct rules/facts; zero invented physical cards or money; current full-domain manifest. Passing rules tests certifies rules, not strategic strength.

## Phase 10: PLO4 policy

**Existing base:** versioned PLO4 heuristic/shadow pack, dedicated preflop kernel and postflop sampler, public-line-conditioned ranges, exact-card facts and Phase 7 tournament utility.

**Actual availability at a8ce:** Registry-selected PLO4 public state/ranges/facts → dedicated shadow program → separate Phase7 shadow utility when applicable → retained proposal/ownership ledger and final baseline witness. Current live candidate controls are forbidden; offline league candidate mode is a distinct consumer.

**Next concrete first-round slice:** Qualify every declared critical domain and actual retained proposal; implement approved promotion/withdrawal integration before claiming this proposal can control live wagers. Detached asynchronous inputs, canonical dealt census, coalesced physical range holdings and honest benchmark controls remain baseline repairs, not strength promotion.

**Build/audit next:**

- Complete the matrix across actual seat limits, dealer-relative positions, aggressor/opponent pairs, supported depth/SPR, every street and action family, cash/tournament, rake, ante, straddle and cap boundaries.
- Verify the range reader is conditioned on the correct opponent's original public actions and freshness/confidence, rather than a different seat or later observation. Record that read-frame identity.
- Preserve phase-specific intermediate hand facts, candidate action distributions, sample/effective work and utility. Distinguish normal supported evaluation from OOD/fallback and diagnostic shadow results.
- Certify joint physical-deck exclusion, exact card use, domination/blocker/counterfeit effects and legal pot-limit sizes through the real worker and executor.
- Extend frozen paired benchmarks to missing intersections; measure actual completion and latency under load, including all position rotations and negative buckets.

**Acceptance:** real consumer and observable calculation in every ordinary supported core case, independent rule/economic checks, complete receipts, reproducible after-rake comparisons, qualified deployment and natural cash/tournament usage. Keep unpromoted strength separate.

## Phase 11: separate PLO5, PLO6 and PLO8 programs

**Existing base:** distinct `plo5-high-round1-v1`, `plo6-high-round1-v1` and `plo8-split-round1-v1` packs, separate samplers/shape calibration and per-variant benchmarks. PLO8 has high/low utility rather than a relabeled high-only program.

**Actual availability at a8ce:** Distinct PLO5, PLO6 and PLO8 public-range/card/utility programs → registry shadow owner → Phase7 shadow comparison → per-variant ledger/retained baseline witness. None of these new proposals is enabled by the current live worker. PLO8 split utility and each variant’s physical-card domain remain separate.

**Next concrete first-round slice:** Close per-pack sparse configurations, performance and natural shadow evidence, then the shared qualified promotion/withdrawal path. A successful PLO4 or PLO5 comparison cannot certify another pack, actual source seat ceiling or PLO8 high/low economics.

**Build/audit next:**

- Repeat Phase 10's complete domain/read/receipt/worker gates for each pack independently. Reconcile current cash/tournament seat ceilings with the physical deck owner; do not substitute historic document limits for current launch validation.
- For PLO5/PLO6, independently verify hole-card count, connectivity, suits, nut/redraw and domination logic; no borrowed PLO4/Hold'em percentiles presented as calibrated equity.
- For PLO8, retain high-only/low-only, backup low, counterfeit transitions, scoop/freeroll, quartering/sixthed outcomes and differing high/low side-pot eligibility.
- Close sparse postflop domain intersections beyond the existing pairwise overlays. Distinguish pot-limit PLO8 from fixed-limit FLO8 ownership in Phase 12.
- Use three separately pinned pack/corpus/effective-work identities and benchmark reports. A pass for one variant cannot certify the others.

**Acceptance:** per-variant critical-case and dimensional ledger, independent split-pot proof, real Phase 7 integration, performance completion share and naturally observed published decisions for each variant. Empty or shadow-only paths remain explicitly classified.

## Phase 12: Short Deck, Pineapple and other enabled variants

**Existing base:** variant registry plus Short Deck, Pineapple, fixed-limit Hold'em and fixed-limit Omaha8 rules/policy paths; Pineapple discard and post-discard identity; per-variant paired controller benchmarks.

**Actual availability at a8ce:** Short Deck/Pineapple/FLH/FLO8 betting programs → registered shadow proposal → common legality/Phase7 and retained baseline witness. Separately, actual Pineapple discard uses its worker request and `performDiscard` acceptance/private receipt; that live discard behavior is not promotion of shadow betting.

**Next concrete first-round slice:** Finish per-variant accepted-worker/discard and fixed-limit coverage. Preserve the canonical completion/cap repair through final execution and distinguish missing discard evidence, forced actions and actual selected removals. Apply the shared promotion/retirement prerequisite only to betting proposals that remain shadow.

**Build/audit next:**

- Build the certification matrix from production registry/rules, including exact deck/ranking/straight conventions, board/card use, betting increments, raise caps and reopen rules.
- Exercise every Pineapple discard choice/timing and restart/idempotence transition. Bind retained/discarded hero cards, public discard proof and known dead cards; never expose opponents' private discarded cards.
- Prove fixed-limit policy sizes remain fixed-limit through the common legalizer/executor; cap, short all-in and tournament utility cases cannot be borrowed from pot-limit.
- Audit runtime variants not covered by these named packs and assign an explicit owner or unavailable boundary; unknown variants must not silently receive Hold'em strategy.
- Complete sparse depth/seat/rake/ante/straddle/board intersections with each variant's own reference and public-line-conditioned sampler.

**Acceptance:** production/certification registry equality, independent physical/ranking/settlement laws, actual discard-to-next-decision worker chain, per-variant receipts/performance/benchmarks and natural published behavior. Preserve the historical post-discard flop regression permanently.

## Phase 13: universal multiway, multiboard and bomb pots

**Existing base:** joint public-line-conditioned sampling, one physical deck, dealt census, multiple boards, individual pot eligibility and opponent-specific response pricing.

**Actual availability at a8ce:** Canonical public census/boards/eligible pots → joint one-deck ranges and responder-specific utility → shadow joint policy, including Phase7 when supported → graph/ledger/retained baseline witness. Candidate mode is offline only; invocation and `fired` do not mean that the proposal was selected.

**Next concrete first-round slice:** Qualify missing critical multiway/bomb/shared-prefix configurations, economic allocations and bounded work, then the shared promotion/withdrawal consumer. Retain explicit marginal-versus-joint approximations; do not certify board covariance or all domains from ordinary pairwise matrices.

**Build/audit next:**

- Complete single/double/triple-board and ordinary/bomb/runout ownership across actual enabled variants, seats and chip rules. Distinguish independent betting boards from shared-prefix all-in runouts.
- Jointly sample legal opponent ranges without duplicate cards, retaining unknown folded/dealt cards and committed away-all-in claims. A seat unable to respond cannot supply fold equity.
- Price each responder with that responder's eligible pots and incremental commitment. Include uncalled excess, ties and board/high-low splits in candidate stack transitions.
- Combine all legal response branches with bounded work and explicit approximation/error. Do not claim legacy aggregate V38 as the new joint model.
- Feed tournament outcomes to Phase 7 and produce complete per-pot/per-board evidence without leaking hidden hands in public telemetry.

**Acceptance:** independently constructed physical and economic cases, critical-domain intersections beyond ordinary pairwise matrices, deterministic paired benchmark and load behavior, final-executor receipts and natural published multiway/board usage.

## Phase 14: evidence-backed daily learning

**Existing base:** observation/discovery acquisition, journaled scoped model consumer, tag registry, diagnostic tuner proposals and historically observed committed-over-10-BB eligibility capture; installed state was not rechecked in this source refresh. Observational signals currently have no causal permission to change live strategy.

**Actual availability at a8ce:** Accepted observations → scoped reads/model consumer and diagnostic tags; private journal plus separately qualified accepted-source export/reference → corrective core → bounded inactive candidate. Eighteen registered tags have audit-only readers; five diagnostic tags do not automatically update policy. This is not a complete daily causal learning loop.

**Next concrete first-round slice:** Finish the Accounting/F06-owned first-acceptance roster provenance/return and private transport before qualifying the new consumer. Compose the known-empty/unknown/contradictory census and corrective reader, then complete source windows, >10BB gross-commitment coverage, independent references, holdout and approved activation/rollback. No retrospective current-profile inference or unsigned export grants authority.

**Build/audit next:**

- Prove every producer/tag has an actual consumer. Mark diagnostic-only and SQL-only tags as such; remove false claims of live reads. Record population coverage, original source windows and missing acquisition work.
- Complete authoritative source windows with stable player/hand/action identities, variant/stakes/seat/position/node/tournament partitions, deduplication and restart-safe watermarks. No future actions or private opponent cards may enter a decision-time read.
- Fit models with sample thresholds, decay, shrinkage and uncertainty; preserve sparse/stale/conflicting evidence and bound update magnitude. A fitted tag is not a causal estimate.
- Produce node-specific candidate changes using available alternative action/EV evidence; freeze training versus holdout populations, reject leakage and run shadow/paired evaluation before activation.
- Implement the daily **gross committed amount >10 BB** review population for every supported cash/tournament/spin/HU/variant, regardless of win/loss and including returned uncalled commitments. Reconcile queue counts and every disposition: pending, reviewed, insufficient evidence, reference unavailable, actionable finding, candidate rejected/promoted/reverted.
- A reviewer must join original decision/read/policy/accepted-action evidence and a qualified reference. Without it, record GTO-unverified. Do not penalize correct losing plays or reward incorrect winning plays.
- Verify gated activation, source/version binding, rollback on confidence collapse/distribution shift/negative movement and proof that a reverted update cannot survive cache refresh/restart.

**Acceptance:** complete source-to-review-to-proposal-to-holdout-to-gate pipeline with natural published shadow receipts, no duplicate or silent population loss, explicit qualified/rejected findings and tested rollback. Learning is not complete merely because every large pot has a tag. No automatic live rewrite is justified by outcome alone.

## Phase 15: policy graph, durable decisions and fleet qualification

**Existing base:** outer graph/registry, component accounting, effect ownership, execution witness, private original read frames, accepted-hand binding, isolated SQLite decision journal, read-only retained-hand reviewer and bounded exact-record writer retries.

**Actual availability at a8ce:** Actual request lifecycle → worker decision/private reads → policy graph/witness → controller acceptance/accepted hand → isolated journal writer and retained reader. Runtime composition R1 strengthens detailed attribution/response/effects/census checks but is unexecuted. The current effects commit applies process-local Maps and sends a volatile ACK; no durable application reader or automatic plan replay exists.

**Next concrete first-round slice:** Implement the receipt R2 producer/ACK/private-writer/reader slices, qualify queue expiry/cancellation/exception/late-ACK behavior, and separately build crash-safe plan recovery with authoritative hand/lease/slot ordering. Complete internal distributions/runtime pins, full request/daily population and natural host-journal evidence; journal-record retry is not strategy-effect replay.

**Build/audit next:**

- Extend graph receipts to the internal policy distributions and modifications, not just outer entry/exit. Every layer has declared authority and a reason for changing or retaining a prior distribution.
- Complete capture for fast/deep paths, fallbacks, discards, aborts, exceptions, stale requests, controller rejection/coercion and shutdown/parent-process failure. Reconcile requested→computed→intended→accepted→persisted counts and explicitly account for loss/unknown windows.
- Pin solver stores, all variant/utility packs, original opponent read frames, observation clocks, governor/effective sampling work and sampling state. Restart replay must use these pinned inputs rather than today's hydrated stores.
- Finish private daily review/export/retention and the real journal consumer. Host journal configuration is a deployment requirement; an unset path is not durable production capture.
- Retain exact bytes and producer/event identity across lost acknowledgements and bounded writer replacement. Test process/host failure separately from a recoverable worker exit and confirm only one writer owns the journal.
- Add domain silence/OOD/stale-policy alarms and fleet coverage by variant/format/stakes/persona/configuration. Separate enabled, evaluated, applied and accepted behavior.
- Complete independent reference/external benchmark acquisition with immutable manifests, after-rake metrics and uncertainty. Report unqualified populations and rejected strength results.

**Acceptance:** complete private receipts reconciled to accepted hands, fresh-process replay of the actual published paths and runtime packs, natural production coverage without silent gaps, proven alert/review consumers, native release identity and separate fleet/strength certification. Neither a functional runner nor a few synthetic replay fixtures establishes fleet readiness.

## Publication and final closeout

For each phase, record G1–G10 individually with the exact source, tests, data and runtime evidence. Check the deployment identity before attributing live counters. Keep the original failed tests/benchmarks and missing inputs alongside successful results. A phase remains open while any applicable core gate fails, is unavailable or has only historical proof.

The release sequence is reviewed local source → source-matched build/test packet → qualified native artifact/deployment → installed identity → natural targeted receipts → independent acceptance. The owner prohibits GitHub use. The native route's owner must finish its existing installation/qualification; this Horse task supplies source and evidence and does not manufacture an alternate route.

The user has explicitly authorized a swarm to continue the foundation audit and execute sequential Phase 8–15 parity audits concurrently. Separate agents own Phase 8–10, 11–13 and 14–15 audit reports and disjoint repairs; the primary agent retains shared-source integration and final checks. This document does not claim the planned expansions have already been implemented. Repairs and their verification are recorded in the accompanying audit packets, including unresolved source, deployment, natural-use and strength gates.

## Selected integration checkpoint and evidence ownership

Use the source-pinned `blueprint-parity-refresh-r2` evidence index rather than combining old passing totals. The baseline is `a8ce6b5697ba17cf92d81b8559e74c1d22f9f595`: historical Darwin verification recorded TypeScript/format success, 13,567 passes, 157 skips and 1,345 unchanged source/configuration hashes. This does not qualify later overlays, native required-check intake, installed identities or natural hands.

The frozen runtime composition selects 25 server files (22 production, three fixtures), with 651 prepared focused cases and zero executed. Its reviewed merged reader preserves shared response/compute/sampling, Phase6 attribution, request lifecycle, retained FAST effects, accepted-action and corrective-census checks. Apply incremental owned deltas to the pinned baseline and qualify the actual server/worker/private-CLI build graph; the comparison resolver is not that production graph. The roster consumer remains dependent on an unimplemented first-acceptance financial producer and independently qualified private authority.

The first-five SQL unit selects validator R4; cutover R2 for fresh events/restoration; F17 invocation history; heartbeat serialization; and the five isolation R3 approval bodies. Cutover R2 and isolation R3 are unexecuted and supersede their named older bodies. The late snapshot-recovery counter race is not solved by those files. Preserve immutable historical answers and complete successful counts; a new counter repair needs shared question-identity serialization across restoration/creation/replay, not an isolated `counter + 1` patch. The selected private F17 history reader R2 is a coded two-file candidate with 82 prepared cases, zero executed, pending independent review and no installation. Its narrow successor fixes the R1 transitive configuration-import defect; history SQL and reader semantics are unchanged. Its own read-only history session and 256-row retained limit plus one sentinel do not qualify the legacy report’s broader queries or whole-window health.

Phase8/10–13 production promotion/withdrawal and Phase4 loaded-policy invalidation remain source-grounded designs, not implemented authority. Unknown approval and transient refresh failure are distinct states; a six-hour refresh interval cannot certify continued approval. First local observation of withdrawal must reach queued decisions, think timers, deep reads and retained effects before a later accepted action. A global zero-use-after-remote-revocation guarantee needs its own authoritative coordination protocol.

Receipt R1's 44 prepared cases remain preserved; R2's 46 prepared cases add the unavailable-worker/ACK contradiction. Neither packet implements the worker/client/store integration or durable plan state. Similarly, the new Phase9 R2 three-case fixture and World R4 freshness/rearm qualification are unexecuted successors to historical, narrower evidence. Do not count their intended assertions as completed checks.

The Accounting/F06 owner response requires a separately protected first-write roster provenance discriminator. Old obligations accept arbitrary extra JSON, so even a stored, hashed, correctly shaped legacy capsule is unqualified. The exact current composed money-wrapper preimage, physical accepted-generation seat mapping, missing classifications, private serializers/digests/return and effective ACLs remain owned prerequisites. Accepted-hand classification does not establish deal-time membership, whole-period population, funding ownership or learning authority.

Membership tournament persona timing also lacks an explicit optional-entry owner and authoritative paying membership; do not attach discretionary delays to mandatory fills, tickets or freerolls. Native source custody, actual execution, installation/publication, natural source-matched usage and strength gates remain independent. Current Documents/AGENTS.md records installation of the shared interface but an unconfirmed preparation refusal; cleanup/provider/required-check admission remains open. Continue authorized disjoint source/review work; do not launch raw tests/builds or use GitHub fallback.
