# Accepted Public Tournament Stage — September 12, 2026

Accepted public action observations now freeze a bounded tournament-stage allowlist from the existing synchronous context cache. The dealing layer binds the reader to the tournament ID of the hand; later table reassignment cannot redirect it. The HandController reads it only after validating the action and before changing poker state. Cash is explicitly not applicable. Missing, warming, stale, incomplete, failed or malformed context is unavailable; a newer cached blind or ante level cannot relabel the hand still using its prior rules.

The captured facts include format, field size, paid places, level, money/bubble/final-table/hand-for-hand state, public registration/recovery windows, bounty stage and satellite configuration. Per-user wallets, affordability, recovery usage and stack/bounty maps are excluded and never enumerated. Tests make private cache getters throw if accessed. This public stage is not a full Phase 7 utility context and is not permission to tune a strategy.

Build and117 focused tests passed across six files, including a real ServerTableEngine dealing-path test that reassigns the table before the accepted action and still reads the original tournament. The initial full suite passed11,119 tests but failed two existing mid-deal safety tests because their constructor literal locator no longer matched the multiline call. The locator was updated; guard ordering, controller capture, single await and no-deal return assertions remain intact.

The integrated source165c76346ef2712f0b9a9864c364612b14b2bd7e on protected parent1e295a033aec4ab69a87008e7eb1ade69680c31e passed build and11,121 server tests, with145 existing skips (763 files passed, one skipped;83.71 seconds). The original branch is preserved. All nine runtime source hashes match the frozen actual-controller proof:504 complete offline hands,6,576 captured public actions,210 private exclusions,maximum1,345 bytes per public node. Gameplay, outcomes and action history match the frozen baseline after removing only the new tournamentStage field. The matrix is metadata/gameplay equivalence; it is not a certification of every tournament launch configuration.

The full Phase14 scoped model, immutable model updates, held-out/shadow separation, proposal evaluation and activation/rollback pipeline remains incomplete. This change neither activates adaptive play nor changes settlements. Protected publication and natural stage receipts remain required.
