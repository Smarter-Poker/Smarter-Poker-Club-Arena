# tests/a-break-its-dead-generation-began-is-finished-by-its-successor.law.test.ts

The successor's movement-admission door (`public.fn_f06_admit_parked_movement`, latest definition 20260926091645) takes a fresh movement proof for a BEGUN break with a manifest as well as an untouched park, takes it from `smarter_private.f06_movement_prior` (moved members by their winner receipts, the rest by their live seats and registrations) before claiming custody, refuses a proof that does not name the break, and holds every admission to `f06_assert_movement`, so a break begun by a generation that died mid-move is finished by the live one through the protocol's own transitions; the law proves it can fail with planted regressions (the door back to untouched parks only, a proof that names no break, custody claimed before the proof, a fresh admission not asserted) and pins that the never-applied 20260926092954 and its schema promise are gone.
