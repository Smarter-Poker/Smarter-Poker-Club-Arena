# tests/a-browser-writer-names-its-caller-in-its-own-body.law.test.ts

A SECURITY DEFINER function that writes and that a browser role can execute states its caller binding in its own body. fn_reduce_agent_credit_v1 and fn_retire_agent_credit_reduction_v1 (union_weekly_accounting_atomic_activation_20260917, EXECUTE to authenticated only) were bound to auth.uid() only through the private helper fn_credit_reduction_lock_v1, so the live definer audit, check-definer-authorization rule 1 and trg_autorevoke_privileged_anon all read them as unbound writers, and the Schema Integrity Audit reported "live: 3, baselined: 2, new: 2" on every run from 2026-09-19. Revoking would break the club manager's credit-reduction flow, which calls both from src/services/CreditReductionOperation.ts, and reviewedExceptions is shrink-only, so migration 20260922160600 repeats the helper's own check in each writer - `auth.uid() IS NULL OR auth.uid() IS DISTINCT FROM p_expected_actor_id`, raising 42501 credit_reduction_actor_changed - by replacing one fragment of the live definition, refusing any source but the reviewed md5, proving the rest byte for byte unchanged, and proving before commit that anon and PUBLIC cannot execute either, authenticated still can, and a caller with no identity or naming somebody else is refused. The law pins that shape, including that the binding refuses a caller with no identity (the 2026-08-28 rebuy hole skipped itself exactly then), and that neither writer is ever forgiven by a baseline or allowlist entry instead.
