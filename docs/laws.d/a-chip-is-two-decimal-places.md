# tests/a-chip-is-two-decimal-places.law.test.ts

Every money column in the conservation set carries numeric scale 2, given by the column TYPE (which rounds on write) and never by a CHECK (which would refuse a payment over a float artifact). Ten columns held no scale at all until `20260906162156` (eight) and `20260906162705` (the two `tournaments` pools, applied inside the :55 maintenance freeze after a rewrite of that 147 MB table deadlocked against live play). The union auto-ledger trigger is captured with `pg_get_triggerdef` and re-issued verbatim, never hand-retyped.
