# server/src/services/aDurableDeferralIsADeferral.law.test.ts

When a page-scoped rakeback period recompute outlives the engine's client deadline, the settler's read-back accepts this call's durable deferral (a fresh blocked request row with an id and the canonical deferral receipt for exactly that club and week) as the direct response path always has, so a slow blocked recompute no longer holds the source cursor forever; stale, anonymous, mismatched, written, reasonless, failed or cleared receipts are still refused.
