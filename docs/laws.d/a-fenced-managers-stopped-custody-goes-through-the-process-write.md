# tests/a-fenced-managers-stopped-custody-goes-through-the-process-write.law.test.ts

A tournament manager whose lease is gone may still get its terminal engine's stopped time-bank custody onto disk, and nothing else: the request hook admits only a POST to engine_presence_parked past the fence, under the marker `fenced-manager-stopped-custody` and never as a manager; the BEFORE INSERT trigger passes every other actor untouched, raises the same `TOURNAMENT_MANAGER_FENCED` for any row that is not a version-1 stopped-custody snapshot, writes only through `fn_park_stopped_time_bank_custody` as the process (which refuses over a later hand, a newer park or a mixed F06 transfer), suppresses the raw upsert only on a confirmed write and raises on every refusal so no acknowledgement is recorded; and the release script names a shut certificate below the budget without changing its verdict (migration 20260926131014, applied 2026-09-26 13:14Z).
