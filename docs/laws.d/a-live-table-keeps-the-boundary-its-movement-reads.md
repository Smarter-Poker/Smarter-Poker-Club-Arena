# tests/a-live-table-keeps-the-boundary-its-movement-reads.law.test.ts

F06 player movement is a permanent obligation proved from two per-hand records that `public.sp_prune_hand_history` deletes at eight days, so both reads are pinned to survive retention. `smarter_private.f06_movement_permits` must not refuse an `accepted` permit on a bare `a.hand_id IS NULL`: the immutable permit plus its `evidence_id` is the surviving witness that the seal was verified, while a commit row that is still present must pass every original post-commit check, a different commit occupying the permit's coordinate must still refuse, and `evidence_id IS NULL`, an above-boundary hand number, `reserved`, any unknown state and any in-flight dispatch must all still refuse. `smarter_private.f06_movement_boundary_retained` must stay defined, STABLE, SECURITY DEFINER and keyed on `max(hand_number)` from `hand_atomic_commits` - the exact row `smarter_private.f06_movement_prior` loads to validate the post-commit payload hash, request hash and stack receipt, none of which can be reconstructed - and the retention job must keep consulting it beside the older `f06_hand_cards_unresolved` guard, keep reading its window from `hand_history_retention_policy.horse_retention_days` so the eight days stay Dan's config decision, and keep pruning both impermanent witnesses so the law's premise stays true.
