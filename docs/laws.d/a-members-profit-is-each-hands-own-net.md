# tests/a-members-profit-is-each-hands-own-net.law.test.ts

`club_member_daily_stats.profit` is written as each hand's own net: what the seat won minus what it put in, read from the accepted-hand envelope's contributor list (20260922144812). Before, all three writers (the live projection, the 15-minute chunked rebuild, the manual whole-table rebuild) differenced end-of-hand stacks and dropped every player's first hand at a table, and `fn_reconcile_club_member_daily_profit` rewrote completed days from two schedulers to cover it. Pins the exact formula in all three writers with the old figure kept only for hands that carry no envelope, the plpgsql-side normalisation of the contributor list, the reconciler standing down from 2026-09-23 (still measuring and logging each club-day), and the migration's live-data proof; and refuses any later migration that brings back a stack-delta-only profit or a reconciler that rewrites exact days.
