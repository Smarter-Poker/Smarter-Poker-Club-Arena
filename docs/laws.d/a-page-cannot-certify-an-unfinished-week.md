# tests/a-page-cannot-certify-an-unfinished-week.law.test.ts

The rakeback settler's page-scoped `fn_rakeback_recompute_periods` call answers
from one unaccrued hand of this club when one is in view (the calculator would
refuse it with written 0 after a full-week read that cost 20-138 s per call and
three calls after every engine restart), and only for page-scoped calls, only on
positive evidence, only under the same club-week lock, and in the calculator's
own refusal shape; everything else still goes to the unchanged calculator. The
test carries planted regressions (door open to whole-period calls, door without
the accrued-batch test, door counting another club, door removed, door answering
ready) and each must be named by the checker.
