# tests/a-public-reader-is-recorded-where-every-audit-reads-it.law.test.ts

A SECURITY DEFINER function that a caller with no account may run on purpose is written down in every list that audits the anonymous surface, in the same change. Migration 20260919153418 made fn_shared_bonus_replay (the public bonus replay link) anon-executable and recorded it in `anonPublicSurface` of scripts/ci/definer-authorization.allowlist.json and in ca_browser_definer_allowlist, but not in `reviewedAnonReaders` of scripts/ci/definer-exposure-baseline.json or in docs/security/anon-executable-definers.json, so the hourly Schema Integrity Audit job "No unaccounted DEFINER writer is reachable from a browser" failed on every run from 2026-09-19 17:25 UTC with "A NEW FUNCTION ANSWERS A CALLER WITH NO ACCOUNT" about a decision already taken. The lists now nest by name - anonPublicSurface inside reviewedAnonReaders inside the anon-executable manifest - and fn_shared_bonus_replay stays baselined only while its signed-out caller exists: /bonus-replay/:shareId routed in the PUBLIC ROUTES block of src/App.tsx, reading through DiamondReplayService.read(shareId, true). If that route moves behind a login, revoke EXECUTE FROM PUBLIC, anon instead of editing the test.
