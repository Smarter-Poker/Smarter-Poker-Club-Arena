# tests/a-retired-compensation-job-is-never-scheduled-again.law.test.ts

A compensation job retired because its writer is correct at the source is never scheduled again. `20260920070402_three_watchers_whose_defects_were_fixed_stop_running` retired `union-seat-provenance-heal`, `ca-bbj-repair-unbanked-15m` and `reconcile-club-table-counts-nightly`; `20260922155223_eleven_compensation_jobs_whose_writers_are_correct_stop_running` retired `ca-redrive-unbanked-rake-15m`, `rake-repair-unbanked-hourly`, `ca-union-rake-attribution-hourly`, `ca-bounty-backpay-hourly`, `ca-payout-sweep-hourly`, `spin_repair_missing_multiplier`, `spin_sweep_unbooked`, `ca-spin-return-unawarded-draws-15m`, `ca-promo-accrual-retry-10m`, `ca-post-commit-orphan-drain-10m` and `ca-pgrst-reload-if-stale`. Every function they ran is still defined, so a job comes back with one scheduling call, and nothing else objects at review: the band-aid gate reads words in a name and seven of these names carry none, its periodic-work rule is one comment line, and the roster law only sees production after Cron Health's next pass.

The law pins that each retiring migration unschedules its names in code and schedules nothing, and binds every migration sorted after a retirement: no `cron.schedule` or `cron.schedule_in_database` of a retired name (by position or `job_name =>`), no row inserted into `cron.job` carrying one, and no schedule whose command calls a function a retired job ran, which is the same compensation under a new name. `fn_tournament_payout_sweep` may still be scheduled in detect mode only, because `tourney_payout_sweep_detect_daily` is a kept observer that runs it with `p_apply` false. It also checks that `docs/attestation/cron-roster.tsv` lists none of the fourteen names, and that the detector refuses and allows the shapes it should. Calls are located in SQL with comments and literals blanked and read from SQL with only comments blanked (CLAUDE.md 7.3); a dollar-quoted body is scanned as code up to its own closing tag.

If a writer regresses, fix the writer (CLAUDE.md 10.12). A schedule that catches up afterwards is exactly what these retirements removed.
