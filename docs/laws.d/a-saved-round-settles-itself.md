# tests/a-saved-round-settles-itself.law.test.ts

No Diamond Spins game may make a player check, refresh, retry or recover anything (owner ruling 2026-09-21, after a Donkey Cross round sat on "Check Your Saved Round Before Starting Another" with every exit held). A saved wager replays itself (useAutoSettle), a ticket the server refused is re-dealt and the wager sent again, a won game starts itself (useAwardAutoStart), the bonus guard never holds a player on a won game that cannot start, a failed award read retries itself, and the server keeps a player's live tickets and refuses a dead one before the entry whose foreign key would otherwise turn it into an exception (migration 20260921185541).
