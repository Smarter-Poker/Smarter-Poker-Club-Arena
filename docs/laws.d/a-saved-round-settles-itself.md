# tests/a-saved-round-settles-itself.law.test.ts

No Diamond Spins game may make a player check or recover a round by hand (owner ruling 2026-09-21, after a Donkey Cross round sat on "Check Your Saved Round Before Starting Another" with every exit held): a saved wager replays itself (useAutoSettle), a ticket the server refused is re-dealt and the wager sent again, the wheel recovers an unconfirmed spin itself, a failed award read, game load, entry quote or ticket deal retries itself and never tells the player to refresh or retry, a won game starts itself after a short visible countdown (useAwardAutoStart) and the bonus guard never holds a player on an award the page cannot start, and the server keeps a player's live tickets and refuses a dead one before the entry whose foreign key would otherwise turn it into an exception (migration 20260921185541). A ticket-gone restart re-sends the exact refused wager on the fresh ticket (never a request rebuilt from what the page shows), and any other refusal re-reads the award and lets the player go until the server's own blockers change (useRefusedAward).
