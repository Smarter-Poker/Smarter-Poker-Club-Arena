# tests/a-spin-is-drawn-stamped-and-booked-by-the-launch-that-deals-it.law.test.ts

A Spin's draw, reserve booking and row stamp commit in the one launch transaction, so no scheduled job mends them (Dan, owner policy v2.9, 2026-09-22). Pinned at the declaration production runs: `fn_spin_draw_and_settle_atomic` books, draws, settles, writes its receipt and stamps the row, reads zero as undrawn, and after booking can only RAISE; `fn_complete_tournament_launch_before_lease_generation`, the only writer of RUNNING, refuses a paid Spin without exactly one `jackpot_draw` equal to a positive stamped multiplier; the engine has no other door to RUNNING and its post-draw write carries no money contract; `spin_tournament_contract_is_draw` refuses any multiplier or prize that is not the one draw, and `spin_reserve_receipt_is_immutable` keeps contribution and draw rows permanent; a Spin is created undrawn and a drawn Spin is never expired. These are the source fixes that left `spin_repair_missing_multiplier`, `spin_sweep_unbooked` and `ca-spin-return-unawarded-draws-15m` with nothing to do (every candidate set empty on 2026-09-22; last multiplier repair 2026-09-06, last booking outside the atomic draw 2026-09-08, no scheduled unawarded-draw return ever).
