# tests/aReleaseMainOutranIsNotABrokenRelease.law.test.ts

On 2026-09-26 the loss of the 02:44 cutover of 399d59f8 was attributed to "FORWARD TARGET BEHIND MAIN"; the run log (36212130903) shows that line is a warning and the transaction died on the in-flight helper's stdout captured as a number (fixed in #5273). This pins, by driving the real `source_target_is_current()` against a fake git history, that a target protected main has moved past still proceeds to the cutover with the newer engine recorded only in its seal reason; that a target a newer sealed release already contains still refuses before mutation with status 1, now in the words (`is stale; protected main requires <sha>`) the workflow's `classify_release_failure` and `check-engine-deploy-starvation.mjs` read as a stand-down, so run 36216296821's kind of refusal is no longer receipted as "the release transaction did not complete"; with a negative proof that the old wording was invisible to the classifier; and that a target off the sealed line keeps its own refusal and is never called superseded.
