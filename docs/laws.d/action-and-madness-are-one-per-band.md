# tests/actionAndMadnessAreOnePerBand.law.test.ts

Action and Madness run exactly one enabled game per (club, variant, blind category), and `public.fn_cash_stake_band` agrees with `stakeBandForBigBlind` on every boundary. Classic is deliberately uncovered. If the two band functions drift, a 0.50 game the fleet calls micro occupies the database's low rung and the duplicate the unique index exists to refuse is allowed straight back in (Dan 2026-09-05).
