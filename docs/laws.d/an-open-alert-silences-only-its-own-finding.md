# tests/an-open-alert-silences-only-its-own-finding.law.test.ts

`fn_ca_tournament_finished_but_not_completed` (cron 304) raised nothing for fourteen days because it skipped every finding while ANY alert from its own source was open, and alert 8c6ce084, about a tournament that COMPLETED six minutes after it was raised, was never closed; on 2026-09-22 it would have named 17, 23, 1 and 4 stuck tournaments at four ticks. Migration 20260922160729 keys the decision on the tournament, through one function the detector calls (`fn_ca_finished_not_completed_open_alert`), closes an alert when every tournament it names has COMPLETED, and proves both directions in a rolled-back savepoint before running the detector for real. This law pins that fix and forbids, from 20260922160729, any migration from defining a function that raises into `financial_alerts`, `ca_drift_incidents` or `ca_diamond_incidents` and decides whether to raise with an EXISTS or PERFORM guard over that table naming no column that says what the finding is about (source, severity, open/closed and time are not keys). The detector is exercised against the shapes it refuses and allows and against the two historical migrations that installed the silence. It does not judge the quality of a key, nor bodies patched in place with `pg_temp.ca_patch`.
