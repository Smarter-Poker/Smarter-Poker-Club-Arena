# tests/ci-tells-the-truth-faster.law.test.ts

CI must be honest and fast without weakening a gate (2026-09-26, PR #5242 and #5260 measurements): a duplicate `CI - Build & Type Safety` run for the same head sha queues instead of cancelling the one in progress, and the fixture gate names a cancelled compilation or native job as NO VERDICT (exit 3, never green, and a real failure beside it is still reported as the failure); the PostgreSQL accounting qualification runs as four matrix shards of `accounting_postgres` with every suite step in exactly one shard, BBJ in shard 1 whose rendered name its deadline probe reads, and the required `Server Engine (typecheck + tests)` aggregate still needing all of them; the software-WebGL Diamond playfield suite gets one retry and every case that passed only on retry is named as FLAKY in the job; and the Diamond games visual baseline is informational, never in the ruleset, and fails loudly when its harness draws nothing.
