# tests/every-guard-has-a-reader.law.test.ts

A guard with no reader is not a guard (2026-09-06, CLAUDE.md 10.86 rule 3): a sweep of `scripts/ci/` found 70 scripts and three checks referenced by nothing in `.github`, `.husky`, `package.json` or `scripts/` - `check-realtime-publication.mjs` (merged hours earlier by the agent quoting rule 3 in its own header), `check-bbj-functions-match-production.mjs` and `check-cosmetic-catalog-drift.mjs`, two of which guard money. The cosmetic one was additionally emitting an eighteen-line false positive nobody had ever read, because a regex without a word boundary matched `table_id:` as `id:` and compared felt asset names against theme ids. This law requires every `scripts/ci/check-*.mjs` to be referenced by something that runs it, or to be named in `MANUAL_TOOLS` with the reason a person runs it by hand, and pins that the three now run in `publish-watchdog.yml`'s `live_drift` job with exit 2 ("could not ask") alarming as loudly as exit 1.
