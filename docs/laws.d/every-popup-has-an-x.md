# tests/unit/everyPopupHasAnX.law.test.ts

Dan 2026-09-23: "there needs to be an X in the top right of every popup (BBJ,
table settings, etc.)". Every SpadeConsole rendered under a dialog carries the
console's own painted `onClose` zone, unless it is named in the law's
by-design list with the reason it may not be dismissed (the TOS gate, the
required profile, the legal welcome, the non-dismissable wheel prompts, the
ranking card that paints its own close). And a console whose plates disable
on a busy flag gates its X on the same flag, or routes it through the
surface's guarded handler: the first pass shipped nine X's that closed a
cashout, a union wallet, a chip mint or a tournament create from the corner
while their Cancel plate was disabled (found 2026-09-24).
