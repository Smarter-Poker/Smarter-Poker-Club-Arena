# tests/five-that-answer-wrongly.law.test.ts

Five things answered confidently without knowing, and two reported defects were
measured and found correct. The cert predicate was SECURITY INVOKER while its
twin was DEFINER, so it answered one thing for service_role and another through
any player-facing path - the second time those two disagreed in one day. The
reward budget refuses nobody since ruling 21 yet read as a control, and its
bigint-max "unlimited" sentinel was being summed into the trial balance, which
printed 9,223,372,036,867,275,807 budgeted on the report a person reads to check
the books. The engine spend journal that replaced a lock-serialising running
total was not enforced append-only. The flip forecast joined FROM the rule table,
so twelve retired rules holding incidents were invisible rather than reported as
retired. And the horse claim loop exists in two copies with nothing able to
notice divergence - not merged, because that path had just settled 759 rewards,
so a normaliser pins their behaviour instead and merging them is recorded as
owed. What each budget should be stays Dan's; a report names which are fiction.
