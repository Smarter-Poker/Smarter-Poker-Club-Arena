# tests/realism-is-one-vocabulary.law.test.ts

#SMARTERCASINOREALISM is one vocabulary: the palette and the three light effects are defined once on :root in the globally imported club-engine.css, and the shared 19-page Rewards Circuit header consumes those tokens (with literal fallbacks) instead of restating hexes, which is what let one family light its live metrics #3aa8ff on one page and #00d4ff on the next.
