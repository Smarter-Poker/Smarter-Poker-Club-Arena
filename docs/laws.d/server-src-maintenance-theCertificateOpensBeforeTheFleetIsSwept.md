# server/src/maintenance/theCertificateOpensBeforeTheFleetIsSwept.law.test.ts

The break is 300000ms and `engine-release-transaction.sh` admits a cutover only while 285000ms remain, so the restart certificate must EXIST within 15000ms of :55:00 or no release can ever land. On 2026-09-25 `beginCountdown()` spent that entire window walking ~324 engines twice - `parkEveryEngine()` then the `unparkedTables()` census - before the write that sets `durableConfirmed`, and the first complete certificate was observed 15375-19846ms after t=0 on seven of seven consecutive breaks (runs 36155409978, 36157652866, 36157811057): the floor missed the window by 375ms and the platform sat 6.7 days unable to restart. The law pins that a positive admission window still exists between the script's reserve and the engine's break duration, that neither the fleet park nor the census sits between the countdown's start and its durable save, that the sweep still runs afterwards, and that a terminal engine holding a frozen stopped time bank PERSISTS it (at the custody's own hand number, never while an accounting outcome is unknown, never over a live engine's newer row, acknowledged only on a real write) rather than reporting one permanently-unconfirmable answer - 137 tables did that for 8 breaks. The residue is named in three outcomes, `unwritten` and `unreadable` both refusing and both zero-seeded on `/metrics`, and no bank or custody reason may ever enter the release script's `PREPARATION_ONLY` allow-list.
