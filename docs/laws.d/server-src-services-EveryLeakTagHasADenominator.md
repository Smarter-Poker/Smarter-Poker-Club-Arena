# server/src/services/EveryLeakTagHasADenominator.law.test.ts

Outcome-independent leak situations are recorded on both sides (`x` on a loss, `x_won` on a win) so the audit can rank by EV rather than by damage; the fold family stays one-sided because a folded hand never wins; and no loss-side name the self-tuner matches may be renamed.
