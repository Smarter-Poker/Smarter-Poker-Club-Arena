# tests/tablevisual-create-table-sheets-stay-on-schema.law.test.ts

The three stylesheets that print inside the create-table SpadeConsole (`CashGameCreateFlow.css`, `TableConfigPage.css`, `CreateTablePage.css`) stay on the #ClubArenaConsole schema: no pointer-over rule, none of the off-schema colours removed on 2026-09-20 (the brown and amber preset chip, `#1a1a2e`, `#dc2626`, the computed blues and greys), no em dash, and in the cash flow no corner radius, gradient or drawn shadow plate because the console is the frame. The Table Config action bar is opaque and sits in flow, the form declares a dark scheme so native parts and the date picker glyph draw white on the black glass, dead global rules that leaked onto other pages stay deleted, and the game type selector's back control is a printed word that names where it goes (Create A Club phase 2, 2026-09-20).
