# tests/a-club-stays-deletable.law.test.ts

A club stays deletable so a fixture can never be stranded with its chips: the thirteen foreign keys into clubs that no valid, non-partial, leading-column index could answer are closed with IF NOT EXISTS statements (the two large ones built CONCURRENTLY against production first, because a plain build of game_management_events blocks writes for 25 seconds), the migration refuses itself if any single-column key into clubs is still unanswerable, and fn_ca_fk_index_gaps asks the same conditions read-only for service_role with nothing scheduling it. Both protected-main publication paths require the strict check before deployment authority or origin credentials can be used. Gaps and unreadable catalog responses block publication. The post-deploy check asks again before live fixtures because database state can change after publication. Pull-request code receives no production credentials.
