# tests/a-club-stays-deletable.law.test.ts

A club stays deletable so a fixture can never be stranded with its chips: the thirteen foreign keys into clubs that no valid, non-partial, leading-column index could answer are closed with IF NOT EXISTS statements (the two large ones built CONCURRENTLY against production first, because a plain build of game_management_events blocks writes for 25 seconds), the migration refuses itself if any single-column key into clubs is still unanswerable, fn_ca_fk_index_gaps asks the same three conditions read-only for service_role with nothing scheduling it, and scripts/ci/check-club-fk-indexes.mjs asks production on every pull request and fails the branch
