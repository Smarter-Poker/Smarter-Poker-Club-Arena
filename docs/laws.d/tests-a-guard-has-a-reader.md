# tests/a-guard-has-a-reader.law.test.ts

This repo runs its own `scripts/ci/check-main-is-green.mjs` on a schedule, on `ubuntu-latest` where `gh` exists, with `issues: write` and both a raise and a close path, branching on `PIPESTATUS[0]` rather than `tee`'s status. CLAUDE.md 10.83 described that detector from the day it was written while naming only the World Hub's copy, so Club Arena had none — and the first run against Club Arena found three workflows red on `main` with no issue naming them, including a monitoring check that had correctly refused to pass and reached nobody (2026-09-06).
