# tests/a-horse-is-named-only-to-those-entitled.law.test.ts

Every browser-reachable reader of the horse flag routes it through `fn_can_see_horse_flag` - staff get the truth in the raw RPC response, everyone else a uniform false; the four that did not (`ca_club_player_breakdown`, `ca_club_player_page`, `ca_club_player_export_start`, `fn_club_cashier_members_page_v3`) are masked, each rewrite asserts it matched rather than silently masking nothing. Since Dan's 2026-09-14 ruling the client itself never paints or derives the flag at all, for anyone - see tests/a-horse-is-never-named.law.test.ts - and a horse is never called a bot in operator-facing copy
