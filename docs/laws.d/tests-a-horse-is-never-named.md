# tests/a-horse-is-never-named.law.test.ts

Dan, 2026-09-14, binding: "NOTHING SHOULD EVER REVEAL A HORSES IDENTITY." This closed the one thing CLAUDE.md 10.5 still allowed on the client - showing `is_horse` as data, a badge, a column or a toggle. Scans every `.tsx` file under `src/pages/**` and `src/components/**` and fails on the literal phrases `Hide Horses`, `Horses Hidden`, `People Only`, `title="Horse"` and `(Horse)`, on the identifier `hideHorses` anywhere, on `is_horse`/`isHorse` quoted as a bare CSV header cell, and on `is_horse`/`isHorse` reached through a dot inside a same-line JSX `{...}` span - never on a type member, a plain object-literal mapping, a Supabase service call, or a comment, which stay legitimate plumbing. Retired the "Hide Horses" toggle and its relabelling on the club dashboard and club data page, the "H"/"Horse" leaderboard badges, the cashier trade roster's and the blacklist picker's "(Horse)" suffix, and the `is_horse` CSV export columns those pages offered.
