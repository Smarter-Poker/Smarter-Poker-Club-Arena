# tests/a-race-that-touched-nothing-names-it-and-waits.law.test.ts

A legacy-engine checkpoint refusal names the term that actually moved - the guard's `drainWitness` walks every field the predecessor's `captureDrainedF06Originals()` reads and names each one that is false, keeping "it moved", "it could not be read" (`<term>:unreadable`) and "every term still holds" (`none`) as three distinct answers, and those sub-conditions reach the runner inside the one allow-listed carrier, `observedDetail`, rather than as keys the fixed allow-list would drop - and a capture-then-reverify refusal that provably touched nothing (stage `preflight`, zero attempted tables, zero completed calls, `checkpointOutcome: not_started`, inspector closed) exits 75 so the release waits for the next break, after the helper has retired byte-for-byte the one-shot intent it wrote itself. The deferrable list is exactly the four capture-then-reverify codes plus `insufficient_reserve` - a late arrival, deferred without moving the guard's 285000ms floor, which still refuses on the identical reading and is still fatal once the attempt got past preflight. Every `maintenance_*` code and `bank_park_write_incomplete` still end the release, and the owning transaction still proves the intent absent for itself before honouring 75.
