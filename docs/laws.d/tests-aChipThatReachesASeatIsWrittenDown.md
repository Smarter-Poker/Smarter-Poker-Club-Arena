# tests/aChipThatReachesASeatIsWrittenDown.law.test.ts

A money path never names its own account as its counterparty, so a chip that reaches a seat is always written down; and a meter reads its balance and its journal at one instant, leaving a mark taken before that instant, so a reading boundary is never mistaken for drift.
