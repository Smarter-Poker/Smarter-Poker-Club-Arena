# tests/community-search-owns-its-classes.law.test.ts

`/search` styles itself through `SearchPage.module.css` and shares no class name with `components/common/Search.css` (whose global `.search-results { position: absolute }` and `.search-clear { width: 20px }` pulled the result list out of the panel and covered the buttons on 2026-09-04); reads clubs, tables and tournaments only from `fn_community_search`, which sums a union's members from its clubs via `fn_batch_union_realtime_member_counts` (1,177, not the house row's stale 328) and resolves `logo_url` before `avatar_url`; searches players only through `fn_search_players` (privacy-aware, trigram); measures "Live Indexes" from which indexes answered instead of the literal 4; revalidates table access at click time; and the migration is one transaction, pg_trgm fuzzy, authenticated only, re-applying the RLS visibility rules a SECURITY DEFINER bypasses.
