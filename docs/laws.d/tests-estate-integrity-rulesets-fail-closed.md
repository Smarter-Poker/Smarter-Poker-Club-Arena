# tests/estate-integrity-rulesets-fail-closed.law.test.ts

The estate audit fails closed whenever any branch-ruleset detail cannot be read and validated, so an unreadable secondary ruleset can never hide an unauthorized release gate.
