# tests/every-member-is-discoverable-in-the-cashier.law.test.ts

Every member the server returned is listed and searchable in the cashier roster (Dan 2026-09-04): the client drops nobody before the search, matches the arena name, the @handle and the printed member ID, and marks - never deletes - the viewer's own row, which is the one row that cannot be a recipient
