# tests/law/DiamondIssuancePassesThroughTheMint.law.test.ts

Diamond issuance passes through the Mint: the house is a destination, the signup 500 is journaled on both trigger paths, a balance born at a profiles INSERT is recorded and never refused
