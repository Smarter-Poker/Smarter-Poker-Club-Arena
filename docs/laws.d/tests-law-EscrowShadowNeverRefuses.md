# tests/law/EscrowShadowNeverRefuses.law.test.ts

The escrow shadow (chip standard Lane B) never refuses: no RAISE EXCEPTION, never writes a tournaments counter, evidence rows only, one info incident per event, warnings capped
