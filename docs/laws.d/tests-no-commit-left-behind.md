# tests/no-commit-left-behind.law.test.ts

The publisher resolves the current protected-main SHA, serializes exact release events without a retry watchdog, and the independent engine lane can never gate the static bundle (Dan 2026-09-02)
