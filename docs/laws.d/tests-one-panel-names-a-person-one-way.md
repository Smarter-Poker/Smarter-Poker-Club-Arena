# tests/one-panel-names-a-person-one-way.law.test.ts

The club breakdown resolves the agent's name through `fn_arena_name` over the full six-column chain, with the raw username only ever the last resort - it read `profiles.username` while the downline list beside it showed the alias, so all 32 agents in the test club were named one way in one list and another way in the list below it, and Phase 6 then made that real name a search target, a sort key, and the breadcrumb above a page of aliases
