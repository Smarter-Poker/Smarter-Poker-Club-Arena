# tests/oneMovementIsOneLeg.law.test.ts

A balance movement is journalled exactly once: a writer stands down when the caller declares it will write the leg itself, and a chip that passed through settlement_suspense and kept no name is a finding rather than a note. A finding is closed only by the measurement that raised it, after two clean runs, and a meter that changed what it measures never calls the difference growth.
