# tests/only-the-real-clubs-exist.law.test.ts

Only the real clubs exist and a certification fixture retires to the Mint: fn_ca_retire_certification_club refuses the four real estates by id and anything that has played, retires the fixture's chips to chip_retirement with a declared journal row, takes both append-only doors by name in the order the foreign keys require, and the certification calls it and fails loudly if a fixture survives
