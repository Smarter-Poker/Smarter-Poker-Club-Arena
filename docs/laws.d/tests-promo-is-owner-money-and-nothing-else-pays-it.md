# tests/promo-is-owner-money-and-nothing-else-pays-it.law.test.ts

Promo is owner money and nothing else pays it: the undesigned splash pot refuses and is unreachable from a browser; add_to_promo_wallet, distribute_promo_chips, transfer_promo_club_to_agent and the orphaned create_user_wallets all refuse and name fn_promo_disburse; the phantom wallets(PROMO) pool is zeroed as a write-off and never as a burn, with its rows kept for the legacy escrow holds that reference them and the wallet guard opened by name for one statement; and the app disburses through the owner door
