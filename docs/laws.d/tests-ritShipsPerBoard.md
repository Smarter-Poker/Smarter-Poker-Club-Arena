# tests/ritShipsPerBoard.law.test.ts

On a run-it-twice hand the stack rises with EACH board's fan (a per-player released ledger, not one boolean flipped after the last one), each pot row holds its full contested amount and retires only when it has paid every run, a board axis no group claims past run 1 is not used to time the ships, and a seat names the hand it made on the run currently on stage. Every pin is a bug that shipped: the chips fanned three times while the number moved once (2026-09-05).
