# tests/the-break-clocks-agree.law.test.ts

The :55 maintenance break constants agree across engine, deploy cron, watchdog, SQL and client, and the resume waves stay inside the :00 minute and read nothing about who is seated, and the database refuses migration DDL across the whole break window (:50-:03 UTC) with a refusal that cites the CLAUDE.md rule governing it
