# tests/the-closed-union-send-door-credits-one-owner.law.test.ts

Migration 20260925145748 is one guarded transaction that asserts its own readback: fn_union_send_chips_to_club credits exactly the one owner it resolves from clubs.owner_id and refuses an ambiguous one instead of crediting every role='owner' row against a single union debit, carries the house authorization check (fn_caller_is_engine or the union owner) plus the club-in-union check, takes its idempotency key from app.union_send_chips_op_id and never invents one, writes one chip_ledger leg naming the receiving club with both auto-journals stood down, records its audit row against a wallet union_wallet_transactions_wallet_check permits, stays revoked from every client role with no new overload, and is not made SECURITY DEFINER; and fn_union_issue_weekly_invoices gains union_squareup_eco_record_fails_its_own_arithmetic - checking the recorded ECO against its own stored base and rate, before the document is written - while keeping both existing refusals and reintroducing no call to the volatile week recomputation
