# tests/the-union-sets-a-rate-per-game-type.law.test.ts

The union sets a rakeback rate per game type and R3 refuses: the weekly close prices each club's basis per game type (union_clubs.rate_cash/mtt/sng/spin/satellite, falling back to club_commission_rate then 0.90), reads the type from the credit (cash when it names no tournament), truncates per club and game type so a remainder stays with the union, and records the per-type basis and rates on the settlement; the R3 money-path guard refuses instead of logging, with the mode in a table so the way back is one UPDATE
