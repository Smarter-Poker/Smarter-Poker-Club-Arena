# tests/unit/dailyBonusPhase3.law.test.ts

The Daily Club Arena Bonus forgives, boosts and gambles on the server, inside the caps, never in chips: the phase 3 migration (`20260910181625_the_daily_bonus_learns_to_forgive_boost_and_gamble`) is one transaction with a lock timeout whose two hot foreign keys are its last statements; a Streak Shield is a `feature_purchases` credit spent by `fn_ca_daily_bonus_open_day` for exactly one missed day (two missed days reset); the lucky 1x-5x multiplier and the mystery prize are rolled by server functions no browser can execute; a Mission Boost is one live `player_boosts` row per player whose EXTRA is paid by `fn_ca_daily_bonus_boost_extra` through `award_diamonds_v2('daily_bonus_boost')` clamped at 125, from inside `claim_daily_challenges_serialized_body` after the missions' own diamonds; the claim rewrite keeps every rule the earlier bonus laws pinned, grants nothing new to browsers, and the read side reports the shield, the boost and a protected day.
