# server/src/benchmark/TheAbsoluteScoreRunsFirst.law.test.ts

The fleet plays itself, so its aggregate result is zero minus the drop by construction and can never measure skill; the league only ever measures config A against config B. Solver agreement is the only ABSOLUTE number in the estate, and it had never been written once - it sat at the END of `runLeague`, after a matchup card whose own source says it is "ALWAYS killed mid-card" (28 matchups on 2026-09-04 and 3 on 2026-09-06, no score either time). `writeSolverAgreement` now runs BEFORE the matchup loop, writes a row on the skip path too (so "no score" and "the chart store was empty" cannot look identical from the database), reads nothing the matchups produce, and is taken by the `alreadyRanToday` short-circuit for any day that has results and no score.
