# tests/the-arena-marketplace-is-the-hub-marketplace.law.test.tsx

One marketplace (Dan 2026-09-21, "CLUB ARENA MARKETPLACE, SHOULD BE THE EXACT SAME PAGES AS THE MARKETPLACE THAT EXISTS IN THE WORLD HUB"): every web visit to the Club Arena `/marketplace` route is handed to the World Hub marketplace page that shows the same thing (`?tab=diamonds` -> `/hub/diamond-store`, `?tab=membership` -> `/hub/vip-membership`, a club's Store / My Items / Manage -> `/hub/club-shop?clubId=<uuid>` with the matching `&view=`, no club -> `/hub/diamond-store`), leaving in a hub tab beside a live table and by a replacing navigation otherwise; the in-app storefront (`MarketplacePage`) renders only in the native app (StoreKit / Play Billing), for a card checkout returning to be verified, and for a top-up carrying a validated `?next=`, and it lets go as soon as the player asks for the marketplace again.
