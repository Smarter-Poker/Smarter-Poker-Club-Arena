# tests/the-console-standard-stays.law.test.ts

The #ClubArenaConsole standard - how every Club Arena page, popup, card and
button is rebuilt on Dan's approved master art - must stay in the repo at
`.claude/skills/club-arena-console/`, keep its runnable kit (the surgery
library, the inventory scanner, the render harness), and stay pointed at from
CLAUDE.md, so every agent on every account and in every tool works from the
same one instead of re-deriving it. Pins existence and reach only; the design
opinions inside are Dan's and are meant to move.
