# tests/the-escrow-ttl-sweep-reads-the-real-status-domain.law.test.ts

`fn_ca_escrow_ttl_sweep` scanned `chip_escrow_holds WHERE status = 'active'`, a value `chip_escrow_holds_status_check` has never admitted, so 2,054 cron firings of `ca-escrow-ttl-sweep-10m` ran over zero rows while 166 holds carrying 3,778,600 chips sat expired and unreported for forty days. The law pins that the sweep reads the status the table enforces and that its migration asserts the domain before writing the literal; that it stays an observer with no release, credit or schedule of its own (CLAUDE.md 10.12, and no debit backs these holds so crediting them would double-pay); that the per-run bound of 25 survives, ordered oldest-first so a replay is the same batch, with each finding carrying the whole backlog's count and total; that the escrow liability is documented against `(held)`; and that the retained RED/GREEN fixture still carries the installed preimage, production's measured 166/539 population and every refusal, ending in ROLLBACK.
