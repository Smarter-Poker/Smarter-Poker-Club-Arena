# tests/the-games-never-pay-more-than-they-take-in.law.test.ts

Diamond Plinko and Diamond Crash (Dan 2026-09-08: two alternates to the wheel, 50x or higher, a crash pays nothing, the 20 percent edge kept, never pay out more than taken in) keep the promise by arithmetic: a bet is whole chips and 0.80 of it is a declared, registered issuance to the host; before a round the pool promises at most cap_fraction of its headroom on that bet (never above the ceiling, never above the bank beyond open reservations) and the round is played against min(table, cap); a plinko table is activated only when it returns exactly 0.800000 on C(16,k)/65536; the crash point is floor(80 x 2^48 / (roll + 1)) cents so every cash-out target returns 80 percent, an open round reserves bet x cap, and the server clock decides it; after every move paid + reserved <= minted + allowance is re-checked; no is_horse filter, no cron, no repair job.
