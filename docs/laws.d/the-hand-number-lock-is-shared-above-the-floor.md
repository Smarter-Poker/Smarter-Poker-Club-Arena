# tests/the-hand-number-lock-is-shared-above-the-floor.law.test.ts

The global hand-number advisory lock in `smarter_private.f06_allocate_number_above` is taken SHARED when the sequence already stands at or above the caller's floor (only `nextval` runs, and a result below the floor is refused), and EXCLUSIVE only on the path that calls `setval`, so one commit stall no longer queues every hand start in the fleet while a number is still never issued twice or at or below a table's last dealt hand; the law reads the latest migration defining the function and proves it can fail with planted regressions (the whole function back on the exclusive lock, setval under the shared lock, a setval in the shared branch, an unrefused below-floor result, nextval before the lock, a session-level lock), and `scripts/ci/probes/hand-number-lock/concurrency.sh` exercises it under real concurrency with a planted rewind that must produce duplicates.
