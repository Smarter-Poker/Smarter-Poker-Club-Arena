# tests/the-monitoring-deploy-deploys.law.test.ts

The monitoring deploy deploys (2026-09-06): the first run of `deploy-monitoring.yml` curled `raw.githubusercontent.com` for this PRIVATE repo, got a 404, piped nothing into `bash`, and reported the Deploy step green having shipped nothing - the same morning the cluster controller was down 65 minutes with `ClusterPassErrors` declared here and not loaded on the box; its verify step then failed for the wrong reason (bare `ssh` with no key). This pins that the workflow ships its own checkout over the deploy key and runs `deploy.sh` from that copy in `MONITORING_SRC_FROM_CHECKOUT` mode, copies in place so bind-mounted files keep their inode, hides no exit code behind a pipe, writes an ssh Host entry so `check-alert-rules-match.mjs` can reach the box, that every file `alertmanager.yml` reads from `/etc/alertmanager` is mounted by `docker-compose.yml` (the `cron_secret` mount lived only on the box for two days), and that `deploy.sh` never copies the placeholder Caddyfile over a live one that carries a real hash.
