# tests/the-operator-sees-the-money-and-the-room.law.test.ts

The Diamond Games operator consoles answer what the host is up or down and how close the cover is to stopping the games, and they answer both out of money that actually moved: fn_diamond_game_pnl states net as intake/rate less chips paid less diamonds paid/rate less the welcome spend, counts the diamond prizes the realised-return windows never did, keeps the welcome spins apart because they are a gift the host chose rather than the paid game losing money, excludes fixture accounts and any crash round that has taken a bet without deciding a payout, and never asks whether a player is a horse; fn_diamond_game_room takes its ceiling from fn_diamond_game_cap_cents, the same function the door uses, and asks it a second time with a bank nothing could exhaust so it can say WHICH ceiling binds, because the intake headroom is the never-pay-more-than-taken-in law working and needs nothing from the operator while the cover is theirs to fix, and reporting them as one number called a fully funded game thin until a probe caught it; fn_diamond_game_players counts the America/Chicago day the caps themselves are counted on so the console and the door agree when the day turns, and names players through the platform helper. All three are reads that write nothing, are revoked from PUBLIC and anon, ask auth.uid() and fn_wheel_can_operate, and run only when an operator opens a page, never on a schedule, because CLAUDE.md 10.12 forbids a monitor presented as the resolution. One shared component renders them on both consoles so the wheel page and the games page cannot drift apart.
