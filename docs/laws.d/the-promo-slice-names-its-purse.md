# tests/the-promo-slice-names-its-purse.law.test.ts

`bbj_pools.promo_balance` is a staging slot the sweep empties continuously; the
purse is the union's `promo_wallet` or a standalone club's `clubs.promo_balance`,
which is what `fn_bbj_promo_payout_atomic` spends. No surface may display or
gate promo money on the staging slot, none may offer `fn_bbj_promo_rain` while
it is a deliberate stub, and the operator bar draws only the two banks the
jackpot actually holds.
