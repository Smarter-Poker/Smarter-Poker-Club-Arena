# tests/the-wheel-never-pays-more-than-it-takes-in.law.test.ts

The Diamond Wheel (Dan 2026-09-07: 80 percent return, 20 percent house edge, "never pay more out than we take in"; 1 diamond = $0.01, 1 chip = $1.00) keeps its promise by arithmetic on the host's own money: only a prize table that returns exactly 0.800000 on 100,000 weight can be activated; nothing is minted, the diamonds a spin takes in are transferred to the host's owner, and a chip tier is drawn only when chips_paid + prize <= what the wheel HAS TAKEN IN plus the host's exposure allowance AND cover can pay it, a diamond tier only when the diamond float and the owner's own diamonds cover it; every chip leaves through one payer that draws the host's promo wallet down first and takes the remainder from the host's chip bank (Dan 2026-09-10: "the back up is the union or club main bank, if the promo pool runs dry"), one journal row per wallet, keyed on the spin; the welcome spin is one per member per host ever, on the same wheel at the same odds, taking nothing in and charged to a separate declared budget so it can never lean on the paid game's intake (Dan 2026-09-10: "free spin should be once for a new user 100 diamonds ... All paid for by the promo wallet"), enforced by a unique index and by a core the browser cannot reach; and the rate is one row, ca_bridge_rate, read by fn_ca_bridge_rate() and never a literal. AMENDED 2026-09-21 by owner ruling (Dan, R12 and R13, with R2 and R15): the mix is a bonus game 50% of the time, an instant chip win 30% and a throwable / time bank / rabbit hunt 20%, and when told that mix could not hold 80% at the old prize values the owner chose "Hold 80% payback", so an item prize is a quarter of the entry instead of a half and the instant-chip ladder is re-solved; back-to-back spins can never repeat a prize or a game across the Upgrade tiers, which the wheel keeps by drawing the spin after ord i from ROW i of a SYMMETRIC follow-up matrix whose rows sum to the base weights, so its columns sum to them too and the unconditional law of every spin is still the published one: the mix stays exactly 50/30/20, the payback exactly 0.8 on both the standard and the VIP table, and no conditional expectation after any prize ever reaches the entry; an active or lifetime VIP never wins a throwable, a time bank or a rabbit hunt, those three cards being instant chip wins of exactly equal value (0.2x, 0.25x, 0.3x on weights 6667/6666/6667 = 5000, the same 5000 the items were worth); and "Diamonds" stops paying half an entry and deals three cards worth half, double and triple the diamonds risked, sealed at the spin and paid once on the pick, so the funding gate asks for three entries where it used to ask for half and every unpicked card holds its triple against the owner's custody and the wheel's diamond float, which is what keeps wheel_pools.diamond_float >= 0 true.
