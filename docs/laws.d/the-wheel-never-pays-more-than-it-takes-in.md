# tests/the-wheel-never-pays-more-than-it-takes-in.law.test.ts

The Diamond Wheel (Dan 2026-09-07: 80 percent return, 20 percent house edge, "never pay more out than we take in"; 1 diamond = $0.01, 1 chip = $1.00) keeps its promise by arithmetic on the host's own money: only a prize table that returns exactly 0.800000 on 100,000 weight can be activated; nothing is minted, the diamonds a spin takes in are transferred to the host's owner, and a chip tier is drawn only when chips_paid + prize <= what the wheel HAS TAKEN IN plus the host's exposure allowance AND cover can pay it, a diamond tier only when the diamond float and the owner's own diamonds cover it; every chip leaves through one payer that draws the host's promo wallet down first and takes the remainder from the host's chip bank (Dan 2026-09-10: "the back up is the union or club main bank, if the promo pool runs dry"), one journal row per wallet, keyed on the spin; the welcome spin is one per member per host ever, on the same wheel at the same odds, taking nothing in and charged to a separate declared budget so it can never lean on the paid game's intake (Dan 2026-09-10: "free spin should be once for a new user 100 diamonds ... All paid for by the promo wallet"), enforced by a unique index and by a core the browser cannot reach; and the rate is one row, ca_bridge_rate, read by fn_ca_bridge_rate() and never a literal.
