# Final eighteen Throwable rigs

The catalogue now contains 80 enabled IDs: 79 atlas rigs and the separate boxing-glove player. This batch adds the final 18 IDs without changing their wire names.

The eighteen performances have 189 timed browser captures, including all four seat-avatar rungs. The browser check exercised 1,555 component animations at each speed multiplier (0.5, 1, 2), found no page or asset-request failures, and found no animated rig groups on a fresh reduced-motion page. The contact sheet shows each performance at a named beat; it does not replace the full timing check.

The 17 new PNG atlases and their runtime WebP files passed exact alpha and visible-RGB comparisons. Missile shares the already authored Rocket airstrike atlas and choreography as specified in the premium animation plan. Donkey is an original procedural 3D render with four expressions; its reproducible renderer is beside the source PNG. The other new atlases were generated for this project. Picker images use the actual cropped component artwork at 192, 320, and 640 pixels.

The first browser pass exposed padded sprite bounds. The corrected pass fixes narrow stanchions, thermometer scale, bandage strips, the heart arrow, and slot reel alignment. Standing Ovation alternates open and closed hand poses rather than scaling one static pose.

Final local validation: 96 focused tests passed after the sound additions and merge of current main. TypeScript and the production build passed with behind-main=0. All six new cues encoded in both formats with measured audible peaks. Publication remains a separate gate.

This is implementation and browser evidence. The broader programme still requires independent evidence for server-side per-item ownership and seasonal gates, store purchase flows, real two-client play, real-device frame rate, and the remaining specialist voice/audio acceptance. An 80/80 catalogue count does not certify those separate requirements.
