{
  "_owner": "agent/claude-diamond-phase-7/feature/cash-game-parity (2026-09-11). src/components/arena/arenaAccess.tsx is a 45-line React context with no imports beyond react: it carries the arena entitlement ArenaAccessBoundary already read from the server down to the guards that need it. ClubMemberGuard is statically imported by App.tsx and decides, before first paint on every club route, whether the viewer sees the lobby or is redirected to a Join page. Diamond Arena membership is an entitlement with no club_members row, so without this context that guard redirects every Diamond player away from their own arena. A lazy import cannot be consumed by a hook the guard calls on its first render, and a suspense fallback around the redirect decision is the redirect happening anyway. The module is the context, its provider and two hooks; nothing else this branch adds reaches the entry, and the initial and total gzip budgets are unchanged.",
  "modules": ["src/components/arena/arenaAccess.tsx"]
}
