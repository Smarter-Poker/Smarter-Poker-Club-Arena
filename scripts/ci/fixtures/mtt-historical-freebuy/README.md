# Three Original Free Buy Fee Proofs

This is the maintained input adapter for the already qualified, explicit three-source historical proof. It does not manufacture a current capture, alter the original-producer cutoff, pay a wallet, or certify tournament completion. The exact migration remains SHA256 `f98f72069a396f547b4731f9271a16985836224ede07b7967c29d7502656e169`.

The existing native driver calls `mtt_historical_freebuy_proof.qualify(execution, root, composed_template, assert_rollback)`. `composed_template` must be the genuine isolated, empty composed preimage before R46 preparation migrations. The caller retains cluster creation, cancellation, final cleanup and its existing full data/catalog rollback checker. The module clones that template and disposes only its own case databases. No connection string, production route, new runner or duplicate baseline catalog is included.

The adapter verifies every maintained input hash before SQL and again after qualification. The current authority supplement and historical input are byte-identical to the original native0006 inputs. The supplement has the original exact function definitions, owners, ACLs, table constraints and enabled immutability triggers. No new permissive predecessor list was added; an incompatible nonempty baseline refuses at fixture composition. The migration itself verifies its exact authority and receipt-schema pins before any append. `input-provenance.json` retains historical source locations as documentary evidence only; none is read at runtime. Numeric JSONB text lexemes are preserved.

The actual core assertions are unchanged from the prior qualified caller: original capture cutoff refusal; missing-source net-plan refusal; exactly three sources/batches totaling 0.30; current independently expected net fees 0.20/0.10, zero refunds, correct source cardinality and payable=false; honest new capture timestamps; no change to original fees, ledger, entitlements, registrations, history, managed terms or fixture wallet/bank rows; exact replay; all nine drift/refusal cases with complete data/catalog rollback; and the production-shaped probe's exact self-aborting error with full rollback. Fault injection uses replica mode only to construct isolated malformed inputs, never during proof/business calls.

The nine negatives are partial capture, raw fingerprint, charge amount, entitlement fee, registration club, managed hash, earning terms, earning ACL and disabled immutable trigger. `production-probe.sql` is run only inside the caller's isolated database by this module. Any proposed production execution remains separately owned and is not performed by this adapter.

Original result: PostgreSQL 17.11 native0006 passed September 17, 2026 at 20:49 UTC; receipt SHA256 `e6850b514f0ad305ab73f2da66ceb3cb49b69366bc1232e3a3128c16c42c0e1b`. The portable adapter has been parsed, source-bound, and compared by AST with that qualified core after path/owner substitutions. Its final invocation in the maintained accounting command must be executed by the composing owner before claiming maintained protection. No new database run, source installation, payment or live closure is asserted by this port.
