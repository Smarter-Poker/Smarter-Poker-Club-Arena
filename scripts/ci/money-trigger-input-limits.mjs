// One MiB per migration/declaration, shared by production and consumption of
// exact-byte proofs. Bundle, inventory and artifact limits remain independent.
export const MAX_MIGRATION_BYTES = 1024 * 1024;
