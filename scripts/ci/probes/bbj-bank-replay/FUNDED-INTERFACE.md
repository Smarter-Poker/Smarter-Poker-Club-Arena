# Pending funded BBJ fixture interface

Status: hosted source successor on `fix/bbj-hosted-caller-20260917`, based on restored `2ee072516821a335ddfda81ee6a33e21a0277c4c`. Original caller5213 and F1 custody remain preserved. All26 current identity/deadline/timing modeled tests pass locally; no PostgreSQL, SQL, actual API request or financial case ran. The changed source still requires independent acceptance and hosted CI. The original replay path remains intact. A genuine preceding-step job observation is now consumed; missing or invalid timing refuses before funded allocation. Root separately owns the existing workflow/classifier/receipt-retention composition.

`funded/custody.py` implements a fixed, hash-pinned mapping for 70 directly selected source/binding files (62 ordinary source inputs and eight historical binding records), with 17 original-seal/runner-derivation provenance files. `funded/SOURCE-CLOSURE.json` maps every original member to its repository path and expected original SHA256. All copied bytes remain exact. The helper verifies each selected member against its original seal, rejects traversal/symlinks/unknown origins, reverifies bytes when consumed, and keeps historical admission records out of executable input lookup. It imports no copied module and starts no process or database. The existing runner now explicitly loads the six regressions from `funded/test_custody.py` inside its receipt/cleanup boundary and before cluster allocation. It rejects incomplete discovery/execution, errors, failures, skips, expected failures and unexpected successes, retaining a fsynced source-stage record/test log and structured results. Final execution remains pending.

The former F1 packet had a narrow classifier change and directly relevant behavioral assertions. They remain preserved as input, but this successor deliberately leaves the restored classifier and workflow untouched. Root must compose the exact runner/nested-fixture selection into the current classifier if it is still absent, retaining those original behavioral checks. Source presence alone does not prove the accounting job is selected.

## Existing path and isolation boundary

The maintained entry point is `scripts/ci/test-bbj-bank-replay.py`, called by the existing `accounting_postgres` job in `.github/workflows/ci.yml`. Preserve its current replay database, installer hashes, 19 cases, eight write tripwires, drift refusals, receipt, and cleanup assertions exactly.

The replay fixture is intentionally insufficient for a funded operation. Its only business tables are `bbj_pools` and `ca_bbj_bucket_moves`; its reserve and ledger functions deliberately raise. It has no original opening producer, journal closure, audit catalog, or raw57 scope. Disabling those tripwires, seeding a bank balance directly, or replacing the raising helpers with success stubs would invalidate the existing contract and would not qualify funding.

The funded composition belongs behind this same maintained entry point, in a separately owned pristine database/cluster lifetime. It must not use the replay database as a template. Reuse the existing PG_BIN resolution, private endpoint, immutable evidence directory and explicit failure propagation; import the accepted funded connection/cleanup assertions without replacing the replay checks. Do not introduce a dispatcher, scheduler, native platform, or a second public runner. The current one-shot subprocess helper is insufficient for the accepted writer/observer transaction protocol.

## Retained source authority

These immutable packets remain the source; their independent reviews are reused. They are not installed dependencies of this repository.

| Packet | INTEGRITY.json SHA256 | Required role |
| --- | --- | --- |
| G8 `bbj-backup-source-0002` | `81d1be4c513e2e8c3764a1f70ed13b9fcaf0ef71ed33145dce4cf7ba16a23dac` | Original main100 to backup25 case and independent financial assertions |
| G8 `bbj-backup-supplemental-0003` | `33cfa2dda4240297c77b97b38cffdec28064022cee848970d2dc3428cca5943d` | Exact fixture support and corrected audit/sequence contract |
| G8 `bbj-backup-root-adapter-0002` | `9d559186b41451eaef2abafac31196bfa2b0feba4638669426845a6750c35476` | Original source normalization, connections, five combined requests and cleanup |
| G8 `bbj-promo-root-adapter-0001` | `91016ba6729d7ceba4ee5aa4d19f588f780ad54ff3e4e171acc39b42cb021516` | Same composition with Backlog0086 pool-promo25 case |
| Accounting `bbj-supplemental-sql-successor-0123` | `741c5f4ff9b8f50a52703079ee02d52f76c1d9a9300d748b7bfa97fbd642698f` | Genuine original opening producer and its assertions |
| Accounting `bbj-root-adapter-successor-0124` | `388c24a7c4e836b3b043aef94d7e5a946004ade5c5a8eab550033a4d443ba808` | Original accepted connection/normalization/cleanup source |
| Backlog `bbj-promo-case-source-0086` | `c624393210a3f723bc4f607f9a7552ffff2a6bfc84a592be697060c9e79429a8` | Original pool-promo25 case |

The original schema0006 build order is `00-roles.sql`, `10-historical-schema.sql`, `15-current-prerequisites.sql`, `30-current-functions.sql`, `20-current-schema.sql`, `40-replay-candidate.sql`, `50-current-attachments-acl.sql`, registry0010 `registry-overlay.sql`, and `90-historical-event-triggers.sql`. The original0124 normalizer and shared supplemental0003 follow. Resource seed0007, physical identity0008, catalog parser0009 and registry0010 remain required. The full schema's 327 original identities and 2957 expression edges cannot be replaced by the two-table replay schema. The supplemental opening adds 12 exact functions/six relation comparisons; backup support adds its original dependency set.

## Maintained binding and remaining admission

1. **Portable source closure, G8 fixture owner — prepared.** The selected exact SQL, metadata, seed, case, comparator and connection modules are now in repository-relative fixture custody, retaining original seals and both original 26-transformation records. The accepted backup adapter's old guard functions still traverse 683 historical pins and dynamically load siblings outside Git. Those unchanged guard implementations are retained source, not an executable current-route binding. `FundedSourceCustody.read_bytes`, `source_path`, `resolve_original_reference`, `group_path` and `ordered_sql` supply only the explicit local mapping. No implicit external-path fallback or rewriting of original source occurs.
2. **Runtime binding, original hosted accounting job.** The restored job is `accounting_postgres` on `ubuntu-latest`, with a15-minute limit and `/usr/lib/postgresql/17/bin`. This successor requires GitHub-hosted Linux X64 and current checkout/event/run/attempt/job identity. It removes the retired ARM runner names and binary hash requirements. Actual binary SHA256 and `--version` values are recorded per current invocation, checked unchanged while observed and before each case. The exact17.11 requirement and real `server_version_num=170011` / physical assertions remain. The hosted job installs major17 only; no saved proof here establishes that its current binaries satisfy17.11. No older provider receipt authorizes the current run; no provider installation was performed.
3. **Current route/operation binding, existing runner caller — source prepared, genuine timing producer awaits existing-job composition.** Use the existing directly invoked accounting CI transaction: exact repository/revision, workflow run ID, run attempt, job, fresh per-case UUID and exclusive evidence path. A constructor must verify that current identity and a pristine physical clone before case effects, durably record each original attempt/output and reject identity reuse. Original historical decisions/consumed authorities remain provenance; never rewrite an accepted-source decision with execution authority false to true. The exact remaining old guard/path functions are listed in `SOURCE-CLOSURE.json`. Replace only their obsolete admission/path layer in a reviewed successor while preserving each business/physical assertion. The original custody helper still supplies no run permit. The successor below replaces these legacy bindings only at the current existing-job caller boundary, without altering original metadata/SQL/case/comparator files.
4. **Exact producer provenance, G8 fixture owner with original Accounting custody.** The accepted opening definition has body MD5 `47980506016942eccb64c037322237de`; the maintained original opening migration has `fbe04a0069d42f71dec9a1b9fc990aea`. A bounded body comparison found only a two-line leaderboard comment changed to a one-line comment. This is an exact-source binding difference, not an observed economic defect. Preserve the accepted definition and record its literal provenance; do not silently substitute or normalize its body pin. The transfer function continues to come from the already merged migration `20260912070357_bbj_bank_move_replay_matches_payload.sql`.

All retained source packages were accessible during recovery. The concrete custody helper and source closure are prepared; the remaining item is their current CI caller/runtime composition, not a missing production-funding repair. Neither a custody receipt nor a source hash proves a financial case ran.

## Exact callback and evidence contract to preserve

The accepted adapters expose `execute_prepared_case(case, connect, seed, base_driver, authorization, combined=None, metadata_check=None, database=None, database_oid=None)`. Preserve the original assertions and implementations while changing only the reviewed path/runtime/admission binding:

| Input or stage | Required behavior |
| --- | --- |
| `connect(label, events)` | Original persistent Psql driver; register ownership before construction; validate database/OID, system identifier, data/socket directory, role, session role, origin replication, backend PID/start and server identity before use. |
| `seed()` | Exact seed0007, once in a fresh clone. Never manually seed the target BBJ balances or journal. |
| Opening producer | Invoke real `fn_complete_club_opening_setup` with a fresh opening UUID. The selected case needs its genuine opening100 as setup; this is not a replay or reacceptance of the previously completed standalone opening proof. |
| Selected transfer | Original `SEQ08_BBJ_BACKUP_POSITIVE_MAIN_TRANSFER_25` or `SEQ08_BBJ_PROMO_POSITIVE_MAIN_TRANSFER_25`, each in its own pristine clone with its own operation identity; main75/backup25/promo0 or main75/backup0/promo25 respectively. Pool promo is distinct from clubs promo. |
| `combined(phase)` | Exact original combined SQL in one real request per phase. Three opening plus two selected-transfer calls, total five; retain actual exit/stdout/stderr/command index and failed requests. |
| `metadata_check(connection)` | Complete original identities/edges/registry and supplemental metadata before/after, full raw57 noninterference, 53 fingerprints, physical xmin and original counteraccount/alias qualifications. |
| Shared supplement | Preserve `install(conn, log)`, `collect(conn, stage)`, `capture_audit(conn, report)` and their original error reports. Root owns connections and transaction lifecycle. |
| Audit install | Original audit/autorevoke enabled; exact source-derived 23 DDL dispatches/25 audit records. Exact sequence configuration, bounded contiguous audit IDs, and unchanged unrelated financial sequence state/raw57. No blanket audit-sequence exclusion, reset, logger disable, drift-derived oracle or retry. |
| Failure | Preserve unknown commit outcomes; fresh independently validated connection attempts metadata, raw57 and bounded audit capture independently. Sequence increments can survive SQL rollback; neither rollback nor a missing response proves unchanged sequence or cancelled financial effects. |
| Cleanup | Every owned partial/complete connection receives independent rollback/reset/unlock/close attempts; prove clone identity before disposal. Preserve original physical shutdown/removal and emergency evidence behavior. Uncertain cleanup must fail and retain evidence. |

## Finite acceptance checklist

- The original replay try/except/finally body remains AST-identical. Migration, replay fixture/cases, all87 retained original inputs and original26 transformations remain exact; replay results are never labeled funded qualification.
- Prepared portable custody includes the selected directly consumed source/metadata inputs and preserves original seed, SQL, comparators, case identity and cleanup. The remaining legacy guard/import replacements must consume only its explicit mapping; external developer paths must not become executable authority.
- Final source/runtime/admission binding is independently reviewed only for its changed composition; accepted historical reviews and actual receipts remain unchanged.
- Existing supplemental audit tests, adapter connection/failure tests and promo composition checks are retained in the same existing verification path, and actually run for the final bytes.
- Queued existing accounting CI proves unchanged 19-case replay, then each new funded selected case with the full original preflight, writer/observer, reconciliation, audit and cleanup evidence.
- Exact runtime and final commit/run artifacts are retained. A missing, failed or unrun stage rejects the funded qualification; no source inspection counts as an executed check.

No new financial execution, database connection, DDL, package installation or heavy test was performed to prepare this interface. Original BBJ opening100 and clubs-promo opening100 receipts remain preserved. Production delivery and the wider G5 foundation remain separate gates.

## Prepared portable caller source

The sole financial entry point remains `scripts/ci/test-bbj-bank-replay.py`. It finishes the original19-case replay and cleanup before loading `funded/caller.py`. A preceding invocation of `funded/current_ci.py --write-job-timing` reads only current Actions job metadata; it cannot run a financial case. No new job, schedule, financial runner, production SQL or application source is added.

- `current_ci.py` requires the current repository, actual checked-out commit, matching PR source ancestry (or the existing scheduled-main path), workflow run ID, attempt, accounting job and original GitHub-hosted Linux X64 accounting job. It records an exclusive invocation and per-case UUID/operation receipt. It rejects historical approval objects, reused case identity, mutated authorization, repeated seed and repeated financial dispatch. It records fresh actual hosted binary hashes, requires unchanged exact PostgreSQL17.11 versions and actual `server_version_num=170011`; no Mac-specific cluster path is used.
- `retained.py` imports exact custody members and binds only the obsolete path/import/admission functions. It constructs a finite source map rather than rewriting historical policy hashes or accepted=false records. One explicitly recorded normalizer substitution replaces its old absolute SOURCE-PINS lookup. The genuine financial case bodies, SQL, independent comparators, driver, audit contract and cleanup source files remain unchanged.
- `execution.py` contains the retained original adapter lifetime with13 recorded portable/current-operation substitutions. `CALLER-TRANSFORMS.json` enumerates every substitution. Original26 derivation transforms remain intact; reversing them still recovers exact0124. The original physical identity/inactivity predicates remain unchanged; explicit `DEADLINE-TRANSFORMS.json` substitutions now bound its wait/removal operations. Each selected case has its own fresh cluster, empty full-schema base, clone, operation UUIDs, writer/observer ownership, five combined requests, audit checks, metadata/raw57 postflight and cleanup. A first failed or uncertain case prevents the next case.
- `checks.py` retains the existing constructor/cleanup/failure, audit-reader, audit-sequence, promo-order/uncertainty and supplemental protocol/model probes. Six additional original test/metadata members are pinned to their existing seals in `regressions/SOURCES.json`. Obsolete historical683-input/admission checks remain preserved provenance; current-route rejection tests replace their execution authority. The supplemental collector uses only this invocation's freshly validated base responses instead of copying a historical native receipt. The five modeled regression groups run once per unchanged invocation and their exact retained receipt is reused for the second case. Native metadata checks still run for both cases.
- `test_current_ci.py` retains the19 original identity/deadline controls and adds7 targeted statement/interrupt/output/timing controls. The existing caller explicitly requires all26 to execute with no failures, errors, skips, expected failures or unexpected successes. Original six custody tests remain unchanged. Modeled tests never qualify financial behavior.

The new code preserves explicit unknown outcomes. ATTEMPT, CLONE, SEED-ATTEMPT and phase-attempt files are exclusive and fsynced. The original persistent driver retains SQL attempts/responses; original RUN/case/postflight files, emergency storage reserve and physical cleanup remain. Source custody continues to report runtime/financial authority false. A future actual funded result may report only the two selected cases after their own final evidence and cleanup pass; whole original cases, the foundation and production remain unqualified by this fixture.

## Exact unfinished route input

`current_ci.py --write-job-timing "$RUNNER_TEMP/bbj-job-timing-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT.json"` is invoked once immediately before the existing BBJ step. Only this metadata step receives `GH_TOKEN` from the current job token, with job permissions `contents: read` and `actions: read`. The BBJ step receives only `BBJ_JOB_TIMING_FILE` pointing to that exact path, never the token. This matters because the unchanged original replay creates its private cluster before the funded continuation. Funded child environments additionally exclude both token names.

The producer makes one bounded read of `/repos/Smarter-Poker/Smarter-Poker-Club-Arena/actions/runs/{run_id}/attempts/{attempt}/jobs?per_page=100`. It refuses redirects, incomplete/ambiguous responses, a different checkout/run/runner/job, unavailable actual UTC start, insufficient remaining time, and an existing output. The actual start comes from the selected running job, not invocation time. It preserves the job's900-second limit, writes the observation exclusively, and logs no token, HTTP headers or error-response body. There is no retry. The current caller revalidates identity, path, actual job fields and timing before copying the observation into its own immutable receipt.

Finite first-qualification ceilings are300 seconds of aggregate case execution (the original setup cap) and90 seconds of aggregate cleanup (three original30-second physical-operation caps). They are operational refusal limits, not measured funded runtimes. Every original per-command maximum is still an upper bound and the remaining aggregate allowance can shorten it. Client rollback/reset/unlock/close, helper reap, ordinary database disposal, physical probes, directory removal and evidence flush share the same90-second reserve. They are not each granted90 seconds. A late/uncertain first result cannot be retried. Each fresh case requires at least390 actual job seconds remaining and qualification of its predecessor; neither full envelope is assumed to fit automatically.

Historical positive main100 and clubs-promo100 server lifetimes were10.096 and10.150 seconds on macOS, with0.002-second immediate shutdown observations. Those are useful context, not complete setup/cleanup measurements or proof of these two hosted routes. Absence of a prior full funded timing measurement is not an approval gate for the first finite qualification. Actual current hosted output must establish completion or preserve failure/uncertainty.

Every persistent statement now owns one original60-second absolute deadline across its write, flush and all buffered reads. The unchanged driver's method/class identity remains exact, and source continuity rejects a retained SQL call that overrides the default. Buffered output cannot renew the original loop's `.001` timeout floor. A late communicate return retains already observed stdout/stderr/exit while its outcome stays uncertain.

Final base postflight, owned clients, helper reap, physical shutdown/proof, reserve release and evidence each receive an independent bounded attempt. A KeyboardInterrupt/SystemExit in one stage is retained with its exception context and cannot skip the later stages. This does not certify cleanup when its reserve is depleted; unknown directories remain preserved and final failure evidence falls back to the owning log if the bounded write is refused. Original physical inactivity assertions and the original26 financial transforms remain unchanged;13 portable and14 deadline transformations describe the executable body.

All26 modeled controls passed with network, process and timer effects mocked. Two new regressions fail on frozen0002 and pass with the repair; the first full modeled run's canonical-path fixture error remains retained. These checks do not qualify hosted runtime, financial SQL or production behavior. No timing producer API call or financial test was executed during source preparation.

Required final gates: independently review the combined changed source; compose the exact preceding metadata step, existing classifier and artifact retention; run original custody/replay,26 current-route modeled controls, retained modeled financial groups and both fresh funded lifetimes through current required hosted CI; retain actual runtime17.11, operation, audit, reconciliation and cleanup evidence. Publication and production financial acceptance remain separate.
