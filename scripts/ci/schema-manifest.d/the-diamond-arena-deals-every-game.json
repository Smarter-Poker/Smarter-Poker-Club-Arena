{
  "_owner": "agent/claude-diamond-phase-7/feature/diamond-deals-every-game. Phase 7 checklist line one. The Diamond Arena dealt one game; the chip cash create screen offers nine, and a one to one clone deals the same nine. Migration 20260912070000, applied once to kuklfnapbkmacvwxktbh, adds fn_poker_diamond_cash_variant as the ONLY place SQL names them (DIAMOND_CASH_VARIANTS is the only place TypeScript does, and tests/a-diamond-table-has-one-shape.law.test.ts holds the two together and derives the TypeScript list from src/config/cashGames.ts so the arena cannot fall behind the chip screen). The plain-cash rule and the hand settler read it; the settler's game is not a feature flag but which table this is, so widening it is what stops a PLO hand the arena just dealt from being unable to settle. fn_poker_diamond_open_cash_table gains a p_game_variant parameter defaulting to nlh: its six-argument signature is DROPPED rather than replaced, because a default cannot be added by CREATE OR REPLACE without leaving an overload behind and an ambiguous staff door is worse than a missing one. The door also now proves the row it wrote would be admitted before returning it. Safe because every place a pot is divided was already unit-aware, the hi-lo split included, which is the only new divider the nine games reach. The predicate below is the only new object; fn_poker_diamond_open_cash_table already existed under a shorter signature.",
  "functions": ["fn_poker_diamond_cash_variant"]
}
