{
  "_owner": "fix/bbj-the-rules-page-and-the-engine-agree. 20260911210458: bbj_pools.mini_reserve_floor's column DEFAULT was the hard literal 5000.00; fn_bbj_default_mini_floor() returns three times the largest ENABLED mini tier, never below 5000, so the floor cannot silently stop covering a bad day when the tiers are retuned. 20260911214135: phase 3's mini switch and reserve floor were keyed on a club and both refuse a club inside a union, and no fn_bbj_set_union_mini_* existed at all, so the larger pool on this platform (a union pool) had neither control; these two are the union's, authorized with fn_is_union_operator",
  "functions": [
    "fn_bbj_default_mini_floor",
    "fn_bbj_set_union_mini_enabled",
    "fn_bbj_set_union_mini_floor"
  ]
}
