# MTT structure creation boundary

`python3 scripts/dev/probe-mtt-blind-contract-pg17.py` creates and removes its own PostgreSQL 17 cluster and accepts no database URL. `baseline.json` captures both current creation function bodies, the real tournament column types, and 23 distinct active MTT structure/stack combinations observed at 2026-09-14T00:53:21 UTC. It contains no participant rows or credentials. Function body hashes are verified before installation.

The original two-stage creator accepts a zero big blind, an unreadable duration, and a string break flag, then incorrectly leaves a four-minute event classified as standard. The new common INSERT/changed-structure trigger refuses these shapes and derives speed from the actual opening clock. The probe executes both unchanged creators, verifies selected paid depth survives, checks their original authority metadata, replays installation, and tests that malformed writes and format-conversion bypasses roll back. Historical rows and unrelated progress writes remain unchanged. Short formats retain their existing authorities. Anonymous callers cannot invoke the new internal functions, and the trigger has its required declaration.

The same 59 vectors execute through the engine validator, client saved-schedule payload builder, and PostgreSQL. Explicit break markers, optional zero small blind for short-deck, fractional blinds, positive clock aliases, decimal strings and existing presets remain valid. New malformed strings, absent/zero big blinds, negative antes, decreasing blinds, first-row breaks and non-positive starting stacks are refused. Existing advertised stacks and durations are not standardized by rewriting them. Speed and starting depth remain separate.

Limits: native authentication and club access use explicit stand-ins. The native fixture installs actual column types and both actual creators, but not the complete financial, management, RLS or trigger graph. This is a creation/structure contract proof, not a funded tournament, dealer, break, payout, HTTP or production certificate. Existing embedded break markers still follow the engine's synchronized break policy; this change does not introduce per-level custom breaks.

Final11groups/59vectors pass at `/var/folders/xm/cl1_6q2j51lg55xmmk7dlrzc0000gn/T/ca-bc-stcji1ja/results.json`; cleanup verified. Installed01:03UTC/history20260914010322 with matching function hashes and original creator authority. Live default privileges retain service_role execution; anonymous/authenticated access to the new internal helpers is denied. RequiredCI and served engine/client adoption remain separate.
