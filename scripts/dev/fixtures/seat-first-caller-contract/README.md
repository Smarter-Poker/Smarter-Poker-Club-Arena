# Current seat-first creator and actual engine payload qualification

Captured2026-09-14T01:19:43.003881Z from production, read-only. `baseline.json` contains the unchanged full `fn_create_seat_first_game_atomic(uuid,jsonb)` definition and274actual tournament/table column names and types. BodyMD5b40dd95b7a87019070a8abf0fcc4fff3 is verified by the engine test and native runner. Live ownerpostgres, SECURITY DEFINER, search_pathpublic,pg_temp, statement_timeout30s; service execution allowed and anon/authenticated denied, verified01:23:41.854530Z.

`actual-caller-payloads.json` was captured by executing actual `createSNG`, `createSatelliteHeadsUp`, `createSpin` and their shared atomic-RPC caller for club/union ownership and300/1000stacks. The normal test compares every generated field to these exact native inputs, excluding the changing start timestamp. Test transport enforces keys extracted from the captured SQL rather than granting every mock RPC success. Horse seeding and player funding are mocked and are not certified.

`probe-seat-first-caller-contract-pg17.py` creates a private PostgreSQL17cluster with no network listener. It loads the actual current SQL unchanged, using captured column types plus explicit synthetic defaults. Purchase freeze is a controlled stand-in; the production trigger graph, club/membership/target authorization, full financial schema, JWT/HTTP, funding and dealer play are not installed or certified. This does not replace the existing full Stage-B seat-first/satellite fixtures.

Native result: eight assertion groupsPASS, twelve committed pairs/exact replays, eight oldpayloadunknown-key refusals/no writes, wrongtarget replay refusal, freeze/ACL refusal, and late-table failure rolling back both rows. Cluster stop and removal verified. `qualification-results.json` records inputs and limits. Preserved log/result directory: /var/folders/xm/cl1_6q2j51lg55xmmk7dlrzc0000gn/T/ca-sf-ohcxccnz.

Re-run engine generation only when intentionally changing a reviewed caller contract: setCA_SEAT_FIRST_CAPTURE_PATH to this fixture's absolute actual-caller-payloads.json and run server/src/tournament/SeatFirstCallerContract.test.ts fromserver. Then run the native script against the newly captured inputs; do not claim old native evidence applies to changed input bytes. NormalCI compares current caller payloads to the committed native inputs without rewriting files.
