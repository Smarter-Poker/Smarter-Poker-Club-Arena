# Cash pot check evidence — source qualification packet

**SOURCE ONLY. UNRUN. NOT INSTALLED. NOT AN ACTIVATION BUNDLE.** No SQL fixture, syntax check, test, build or installer has been executed for this packet. Source base is 0b6ba0aad500082f8138ca66dfe3c0538bda1f33 in the parent-owned operational-alert-monitoring-worktree. The eight owned paths are listed in cash-pot-check-evidence.manifest.json. The parent owns the separate failed-run intake and source custody.

This addresses the demonstrated evidence-loss mechanism behind received inbox40538, original financial UUID33f6a110-4591-4ce7-a249-6311e0584f9b, at 2026-09-06T14:34:00.384087Z. Its 600-chip/no_winner_recorded context has no hand/table/sample identity. The actual historical hand, recipient and cause remain **unknown**. The literal original is retained in original-40538.sql. Synthetic c40538… fixture hands are explicitly new test identities, never attribution of that historical600. A later clean retained window is not its repair.

## Candidate and authority

The guarded component is outside automatic migrations. It replaces only public.fn_cash_pot_conservation_check(integer), retains postgres SECURITY DEFINER, the exact existing service-role EXECUTE ACL and search_path, and creates:

- public.ca_cash_pot_check_evidence: one immutable complete positive check with exact result/window/link metadata, complete narrow anomalies array, counts and byte totals.
- public.ca_cash_pot_check_anomalies: a security-invoker read-only typed view exposing every admitted anomaly's16 columns, including UUID, nullable table/hand number, timestamp, recorded amounts, shape/count, residuals and per-element byte count. No raw cards, player identity list or raw winner arrays.
- public.fn_ca_cash_pot_evidence_append_only(): reconciliation and append-only guards. Service role can read; ordinary roles cannot read/write; only the postgres-owned checker writes.

Captured full pg_get_functiondef MD5 is 484ca087624199de5a28678a2799ff75. Candidate full-definition source MD5 is a7080e4bdd9a513c2f11c67c51b5d1a6; guard is 8b1f89fa4c293c62e1df59937783d567. These are source hashes, not installed postimage receipts. The native owner must capture the actual normalized pg_get_functiondef, full authority and owned relation catalog after CREATE and compare exact bytes.

Dependency full definitions are pinned: financial writer264d32bcba8f6430ea68b7ca9738fbc8; queue writer36601e205494e8768f5a1dce09f4a186; financial incident bridge00a43ae03ab12cec9505e2bfed71d937; guard watchlist92ee208d0887728444bda396d0b4d442. The install/rollback check exact authority, consumed columns/defaults, queue constraints/identities, ready/valid indexes, and target guard mode. Owned relation replay compares independently specified temporary model schemas, never LIKE a possibly altered live relation, including attributes/defaults, keys/checks, indexes, policies and triggers; the view additionally pins object kind, security-invoker option, ACL, all typed columns and exact definition. No source table, queue writer, financial writer, schedule or historical migration is altered.

## Observation and receipt contract

The original hours clamp1..48, now()-hours inclusive lower bound, cash-only/pot-positive predicate and lack of an upper bound stay unchanged. One cursor SELECT owns the hand snapshot across all batches. Counts cover **all selected hands**; only positive narrow anomalies are stored. A second source query never reconstructs a different snapshot. Detection predicates, original amounts/rounding, condition order, financial message/context/severity/dedupe and legacy return JSON remain unchanged.

The source cursor buffers only admitted narrow row objects in a jsonb[] using array_append, then converts that bounded buffer once. It never aggregates unbounded source history or repeatedly concatenates a growing JSONB array. One complete immutable row is inserted after validation of all element types/keys, hand uniqueness, contiguous ordinals, predicates, arithmetic, counts and bytes. There is no provisional header, physical detail table or FK. A complete committed header has no omitted stored anomalies. Operational receipts are appended in the same transaction only after reconciliation, and exact returned ID/source/key/alertname/status/severity/payload are read back. A positive numeric receipt ID alone is insufficient.

Queue source is cash-pot-conservation-measurement; key is server-generated check UUID plus ":" plus condition kind. Alertname is CashPotConservation:kind, status firing, original condition severity critical or warning. Every receipt binds target_task_id01a09b86-5ba8-7290-8657-1041f13dd3ca. The financial UUID is exactly the existing writer's return: it may name an unchanged old unresolved row. NULL is explicitly rpc_returned_null, not a made-up financial identity or a successful new financial insert.

Preview order is **created_at,hand_id**, at most20 rows, additionally limited by32KiB total envelope bytes. Preview omission does not discard stored rows. Complete-store readers paginate by check_id/ordinal and reconcile all header counts/amounts/bytes before claiming completeness. The captured serialization timezone is necessary for faithful timestamp byte reconstruction. The queue itself already limits payload to256KiB; this source's32KiB limit is tighter.

Healthy checks return their original JSON and create no header/detail/financial/queue event. This component creates no automatic recovery or queue-status closure. It moves no money and proves no actual payment from recorded winner arithmetic.

## Bounds and failure semantics

Proposed admission bounds are10000 anomalous rows and8388608 UTF-8 bytes of actual complete anomalies array JSON text plus exact header summary JSON text. detail_bytes now counts the full stored array, including every element's evidence_bytes field, commas and brackets; header_bytes counts summary text. Individual element evidence_bytes still excludes its own byte field. The accumulating array estimate is compared with actual final serialization, and the generated total must equal both exact stored byte counts. These semantics supersede the earlier uninstalled two-table approximation. These are logical evidence bounds, **not a demonstrated backend memory/WAL/latency cap**.

Overflow raises SQLSTATE54000 with primary message cash_pot_check_evidence_limit and bounded JSON DETAIL carrying actual invocation UUID, limit_kind, maximum, observed_lower_bound and partial:true. It aborts the entire check, including any financial alert insertion, typed detail, header and queue row. It never returns invented complete totals. Invalid source numeric values and receipt failures likewise remain errors. If an outer caller later aborts, all same-transaction observations roll back. INSERT-then-RAISE is not durable failure intake.

This positive component **must not be installed alone**. The separately owned cash-pot-failed-run-intake reader must be source-bound, natively co-qualified, independently scheduled and observed before activation, retaining actual cron259 failed-run identity/error in a separate successful transaction. No reader may claim that an internal UUID, SQLSTATE or partial identity set survived unless present in the actual retained error. No healthy queue flood, silent failed-run filter or generic writer change is part of this packet.

The8MiB/10000 caps remain UNQUALIFIED until the protected native owner measures the exact candidate under representative full48h selected cohorts, worst admitted winner-array/numeric inputs, simultaneous scheduled load and failure paths. The120s/5s fixture statement/lock limits are local fixture safeguards, not production-performance qualification; the concurrency fixture uses15s lock bounds. The owner must admit and bind CPU, peak RSS, relation/WAL/temp bytes, disk floor, external wall deadline, bounded logs and complete actual cleanup. Source logical caps may need tightening after measurement.

## Constraint compatibility and bounded caller evidence

The earlier uninstalled two-table draft depended on a deferred FK and failed ALL IMMEDIATE callers. This revision removes that dependency entirely. It never issues SET CONSTRAINTS. The source fixture requires baseline/candidate success equality under ALL IMMEDIATE and verifies an unrelated deferrable sentinel remains immediate afterwards. Those cases remain UNRUN; source reasoning is not native caller-parity proof.

Read-only inventory0322 found the exact installed baseline and one literal live caller, cron259. Retained statistics show its execution shape20 times (queryid7631287357514282263), but59 deallocations sinceSep10 prevent historical completeness. Five source roots contain no current literal runtime caller;363 registered CA worktrees yielded358 GameServer files, none with the former cash/heartbeat call. Three local historical commits preserve a former GameServer Supabase RPC caller. The health registry does not dynamically execute its names. No installed ALL IMMEDIATE body/cron match was found; the only named constraint switch affects a different Diamond constraint. These are bounded findings, not a complete inventory of external/service/operator or dynamic callers.

Neither candidate relation exists in the read-only production capture. The new installer accepts only absent new storage with the exact baseline or its own exact new postimage. An unexpected old candidate detail TABLE is refused, never silently converted or dropped. No historical evidence migration is authorized. The caller evidence and approved impact proposal are pinned under helper-alert-intake-cash-40538-callers-\*-0322 in the operational-alert-routing evidence directory.

## Native source cases and limits

cash-pot-check-evidence.sql is an unexecuted psql source. It requires a fresh local PG17 database named qual*cash*<approved UUID without dashes>, postgres authority, the three preprovided roles and an explicit approved_execution_uuid input. Those public-input guards do not authenticate approval. The protected executor independently supplies approved execution identity, source/provider closure and allocation. No production secrets/network, package install or direct runner fallback is authorized.

The fixture creates a narrow schema model and actual captured function definitions. It compares the candidate with the literal baseline body (only function namespace/name changed); baseline financial writes roll back via a private sentinel. Cases cover the literal original, synthetic21/20 preview retention, exact legacy result and original-row equality, header/byte reconciliation, append-only and role guards, immediate-constraint success parity and unchanged sentinel mode, hours clamps, threshold/jackpot/exclusion/JSON cases, signed overaward, lower-inclusive/future timestamps, healthy silence, non-UTC serialization, invalid numeric rollback, financial rate-cap NULL,10000 versus10001 rows, an oversized numeric-scale8MiB runtime failure, guard-only exact8388608/8388609 storage edges, duplicate hand/ordinal/missing-key/count rejection, false/missing/colliding receipts, schema/ACL/dependency/view-kind/view-security/view-filter drift refusals, exact retaining rollback and source replay.

The byte-overflow fixture exercises the real candidate with narrow numeric fields, not a mocked cap. A separate native guard-only copy case pads summary text to exactly8388608 and8388609 stored bytes; it does not claim that the normal checker generates that padded summary. Both remain UNRUN. Neither case establishes worst-case total source allocation, all arithmetic extremes or a performance budget. Those remain explicit native admission/qualification needs. Schema drift cases are representative; the complete catalog comparison must itself be read back under PG17 and all installed trigger dependencies must be composed.

The captured financial-to-incident function is present for authority checks in the narrow model, but the full production financial trigger graph is not attached. This is deliberately not claimed as integration coverage. Complete-bundle qualification must compose the actual full trigger/caller graph and prove warning/critical behavior, all financial-row/ledger invariants, queue receipt ownership and failure intake. The candidate also inherits the original financial writer's concurrent dedupe behavior; it does not claim to repair that separate writer.

cash-pot-check-evidence-concurrency.spec is unexecuted isolationtester source. Prepare and COMMIT a separate fresh admitted allocation from the exact guarded preimage fixture, literal40538 fixture and component. Set the same approved execution GUC on every backend. The spec verifies the actual candidate/guard source MD5 before adding a clearly identified fixture-only source view with a volatile numeric observer. The observer returns the unchanged source pot while waiting on the owned advisory lock inside the source SELECT; the checker body remains pinned. This source-view distinction does not qualify the production table plan or performance. It uses one permutation: an observed source-snapshot advisory wait; competing updates/insertion after the source cursor opened; all100 original snapshot identities across fetch batches; no outside visibility before commit; exact receipt after commit; then two overlapping invocations retaining distinct UUIDs beside the unchanged original financial row. The owner must retain native step/wait output and actual cross-backend assertions. This is not a restart/WAL/power-loss proof.

The primary fixture rolls its complete allocation transaction back. The concurrency case intentionally leaves committed evidence for export; no evidence is deleted in its teardown. The owner must stop and observe every backend/container, preserve results and destroy only the exact disposable allocation. Neither final SQL output nor isolationtester completion is a cleanup receipt. Transport loss, whole-database restart recovery, actual execution of the exact8MiB edge, bounded caller-mode qualification, full bridge composition, failed-cron scheduling/intake, representative capacity and production deployment remain unproved.

## Installation, replay and rollback

Installation requires independently reviewed complete bundle admission, exclusive protected DDL lifecycle, current exact preimage/authority/schema/dependencies, and all release blockers resolved. The component performs no checker call and no schedule activation. It accepts exact baseline with absent new storage, exact candidate with complete storage, or retained complete storage after rollback. Partial/unknown storage, authority/body/schema drift or missing evidence under an installed candidate is refused. Replaying an exact known install validates the whole owned schema and does not delete evidence.

Rollback is executable guarded source in cash-pot-check-evidence.rollback.sql. It pins the same exact candidate/baseline, authority, dependencies and complete schema/trigger postimage, restores only the captured checker definition, and retains the immutable complete record table, typed read-only view, all rows, policies and guards. It never drops audit evidence or rewrites financial/operational history. Known baseline replay with complete retained storage is accepted; other drift fails closed.

Native rollback qualification must capture the actual complete installed table/view/guard postimage before rollback and compare retained complete header/typed-view/queue rows afterwards, then verify a source replay. Calls already executing at rollback may finish against their loaded candidate; protected lifecycle admission must account for in-flight callers. Rollback does not cancel them or erase their eventual receipts. No data retention/archival deletion policy is invented here.

Buffer growth/copies, final array conversion, repeated guard expansion and typed-view pagination require measured native admission;8MiB logical text is not a memory ceiling. A source manifest, passing narrow tests, or restored body alone cannot establish runtime admission, native caller parity, scheduled failure intake, publication, live delivery or repair of original40538. All such receipts are absent.
