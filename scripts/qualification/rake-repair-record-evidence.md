> September 17 delivery-1 replay proposal: the retained packet below is historical source custody, not an execution receipt or active runner instruction. Current Documents/AGENTS.md controls execution. Qualification uses the existing GitHub-hosted PG17 accounting job; no retired local/native service is permitted. The finite driver `scripts/ci/test-alert-evidence-postgres.py` is proposed and unrun. Original financial evidence and every assertion remain retained.

# Rake recovery record reporting — source-only handoff

Status: **UNRUN, locally committed source only, uninstalled, unpublished.** Exactly one runtime message expression changes in fn_rake_repair_unbanked(integer,integer). Guarded components remain outside automatic migrations. No tests, syntax checks, builds, installations, financial calls or original/status mutations were executed.

## Proven defect and exact scope

Received40522/originala3760f99-e341-4328-9407-322a5fa8dd30 says62 unbanked hands/85.48 were recovered because an engine died. All62 repaired record UUIDs and hand UUIDs are retained, but their124 bank/accumulator claims predate that repair. Exact treasury claim times are10:00:05.567519–11:01:09.536881 versus record creation17:52:02.704400Z. No new bank claim at the repair timestamp exists. Historical chip-ledger rows were unavailable at those exact timestamps; this does not prove the earlier balance movement or an outstanding loss.

The producer counts every nonthrowing atomic call. Atomic applied=true means record insertion, while separate claim guards determine whether bank credit is new. Therefore the unchanged counter cannot truthfully be called either newly credited money or always newly created records; concurrent already-processed replies also count. The new text says completed missing-rake-record recovery calls covering recorded hand-rake chips, explicitly disclaims measuring newly credited funds, and says contributions were not supplied by this recovery and the interruption cause is unverified.

Only that expression changes. Selection/lookbacks/pending exclusions/order/limit, NULL financial arguments, exception/retry behavior, return rows, counters, chips rounding, threshold50, severity/status/resolved_at, financial INSERT count, source and legacy repaired/chips keys stay exact. No attribution is synthesized. No old message is rewritten. The six old recovery reports remain individual historical evidence.

Original message identity compatibility is preserved through the actual intake contract: source financial-alerts-backfill/event_key=financial UUID; status reads resolved, not the word Recovered. Updates use original UUID plus full-row snapshot hash. The component only changes future inserted messages, so it does not rekey original rows or make historical warning records resolved. Current CA/WH source searches found no literal consumer of this rake-repair message/source; the nearby BBJ self-heal message is a different producer and is untouched. This bounded search is not a claim about arbitrary unknown external consumers.

## Exact custody and guards

Implementation base remains b5ff6bd3ad8ead65f8bd621382ed712aec1ace8f. Parent separately committed the frozen two-wording predecessor while this component was written; that aggregate custody is separate. Parent also owns sibling Spin component integration. No branch change or commit by this helper.

Current installed body MD5:3d2ae7b1afa7510d91057d9ceca9c9b6.
Exact source-derived postimage MD5:825c2564f326b54eb16e7f90303aa453.
Atomic dependency MD5:1b9bc9a006f45908bf65eb70a12e5f00.
Full pre/post/atomic bytes are retained in fixtures/rake-repair-record-evidence/images.json. These hashes have not been verified through native execution.

Install and rollback require exact reversible whole bodies, single anchor, postgres SECURITY DEFINER, public path/540s/service-only target authority, exact atomic body/public/30s/service authority, current watchlist definition and explicitly absent repair baseline. They also pin financial UUID default/PK and original source/severity/message/context types, unique hand record identity and unique bank-leg identity. Unexpected drift refuses the single atomic DO. Exact install/replay/rollback/reinstall is supported; no guard baseline/history is changed because the target is explicitly unwatchlisted. Future watchlist or composed-source changes require separately reviewed custody, not a hash fallback.

Inputs are saved in ops/evidence/helper-alert-sources-unbanked-40508-40527-manifest.json, source-proposal.md, canonical-hand-links.json and originals.json; fresh authority is rake-repair-record-authority-0240.json (actual DB time2026-09-15T02:27:32.666724Z). Intake identity comes from reconcile-financial-alert-history.sql, read-only source review only.

## Qualification source and honest limit

The native script requires an empty protected private PostgreSQL17 allocation, approved `qualification.execution_uuid`, DBname `qual_repair_<uuid-without-hyphens>`, local-only server, postgres fixture owner and existing API roles. The native owner must additionally guarantee no production credentials or outbound network, an external absolute deadline and independent cleanup. The caller preserves its540s function setting, so statement_timeout15s alone is not a whole-job deadline.

Actual installed atomic bytes are loaded only for dependency/install guards. **Actual atomic banking is never executed.** Functional before/after cases explicitly substitute a private atomic spy with the same signature to distinguish record receipt, bank-claim model and no-op/throw outcomes; the exact atomic definition is restored before every component/rollback. Role tests clear model hand candidates before calling the real caller with the actual atomic definition installed.

The six cases are:

- 62 exact40522 hand/record/table/amount/claim identities, with all124 prior claims. Original missing hand pot and created_at are unavailable, so pot100 and now-minus-one-hour are clearly synthetic eligibility inputs. This is a model of caller measurement, not reconstruction of original settlement.
- Fresh50 and50.01 preserve info/resolved versus warning/open thresholds.
- An already_processed=false-applied reply still counts one completed call.
- A throwing reply stays uncompleted, creates no success alert, and leaves retry eligibility.
- An existing record and pending fee remain excluded without any spy call.

Before/after assertions compare exact caller arguments, returned hand rows, counters, independent model claims/bank moves, source, context, severity, status/time and original UUID/payload. The original40522 financial row is copied as immutable fixture input and must remain identical. Sequence evidence distinguishes a thrown attempt from no attempt despite inner transaction rollback. Canonical postimage, authority,11 missing/drift refusal cases, API roles, install replay, refused rollback, exact rollback/replay/reinstall and fatal cleanup checks are included.

This qualifies only the actual reporting caller against controlled receipts. It does not qualify real banking, full-schema trigger behavior, real concurrent races, player attribution, historical balance movement, remote cancellation or provider delivery. None of these tests has run. No success count is claimed.

## Remaining acceptance

Parent review/freeze/custody → separately qualified exact component composition → admitted private native qualification with full logs and cleanup → protected publication and exact served body/ACL/index readback → natural future repair message with unchanged durable UUID/status contract. No financial repair, repayment, reclassification or historical closure is authorized by this component.
