# Spin expiry lock order — protected qualification contract

**Current release scope, September16:** FIFO inbox5 expiry lock-order repair only. Selected catalog qualification passed original execution `0045906d-4869-412c-b4de-9ab84176945b`:17 refusal cases, install/replay/rollback/replay, exact catalog and public-schema-ACL restoration, original allocation cleanup and stable source. Final result SHA256 `c1d80aca2eb6981ee2ee8741c3cc4b38a882b7d745ab20e1eea0a7273d995fe8`. Financial/race checks, production installation and live acceptance remain pending. The original scalar22 does not identify22 tournaments, and this source defect has not been attributed to an observed historical deadlock.

The existing required `accounting_postgres` CI job is the first actual financial execution route. It must fail closed on the exact checkout and versioned fixture inputs and preserve original failures. Use two isolated PostgreSQL17 allocations: preimage/order negative control; then candidate/order, candidate/timeout and committed-refund last. Each rollback case must prove exact selected business/catalog restoration and original client/backend cleanup before the candidate fixture is reused. One database execution identity remains honest across explicitly named cases; a failure or uncertain commit stops the allocation without retry. Preserve each maintained20-second schedule plus5-second cleanup bound and the outer240-second work plus30-second cleanup bound.

The real funded fixture uses authentic captured entry functions, genuine Mint, authenticated owner-to-player bank transfers, the real creator and exactly two authenticated paid seats. Three zero-valued synthetic principals are logically restored before user triggers attach; all subsequent business writes use current real triggers. An explicitly authorized isolated positive1-minute policy is legal under the captured constraint; seats age naturally for65 seconds after setup commits. Never backdate seats, inject balances, fake journals, disable guards or test on production games. Captured store policies include the actual `prize_liability` treatment. Retain request identities, issuance/transfer/payment receipts and refund/escrow/ledger conservation evidence.

The older broader DDL/race/parity proposals below are retained history, not a new mandatory qualification platform or permission to expand this finite release. Reuse accepted unchanged parity evidence. Do not run a separate manual catalog duplicate. Retain that unchanged catalog qualifier once inside the candidate allocation of required CI, before candidate installation, so future qualifier and rollback edits remain enforced.

Historical source custody began at served-source baseline365642c61a4e59eb96e6f4779c31f10c2ebd231c; the parent's three wording files advanced that branch to49aedeb7d0f5603549405cae21a62a4dcde2ef01. The maintained payload for this delivery is donor266ae2218d02a9f966a9753e94415bbf1cadd026. The hosted adaptation changes R1/R2 transport and its declared source hashes, not financial assertions. No old CPU, historical-seat or other owner's runtime files are imported.

## Hosted execution and fixture provenance

`scripts/ci/test-spin-expiry-postgres.py` is invoked directly by the existing GitHub-hosted `accounting_postgres` job using its installed PostgreSQL17 tools. It owns two fresh disposable clusters and uses only each allocation's private mode0700 Unix-socket directory, logical port5432, with TCP listeners disabled and an empty private password file. Subprocesses receive a restricted environment without project/production credentials. This is database endpoint isolation, not a claim of outbound network-namespace isolation. No retired local runner, VM, systemd service, allocator, admission helper or background release mechanism is used.

The wrapper binds the current checkout and `scripts/ci/probes/spin-expiry/manifest.json`: exactly17 fixed fixture inputs plus15 maintained qualification/component leaves. Historical provenance is the unchanged48-leaf v6 packet, manifest SHA256 `bee0d56349f89b0324962455b770fde4b5c322970b2b7b5a11ad69536b3ff580`. The maintained schema derives only removal of the unused external-provider sequence declaration; its manifest records original and derived hashes. The first-party signup-error timestamp field remains. Historical evidence is not a pass for this final hosted version.

Retain original stage logs, result/journal identities, source pins and observed original-client/whole-cluster cleanup. A failed case, unknown commit, forced cleanup or source drift remains a failure; no retry or fresh deadline replaces it. The wrapper's narrow result retains `full_qualification:false` and `connected_services_qualified:false`. Production installation and live verification are separate root-owned steps, never authorized by a protocol/mock receipt.

## Exact change and refusal scope

The forward component transforms complete expiry definition MD5cda7ea3e231248f1d2b597c74e04bb72 into84abf6526499efe148f770c5568ee26c by one insertion: call existing `fn_ca_lock_settlement_lane_global()` before the loop's first parent row lock. Its actual helper obtains transaction advisory G thenB. Current `atomic_cancel_tournament(uuid,uuid)` already takes that order before the parent. The old expiry takes parentT then calls that cancellation path, permitting a T→G versus G→T cycle. This is source evidence of a possible cycle, not actual historical invocation or deadlock proof.

The full preimage is embedded, the only replacement anchor is unique, and both complete images are checked. Forward and rollback admit only these exact images; they do not accept arbitrary text with a matching fragment. Target identity, metadata/ACL, three direct callees, watchlist state and ten selected critical bindings are checked before and after replacement. They are selected closure guards, not a certificate for every transitive financial function/table/role. The directly affected financial provider and maintained business checks must match their captured authority; selected catalog success alone is insufficient.

The fresh watchlist read at06:40:11Z establishes expiry is not watched and no guard baseline exists for expiry/global/cancel. Declaration rejects unlisted functions; this component therefore does not call it or write baseline/history. The baseline negative-case schema is independently captured at06:52:17Z: five columns, updated_at NOT NULL DEFAULT now(), PK(proname), no user triggers. Changed schema is a fixture setup refusal.

Rollback is exact source restoration, not a financial compensation. Install/rollback invoke no business function. Every existing candidate/age/status/start/draw/full-field predicate, fresh post-parent read, cancellation argument, sync call, limit, counter and estimate expression remains unchanged.

## Existing protected migration and concurrent-DDL boundary

Apply the exact forward component only through the existing root-controlled protected migration path, with one writer and exclusion of competing target/dependency/binding/baseline DDL throughout preflight, commit and independent readback. Preserve the component's exact preimage/postimage, metadata/ACL and selected binding guards. Record the actual migration identity and final full-definition readback. This package adds no serializer, admission service, watcher or separate release framework.

Read/replace/read alone cannot prevent a different session from redefining the target between comparison and replacement. Existing owner serialization must therefore cover the whole interval; do not infer it from a GUC, invented advisory lock or successful source comparison. If that existing boundary is unavailable, report that specific installation gap. A definition rollback restores source only and never undoes committed refunds.

## Accepted catalog qualification source

`spin-expiry-lock-order.sql` runs inside the hosted wrapper's private PostgreSQL17 allocation through its owned Unix socket, logical port5432, postgres identities, an execution-bound database name and no other database backend at its initial check. The older SQL endpoint tripwire also recognizes loopback; the maintained wrapper and race sessions permit only the private socket with TCP disabled. Tripwires alone are not complete network isolation or continuing exclusivity. The wrapper must retain actual endpoint, identity and cleanup observations; no credentials or production configuration are inputs.

The script embeds exact forward/rollback text through `spin-expiry-lock-order.component-inputs.sql`, plus exact pre/post images. Under one rollback-only transaction it checks full source install/replay/rollback/replay and17 specific catalog drifts. Each mutation remains outside the inner component-error handler, so inability to set up a drift is a setup failure. Each expected P0001 requires the exact component message; an unrelated P0001 fails. Deliberate catalog changes execute no gameplay/cancellation/seat/draw call, are rolled back in their subtransaction, and restore the original selected catalog. The final rollback retains no qualification schema or evidence mutation. The hosted wrapper must also retain the required catalog/business comparisons and independently verify connection/transaction cleanup; selected catalog equality is not that full proof.

The hosted wrapper and root must retain PostgreSQL/psql versions, full imported catalog/source/ACL pins, exact versioned input hashes, stdout/stderr/SQLSTATE, all result labels, actual transaction rollback, all original pre/post definition hashes and an independent stopped/empty allocation receipt. Missing labels, setup failures, timeouts or open sessions are failures, not skipped passes. No status in a fixture input is accepted as evidence.

## Business/race fixture lineage and current requirements

The retained September11 owner probe has26 groups with a real creator, three paid seats, funded draw, played continuation and terminal controls in one BEGIN/ROLLBACK. It does not expose a committed one/two-paid-chair old undrawn fixture to separate race sessions, and it does not exercise expiry. Its cancellation BODY MD5623100aa87ed6d0ef1a3598fb9ccb8b3 corresponds to full-definition MD5455f7f1934d136e27c13310eab07e3c6; neither qualifies current full6dac23baee41ff69ee0e1243f0a26c8e.

The hosted wrapper must provision the captured v6 schema/financial closure and run `spin-expiry-real-funded-fixture.sql` through the real authorized creator/payment/seat path, creating one uniquely eligible undrawn Spin with independently bound paid entry/custody/roster/seats, age authority and immutable creation/payment receipts. Retain the exact tournament/player/table identities and full estate snapshot; do not fabricate opening money, backdate business rows, disable triggers, stub a financial writer or reuse any of the historical22 as test fixtures. Fixture source alone is not evidence that creation or funding succeeded. Because genuine setup commits across sessions, the wrapper must retain whole-allocation disposal evidence; one connection's ROLLBACK cannot undo another committed transaction.

The maintained race runners connect as the allocation's nonsuperuser `postgres` identity. Observer and lock-control transactions retain empty request claims; actual expiry/cancellation transactions switch to `service_role`, set matching role claims and require `auth.uid()` to be NULL. The fixture separately invokes the real creator as `service_role` and paid-seat entrypoints as each authenticated player with its actual isolated session. Preserve the current callee chain and actual captured constraint/trigger modes; qualification must not bypass auth or maintenance guards. An independent observer reads locks/results. The broader draft schedules below are historical plans; current release scope is the admitted preimage/order allocation and candidate order, timeout, and committed-refund checks.

For each race start a fresh bounded transaction with `statement_timeout='8s'`, `lock_timeout='4s'`, UTC and explicit public,pg_temp search path; the owner's outer operation deadline is at most20s plus independently bounded cleanup. Retain actual deadlock_timeout; if it is not shorter than4s, the negative-control setup is insufficient, not a deadlock-free result. These are fixture bounds, not new production budgets. Never grant a pass merely because a caller timed out. Observe every backend idle/terminated and no retained transaction/advisory/parent locks before disposing the allocation. If current financial provider cannot complete within these bounds, mark qualification insufficient and return measured evidence to its owner, not broaden the budget silently.

## Historical broader race/control proposals

R1, preimage negative control. SessionB begins and invokes the **real** `SELECT public.fn_ca_lock_settlement_lane_global();`, retaining G/B. SessionA invokes real `SELECT public.fn_spin_expire_unfilled(1);` against the one eligible fixture. Observer must prove A is blocked byB onG and that the exact parent is held byA: a third NOWAIT parent-lock attempt must fail with55P03, never acquire or update it. InB call real `SELECT public.atomic_cancel_tournament(:tournament_id::uuid,NULL::uuid);`. Retain actual wait-for edges/deadlock log and40P01 from the real backend. Do not prescribe the deadlock victim: old expiry can catch the cancellation exception and return failed, so inspect warnings/returned JSON as well as both transaction states. Failure to create the exact cycle is an inconclusive negative control, not a pass. Roll back every surviving transaction; discard the complete private allocation after independent verification.

R2, corrected order. Start from the exact admitted candidate and a fresh identical real fixture. B owns G/B, A invokes expiry(1). Observer proves A waits forG; a NOWAIT parent read-lock attempt must **succeed** and be immediately rolled back, proving A has not acquiredT. B performs actual cancellation and forces deferred constraints with `SET CONSTRAINTS ALL IMMEDIATE`; inspect the exact canonical cancellation/refund/custody/ledger receipts, not chips_refunded_estimate. CommitB only under the disposable-allocation cleanup contract. Once B releasesG/B, A obtains the lane and must re-read the now-terminal parent and return skipped_raced=1, expired=0, failed=0, empty tournament_ids, with no second refund or changed terminal evidence. Roll backA, read the same receipts independently, then remove/restore the complete allocation. The owner must prove this outcome rather than infer it from the absence of a deadlock.

R3, no-commit ordering control. Repeat B holding G/B and A blocked with the parent NOWAIT proof, then roll backB. A must execute ordinary expiry against the still-eligible fixture, return exactly one expired ID and pass immediate deferred constraints. Read canonical refund evidence withinA, roll backA and compare the complete original estate independently. This is additional ordering/rollback evidence; it is not a substitute for R2's observed terminal recheck.

R4, third-seat/draw race. Through the existing owner authority create a concurrent paid final-seat/draw transaction which owns the parent; run expiry after its candidate scan. Observe the relevant owner lock order, then commit/rollback under the real fixture contract. Require either an observed busy-parent skip or a fresh postlock full/drawn refusal consistent with the exact schedule, and no refund of the new paid/drawn state. Merely arranging an already-drawn fixture does not test this race. Do not add a fake pause inside expiry or substitute a stubbed cancellation callback. If the provider cannot schedule that interleaving through real locks, mark this exact case missing.

R5, new acquisition-failure boundary. With B holding realG/B until A reaches its bounded global lock timeout, require actual error/transaction failure from A and complete rollback/no cancellation receipt; do not expect the existing inner failed counter. The inserted acquisition is outside the existing per-cancellation exception block, so a lane timeout now propagates before that block. This placement effect is explicit; no counter/handler has been rewritten to hide it. Independently verify lock release and retry eligibility after rollback.

R6, increased contention on skipped candidates. An old incomplete board with a booked draw now acquires G/B before the existing skip; preimage skips afterT without reaching cancellation's lane. Also include a busy parent and batches1/50 with booked/eligible mixtures. Observe actual G/B wait/hold durations, hand-settlement and terminal competitor progress, skipped/failed counts and held locks through transaction end. There is no invented acceptable latency threshold or accepted production load budget here; the platform owner must bind its existing service limits and accept measured effect before release. Correctness without this measured effect is incomplete qualification.

## Required business/parity controls

Run each preimage/candidate pair on independently equivalent real fixtures and retain canonical before/after state, not JSON estimates alone: disabled policy0; missing-policy default30 unchanged; no eligible candidate; strict age boundary and default/NULL/0/negative limit behavior; one/two old paid live chairs; full undrawn field; multiplier>0; booked jackpot draw with multiplier0; draw receipt with no multiplier; started/RUNNING/terminal state; parent busy; a candidate whose eligibility changes after its scan; played board refusal; canonical cancellation replay; one callee failure followed by another candidate; sync failure outside the existing inner catch; immediate/deferred financial constraint failure with whole-transaction rollback; exact payment, refund, escrow and ledger no-duplicate conservation. Preserve actual current cutover/maintenance/scope/lease authority and retired-path refusals.

The parent-before-global source error is not sufficient to declare original5 repaired. Historical membership/outcomes, actual admitted full-provider qualification, accepted contention/timeout placement effects, protected publication, installed exact-definition/ACL/trigger readback and natural subsequent sweep/receipt evidence remain separate unmet acceptance facts. Booked/played boards are not force-cancelled to clear the alert. No new financial repair, historical rollback or owner admission rule is part of this change.
