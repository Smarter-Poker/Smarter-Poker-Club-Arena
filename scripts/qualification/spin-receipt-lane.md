# FIFO5 receipt statement lane: finite partial qualification

The cohort-guard revision is source-wired; its native execution is pending.
This packet does not admit, recreate,
rank or settle the 415 currently identified missing original histories (including
the original 57-record subset). It introduces no new image,
workflow, runner service, timer or dependencies. The existing Spin required
accounting driver runs it only in its candidate allocation, before all original
retention and funded cases, inside the unchanged 240-second work and 30-second
cleanup limits. Existing preimage and completed-receipt images are unchanged.

The provider restores exactly four absent current functions and the six-column
compaction-policy relation with its one captured reference row from the retained
2026-09-17T18:21:53.810361Z catalog. Full definitions, owners, ACLs, configuration,
RLS, constraints, indexes, columns and row are compared to the original capture.
No successful receipt, history, money, rank, player or table is synthesized. The
original provider emptiness proof runs before this reference setup. The existing
commission and terminal authorities remain supplied by the original authentic
provider and are not replaced.

The catalog consumer executes the original and candidate compactor against empty
history, accepts its original bounded early path, and refuses an unrelated UPDATE
trigger plus seven independently rolled-back own-hook faults: disabled, wrong
columns, row shape, changed body, owner, security mode and search path. It verifies
missing-prerequisite, helper owner/ACL drift and replay refusal, exact existing procedure OIDs/metadata
except the intended bodies, the guarded reverse component, and outer rollback.
Rollback drops all three new bindings and the private handler. Full business rows
remain equal. Raw `relhastriggers` values are retained separately: PostgreSQL can
keep that conservative cache hint true after DROP; exact `pg_trigger` rows prove
semantic trigger restoration. No other catalog field is omitted.

The forward installer also requires the two original terminal definitions
already required by its reverse installer: complete-terminal full MD5
`480be3139fe0878e637ce54f533a2170` and places full MD5
`c412c8b17186976df139f73a706175f2`. The live terminal wrapper observed on
September 17 at 20:35 UTC belongs to a newer finish-lane contract and is
deliberately refused. This change does not repin or redesign that financial lane.
The catalog control reconstructs the exact committed `79d045d4` forward from
the candidate by removing only these two clauses. Its independent frozen body
digest must match before execution. A rollback-local timeout-metadata change
then demonstrates the old forward really installs while the unchanged reverse
returns its exact `55000` refusal. Four corrected controls separately remove or
change each prerequisite and require the original forward refusal. No financial
body is replaced or invoked. Each subtransaction restores the complete observed
catalog, business state and handler absence. Actual before-reproduction and
refusal-count values survive the outer rollback only as psql output variables;
the original observed database state remains restored. These new native results
must be obtained through the existing required CI before being claimed as passed.

The session consumer imports the unchanged, source-pinned R1 Session class. A
separate observer and two real backends exercise INSERT/UPDATE/DELETE of zero rows
on the actual receipt relation as service_role, INSERT and identity UPDATE of zero
rows on history as postgres, and the original reported metadata UPDATE outside
the new identity hook. Each relevant statement waits behind the actual global
G/B holder; the reverse schedule proves shared-to-exclusive blocking. TRUNCATE
first waits behind an owned relation reader and then returns the exact 55000
refusal; it is not described as taking the advisory lane before its relation lock.
Every empty-statement row count is zero. The helper owner/ACL is explicitly checked immediately
before service-role statements. A ninth schedule invokes the actual legacy settlement RPC as service_role with
zero rake, zero pot and empty contributions. Its captured zero-rake branch returns
before monetary work. While blocked behind the global holder, the writer must
have no receipt RowExclusiveLock: this distinguishes the new entry barrier from
blocking later at the INSERT statement hook. After release, the real RPC must
create exactly one succeeded zero-rake receipt. Same-key replay preserves the exact
result, entire receipt and its tuple identity. Rollback removes that request's
receipt and restores the full state. These isolated request IDs are not histories,
real played hands or financial completion evidence.

The finite session schedule has one 20-second work budget and one five-second
cleanup budget within the original allocation deadline. Original transcripts,
roles, backend PIDs, observed blockers, full before/after rows/catalog, exact input
pins and final fresh-backend absence/lock observations are retained. Client exit
alone does not establish cleanup. Any failed, unknown, cancelled or unobserved
outcome remains failed, and the original whole-allocation cleanup/disposal rules
remain required. There is no retry or resume.

This proves only the named empty-statement and empty-compactor boundaries when
actual native execution passes. Row-bearing writer races, table/tournament-specific
T locks, positive-rake financial RPC/replay, physical compaction of real rows, mixed
terminal completion, restored original history authenticity and historical closure
remain unqualified. Every result retains `full_qualification: false`.
