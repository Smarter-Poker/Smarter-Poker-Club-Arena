// IMPROVE 2026-07-21: RevenueSplitEditor + UnionSettingsPanel removed — they
// were exported but never mounted anywhere in the app (dead code since the
// union dashboard superseded them). CreateUnionModal is the live component.
export { default as CreateUnionModal } from './CreateUnionModal';
