/**
 * ═══════════════════════════════════════════════════════════════════════════════
 *  WALLET COMPONENTS — Centralized Exports
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * 2026-08-25: the briefcase emoji that used to sit in this banner is gone.
 * House rule 5.3 — no emoji in source files, they break the SWC compiler — and
 * a barrel file is exactly the kind of place nobody reads until it fails to
 * build.
 */

export { TransactionHistory } from './TransactionHistory';
