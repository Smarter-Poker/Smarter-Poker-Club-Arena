# Captured weekly accounting activation fixture

**Source packaged; UNRUN from these repository paths.** Only the protected local pipeline may admit this payload. The common `sp-native` interface is installed, but its read-only status does not establish source-job readiness or required-check success. This fixture does not authorize direct execution or publication.

The September 14 capture contains the referenced catalog needed by the earlier 17-component replay: 255 tables, 741 functions, 537 triggers, 226 policies, and explicit owner/ACL observations for 1,000 objects and eight roles. It is not a complete production database export. Table rows are absent; `seed-registry.sql` contains system custody declarations and constraints. Lifecycle users, balances and contracts come from the separate synthetic tournament fixture. No credentials or dependency trees are included.

`source-binding.json` binds the captured JSON, generated schema, policies, access and registry fixture bytes. The generated files are retained as captured verification inputs. `build-schema.py` records how the table/function/trigger schema was assembled; it does not regenerate the separately captured access, policy or registry SQL. It must not be used to substitute a fresh capture without source review.

The later installed-writer readback at **2026-09-15 00:39:03.686932 UTC** supplies two original functions absent from that first catalog: `fn_rakeback_periods_bulk_upsert(jsonb)` and `fn_create_settlement_period(uuid,uuid,date,date)`. Load `legacy-period-writers-definitions.sql` and then `legacy-period-writers-access.sql` before the candidate. Their sidecar `legacy-period-writers-source-binding.json` records the actual captured owner, full ACL, effective role checks and definition/source MD5; these are observed access rights, not fixture grant assumptions. The access fixture verifies the captured function hashes before reproducing grants. All three files are included in the main SHA256 custody binding. The dormant invoker factory references an uncaptured `player_agent_assignments` table; its regression covers denied client entry only, never execution of that legacy body as owner. Bulk-upsert is an elevated arbitrary period writer requiring RPC retirement; the other function is an invoker and loses its write path when caller table INSERT is revoked. Packaging and the expanded fixture remain **UNRUN**.

The protected plan must first generate and check the final atomic candidate with `scripts/ci/build-weekly-accounting-activation.py`, then admit `scripts/dev/test-full-weekly-accounting-activation.sh` using the qualified PostgreSQL 17 provider and reserved scratch/output directories. The final retirement component requires the **actual pg_cron extension**, its reviewed original job metadata, and `cron.launch_active_jobs=off` in this disposable fixture. A fake cron table/function cannot qualify full activation. Provider qualification and the new scheduler fixture inputs remain pending.

`supplemental-function-access.json` and its SQL companion add actual later owner/ACL observations for the old all-club recompute function and the two original P&L wrappers. The runner loads those captured rights after their original definitions. The earlier `legacy-recompute-access-assumptions.sql` remains historical and is no longer loaded. Installation still requires a fresh matching readback.

The successor runner verifies captured fixture, lifecycle and candidate bindings and uses twelve sequential fresh clusters, avoiding a clone of the connected cron database. Ten full-bundle rejections alter the earning-club default, inject inherited column UPDATE rights, grant authenticated BYPASSRLS, change the installed Messenger search contract, add an unsafe client push writer, inject a conflicting subscription revision column, change the credit-request notification definition, change the cashier approval writer definition, change the correction writer configuration, and change the legacy period status writer configuration. They must refuse at their respective late guards, including after scheduler transition and legacy-writer retirement. Each requires exactly one emitted root P0001 error with the complete expected message; the cashier, correction and browser-period refusals must also name the exact dependency in DETAIL. Guard names appearing only in error context cannot pass. Each refusal requires identical before/after schema, ACL, application/auth/cron rows, and global role attributes/membership. Nontransactional sequence allocations are explicitly excluded.

The separate privacy negative phase loads the builder's manifest-bound `qualification-preprivacy.sql` through 161000, then runs the actual 161500 component with an injected inherited service role. This prefix now contains 24 components, including the new actual-cron transition; it is a qualification prerequisite only, never an activation artifact. The runner requires the exact privacy error, actual ROLLBACK and both unchanged/restored-state assertions. It also compares original schema/ACL, data and role snapshots. The acceptance phase installs all 35 source components and runs the five real tournament lifecycle probes, then the separately pinned P&L hook, historical-conflict, write-authority, payee-privacy, Messenger reader/weekly preview and endpoint ownership fixtures. All new cases remain UNRUN.

The actual September 15 cron capture replaces the former historical 5,35 job assumption in the full fixture. The bundle must preserve the final direct JSON-result command and every job identity/role/destination field, retire recompute, change only the final close schedule to 0,30, and leave an unrelated actual extension job unchanged. The missing Messenger readers and notification preference schema/ACL are reproduced from actual timestamped captures in `../messenger-private-accounting/`. No managed admin-role graph or production-owner equivalence is synthesized. Continuation/timing unit seams and the two-session timing/ownership sources require their separate protected qualification; the full activation runner does not execute them implicitly.

The previous scratch-path execution installed all 17 then-current components in one transaction and passed 124 tournament assertion executions. Its late refusal left the baseline schema and access controls unchanged. Those are historical local observations. The relocated runner, added scheduler/PNL hooks, final expanded candidate, and protected execution have not been verified.

These lifecycle probes exercise actual producer and terminal functions with synthetic opening data. They do not supply full weekly cash-to-union-to-club-to-agent-to-player acceptance. They also do not establish public/authenticated/service role behavior across every reachable object, complete P&L ownership/coverage, historical commercial agreements, installation, deployment, scheduler operation, or recipient device delivery. Those remain separately required checks in the accounting validation plan.

Before financial test seeds, the acceptance phase saves actual complete schema/ACL definitions, typed function hash/owner/access metadata, period policies, money registry and role/state snapshots. The selected table metadata also records effective table and any-column privileges for anon, authenticated and service_role, alongside raw ACLs, so inherited rights are visible; the PostgreSQL 17 table set includes MAINTAIN. The runner stops its own database and preserves logs on both outcomes. The protected executor must independently witness stopped/empty execution and bind the complete logs to its receipt. A runner's printed PASS line is not a release receipt.

The 40643/40644 focused successor includes `accounting-alert-38644/coordinator-retry.sql` in the historical fixture's existing rollback transaction. Historical evidence now runs before P&L hook seeding so the real public service-role coordinator sees only the disputed standalone scope. Four calls must prove first refusal, treasury-only stable alert identity, an explicitly synthetic materially changed refusal with a distinct financial original, and stable changed-refusal replay. The actual clock must be due/unfrozen and below minute 40 at preflight; the native plan must provide suitable headroom. No clock or financial function is replaced. The test is UNRUN, retains its results in `historical-conflict.log`, and does not qualify drift-incident aggregation, delivery or historical entitlement.

The sealed preceding 32-component source retained the original 30 component files and adds authenticated subscription rotation plus atomic credit-request review. Load the separately captured `credit-request-authority/captured-preimages.sql` before the full candidate: it supplies the actual credit-request table, access, trigger and notification dependencies missing from the first catalog. Acceptance records the installed authority before seeds, then invokes the rotation and credit-request regressions in their own rollback transactions. Corresponding logs are retained as `push-rotation.log` and `credit-request.log`. Their source cases and the expanded runner are UNRUN. Authenticated rotation deliberately refuses sessionless background requests until a client-held account capability exists; row revision is not account/installation epoch proof. These additions do not establish full financial export, manual cashier/promo receipt coverage, correction semantics or device display acknowledgment.


## Cashier document successor

Component 33 retains all 32 preceding component bytes and adds the single private cashier transition/document authority. The full runner loads `cashier-document-authority/captured-preimages.sql` after the credit-request capture. It supplies the exact additional escrow table/schema/ACL and retained cashier functions; no production row data or synthetic dependency implementations are used. Its manual-writer capture is from 2026-09-15 03:53:30.3844 UTC, its additional cashier catalog from 04:18:21.923474 UTC, and the four older service-route writers from 04:46:07.43764 UTC. All seven old interactive/service entry points are explicitly retired. The cashier negative phase mutates the old approval configuration and requires `cashier_writer_preimage_changed` with whole-transaction restoration. The acceptance phase also runs `cashier-document-authority/regression.sql` and preserves `cashier-document.log`. Both source and final runtime are UNRUN.

The new client tests cover explicit financial receipts, durable identities within one module, account/club transitions, true current request status, unreadable lists and missing balance. Cross-tab semantic-intent coordination, old holds without canonical provenance, missing refund wallets, full departure atomicity and actual expiry scheduling remain separate open boundaries. Cashier eligibility and unsupported legacy inventory must never be reported as full queue completion. This source packet is not complete weekly-accounting, provider or deployment acceptance.


## Correction-document successor

Component 34 preserves the sealed33 files and extends the existing document authority for eligible new correction ledger rows. The actual correction writer is loaded from `correction-document-authority/captured-preimages.sql` after cashier preimages; its implementation is preserved. The extra full-bundle rejection changes its configuration, requiring `correction_document_dependency_preimage_changed` and the exact `fn_ca_post_correction(text,uuid,text,uuid,numeric,text,uuid,bigint,uuid,uuid,jsonb)` dependency DETAIL. It must restore the entire incoming schema/access/rows/role state. Acceptance runs `correction-document-authority/regression.sql`, retains `correction-document.log` and captures the new private provenance authority before test seeds. That addition brought the sequence to eleven fresh clusters; component35 below adds another rejection before acceptance. Actual pg_cron launches remain disabled.

The correction document is a journal record, not a new payment, amount due or incident closure. Prior paid corrections are not relabelled or adopted. Unsupported unscoped endpoints and the separate tournament escrow-overlay effect stay explicit. WH typed proof and CA weekly-summary exclusion tests require separate protected application execution. All added source, runtime, permission and deployment checks are UNRUN; this fixture does not establish historical impact or full accounting completion.

The separate protected application catalog must include CA `tests/unit/ClubWeeklyAccountingReader.test.ts`, `tests/cashier-weekly-statements.test.tsx`, `tests/unit/FinancialExportService.test.ts`, and their exact `tests/helpers/clubWeeklyStatement.ts` fixture; WH must include `__tests__/correction-accounting-display.test.mjs` together with existing cashier/Messenger regressions. These cover the actual scoped reader, mounted cashier weekly tab, bounded club-weekly CSV path, required club identity, exact cents and stale-account/view refusal. They do not qualify all seven export types or immutable complete history.

Historical corrections require separate recipient-bound identity proof from real invoice/message/delivery/source joins. The private projection keeps the actual invoice type/ID while returning invoice_identity_verified=true, correction_unverified=true and accounting_verified=false; it omits amount/status/dates/diagnostics. Normal verified projections omit both identity-only flags. Required native reader-to-WH cases include malformed/copied flags, wrong or missing UUIDs, unavailable proof, canonical card/Bubble routing, and unchanged original invoice identity. Raw old invoice/message/notice reads must deny; guarded search/thread previews may expose only the sanitized placeholder to the authorized recipient. A club issuer or prior delivery cannot expose an individual payee's correction or even its placeholder when the actual source recipient gate excludes that issuer. Actual policy/reader tests remain separate from mocked handler tests and from source custody.


## Browser period observer successor

Component35 preserves all34 predecessor bytes and retires four old period signatures, including both get-current overloads, without dispatching any replacement payer. Load browser-period-observer/captured-preimages.sql after correction preimages. A changed fn_set_settlement_period_status(uuid,text) configuration adds a rejection before acceptance, bringing the sequence to twelve fresh clusters; it requires the single root P0001 legacy_period_function_preimage_changed and exact signature DETAIL, with whole-bundle schema/access/row/role restoration. Acceptance records settlement_periods/union_accounting_runs effective privileges, runs browser-period-observer/regression.sql and retains browser-period-observer.log.

The15-field observer binds actual actor, exact union/standalone scope and Pacific week, returns the existing Chicago deadline and requires complete-v3 recorded result plus unique settled/closed exact period for posted. It never returns money, payees or raw diagnostics. Missing club scope history is unavailable, not proof that union accounting did not run. Private weekly club records remain the existing issued summary authority. Client parser/workspace, startup retirement and actual nested-loader tests are separately required; source binding does not execute them. All new source and full35 execution are UNRUN.
