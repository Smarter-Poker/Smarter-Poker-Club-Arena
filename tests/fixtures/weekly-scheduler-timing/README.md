# Scheduler timing successor qualification

Source only, UNRUN. These files are relocated from the independently reviewed September 15 timing proposal. No live cron row or financial record was changed. The actual cron capture was taken at 2026-09-15T02:35:15.903162Z; its source artifact is bound with this fixture. It contains public job configuration and the separately scoped six-row diagnostic, not credentials.

The full activation order preserves the original 24 component bytes and inserts 155450 immediately before 155500. That transition requires actual jobs 271/272, including IDs, exact commands, roles, database, host and port. It prepares the sealed retirement's intermediate preimage; the later 163000 component restores the original JSON-result command and changes only the surviving schedule to minutes 0 and 30. The whole candidate is one transaction. No intermediate prefix is a deployment artifact.

`captured-cron-prerequisites.sql` uses actual extension-owned PostgreSQL 17 pg_cron with launching disabled before server start. It replaces the older historical job seed only in the full activation runner. The existing continuation unit still uses its explicitly declared historical scheduler seam. The full runner now verifies the final job row against the incoming actual row, duplicate retirement, unrelated-job equality, daylight-saving due instants and no launched fixture jobs. Its added Messenger and push late-refusal phases compare full before/after schema, role and application/cron row state.

The isolated `captured-cron-transition-regression.sql` tests the first transition only. `cron-regression.sql` requires a separately staged exact predecessor through 162500 and then applies 163000; it must not be applied after the full candidate. Both need actual pg_cron. They remain separate unexecuted qualification sources, not additional live timers.

The `load.sql`/`due-regression.sql` and `post-lock-setup.sql`/`post-lock-regression.py` cases use declared synthetic clock, preparation/P&L and empty R1 seams inherited from the continuation fixture. They retain the actual private coordinator/routed stages/statement code. Snapshot equality includes wallet, period, close, round and exact run state, excluding only scheduler visit time for completed replay. A pre-minute-45 case tests not-yet-due behavior; the final microsecond before 04:00 tests the preserved maintenance refusal. The two-session source covers period-lock waits across the minute, deadline and freeze boundaries. These cases do not certify a real earning book, historical ownership or a provider cancellation.

Protected execution must resolve the Python PostgreSQL driver and supervise both database sessions, timeouts, artifact finalization and stopped processes. The full activation runner does not execute these unit/race sources merely because they are hash-bound. An admitted separate timing runner and whole-book financial acceptance remain required. A 04:00 invocation opportunity does not establish a successful 04:00 completion or override maintenance thaw requirements.
