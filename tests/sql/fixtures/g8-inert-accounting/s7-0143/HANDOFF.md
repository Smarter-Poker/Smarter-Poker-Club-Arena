# S7 cardinality correction0143

SOURCE ONLY; SQL UNPARSED AND UNRUN. Responds to0183 without changing accepted0140 helper or previous0142 packet. Every one of14 original fixture call sites now materializes once, requires exactly2 rows and exactly1 from plus1 to side before any loop or selected-side assertion. This applies on every iteration of column/type loops. Missing ledger ID also requires status unresolved. All prior exception assertions are retained and helper bytes identical.

Static readback verified14 call sites/14 distinct cardinality guards, original assertion labels retained, and exact helper identity. No SQL parsing, mutation fault injection or database test ran; empty/duplicate-side behavior still requires original Diamond protected fixture execution. No new runner, installation, active path or grant. Cardinality guard source prevents vacuous assertions once executed; source presence does not establish enforcement.

Route solely into Diamond existing S7 constructor after independent source review. Retain exact schema/default ACL/role inheritance/function absence and atomic protected installation/readback prerequisites. No complete S7, financial, deployment or runtime acceptance follows.
